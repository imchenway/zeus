# Zeus 任务与会话语境

Zeus 用任务表达待完成的工作，用会话承载人与 AI 的持续交互；两者相关联，但生命周期和操作责任不同。

## Language

**发布外部等待**：
Zeus 发布命令已经把工作交给 GitHub、模型服务或其他外部系统，并在等待真实结果的状态；命令日志可以暂时没有变化，但 Zeus
界面仍应保持可操作。
_Avoid_: 假死、界面无响应、发布失败

**Zeus 界面无响应**：
用户无法正常点击、滚动或操作 Zeus 主窗口的状态；它与发布命令是否仍在执行、是否等待外部系统是两个独立事实。
_Avoid_: 发布外部等待、命令无输出、网络慢

**菜单栏用量浮窗**：
用户从 macOS 菜单栏 Zeus 入口打开、可按模型供应源切换的只读用量摘要窗口；它与菜单栏图标、右键应用菜单和代码编辑器底部状态栏是不同对象。
_Avoid_: 状态栏、用量卡片、原生菜单

**下拉浮层首帧最终宽度**：
Zeus 下拉列表首次对用户可见时已经达到由本次完整选项集合决定的稳定宽度；触发器尺寸和业务布局不属于该宽度。
_Avoid_: 延迟扩宽、触发器自适应宽度、打开后再调整

**任务**：
需要被跟踪、验收和归档的工作目标；任务只记录工作状态，不直接拥有 AI 执行控制。
_Avoid_: Runtime、会话

**任务编码**：
任务在所属项目内唯一的短标识；编码本身不承担任务顺序语义，也不能脱离项目上下文充当全局任务身份。
_Avoid_: 任务序号、全局任务 ID

**任务层级**：
同一项目任务之间最多形成三级归属：根任务为第一级，其直接子任务为第二级，第二级任务的直接子任务为第三级；一项任务最多拥有一个直接父任务，可以拥有多个直接子任务。任何关系调整会产生第四级时，系统必须说明原因并阻止保存，用户不能通过确认绕过。父子任务各自管理状态，不互相拦截或自动改写。
_Avoid_: 多父任务、第四级任务、无限层级、通用任务关联、会话分组

**父任务删除处置**：
删除仍有直接子任务的父任务时，对这些直接子任务及其下级结构作出的明确处置；用户可以为全部直接子任务统一选择一个新父任务、连同全部下级任务一起删除，或把全部直接子任务保留为根任务。移动或保留时维持直接子任务下面的原有层级，系统不得静默选择结果。
_Avoid_: 普通任务删除、自动级联删除、隐式解除父子关系

**关联任务**：
与当前任务处于同一项目并存在双向普通关系的其他任务；它只表达工作内容有关，不表示依赖、阻塞，也不自动影响任何任务的状态或执行。关联数量不设业务上限，按关联任务最近更新时间倒序展示。任一任务删除时解除当前普通关联，恢复任务不会自动恢复关联，但审计记录继续保留。
_Avoid_: 父任务、子任务、依赖任务、状态联动

**关联任务首发上下文**：
用户在任务首发时显式选择的关联任务内容，以及逐项选择的关联任务会话和附件；会话只以服务端复验后的真实 JSONL 路径发送，不展开复制会话正文。它只补充本次新会话的首条输入，不改变任务关系，也不追改既有会话。
_Avoid_: 自动携带全部关联任务、父任务上下文、任务关系同步、重写历史会话

**当前任务历史会话信息**：
用户在任务首发时从当前任务已经存在的会话中逐项选择的会话 JSONL 文件路径；它只补充本次新会话的首条输入，不复用或改写历史会话，也不展开复制会话正文。
_Avoid_: 当前任务历史会话上下文、自动携带全部历史会话、复用历史会话、复制会话正文

**任务优先级**：
表示任务相对处理顺序的 P0 至 P4 标记；P0 表示立即开始处理，但不会自动启动 AI、会话或执行轮次。
_Avoid_: 任务状态、管理阶段、自动执行开关

**任务类型**：
任务工作目标的必选分类，固定为需求、缺陷和优化；它决定当前任务使用哪些类型专属内容字段，但分类本身不自动进入任务首发正文，也不改变任务状态、优先级或执行方式。
_Avoid_: 标签、上下文来源、任务状态、执行模式

**类型专属内容**：
随任务类型变化的一组任务内容：需求使用需求描述，缺陷使用现状、预期和复现步骤，优化使用现状和预期；切换类型时其他类型内容继续保留，但不作为当前任务内容展示或发送。当前类型字段只有包含文字或字段归属附件时才进入任务首发。
_Avoid_: 通用说明、隐藏内容删除、AI 执行配置

**任务首发有效字段**：
当前任务类型下至少包含非空文字或字段归属附件的类型专属内容字段；它在任务首发中以隶属于任务标题的字段段落保留字段名称和真实内容，完全为空的字段整段省略。任务类型、标签和标签附件不属于任务首发有效字段，但继续属于任务内容。
_Avoid_: 未提供占位、任务全部字段、任务类型、标签、空字段标题

**任务提交说明建议值**：
Zeus 根据任务类型、任务编码和任务标题生成的可编辑任务代码提交说明；需求、缺陷和优化分别使用 `feat`、`fix` 和 `perf`，代码交付合入提交不属于此概念。
_Avoid_: 强制提交说明、合入提交说明、任务标题

**任务管理状态**：
用于跟踪工作推进阶段的可编辑标记；任务列表、任务详情和会话标题使用同一项目配置、事实源与修改逻辑。任务首发成功时只允许“待开始”自动进入“开发中”，其他非终态阶段不被会话活动覆盖；进入“已完成”或“已取消”时执行统一的任务终态清理，单独修改状态不负责恢复、归档或停止会话。
_Avoid_: Runtime 状态、会话状态、执行控制

**任务管理状态配置**：
单个项目拥有的任务管理阶段集合及其显示名称、颜色、顺序和系统角色指派；所有状态拥有相同的增删改能力，系统行为不由状态名称或来源决定。它不包含 Coding Agent 运行状态，也不改变任务优先级的 P0 至 P4 语义。
_Avoid_: 运行状态配置、优先级配置、任务枚举统一配置

**任务状态系统角色**：
任务管理状态承接的新任务默认、任务首发后、完成和取消等既有系统用途；它不形成额外的用户配置层，也不使承载用途的状态成为不可编辑的内置状态。删除该状态时，替代状态同时承接原有用途。
_Avoid_: 内置状态、固定状态名称、状态身份

**任务状态默认模板**：
创建新项目时复制为该项目任务管理状态配置的全局起始模板；复制后项目配置独立演进，模板变更不追改已有项目。
_Avoid_: 全局实时状态配置、已有项目同步源、项目共享状态

**任务状态主色**：
用户为一个任务管理状态选择的单一识别色；Zeus 从中派生适配当前主题的标签底色、文字和边框，并在所有任务状态入口保持一致。
_Avoid_: 独立背景色、独立文字色、仅任务列表颜色

**任务状态替代迁移**：
删除正在使用的任务管理状态前，把引用该状态的全部任务明确改为用户选择的另一状态；迁移完成后才能删除原状态。
_Avoid_: 清空任务状态、静默回退默认状态、删除后再修复

**任务事件**：
任务发生有意义变化时形成的可追溯记录；面向用户时以可读标题和时间表达，不把内部事件编码当作业务文案。
_Avoid_: 运行日志、事件编码

**任务终态清理**：
任务进入“已完成”或“已取消”时，停止并归档关联会话、回收物理任务 worktree 的统一收尾动作；任务分支、远端分支、会话消息和交付记录继续保留。存在未提交或未跟踪内容、冲突或活动会话时，必须在用户明确确认后才能永久删除本机内容并完成状态修改。
_Avoid_: 代码交付、放弃分支、删除会话历史、删除任务分支、待回收状态

**任务开发线**：
由一次任务推送创建、围绕独立任务分支持续开展开发、审查和交付的一条生命周期；同一任务再次推送会创建新的任务开发线，不继承其他开发线的分支内容。
_Avoid_: 会话、任务管理状态、内部工作区标识

**当前会话开发线**：
主窗口当前选中会话所绑定的任务开发线；它只在该会话属于正在查看的任务时成立，并随当前会话变化。代码交付中用户手动选中的开发线和打开窗口时使用的开发线都不能冒充当前会话开发线。
_Avoid_: 当前选中分支、打开来源、最近查看开发线、活动会话

**任务开发线状态**：
任务列表对一个任务全部开发线当前阶段的只读汇总；多条开发线状态不一致时优先显示需要处理或尚未完成的状态，完整分支事实继续在任务详情和代码交付中逐条展示。
_Avoid_: 任务管理状态、单一分支状态、会话状态

**来源分支**：
用户为一次推送逐仓选择的真实命名分支；有远端的仓库只允许选择刷新成功后的远端分支，纯本地仓库才选择本地分支。分支名称按 Git
自身规则判断是否合法，中文和 `#` 等 Git 合法字符不改变其身份。在环境代码交付页中，它是当前任务分支唯一允许合入的本机分支，由任务 worktree 的 `sourceBranch` 事实确定，不默认等于 `main`。
_Avoid_: 过期远端记录、任意 Git ref、ASCII 分支名

**远端来源基线**：
有远端的仓库在创建任务开发线前成功刷新远端后，来源分支当时对应的精确提交；任务工作区只从这份已提交快照创建，不携带原工作区的已暂存、未暂存或未跟踪内容。
_Avoid_: 本地来源分支当前值、缓存远端记录、原工作区未提交内容

**本地来源基线**：
纯本地仓库为一次任务开发线冻结的本地来源分支精确提交；默认不携带原工作区未提交内容，只有用户显式选择时才允许带入。
_Avoid_: 远端来源基线、默认复制脏工作区、当前工作目录

**任务分支**：
Zeus 为一次任务开发线创建并管理的本地命名分支；名称保留 `zeus/` 前缀，前缀后的合法性按 Git 自身规则判断。
_Avoid_: 来源分支、任意无前缀分支、ASCII 分支名

**来源分支偏好**：
用户在一个项目仓库中最近一次成功创建推送工作区时使用的来源分支；它只用于下次新建推送工作区的默认选中，不改变任务开发线已经冻结的来源基线。
_Avoid_: 任务来源基线、当前分支、默认主分支

**代码提交**：
用户审查并选择任务开发线中的文件后创建本地 Git 提交的动作；它不包含远端推送、工作区回收或目标分支合入。
_Avoid_: 代码推送、提交并推送、代码交付

**代码提交格式整理**：
代码提交前依据项目已经声明的格式规则整理本次选中文件的动作；空白格式差异不等同于 Git 冲突，也不承担发布合规门禁。
_Avoid_: Git 冲突检查、发布格式门禁、提交失败

**代码推送**：
用户把任务开发线当前已提交的 HEAD 推送到其记录的远端分支并校验远端提交的动作；未提交和已暂存改动保持原样。
_Avoid_: 自动提交、强制推送、工作区回收、代码交付

**任务分支远端一致**：
有远端的仓库中，任务开发线本地 HEAD 与刷新后远端任务分支 HEAD 完全相同的交付门禁；远端分支缺失、领先或分叉都不能进入目标分支合入，也不能通过强制推送覆盖。
_Avoid_: 本地领先、可选远端备份、强制同步、仅比较分支名称

**代码交付准备**：
任务开发线进入目标分支合入前，对代码差异和本地提交进行的显式处置；活动会话只作为可能继续写入的事实提示，不参与提交、推送或准备状态门禁。停止会话属于会话操作，回收
worktree 时才需要处理其写入风险。
_Avoid_: 任务已完成、代码合入、提交并推送、自动提交、停止活动会话

**任务分支代码差异**：
任务分支相对其来源分支共同起点产生的全部已提交代码变化；它与任务 worktree 中尚未提交的本机变化分开展示。
_Avoid_: 工作区未提交变化、远端领先数量、工作区是否干净

**代码差异软换行**：
代码差异中的超长单行在左右各自内容栏内形成的视觉换行；它允许连续字符在栏宽边界断开，但不改变文件内容、源码行号或差异行的左右对应关系。
_Avoid_: 修改文件换行、截断内容、合并源码行、左右独立行高

**AI 代码审查会话**：
围绕当前开发会话实际执行环境相对冻结来源基线的完整代码变化进行独立复核的专用会话；它包含已提交和未提交变化，但不隐式扩展到同一任务的其他仓库环境。
_Avoid_: 开发会话续聊、仅未提交变更、整条多仓开发线、Git 变更查看、代码交付

**项目工作区入口**：
当前项目顶部用于切换任务、Git、源码、图谱和命令五个完整工作面的并列入口；它们按界面从左到右的固定位置形成同序数字快捷入口。
_Avoid_: 顶部功能、普通页签、代码模式页签、主导航

**项目 Git 工作台**：
用户点击项目工作区顶部 Git 入口后打开、面向当前项目全部已发现项目仓库的 Git 查看与操作空间；项目名称本身默认进入任务列表，Git 与任务、源码、图谱和命令并列，不继承某条任务开发线的归属或交付状态。
_Avoid_: 任务代码交付、任务开发线、通用终端、源码编辑器

**IDEA 布局参考**：
仅用于对齐 Git 功能范围、信息区域和对象操作关系的外部参考；Zeus 实现必须继续使用自身类 Codex App / Apple 的颜色、字体、密度、圆角、阴影、图标和控件。
_Avoid_: IDEA 视觉复刻、新 Git 主题、重蓝色选中、彩色提交图谱、独立 macOS 伪窗口

**项目默认工作面**：
用户点击侧边栏项目名称时打开的该项目任务列表；项目前的独立箭头只控制该项目会话展开或收起，不能改变项目名称的导航语义。
_Avoid_: 项目 Git 工作台、展开会话、进入最近会话

**环境代码交付页**：
从会话环境信息打开、绑定当前任务 worktree 与当前任务分支的交付工作面；它不选择开发分支，只提供提交当前分支、推送当前分支、将当前分支合入来源分支三个业务动作，并以文件列表和差异审查为页面主体。
_Avoid_: 项目 Git 工作台、通用 Git 菜单、分支选择器、项目全部仓库

**代码工作窗口**：
代码交付、Repository Diff 与冲突处理使用的独立 macOS 全窗工作面；内容从原生标题栏下方铺满，关闭由红色窗口按钮负责，返回上一工作面只使用统一标题工具栏中的紧凑无文字左箭头。
_Avoid_: 网页式居中大弹窗、灰色外围背景、内容右上角关闭 X、返回大按钮、独立返回列、面包屑导航

**项目 Git 直接操作**：
用户在项目 Git 工作台中对所选项目仓库直接发起、并以 Git 自身结果为准的操作；它不受任务、开发线、代码交付、HEAD 并发、脏状态保护或审计门禁约束，但不能把当前项目之外的任意路径声明为项目仓库。

**同步分支控制**：
项目 Git 工作台中的多仓设置；关闭时分支动作只作用于用户选中的项目仓库，开启时对全部项目仓库的同名分支同步执行 checkout、merge、delete 等动作，并逐仓保留真实结果。它不提供跨仓原子性保证。
_Avoid_: 任务代码交付、任意本机仓库、Zeus 并发保护、受控高风险命令

**代码交付**：
用户把已经完成交付准备的当前任务分支显式合入该任务开发线来源分支的过程；环境代码交付页不再选择其他目标分支，合入只修改本地来源分支，远端推送是后续可选动作。可能回收任务
worktree 时，活动会话会触发风险确认，但不会阻止此前的提交和推送。
_Avoid_: 写死 main、另选目标分支、代码交付准备、任务管理完成、隐式合入、强制要求远端

**远端目标基线**：
有远端的仓库在开始或重建代码交付候选前成功刷新远端后，目标分支当时对应的精确提交；隔离合入候选必须从这份提交创建，不能从本地同名分支或过期远端记录创建。
_Avoid_: 本地目标分支、缓存远端记录、任务来源基线

**本地目标基线**：
用户开始代码交付或过期候选重建时，本机目标分支对应的精确提交；冲突处理不为获得该基线自动访问远端，远端刷新和推送继续是独立用户动作。
_Avoid_: 远端目标基线、缓存远端记录、自动拉取

**过期合入候选**：
隔离合入候选创建后，本机目标分支已经前进，导致候选不再建立在最新本地目标基线之上的状态；Zeus 必须自动重建候选，冲突指纹一致的草稿继续保留，真正变化的冲突重新交给用户确认。
_Avoid_: 自动 rebase、强制推送、继续使用旧候选、无条件复用草稿

**合入冲突**：
目标分支与任务分支对共同基线的修改无法直接组成唯一结果的状态；用户需要检查差异并确认最终内容。
_Avoid_: 普通代码差异、自动选择一侧、代码交付完成

**冲突审查工作台**：
以当前冲突和可编辑合并结果为核心的人工审查空间；确定性自动合并与 AI 冲突处理都是辅助动作，不能取代用户对最终结果的确认。
_Avoid_: AI 处理页、权限确认页、代码交付总览、自动合入

**简单冲突自动合并**：
用户主动触发后，以共同基线、目标分支和任务分支的三方差异为依据，把能够证明互不覆盖的插入、删除和修改组成当前文件草稿的确定性动作；无法证明安全的冲突块继续保留，不调用智能体猜测业务意图，也不自动保存。
_Avoid_: 相同行数限制、自动解决全部冲突、模型改写、隐式保存

**AI 冲突处理会话**：
用户每次点击“AI 处理”都会新建、独占一份隔离合入现场的可见会话，即使已有冲突处理会话仍在运行也允许并行新建；用户在开始前为这条新会话确认一次写入权限，会话以“冲突处理：”标识其用途，并在执行内核回放前即可看到本次处理要求。智能体只解决并暂存冲突，不执行提交、目标分支更新或远程推送；轮次完成后由 Zeus 复验并自动完成本地合入，代码交付展示真实结果。
_Avoid_: AI 冲突草稿、复用旧冲突会话、并行共享同一合入现场、串行等待旧会话、默认自动授权、普通开发会话、不可见临时会话、智能体直接提交或推送、结果停留在隔离区

**AI 冲突处理权限确认**：
每次新建 AI 冲突处理会话前展示的紧凑居中弹窗；它只让用户在“自动”与“完全访问”之间确认本次会话权限，不承载冲突编辑、模型选择或长时间准备状态。
_Avoid_: 权限选项卡页、就地权限菜单、分裂按钮、AI 处理进度弹窗

**AI 冲突会话创建信封**：
用户确认权限后立即由 Zeus 本机持久接受的创建请求；它绑定会话稳定身份、当前冲突草稿、权限与准备状态，让主窗口可先进入会话，而不必等待独立合入现场和 Provider 首轮准备完成。
_Avoid_: 临时加载状态、Renderer 内存草稿、已完成会话

**冲突块选择**：
用户针对单个未解决冲突块明确采用目标分支、任务分支或两侧内容的动作；它只改变当前编辑结果，保存前不写入文件。
_Avoid_: 整文件覆盖、自动保存、自动推送

**当前文件冲突已处理**：
当前冲突文件的所有冲突块都已在可审查草稿中得到处理、但结果尚未写入隔离合入工作区的状态；用户需要检查结果并明确保存该文件，之后才能进入下一冲突文件或冲突收尾。
_Avoid_: 全部冲突已保存、合入完成、自动暂存、自动进入收尾

**冲突收尾确认**：
最后一个冲突文件保存后、真正合入并推送目标分支前的明确确认状态；它只展示冲突已经全部处理和即将写入的目标，不再显示空白冲突编辑器，也不自动执行远端写入。
_Avoid_: 空冲突页面、自动完成推送、仍有冲突

**本地待同步**：
目标分支因正被含未提交代码的工作区使用而保持原提交位置的状态；有远端时远端结果可以已经交付，无远端时隔离合入结果必须保留并允许清理原目录后重试，不能冒充本地已更新。
_Avoid_: 合入结果丢失、自动暂存、本地已更新

**任务内容**：
用于描述一个正式任务的标题、任务类型、类型专属内容、优先级、标签和附件关联；它与任务管理状态、会话历史和执行轮次彼此独立。
_Avoid_: 任务状态、会话消息、执行上下文

**保真正文**：
任务说明、用户消息、智能体正文和思考摘要中需要忠实呈现来源排版的可阅读内容；原始空白属于内容，超出可用宽度时允许只改变显示的软换行。
_Avoid_: 标题、按钮文案、表格摘要、改写原文

**消息操作栏**：
附属于单条用户消息或智能体消息的轻量操作集；它不属于保真正文，鼠标移入该条消息的正文可见区域或键盘焦点进入时显示。
_Avoid_: 消息内容、常驻元数据、会话级工具栏

**任务内容编辑**：
用户修正任务内容的动作；成功修改只更新任务本身和此后新建会话使用的任务内容，不追改既有会话，也不隐式改变任务管理状态。
_Avoid_: 会话追问、重写历史、状态迁移

**任务更新时间**：
任务内容、任务管理状态或任务自身关系最近一次真实变化的时间；查看或打开任务详情不会更新它，也不能由关联会话的内容或阶段变化代替。
_Avoid_: 任务详情打开时间、会话最后更新时间、会话阶段更新时间

**任务附件关联**：
任务与 Zeus 托管本机资源之间的可编辑关系；资源身份不由某次安装的数据目录绝对路径决定，数据目录迁移后必须在当前托管目录重新解析。移除关联不会删除托管资源，既有会话和历史证据仍可继续引用该资源。
_Avoid_: 历史绝对路径、删除本机文件、会话附件、上传文件

**字段归属附件**：
用户在支持附件输入的任务内容或标签字段中添加、并持续归属于该字段的图片、文件、文件夹或超长文本资源；任务标题不接受附件。查看与编辑必须保留这项归属；任务首发只发送任务首发有效字段的附件，标签附件继续保留在任务中但不进入首发。字段区域固定先显示附件、再显示文字，不表达文字中任意光标位置。同一真实资源在一项任务中只有一个字段归属，重新粘贴到其他字段时以最后粘贴的字段为准。
_Avoid_: 标题附件、字段归属图片、通用任务附件、界面临时排序、正文内附件路径

**历史附件自动归属**：
旧任务中没有字段归属的附件按迁移时任务类型确定一次归属：需求进入需求描述，缺陷进入缺陷现状，优化进入优化现状；结果持久保存，后续任务类型切换不重新计算。
_Avoid_: 未归类附件区、按文件名猜测、动态跟随任务类型、自动归入预期

**任务首发布局快照**：
服务端在复验当前任务、手选父任务和关联任务、会话路径及附件后，为本次新会话冻结的有序输入结构；它记录任务首发有效字段的文字与附件位置、会话 JSONL 路径和上下文顺序。排队、重试、Provider 派发和已发用户消息都使用同一快照，不从后续变化的任务重建。
_Avoid_: 底部附件数组、解析提示词恢复位置、实时回读任务、未验签客户端布局

**任务编辑冲突**：
用户开始编辑后任务内容已被其他窗口或流程更新，导致当前修改不能安全覆盖最新内容的状态；它要求用户在保留本地内容和载入最新内容之间明确选择。
_Avoid_: 普通保存失败、静默覆盖、自动合并

**未完成任务筛选**：
任务页基于任务管理状态形成的派生视图；它包含除“已完成”和“已取消”之外的全部任务，并按项目记住用户最后一次显式选择的筛选按钮。
_Avoid_: 新任务状态、运行状态、未启动任务

**会话**：
围绕一个项目持续保存用户消息、AI 输出和执行上下文的交互空间；一个任务可以关联一个或多个会话。
_Avoid_: 任务、Runtime 进程

**跨项目会话导航**：
用户从全局侧边栏选择其他项目会话时，目标项目与目标会话组成不可分割的导航对象；两者不能出现分别指向不同项目的中间工作态。
_Avoid_: 只切换会话 ID、沿用旧当前项目、跨项目会话选择错位

**会话切换过渡**：
目标会话首次历史尚未就绪时，主区暂时保留上一会话正文并只在底部说明正在加载；旧正文内的原有动作继续归属旧会话，新输入则归属目标会话并等待目标身份与历史就绪后再提交。
_Avoid_: 整页空白、把目标输入发往旧会话、把旧正文动作改投目标会话、把旧正文冒充目标历史

**询问回答附件**：
用户在普通用户询问的自定义回答或纯自由输入中添加的图片、文件、文件夹或超长粘贴文本；它属于具体问题的当次回答，必须与回答文字共同交给 Agent。
_Avoid_: 普通会话附件、任务附件、无问题归属的底部附件

**完整快照**：
某个会话条目在当前时刻已经确定的全部正文；完成条目的完整快照不会再追加内容。
_Avoid_: 完整答案缓存、最终字符串

**会话时间线时间**：
会话内容首次进入对话顺序的稳定时间。普通会话条目使用开始时间，普通用户消息使用创建时间，只有来源没有提供这两种时间时才回退最后更新时间；条目后续流式增长或完成不能改变它在时间线中的位置。已回答询问记录使用答案实际提交时间，因此展示在用户完成该次询问交互的位置。
_Avoid_: 最后更新时间、界面渲染时间、刷新时间、按完成时间重排全部条目

**轮次现场展开态**：
执行轮次尚未产生最终回答时的对话展示状态；过程说明、工具活动、普通用户询问的已回答记录和用户中途引导按会话时间线时间完整穿插展示，使用户能看见当前工作如何被持续引导。此时不能因为条目属于同一轮次而提前把过程整理到顶部。
_Avoid_: 最终回答收口、提前折叠、按轮次搬动过程、中途引导汇总

**用户中途引导**：
用户在执行轮次仍进行时补充给当前轮次的用户消息；它按会话时间线穿插在过程内容中，并始终保持用户消息的身份与右对齐视觉语义。
_Avoid_: 过程说明、工具活动、下一轮队列消息、左对齐过程条目

**轮次最终回答收口**：
执行轮次产生最终回答后对该轮历史进行的自动整理；过程说明和工具活动收纳为一个处理耗时摘要，用户中途引导继续完整可见，最终回答位于这些引导之后。是否收口只由最终回答到达决定。
_Avoid_: 轮次完成事件、已回答询问、等待用户、按时间自动收口、隐藏中途引导

**自然语言流**：
回答或过程说明在生成期间持续增长的可阅读正文。
_Avoid_: 打字机文本、逐字输出

**结构化片段**：
具有整体阅读含义的路径、链接、代码、命令、文件变更或工具结果；片段不应被当作自然语言字符序列展示。
_Avoid_: 特殊字符串、长单词

**自适应追赶**：
会话界面依据语义边界、积压量、停顿和完成状态追上自然语言流的展示方式，不承诺固定的逐字符速度。
_Avoid_: 打字速度、固定流速

**会话创建操作**：
用户从任务发起、等待 Zeus 建立真实会话的一次后台操作；尚未建立运行内核原生会话或真实执行轮次时，进行中或失败的操作不属于真实会话。任务首发工作面可以把它投影为稳定列表行，但不能因此获得真实会话身份。原生会话与真实执行轮次已经建立后，即使该轮次随后失败，仍属于可恢复的真实会话。
_Avoid_: 临时会话、发送中会话、真实会话身份、隐藏真实失败轮次

**任务首发工作面**：
用户提交任务首发后立即进入、由会话创建操作承载的会话形态工作区；它即时展示本次冻结的首发内容，在会话列表以任务标题形成稳定行，并在首发消息后原位表达真实连接进度。真实会话建立后由同一列表行和工作面原位接管，连接提示随即消失，不重新挂载或替换用户现场。
_Avoid_: 临时会话、占位会话、已发送会话、列表行替换

**Zeus 专属 Codex 登录**：
Zeus 自己管理的 Codex 账号会话，只供 Zeus 启动的 Codex 运行内核使用；它与 Codex App 的登录状态彼此隔离。选择需要 OpenAI 认证的 Codex 模型时，登录就绪是创建会话的前置条件，未登录不能先生成必然失败的会话。
_Avoid_: 自动复制 Codex App 凭据、共享认证文件、创建后再提示登录

**登录完成回交**：
Zeus 确认专属 Codex 登录就绪后，把用户带回发起登录的 Zeus 工作现场并继续原操作的产品收口；外部认证网页上的产品跳转不属于该流程的必要步骤。
_Avoid_: 打开 ChatGPT、打开 Codex、等待用户手动切回、重新发起原操作

**会话归档**：
用户在会话没有活动执行、排队消息或待处理交互请求时，把已结束使用的会话移出普通列表的可恢复收纳动作；它同时保持 Zeus 产品会话与所属运行内核原生会话的归档状态一致，并保留消息、任务关联、执行上下文和工作目录。归档不改变任务管理状态，不清理任务工作区，也不执行 Git 操作。
_Avoid_: 删除会话、停止执行、完成任务、仅隐藏列表、清理工作区

**已归档会话**：
已经完成会话归档、默认不出现在普通会话列表中的会话；用户可以在设置中查看并恢复到原项目和原任务位置。
_Avoid_: 已删除会话、历史只读会话、已完成任务

**会话初始阅读位置**：
用户每次打开或重新切回已有会话时看到的首个消息位置；它固定为最新消息，不恢复上一次离开时的历史阅读位置，也不播放从旧消息滑向最新消息的过渡。
_Avoid_: 会话阅读进度、消息跟随状态、返回最新消息

**会话最新消息跟随**：
用户停留在当前会话底部时，自己刚发送的消息和随后新增的智能体内容持续保持可见；用户主动向上阅读历史后暂停跟随，直到用户返回最新消息。切换或重新打开会话不继承暂停状态，而是重新从最新消息开始。
_Avoid_: 会话初始阅读位置、强制置底、恢复历史阅读位置

**会话生命周期投影**：
同一会话当前生命周期在会话列表、任务表运行状态、会话抽屉和当前会话标题区的全部只读展示；各位置必须随权威会话状态自动收敛，不由点击、选中或切换页面触发刷新。
_Avoid_: 任务管理状态、会话选中态、组件局部状态、手动刷新结果

**会话列表展示状态**：
会话列表对每个会话当前生命周期的只读投影；后台会话进入响应、等待用户、等待批准、完成、失败、暂停或排队时都必须自动更新，点击会话只负责打开内容，不能承担刷新状态的职责。
_Avoid_: 选中态、任务管理状态、手动刷新结果

**会话阶段**：
会话列表展示状态中用于表达会话当前生命周期位置的阶段，例如排队、响应、待回复、待批准、完成、失败和暂停；同一阶段内的消息增长、阅读和配置同步不形成新阶段。
_Avoid_: 会话内容更新时间、选中态、任务管理状态

**会话阶段更新时间**：
会话最近一次从一个会话阶段进入另一个会话阶段的时间；只有阶段真正变化时才更新，并作为普通会话列表的首要倒序依据。
_Avoid_: 会话更新时间、最后打开时间、最后消息时间

**会话最后更新时间**：
会话自身内容或会话配置最近一次真实变化的时间；它不等于任务更新时间，也不作为按会话阶段排列列表的依据。
_Avoid_: 任务更新时间、会话阶段更新时间、最后打开时间

**侧边栏会话关注状态**：
项目侧边栏对该项目全部会话的聚合关注状态；待用户回答或审批的会话优先于排队、启动或执行中的会话，完成未读、暂停和失败不属于该状态。
_Avoid_: 会话列表展示状态、完成未读、当前页面

**运行状态入口**：
任务表运行状态对其所依据的最近关联会话提供的会话抽屉入口；用户留在任务列表即可查看并继续该会话，关闭抽屉不停止执行。任务没有关联会话时只展示“未启动”，不创建会话或打开任务详情。
_Avoid_: 会话页跳转、任务详情入口、自动创建会话、任意历史会话

**会话抽屉**：
在当前页面上层展示一条真实会话的完整交互空间；它允许查看消息、继续输入、处理待办交互和停止当前执行，关闭后返回原页面现场。
_Avoid_: 只读会话预览、独立会话页、临时会话、关闭即停止

**任务会话显示标题**：
任务会话在会话列表和会话工作区顶部用于识别归属任务的标题，只取所属任务的当前标题；它不包含任务编码或会话自身标题，同一任务的多个会话允许显示为同名。
_Avoid_: 任务编码拼接标题、会话记录标题、项目对话标题

**会话执行现场**：
会话实际绑定的工作目录，以及该目录当前所属的 Git 分支；任务会话的工作目录应是任务开发线对应的 worktree，而不是项目主工作区。任务会话缺少持久目录和工作区证据时必须显示“不可用”，目录不存在或不是 Git 仓库时必须显示真实的非 Git 状态，不能回退成项目主路径或推测分支。
_Avoid_: 项目本地路径、来源上下文路径、任务分支预期值

**会话可继续性**：
用户能否在原有上下文中继续发送消息的产品能力；底层会话的存储或运行状态不是用户术语。
_Avoid_: provider 归档、恢复并继续

**会话自动恢复**：
Zeus 重启或重新连接后，依据原生会话的权威状态把可安全续接的原会话恢复为可继续；它只恢复会话状态，不发送任何用户输入。
_Avoid_: 自动重发、自动重试、创建新会话

**发送触发续接**：
用户在没有阻塞式普通用户询问的原生会话底部输入并发送消息后，Zeus 只在当前产品会话内校准并继续原生线程；不能安全继续时保留当前输入并说明失败，不自动创建另一条会话。普通会话输入框在连接和上一轮失败时保持存在，普通用户询问显示期间暂时隐藏，回答、跳过或关闭询问后恢复。
_Avoid_: 需要恢复、自动承接会话、空白底部、询问与普通会话双输入区、回退项目主工作区

**显式另开会话**：
用户明确通过新建会话入口创建另一条产品会话的动作；原会话失败、连接中断或消息结果不确定都不能隐式触发该动作。
_Avoid_: 自动承接会话、发送普通消息、恢复原会话、静默分流

**阻塞交互请求**：
执行轮次向用户发出的、必须按原请求身份回答后才能继续的选择或批准；它不是普通会话消息，也不能用新增消息冒充回答。
_Avoid_: 普通提问、下一条消息、会话回复

**交互请求发现**：
Zeus 已知道执行轮次产生了交互请求、但尚未取得完整可回答内容的内部同步事实；它不构成用户待办，不能展示“需要回答”或隐藏普通会话输入区。
_Avoid_: 阻塞交互请求、待回复、请求详情加载卡

**远程接管**：
另一台已授权设备接入 Zeus 持有的同一个执行现场，并能回答当前阻塞交互请求、继续同一执行轮次的能力；远程用户操作的对象仍是 Zeus 会话，只同步会话内容或允许新增消息不算远程接管。
_Avoid_: Codex 会话接管、会话同步、远程查看、跨客户端新开轮次

**远程普通消息**：
用户在已授权远程设备的普通输入框中向 Zeus 持有的同一会话发送的新用户输入；它形成或引导真实执行轮次，属于原会话消息，但不属于阻塞交互请求的答案。
_Avoid_: 远程请求答案、普通用户询问、Zeus 本机提交、独立远程会话

**多端无感续接**：
用户在 Zeus 与已授权远程设备之间轮流向同一会话发送消息，所有输入、执行过程和结果按权威宿主接受顺序自动收敛到各端；切换设备不改变会话身份，也不要求手动刷新或重新连接。
_Avoid_: 多端同时执行、跨端复制会话、手动刷新同步、独立远程会话

**同步检查点**：
Zeus 已确认接纳到本地的最新 Provider turn 或 item 身份边界；断线或重启后只校准该边界之后的新内容，不用它扫描或补写功能修复前的历史缺口。
_Avoid_: 全量历史恢复、按正文猜测位置、本地时间戳、修复前消息迁移

**消息来源元数据**：
Zeus 为同步身份、幂等去重、诊断或低干扰来源图标而记录的本地结构化属性；它不是消息正文，可以驱动非 Zeus 本机用户消息底部的小手机图标，但当前 Provider 事件不能证明具体远程设备或平台，不得把图标解释为精确的 iPhone 身份，也不得发送给 Provider 或进入模型上下文。
_Avoid_: 来自手机文本、提示词前缀、正文标签、模型上下文、按正文猜测来源、精确设备归因

**未发送内容**：
已经被 Zeus 持久化、但尚未绑定任何 provider 执行轮次的用户输入；会话重新连接后它回到当前会话的可编辑草稿并等待用户确认，不自动带入另一条会话。
_Avoid_: 待发送、发送中、失败轮次、自动重试、自动承接会话

**消息结果不确定**：
Zeus 已尝试发送一条消息，但暂时无法证明 Provider 已接收或未接收的消息级状态；它只约束该消息不得自动重发，不锁死整个会话，也不创建另一条会话。
_Avoid_: 需要恢复、会话失败、自动重试、自动承接会话

**消息发送失败**：
Zeus 已从权威快照证明一条用户消息没有进入 submission、Provider 用户项或持久用户消息的消息级状态；原消息和可读原因继续留在时间线，原输入同时回到当前会话输入区供用户修改或安全重试。
_Avoid_: 会话失败、气泡消失、静默回填、自动重发

**发送中消息**：
用户已点击发送、Zeus 已在当前会话显示，但尚未取得 Provider 持久回放的本地消息投影；它应立即显示本地受信的文字和图片内容，同时保留发送中、引导中或排队中状态。Provider 持久消息到达后，它按稳定消息身份原位交接为权威消息，不重复显示或改变消息顺序。
_Avoid_: Provider 已确认、未发送内容、持久会话资源、等待回放后才显示

**队列消息**：
用户在当前会话或任务首发工作面尚不满足 Provider 发送条件时保存、等待当前或下一轮执行的后续消息；等待条件解除后按顺序自动处理，并允许在等待期间编辑、删除、调整顺序或在当前执行可接收时引导。真实会话尚未建立只改变后台调度时机，不能禁用用户输入。
_Avoid_: 已发送消息、普通用户消息、引导消息、只写“排队中”而不说明等待原因

**引导消息**：
用户已明确选择补充给当前执行轮次、正在等待 Provider 确认接收的会话输入；它不再是队列消息，也不会创建新的下一轮。
_Avoid_: 队列消息、待发送消息、立即发送消息

**会话输入区**：
用户为当前会话编写下一条普通消息、添加附件并调整下一轮配置的底部工作面；权限模式和计划模式位于左侧，上下文占用、速度模式、完整模型名称和推理强度位于右侧。活动轮次期间 Enter 默认形成队列消息，“引导”属于具体队列消息，不在输入区常驻。
_Avoid_: 会话框、执行轮次控制台、阻塞交互请求、当前轮配置

**执行轮次**：
会话中一次从用户输入开始、以 AI 完成或被中断结束的执行过程。
_Avoid_: 暂停会话、暂停 Runtime

**停止活动工作并退出**：
普通退出时向各 Agent 请求中断后立即强制结束本机执行宿主、取消尚未开始的队列消息并关闭 Zeus 的用户动作；活动轮次对用户显示为已中断，历史会话、已完成消息和既有文件变更继续保留。它不表示已经撤销或确认停止发往外部系统的副作用。
_Avoid_: 终结所有对话、删除会话、归档会话、清空文件变更

**会话失败原因**：
真实会话或执行轮次失败后留在原会话时间线中的可理解说明；它包含恢复建议，并允许查看经过脱敏的技术详情，但不泄露密钥、完整路径或堆栈。
_Avoid_: 本轮失败、全局错误横幅、原始日志直出、创建操作失败

**上下文整理**：
智能体运行内核在长会话接近容量时，压缩较早对话与过程信息以继续当前执行的正常活动；它不代表会话被清空、执行失败或开启了新会话。
_Avoid_: 本轮错误、会话清空、新会话、失败重试

**活动计划进度**：
输入区上方对当前未终止执行轮次计划步骤的临时投影；轮次终止后立即隐藏，不回退展示历史计划。
_Avoid_: 最近计划、历史计划、计划产物

**智能体运行内核**：
承载模型调用、工具循环、上下文处理、会话事件和扩展接入的底层执行能力；它可以由 SDK 或独立运行时提供，但不包含完整产品的界面、状态投影、安全治理和运维闭环。
_Avoid_: 完整智能体产品、模型本身、界面功能清单

**智能体客户端协议**：
产品客户端与智能体运行内核之间交换命令、响应、事件和交互请求的边界；协议可以由独立子进程提供，也可以被同进程 SDK
调用替代，但协议形状不等同于运行内核行为。
_Avoid_: 模型 API、智能体运行内核、产品界面

**模型供应源**：
向智能体运行内核提供模型推理能力的账号、API 或本地服务；相同模型供应源可以被不同运行内核使用，不能据此推定工具、提示、会话、权限或恢复语义相同。
_Avoid_: 智能体运行内核、客户端协议、产品功能对齐

**模型发行方**：
创建并维护一个模型系列的组织；它与实际提供 API、计费、区域和容量的接入渠道可以不同。
_Avoid_: 模型供应源、接口域名、托管平台

**模型接入渠道**：
本次请求实际使用的 API 服务、区域、计费计划和部署来源；同一模型通过官方直连与阿里云百炼等平台托管时属于不同接入渠道，能力必须分别验证。
_Avoid_: 模型发行方、模型名称、全局供应商能力

**用户配置模型供应源**：
用户在 Zeus 中保存名称、接口类型、服务地址、凭据状态和模型目录的模型供应源；凭据明文不属于该配置，供应源也不因此成为独立 Agent。
_Avoid_: 自定义 Agent、API Key 配置、模型别名

**模型连接配置**：
Zeus 全局保存的一套模型 API 接入身份，包含展示名、接入渠道、服务地址、协议和凭据状态；同一渠道的不同账号或密钥分别保存为多个连接，凭据明文只进入系统安全存储。
_Avoid_: 项目模型配置、供应商全局能力、明文密钥

**模型选择身份**：
Zeus 在任务推送、会话续接和逐轮改选中定位具体模型的唯一身份，由模型连接 ID 与该连接内的模型 ID 共同组成；连接展示名和裸模型名只用于显示，不能在撞名时替代身份或猜测供应源。
_Avoid_: 裸模型名、供应商展示名、按列表顺序命中

**项目可用模型集**：
项目从全局模型连接中明确允许任务推送和会话使用的多个模型，以及其中一个默认选中项；默认值只决定入口初始选择，不限制用户改选其他可用模型。
_Avoid_: 唯一模型、全局模型目录、会话固定模型

**会话逐轮模型选择**：
同一 Pi 会话在没有活动执行时，为下一条用户输入改选项目可用模型的能力；选择只对下一次派发及其后续轮次生效，每轮必须保存实际连接、模型和能力档案。执行期间不得切换，模型历史无法兼容时必须提示新建会话；Codex 与 Pi 等不同 Agent 身份之间不能通过该能力切换。
_Avoid_: 执行中热切换、跨 Agent 切换、重写历史模型、默认模型

**模型能力档案**：
Zeus 针对一个模型保存的可选能力及其证据，包括推理等级、服务档位、工具调用、输入类型和用量等；能力状态必须区分已支持、不支持和未验证。
_Avoid_: 模型列表、供应商宣传能力、默认全部支持

**自动能力识别**：
Zeus 根据模型接入渠道、模型目录声明、内置模型档案和真实运行结果生成模型能力档案的过程；普通用户只查看识别结果，不负责选择协议字段、推理等级映射、工具调用或图片输入能力。无法形成可靠证据时保留未验证状态，不以模型名称猜测为已验证。
_Avoid_: 用户手工能力配置、默认全部支持、模型名称即能力证据

**高速模型变体**：
发行方以独立模型 ID 提供的高速、Flash、Turbo 或 High-Speed 模型；选择它属于切换具体模型，不属于当前模型上的服务档位开关。
_Avoid_: Fast 服务档位、关闭思考、降低推理等级

**运行内核适配器**：
Zeus 连接一种智能体运行内核并保留其原生会话、能力和交互语义的产品边界；一个适配器可以服务多个模型供应源，但不得把另一运行内核的能力名称冒充为自身能力。
_Avoid_: 模型适配器、最低公共功能、协议名称复用

**Agent 身份**：
Zeus 用于区分完整智能体工作方式的稳定身份；当前原生基线是 Codex，通用多模型扩展路径是 Pi，未来 Claude Code 只能作为另一套独立
Agent 接入。Agent 身份不表示模型供应源。
_Avoid_: provider、模型品牌、模型 ID、传输协议

**通过 Pi 使用模型**：
由 Pi 负责工具、上下文与会话，并把 Grok、Kimi、DeepSeek 或 ZAI/GLM 作为模型供应源的运行方式；产品文案必须表达为“Pi Agent ·
模型供应源”，不能简称为该供应商的原生 Agent。
_Avoid_: Kimi Agent、Grok 原生 Agent、官方 Coding Agent 等价

**Pi 阶段回答**：
Pi 在一次工具循环中为说明下一步动作而结束的非最终 assistant
消息；它可以作为执行中的阶段提示显示，但不能把同一轮次的回答投影提前封口。最终回答到达时，同一轮次的阶段回答必须被完整替换。
_Avoid_: 最终回答、独立会话消息、轮次完成

**Pi 最终回答**：
Pi 在一次执行轮次结束前返回、停止原因为正常停止或长度结束的最终 assistant 消息；它是该轮次回答投影的完成内容，必须覆盖此前显示的阶段回答，并与持久消息正文保持一致。
_Avoid_: 工具调用前说明、思考摘要、首段 assistant 消息

**Pi 工具安全接管**：
Pi 提出文件、命令或项目边界相关工具动作后，由 Zeus 在执行前统一判断权限并承接用户审批；未获得明确允许时动作不得执行。
_Avoid_: Pi 直接全权限、项目可信即授权、执行后补审计

**Agent 能力证据**：
Zeus 针对特定 Agent、二进制与协议版本完成真实运行核验后保存的能力结论；状态区分已支持、不支持和未验证，静态代码、公开文档或模型目录不能单独构成已支持。
_Avoid_: 功能开关、代码已存在、模型可调用、SDK 支持

**Pi 工作进程**：
由 Zeus 独立执行宿主管理、通过 Pi RPC 与单个活动 Pi 会话通信的隔离子进程；它不属于 Renderer 或 Electron Main，也不能仅凭
PID 在宿主崩溃后被新宿主视为可接管。
_Avoid_: Zeus 执行宿主、Codex App Server、Pi 模型供应源、同进程 SDK

**公共框架状态**：
Agent 的代码契约、路由或数据结构已经存在，但尚未完成真实模型、安全与恢复验收的内部状态；该状态默认对普通用户隐藏，并且所有执行能力均视为未验证。
_Avoid_: 实验支持、正式支持、即将可用入口、灰色模型选项

**产品编排层**：
Zeus 自身负责的任务、项目、工作区、运行内核适配器生命周期、产品会话投影、界面交互、异常恢复和能力治理；它可以承接运行内核发出的工具或审批请求，但不因此拥有模型驱动的提示、上下文与工具循环。
_Avoid_: 智能体运行内核、Codex core、重新实现 agent harness

**能力驱动界面**：
Zeus 保持统一会话主路径，并依据当前运行内核适配器真实声明且已验证的能力展示操作与状态；未支持的能力必须隐藏、禁用或说明边界，不能由模型名称推断。
_Avoid_: 所有运行内核强制同功能、各运行内核复制整套界面、功能占位

**双层会话身份**：
一个 Zeus 产品会话同时保留自身稳定身份与所属运行内核的原生会话身份；两者建立明确绑定，但不互相冒充，也不因界面导航或模型选择而静默改绑。
_Avoid_: 只保存界面会话、只保存 provider 会话、跨运行内核复用原生身份

**跨内核会话承接**：
用户希望从一种智能体运行内核改用另一种时，Zeus 显式创建新会话并带入用户确认的必要上下文；原会话及其原生身份保持不变，不把历史转换伪装成原生续接。
_Avoid_: 原会话静默切换运行内核、自动重放全部历史、从零新建

**产品级智能体能力**：
编码智能体面向真实用户持续提供可用结果的整体能力；它同时包含运行内核、会话生命周期、状态一致性、执行安全、异常恢复、可观测性和交付运维，不能由底层
SDK 的功能覆盖单独证明。
_Avoid_: 演示可用、SDK 功能数量、模型能力

**Token 用量**：
Codex 为当前会话报告的累计输入、输出与总 Token 精确数量；它不表达账户限额、服务档位或上下文剩余量。
_Avoid_: 账户限额、服务档位用量、上下文余量

**上下文占用**：
智能体运行内核为当前会话报告的、当前有效上下文已占模型上下文窗口的比例；它不等于当前会话累计 Token 用量，也不表达账户限额或服务档位用量。
_Avoid_: Token 用量、账户限额、服务档位用量、累计输入 Token

**Token 紧凑显示**：
Token 用量在界面中的派生读数，按千、百万和十亿显示为 K、M、B，同时保留可读取的精确数量；它不改变底层 Token 用量。
_Avoid_: 计量单位升级、近似用量、改写 Token 数

**菜单栏 Zeus 入口**：
macOS 菜单栏中由 Zeus 主进程维护的常驻图标，是应用窗口隐藏时查看用量摘要和执行应用级动作的入口。
_Avoid_: 窗口状态栏、标题栏 Zeus 按钮、应用菜单

**菜单栏用量浮窗**：
由菜单栏 Zeus 入口唤起、按模型供应源切换的只读统计浮窗；当前供应源只展示其真实可得的账户限额、重置时间与 Zeus 本地用量，不把不同口径合计，也不提供购买、重置或消费动作。
_Avoid_: 原生右键菜单、顶部 Zeus 弹层、完整用量页面

**全部供应源摘要**：
菜单栏用量浮窗中不筛选模型供应源时显示的逐供应源摘要列表；列表只包含 Zeus 本地账本曾记录真实 Token 的供应源，每个供应源独立表达其最关键余量、重置时间和数据新鲜度，不产生跨供应源配额、Token 或费用总数。
_Avoid_: 全部供应源合计、统一剩余额度、跨供应源总 Token

**用量浮窗当前供应源**：
用户在菜单栏用量浮窗最后一次明确选择的“全部”或具体模型供应源；下次打开继续使用该选择，目标不可用时回退到“全部”。
_Avoid_: 项目默认模型、最近运行模型、自动最紧迫供应源

**供应源用量口径**：
一个模型供应源能够真实提供的用量事实集合；订阅口径以限额窗口、余量和重置时间为主，API 口径以 Zeus 本地 Token、调用量和估算费用为主，缺失事实保持不可用而不跨口径补位。
_Avoid_: 统一供应商字段、订阅与 API 混算、用估算补官方数据

**官方积分余额**：
模型供应源明确返回的当前账户积分余额；它必须标明供应源身份，不能由 Zeus 根据 Token 与公开价格推算。
_Avoid_: 估算点数、API 等价费用、配额剩余

**近 7 日估算费用**：
Zeus 根据一个模型供应源近 7 日的本地 Token 账本、模型实际身份和带日期价格目录推算的美元成本；它适用于全部供应源详情，但不是供应源账单，缺少价格时保持不可用。
_Avoid_: API 等价、实际费用、官方账单、零费用

**今日 Zeus Token**：
按本机当前日界线汇总、已经进入 Zeus 本地用量账本的单个模型供应源 Token；它只覆盖 Zeus 内使用，不代表该供应源账户在其他客户端的今日总量。
_Avoid_: 今日官方 Token、账户今日 Token、跨供应源今日总量

**账户今日 Token**：
模型供应源明确提供且日期口径可证明的账户级今日 Token；缺失当天数据时保持不可用，不能以零代替，也不能由今日 Zeus Token 回填。
_Avoid_: 今日 Zeus Token、昨日 Token、缺失即零

**用量最近成功快照**：
Zeus 对每个模型供应源最后一次成功读取并持久保存的用量事实；菜单栏浮窗打开时先显示该快照，再在后台校准并明确标注数据新鲜度。
_Avoid_: 加载占位、当前实时值、离线估算

**会话运行摘要栏**：
位于会话顶部、汇总 Token 用量、缓存命中率并可展开运行详情的完整横栏；宽屏常驻环境卡片必须位于该栏及其展开详情下方，不能覆盖或压缩摘要内容。
_Avoid_: Token 消耗横栏、环境卡片背景、仅 46px 固定摘要行

**速度模式**：
用户为受支持模型选择标准或 Fast 处理速度的下一轮配置；它在界面中由闪电直接切换，独立于模型身份和推理强度，底层映射到运行内核的服务档位参数。
_Avoid_: 模型、推理强度、跟随 Codex、性能模型

**标准速度**：
用户明确要求同一模型使用常规处理速度与常规用量规则的速度模式。
_Avoid_: 跟随 Codex、低推理强度、慢速模型

**Fast 模式**：
模型目录为受支持模型提供的加速服务档位；它提高同一模型的处理速度并增加用量，不改变模型身份或推理强度。
_Avoid_: 低推理强度、Codex Spark、快速模型

**当前轮配置**：
运行服务开始当前轮次时已经采用的模型、推理强度、速度模式、权限模式与计划模式；当前轮次结束前不会被后续选择改变。
_Avoid_: 下一轮配置、项目默认值、界面当前选项

**下一轮配置**：
用户为同一会话最后选择并持久保留的一组配置；所有尚未开始执行的轮次在启动时统一采用这组最新选择。
_Avoid_: 当前轮配置、入队时快照、项目默认值

**会话工作态**：
只属于一条具体会话的未发送文字、待提交资源与下一轮配置；用户修改后立即持久保留，切换会话或重启 Zeus 后仍恢复到原会话，发送成功只清空本次文字和待提交资源。它不能被其他会话、本地旧快照或项目类型默认配置覆盖。
_Avoid_: 项目默认值、当前轮配置、跨会话草稿、发送后重置配置

**项目会话类型默认配置**：
用户在一个项目内为任务开发、冲突处理、代码审查或项目普通会话最近选择的模型、推理强度、速度模式、权限模式与计划模式；选择变化时立即记忆，只初始化以后新建的同类会话，不追改其他已有会话。所选模型不支持 Fast 时，速度回退为标准且闪电不可点。
_Avoid_: 会话工作态、项目统一默认值、追改历史会话、跨会话类型共享

**任务推送能力快照**：
用户打开任务推送工作面时取得的模型、账户、仓库和可选上下文能力集合；模型与仓库等创建必需能力缺失时必须阻止创建，可选会话、附件、父任务或关联任务集合缺失时按没有可选内容处理，并保证预览与最终创建使用同一语义。
_Avoid_: 任务正文、项目默认配置、把缺失必需能力当空集合、预览与创建分别猜测

**尚未开始轮次**：
已经排队或将在之后提交、但运行服务尚未开始执行的会话轮次。
_Avoid_: 当前活动轮次、已完成轮次、会话草稿

**无损升级**：
升级过程中，仍在持续生成结果或产生副作用的执行阶段继续由原运行环境负责；轮次到达等待用户回答、等待审批或其他已经持久化的可恢复阶段边界后，可以切换执行宿主，并由新宿主恢复原会话继续处理，不能重跑已经完成的工作或重复产生副作用。
_Avoid_: 会话记录不丢、升级后重新执行、等待全部任务完成后升级、等待交互时长期保留旧宿主

**升级切换**：
用户确认升级后，旧界面退出、新界面启动并重新接管现有执行轮次展示与交互的计划内版本切换。
_Avoid_: 普通退出、应用崩溃、系统重启

**应用启动就绪**：
Zeus 主窗口与工作台已经正常打开，用户可以读取现有项目、任务和会话，并继续使用上一正式版已经提供的新建会话、任务推送和执行能力；执行宿主正在交接时，只能限制旧宿主从未支持的新版专属能力，不能把整个应用关闭、白屏或转入通用崩溃页。
_Avoid_: 执行宿主已经切换、全部新版能力可写、只显示等待页、进程已经存在、交接期间禁止新会话

**可恢复阶段边界**：
执行轮次已经停止持续生成结果和产生副作用，当前状态、原生会话身份及待处理交互已经持久化，可以关闭旧执行宿主并由新宿主恢复的时点；等待用户回答、等待选择和等待审批都属于该边界，不要求再保持一段空闲时间。
_Avoid_: 整个会话完成、所有任务空闲、固定十秒等待、直接丢弃待处理交互

**宿主交接检查点**：
旧宿主关闭前只读采集并由新宿主恢复的待回复请求身份，包含来源宿主、原会话、原运行代、请求类型和采集时间；它不携带用户尚未提交的答案，也不能作为跨操作授权。用户回复后必须重新校验原请求，审批类决定只适用于检查点记录的精确操作。
_Avoid_: app-server 实时请求通道、重复执行凭证、通用审批、第二数据库写入者

**交互恢复检查点**：
Codex app-server 请求通道退出后，由 Zeus 保留的待回复交互投影；它允许用户在同一产品会话中提交原问题的答案，但不冒充新
app-server 的原生请求。答案必须先按原请求重新校验，再创建幂等的续接轮次；命令、文件和权限决定仍需在新轮次重新明确申请。
_Avoid_: 当前代原生请求、直接回复旧 requestId、自动重跑旧轮次、跨操作授权

**宿主安全交接**：
新版界面暂时连接仍在持续执行存量工作的旧版执行宿主，并在所有活动轮次到达可恢复阶段边界后立即关闭旧宿主、释放数据库写入权，再由唯一新版宿主恢复原会话的过程；交接期间应用保持启动就绪，旧宿主不兼容的新能力只在对应局部明确受限。
_Avoid_: 应用启动等待、等待交互时继续保留旧宿主、固定空闲观察期、双宿主写库、通用界面崩溃

**公开稳定版**：
通过不可变 Git 标签、GitHub Release、更新清单和 Homebrew Cask 对外提供的正式版本事实；同一版本不得静默替换二进制。
_Avoid_: 升级切换、测试包、覆盖发布

**发布阶段摘要**：
面向发布操作者的当前阶段、可验证进度、真实结果和恢复动作说明；它不以工具片段关键词代替发布编排的权威状态。
_Avoid_: 原始终端输出、关键词命中、Runtime 会话状态

**发布原始详情**：
一次发布中 Git、GitHub CLI、Node 和各校验工具的真实输出证据；它用于展开排查和导出，不是用户默认阅读的阶段结论。
_Avoid_: 发布阶段摘要、友好错误结论、成功证明

**发布安全续跑**：
发布因网络超时或临时故障中断后，从已复验的真实阶段继续的恢复过程；只读操作可以有界重试，远程写入结果不确定时必须先核对外部事实。
_Avoid_: 无条件重跑、重复远程写入、猜测上次失败

**日常正式应用**：
用户日常启动和持续使用的唯一生产身份 Zeus；当前本机版本必须以它的真实应用包和运行进程为准。
_Avoid_: 测试应用、发布候选应用包、开发验收包

**测试应用**：
所有开发、任务 worktree、临时验证和真实运行验收共同使用的 Zeus Test 身份；不同版本可以存在多份，但都必须与日常正式应用隔离名称、应用身份、单实例锁和用户数据。
_Avoid_: 日常正式应用、生产身份副本、发布候选应用包

**发布候选应用包**：
正式发布链路生成、用于封装公开安装制品的生产身份应用包；它只接受结构、签名、安装介质和清单校验，不作为本机 GUI 验收入口启动。
_Avoid_: 日常正式应用、测试应用、开发验收包

**实用发布**：
缺少 Developer ID 签名或 Apple 公证时，仍以明确限制对外提供的 ad-hoc DMG 与 Homebrew 版本；它只支持用户确认后的手动安装，不代表自动安装已经启用。
_Avoid_: 已公证发布、静默升级、测试包

**Homebrew 托管升级**：
Zeus 向用户展示下载和重启决策，并把日常正式应用的安装与版本登记交给其已配置的 Homebrew Cask 完成；Zeus 不在该路径中自行替换应用包。
_Avoid_: 打开下载页、Zeus 内置安装、静默升级

**更新预取**：
在不替换日常正式应用的前提下，把目标 Homebrew Cask 产物下载到本机缓存并完成完整性校验的准备阶段。
_Avoid_: 升级完成、应用已替换、已重启

**待安装更新**：
更新预取已完成、目标产物已在本机缓存中通过完整性校验，但日常正式应用尚未替换的状态。
_Avoid_: 已安装更新、新版已生效、等待普通重启

**非阻断更新进度窗口**：
由 macOS 原生界面展示更新预取阶段和可验证进度的独立窗口；它不锁定 Zeus 主窗口，不暂停用户输入、会话或执行轮次。
_Avoid_: Zeus 内容弹窗、模态阻塞、系统通知、伪原生样式

**执行宿主**：
拥有执行轮次、交互请求和执行事件处理责任的本机运行边界；它的生命周期由尚未结束的工作决定，不从属于界面是否打开。
_Avoid_: Zeus 窗口、会话记录、任务

**界面租约**：
Zeus 界面附着到执行宿主并使用其本机能力的临时关系；租约失效只表示需要重新附着，不代表执行宿主、会话或历史已经结束。
_Avoid_: 应用退出、宿主关闭、会话中断

**会话历史可用性**：
Zeus 是否已经从权威本机来源取得当前会话历史的可读状态；暂不可用表示尚未成功读取，不能被展示为空会话或历史丢失。
_Avoid_: 空会话、历史已删除、发送第一条消息

**业务数据可用性**：
Zeus 是否已经从权威本机数据目录取得项目、任务、会话及其关联资源的可读状态；新建空库、错误数据目录或迁移未发生只表示数据尚未加载，不能被解释为历史数据已删除。
_Avoid_: 空白侧边栏、空数据库、数据丢失

**本机数据资产**：
Zeus 及其运行内核在本机保存、并具有明确所有者和保留意义的数据；它必须先按生命周期判断，不能仅凭目录名或文件大小决定是否清理。
_Avoid_: 杂项文件、都叫缓存、可直接删除目录

**分层本机数据根**：
Zeus 业务数据使用的权威根目录；其顶层固定为 `data`、`artifacts`、`providers`、`backups`、`runtime` 和 `profile`，Electron Profile 只是其中 `profile/electron`，不等同于业务数据根。
_Avoid_: Electron userData、平铺杂项目录、模块自行拼接根路径

**旧数据根**：
目录迁移前曾被 Zeus 使用、且在受管路径完成重绑前仍可能承载引用的外部根目录；它只作为短期迁移回滚源，满足镜像校验、零受管引用、数据库健康和执行宿主退出后应被显式物理回收。
_Avoid_: 永久备份、第二权威数据源、运行中直接删除

**核心事实**：
项目、任务、会话关系、设置和审计等不可由其他本机来源完整重建的数据资产。
_Avoid_: 缓存、临时文件、Provider 原生历史

**托管资产**：
由 Zeus 负责保存、校验引用和执行生命周期管理的附件、变更恢复材料或运行产物；没有当前引用不等于可以立即物理删除。
_Avoid_: 任意本地文件、可重建缓存、Provider 资产

**可重建缓存**：
删除后只会产生重新计算或重新下载成本、不改变登录、授权或业务事实的数据资产。
_Avoid_: Cookie、站点授权、图谱业务来源、托管附件

**Provider 资产**：
由 Codex、Pi 等运行内核拥有格式和生命周期的原生会话、状态库、插件运行包与安装现场；Zeus 可以展示占用或调用正式管理能力，但不把它们当普通缓存强删。
_Avoid_: Zeus 核心事实、Zeus 托管资产、可随意清理的 Agent Home

**隔离回收**：
候选资产先离开活动目录、保留可恢复宽限期并完成引用复验，之后才允许物理删除的回收状态。
_Avoid_: 立即删除、回收站等于已释放空间、无引用即垃圾

**会话热缓存**：
Zeus 在本次应用运行期间保留的、已经成功读取过的真实会话快照；用户切回会话时先展示该快照，并在后台读取权威状态完成校准。它不会跨应用重启持久化，也不能把刷新失败伪装成内容最新。
_Avoid_: 权威会话状态、磁盘持久缓存、假加载数据

**关键会话**：
正在处理、等待用户回答、等待审批、正在停止、排队或包含消息结果不确定项的会话；这些会话的热缓存不参与普通数量淘汰。其他会话只保留最近使用的十个。
_Avoid_: 当前选中会话、全部历史会话、普通最近会话

**运行时世代**：
一组由相同执行引擎和协议能力承载的执行轮次；执行轮次一旦开始就固定归属该世代，后续升级不会迁移其执行责任。
_Avoid_: 应用版本、会话版本、任务版本

**当前运行时世代**：
执行宿主已激活且唯一用于承接新执行轮次的运行时世代；升级只切换新轮次的默认归属，已经开始的旧轮次继续归原世代。
_Avoid_: 最新应用界面、新执行宿主、把已开始轮次迁移到新版本

**排空世代**：
不再接收新执行轮次、只负责完成已有执行轮次与待处理交互的运行时世代；所有归属轮次结束后才可退出。
_Avoid_: 已停止 Runtime、等待升级、强制清理

**恢复确认**：
后台宿主或机器级异常后，Zeus 无法证明原执行轮次仍在继续或已经结束时要求用户作出的恢复选择；未知状态不得自动重跑。
_Avoid_: 自动续跑、失败重试、普通审批

**会话关注状态**：
会话中存在用户尚未真实看见或仍需处理的新内容时，对其当前最高优先级含义的统一表达；能够识别待批准、需要回复、正在响应、已完成、失败或已中断时使用专属状态，否则回退为普通未读。
_Avoid_: 会话生命周期、任务管理状态、同时堆叠多个提示

**普通未读**：
会话已经产生用户可见的新内容、但 Zeus 无法把它归入更具体专属关注状态时的兼容性保底状态；界面使用蓝色小圆点表达。
_Avoid_: 完成未读、思考过程更新、工具日志增长

**会话已读确认**：
Zeus 处于前台、目标会话正文真实显示且最新关注内容已经进入可见区域时形成的阅读事实；仅选中会话、停留在其他页面、窗口被遮挡或向上阅读历史都不构成确认。
_Avoid_: 会话选中态、通知点击、打开项目

**思考摘要**：
智能体运行内核在执行轮次中提供的阶段性可读说明；它保留来源记录中的原文和原始语言，有中文时显示中文，没有中文时保留英文。
_Avoid_: 完整思考过程、翻译结果、最终回答

**思考占位状态**：
执行轮次已经开始、但尚未出现新思考摘要、工作活动或回答时的临时过程反馈；它不是运行内核返回的正文，也不与真实反馈同时存在。
_Avoid_: 完整思考过程、第二条思考摘要、永久运行状态

**轮次耗时入口**：
展示单次执行轮次实际处理时长并控制该轮过程内容展开或收起的入口；运行中位于过程内容末尾，完成后位于最终回复正文上方，展开的历史过程显示在入口下方。
_Avoid_: 思考摘要、固定底栏、模型回复正文

**PLAN 协作模式**：
用户明确要求 Codex 先分析并形成可确认计划的会话提交模式；它逐次随提交记录，不等同于计划步骤进度，也不代表已经授权实施。
_Avoid_: 计划进度、自动执行模式

**计划产物**：
app-server `type: "plan"` item 所承载的持久化 Markdown 计划正文；消息流计划卡片与右侧计划工作区只是同一份内容的两种视图。
_Avoid_: 计划进度、复制出的文件、套餐

**计划实施请求**：
PLAN 执行轮次成功完成并形成非空计划产物后，由 Zeus 持久化生成的独立确认请求；它负责实施、修订或跳过计划，不伪装成 app-server
`request_user_input`。
_Avoid_: 普通用户询问、审批请求、计划产物

**普通用户询问**：
app-server `request_user_input` 发出的阻塞式问题，按 provider 问题与选项原样回答；它与计划实施请求使用相近视觉原语，但协议、生命周期和响应规则独立。
服务端接收、答案校验和界面显示共用同一份问题读取规则；界面只关心能否构造回答所需的问题内容，忽略信封中的全部未知元数据。
_Avoid_: 计划实施请求、命令审批、请求信封白名单、用协议完整性阻止问题显示、已落库但不可见的问题

**已回答询问记录**：
普通用户询问提交后的持久对话记录；一次询问只形成一条记录，多问题按原顺序归在同一区域。记录直接显示问题全文、当时提供的全部选项和用户实际选择，不要求再次展开；敏感回答仍保持脱敏。它按答案实际提交时间进入会话时间线，表达一次交互已经发生，不重新创建普通用户消息，也不允许再次提交。
_Avoid_: 待回答询问、普通用户消息、可重复提交的回答卡

**冲突处理尝试**：
用户每次显式发起 AI 冲突处理所创建的一次独立候选合入；每个尝试拥有自己的 detached integration worktree 和会话身份，不创建临时业务分支，也不与其他尝试共享可写目录。候选结果只有在目标分支 HEAD 仍等于尝试创建时冻结值时才能安全落地；其他尝试随后标记为过期，不自动覆盖。
_Avoid_: 共用冲突工作区、临时业务分支、并行覆盖、自动落地过期结果

**自定义答案入口**：
普通用户询问允许用户不选预设项、改为输入自己答案的 Other 入口；启用入口只切换当前问题的回答方式，键盘输入形成该问题的草稿，提交后仍通过同一个 `request_user_input` 请求完成回答。
_Avoid_: 会话自由输入、普通消息输入框、计划修订意见

**自定义回答结果**：
用户通过自定义答案入口实际提交的当前问题答案；它与预设答案一样属于已回答询问记录中的真实选中项，不是附注。
_Avoid_: 用户备注、补充文字、未选择答案

**跳过询问**：
用户通过普通用户询问中的“跳过”或关闭入口，明确以空答案结束当前询问的动作；它不会中断当前执行轮次。询问自由输入状态中的任何键盘按键都不属于跳过询问。
_Avoid_: 中断当前执行、关闭输入框、键盘误触

**询问自由输入状态**：
普通用户询问的自定义答案或纯自由输入已经获得编辑焦点的状态；数字、方向键、空格和普通字符只编辑当前草稿，不选择、跳过或关闭询问。Enter 提交当前答案，Shift+Enter 插入换行，输入法组合期间的 Enter 只确认输入法候选。Escape 只退出编辑焦点，并保留当前回答方式和完整草稿。
_Avoid_: 会话自由输入、答案数字快捷键、普通会话消息、键盘误选、跳过询问

**答案行**：
普通用户询问中承载一个可选答案的完整横向单元，由编号、主描述、可选推荐标记、补充说明和选择反馈组成。
_Avoid_: 选项文案、问题行、自定义回答行

**主描述**：
答案行中用于识别答案核心含义的短文本，例如“安全自动恢复”；它与可选推荐标记组成标题区，随整行内容垂直居中，并为同一组选项的补充说明提供统一起点。
_Avoid_: 补充说明、答案全文、标题

**补充说明**：
答案行中解释该答案条件、影响或代价的辅助文本；同一组选项共享统一左侧起点，并根据剩余宽度自然换行。
_Avoid_: 主描述、推荐标记、问题描述

**自定义回答行**：
普通用户询问中让用户输入未列出答案的无边框输入行；点击铅笔或输入行任意位置都会启用并聚焦输入，初始高度遵循普通答案的 `44px` 最小节奏，选中并输入多行内容后可以继续增高。
_Avoid_: 答案行、补充说明、会话输入框

**类似命令授权**：
用户接受 provider 明确提出的一条命令规则，使符合该规则的后续命令无需逐次批准；它只覆盖展示给用户的确切规则。
_Avoid_: 本会话允许、允许所有命令、自动审批

**单次允许**：
用户只批准当前一条命令或当前一次文件修改请求；协议分别使用对应请求类型的 `accept` 决策。
_Avoid_: 始终允许、自动审批

**会话文件允许**：
用户在只读会话中明确允许当前会话后续的全部项目工作区文件编辑；Zeus 持久记住这项会话级例外，并对后续项目内文件审批真实放行。它不允许命令、项目外路径、Git 提交、推送或其他高风险操作，也不改变其他已有会话。
_Avoid_: 项目永久授权、允许任意路径、自动模式、完全访问、关闭全部审批

**自动模式**：
以项目目录为可写根并按需审批的权限模式；普通项目内编辑通常直接执行，高风险命令、网络或越界访问仍可能申请审批。
_Avoid_: 完全访问、永不审批、只读

**会话权限模式**：
用户为一条会话选择、并由该会话当前轮次执行边界实际采用的只读、自动或完全访问级别；创建期工作面、会话控件和持久记录只是同一权限事实的投影，不能各自形成不同权限。
_Avoid_: 界面权限标签、任务代码开关、工作区类型、单次审批

**会话完全访问确认**：
用户为现有会话选择完全访问前作出的独立风险确认；它只覆盖该会话的权限模式切换，不代表任务首发选择或外部服务授权。
_Avoid_: 普通审批、任务首发权限、外部应用授权

**任务首发**：
以任务内容作为首条用户消息创建新会话和独立推送工作区的用户动作；界面名称为“推送到新会话”，每次都从本次选择的来源基线开始，不复用历史会话或其他推送的任务分支。
_Avoid_: 创建流程、Runtime 启动、复用任务环境

**任务首发成功**：
任务对应的真实会话已经建立，且首条用户消息已经进入执行轮次；仅点击推送、只建立会话或首轮发送失败都不属于成功。
_Avoid_: 点击推送、会话已创建、等待重试

**精简任务首发**：
只把任务标题、任务首发有效字段、用户明确选择的上下文和补充信息作为用户输入发送的任务首发；完全为空的内容字段、任务类型、标签、标签附件、执行目录、模式、权限与其他运行配置不属于用户消息正文。
_Avoid_: 未提供占位、完整任务包装、系统提示词正文、来源上下文 JSON、任务管理元数据

**本次推送附件**：
用户在任务首发的补充信息区域临时添加、只归属于这次新会话首条输入的结构化资源；它不写回任务附件，也不影响后续其他任务首发。
_Avoid_: 任务附件、补充信息正文、会话后续附件、永久附件

**子任务首发上下文**：
用户为一次子任务首发显式选择的祖先任务信息、会话 JSONL 文件路径和附件；它只影响这次新会话的首条输入，不改变任务父子关系，也不追改既有会话。父任务信息进入正文，会话以真实本机绝对路径进入正文，附件继续使用结构化附件通道。
_Avoid_: 自动继承全部历史、任务关系变更、追改历史会话、附件路径正文

**中断当前执行**：
终止会话中正在进行的执行轮次，同时保留会话及其历史，使用户之后仍可继续发起新的执行轮次。
_Avoid_: 暂停任务、停止会话

**MCP 服务标识**：
app-server 在 MCP 启动状态中提供、用于索引单个 MCP 服务状态的名称；它是服务身份标签，不是凭据字段，名称中可以合法包含
`api-key` 等文本。
_Avoid_: API Key、密钥值、provider 负载字段

**本机 Codex 依赖**：
由用户自行安装、登录并由 Zeus 检测或显式配置路径的 Codex CLI；Zeus 不随安装包分发 Codex、不自动安装，也不保留内置回退。缺失或能力不兼容时，Zeus
阻断 AI 执行并提供安装或升级引导。
_Avoid_: 内置 Codex Runtime、包内 Codex、自动下载安装、静默回退

**项目导航展开状态**：
用户对单个项目在左侧导航中展开或折叠的本机偏好；首次默认展开并跨应用重启保留，不代表当前项目或当前页面。
_Avoid_: 当前项目、页面选中态

**导航行表面范围**：
导航反馈实际覆盖的可见区域；项目根行使用整行圆角表面并包含右侧操作槽，项目内页面入口同样使用整行表面；设置与更多仍是独立点击目标。
_Avoid_: hover 宽度、项目身份表面、操作槽游离

**项目根行状态**：
项目名称所在根行只负责展开或折叠项目，拥有 hover、focus、expanded 和 collapsed，不拥有 selected 或 current；selected / current 只属于任务、代码、会话等项目内导航入口。
_Avoid_: 当前项目选中态、项目名称 active

**项目辅助操作可见性**：
项目设置与更多操作默认隐藏，只在项目根行 hover、键盘焦点进入或更多菜单已打开时出现；两者共享父行的单一灰色表面，不绘制独立按钮底色。按钮悬停通过图标变深和轻微缩放反馈，按下时轻微收缩；设置图标与命中区不得小于同栏更多操作。
_Avoid_: 常驻更多图标、图标方形底色、灰底叠白

**项目显示名称**：
Zeus 内用于识别项目的可编辑名称；重命名只改变该名称，不改变本地代码库目录、Git 工作区或既有会话归属。
_Avoid_: 本地文件夹名、仓库路径、项目 ID

**项目本地路径**：
项目记录指向的真实本机代码库目录；“在 Finder 中显示”只定位该目录，不修改路径、目录名或目录内容。
_Avoid_: 项目显示名称、Finder 快捷方式

**抽屉关闭命中层**：
抽屉打开时覆盖整个应用窗口、但不包含抽屉面板自身的外部点击区域；无论视觉上透明或变暗，点击侧栏、工作区空白等抽屉外位置都只关闭当前抽屉。
_Avoid_: 工作区蒙层、侧栏穿透、侧栏导航

**单一纯色表面**：
表单弹窗、设置内容和设置抽屉使用同一连续底色表达一个完整工作对象；标题、内容和动作通过留白与排版分组，只有输入、选择、危险确认等功能边界保留轮廓。
_Avoid_: 灰度条带、装饰横线、卡片堆叠、无边界控件

**滚动条视觉状态**：
所有可滚动区域共享的透明轨道与低对比度滑块状态；默认保持安静，只有 hover 和 active 时逐级增强可见性。
_Avoid_: 页面滚动条颜色、深色滚动条

**系统架构图**：
以项目边界、业务工作负载、共享契约和公共基础之间的静态依赖为主体的代码图谱视图；它建立系统全局心智模型，不表达运行时调用顺序。
_Avoid_: Maven 文件树、运行时调用链、普通源码节点云

**架构边界**：
用于表达项目或模块聚合范围的空间容器；边界内部对象天然属于该范围，不再额外绘制“包含”关系线。
_Avoid_: 普通模块节点、依赖箭头、装饰分组框

**业务工作负载**：
可独立启动的服务模块与其启动入口组成的单一架构对象；系统架构图中两者共同展示，源码事实仍分别保留。
_Avoid_: 聚合模块、共享契约、启动类散点

**架构依赖**：
系统架构图中跨业务工作负载、共享契约或公共基础边界的静态模块依赖；只有真实扫描证据存在时才绘制箭头。
_Avoid_: 包含关系、推测调用、运行时请求

**节点关系图**：
从系统架构图选择一个架构对象后，以其真实模块节点为中心生成的一跳或两跳静态邻域图；它延续图谱阅读路径，详情抽屉只解释邻域图中再次选择的具体对象。
_Avoid_: 仅打开详情抽屉、完整模块源码图、运行时调用链

**邻域深度**：
节点关系图从中心节点沿真实关系向外遍历的最大跳数；一跳只包含直接相邻对象，两跳继续包含直接邻居的相邻对象。
_Avoid_: 节点数量上限、视觉缩放级别、详情抽屉聚焦

**项目子导航层级**：
项目下的任务、代码、会话通过紧凑缩进、图标和选中态表达归属，不使用贯穿式竖线；项目列表只允许纵向滚动。
_Avoid_: 层级轨道、树形竖线、侧栏横向滚动

**任务表格辅助动作**：
批量选择、列设置和更多恢复动作是任务表格的低频能力，收进主筛选工具条；表格有数据时不再显示视图控制摘要或默认字段摘要。
_Avoid_: 第二层视图工具条、默认视图说明行

**任务层级视图**：
按最多三级的父子任务关系浏览同一项目任务的任务页视图；它用于理解工作拆分，与任务自身状态彼此独立。列排序只调整同一直接父任务下的任务，父任务始终显示在后代任务之前；筛选命中下级任务时保留其上级路径作为层级上下文。
_Avoid_: 无限层级任务树、任务分组、状态汇总、会话树

**任务扁平视图**：
不改变任务父子关系、把同一项目任务作为单一集合浏览的任务页视图；它用于全表排序与筛选。
_Avoid_: 解除父子关系、根任务列表、无层级任务

**任务视图偏好**：
用户为单个项目最后选择的任务层级视图或任务扁平视图；项目首次使用任务页时默认采用任务层级视图。
_Avoid_: 全局任务视图、任务表布局偏好、任务筛选

**任务层级展开偏好**：
用户在单个项目任务层级视图中对各父任务作出的展开或折叠选择；重新进入该项目任务页时恢复，不改变任务父子关系。
_Avoid_: 全局展开状态、任务视图偏好、解除父子关系

**任务表布局偏好**：
用户对任务数据列显隐、位置、宽度和当前排序状态的本机视图选择；列头拖动只先形成布局草稿，可以作为全部项目默认值，也可以由单个项目覆盖，不改变任务本身。
_Avoid_: 任务字段、项目配置、任务状态

**任务表拖动预览**：
用户拖动任务数据列时看到的目标插入位置；拖动中不改变整列顺序，松手后表头与所有行数据一起落到目标位置并成为任务表布局草稿，取消拖动不会留下更改。
_Avoid_: 拖动中整列实时重排、已保存列顺序、即时持久化

**任务表列内容锚点**：
任务表标题与数据值各自遵循的稳定水平参照；普通文本列的标题和值使用左边缘，时间等数字扫描值使用右边缘，短时间列标题使用列内中心。排序指示、换位把手等列头操作出现或隐藏时，不能推动标题或数据值各自的参照位置。
_Avoid_: 列宽轨道、表头按钮缩进、把时间值居中、用排序图标推动标题

**全局按钮原语**：
Zeus 对文字按钮的统一 React 结构与交互契约；业务页面只选择主操作、次操作或危险操作语义，以及紧凑或常规尺寸，不借用其他页面 class，也不重新定义边框、悬停、焦点、禁用和忙碌状态。
_Avoid_: 按钮颜色工具类、页面按钮皮肤、一次性危险按钮样式

**任务表离开确认弹窗**：
任务表布局草稿未保存时覆盖在当前工作区之上的阻塞式选择；当前任务页仍作为背景保留，用户可以继续编辑、放弃草稿或保存后离开。
_Avoid_: 独立确认页面、页面替换、普通提示条

**任务枚举排序规则**：
用户在全局设置中定义的优先级、管理状态和运行状态升序先后；降序使用其反向顺序，规则不改写枚举值本身。
_Avoid_: 多列组合排序、任务状态迁移顺序、优先级修改

**内置浏览器标签**：
在 Zeus 会话中打开并与该会话保持归属关系的网页视图；它拥有独立于用户日常浏览器的页面状态，不等同于系统浏览器标签或会话本身。
_Avoid_: Chrome 标签、会话、网页附件

**Zeus 浏览器工具**：
当前 Zeus 会话提供给 Agent、用于操作该会话内置浏览器标签的动态工具；用户未指定其他浏览器时，它是 Zeus 浏览器请求的首选能力源，不依赖 Codex Browser 插件的浏览器实例。
_Avoid_: Codex Browser 插件、外部 Playwright、系统浏览器自动化

**Codex Browser 插件**：
由 Codex 产品环境提供并管理浏览器实例的独立插件能力；其浏览器列表为空只表示该插件当前没有可用实例，不能据此判定 Zeus 内置浏览器不可用。
_Avoid_: Zeus 浏览器工具、Zeus 内置浏览器、浏览器总开关

**会话浏览器分栏**：
当前会话记录和输入框保留在左侧、内置浏览器作为同一会话右侧工作面打开的桌面布局；分隔条只调整两者宽度，不改变浏览器或会话的归属。
_Avoid_: 浏览器替换会话、独立浏览器窗口、左右两个会话

**会话浏览器关闭状态**：
当前会话没有打开的内置浏览器标签，右侧浏览器工作面不显示；它不是自动补建的空白标签，再次打开分栏时才创建新标签。
_Avoid_: 最后一个标签常驻、空白浏览器分栏、浏览器禁用

**会话浏览器显隐进度**：
右侧浏览器工作面从关闭到打开的连续 `0 → 1` 状态；它同时决定浏览器占用宽度和整体透明度，关闭动画归零后才进入会话浏览器关闭状态。
_Avoid_: 浏览器加载进度、页面加载动画、瞬时挂载开关

**标签关闭热区**：
内置浏览器标签中用于关闭该标签的完整指针与键盘命中范围；关闭图标位于热区内，鼠标悬停反馈只强调图标周围的圆形表面，不改变标签尺寸。
_Avoid_: 关闭浏览器、标签选中背景、仅图标像素可点击

**活动会话上下文标签**：
当前会话右侧上下文工作面中正在接收键盘操作的内容标签；浏览器可以有多个标签，源码预览、计划和轮次审查各自以当前唯一内容作为一个标签。它与 Zeus 主窗口不是同一关闭层级。
_Avoid_: Zeus 窗口、仅可见的右侧工作面、后台浏览器标签

**注释模式**：
内置浏览器中用于选取已渲染元素、文本或区域并创建页面批注的临时交互状态；进入该状态不会修改项目源码，也不会立即向模型提交内容。
_Avoid_: Agent 浏览器控制、页面编辑模式、自动发送

**页面批注**：
用户针对内置浏览器页面中的元素、文本或区域保存的结构化修改意见；保存后仍是浏览器草稿，只有批注集合经会话输入框提交后才成为模型上下文。
_Avoid_: 截图涂鸦、代码审查评论、普通附件

**批注集合**：
用户从内置浏览器暂存到当前会话输入框的一组页面批注及其页面截图佐证；它可以和输入框文字一起编辑、移除和统一提交，暂存动作本身不等同于发送给模型。
_Avoid_: 页面批注、普通图片附件、浏览器内直接发送

**页面锚点**：
页面批注所指向的渲染对象身份，组合页面位置、目标语义和几何范围，使意见能够对应到具体元素、文本或区域。
_Avoid_: 屏幕坐标、源码行号、截图裁剪框

**样式调整请求**：
用户在页面批注中预览并确认的文字或视觉属性变化；它表达期望结果，不代表页面源码已经被修改。
_Avoid_: 已应用样式、设计令牌、页面批注正文

**会话资源**：
模型回复或用户消息中引用、并归属于具体会话与执行轮次的文件、网站、附件或轮次变更结果；它具有明确类型和受信打开边界。
_Avoid_: 任意 Markdown 链接、网页正文、未校验本地路径

**环境卡片来源**：
会话环境卡片对当前输入附件和会话中已有结构化资源的合并展示；它用于快速添加与打开资源，不表示 Agent 访问过的全部内容。
_Avoid_: Git 来源分支、Agent 访问记录、独立来源库

**消息 Markdown**：
会话消息使用的常用 GitHub 风格 Markdown 表达；它支持表格、任务清单、删除线、嵌套列表、链接、图片和代码，但不执行消息内嵌 HTML。
_Avoid_: 富文本编辑、可执行 HTML、任意远程内容

**正文图片资源**：
由消息图片语法引用、并已登记为受信会话资源的图片；它在消息正文中显示缩略图，打开时仍遵循资源打开目标与权限复验。
_Avoid_: 任意远程图片、未授权本地路径、重复附件卡片

**文件引用**：
指向当前项目或受信附件根内真实文件、并可携带行、列或行范围的会话资源；文件名与位置用于展示，打开前仍需复验真实路径。
_Avoid_: 普通路径文本、任意 file URL、代码块中的示例路径

**资源打开目标**：
用户为一个会话资源选择的实际去向，例如 Zeus 源码预览、Zeus 内置浏览器、外部编辑器、系统浏览器或 Finder；打开目标不是资源本身。
_Avoid_: 会话资源、文件类型、默认应用名称

**网站资源**：
目标为受支持 HTTP(S) 地址、可以在 Zeus 内置浏览器或外部浏览器中打开的会话资源；页面标题、图标和正文不构成权限依据。
_Avoid_: 内置浏览器标签、普通网页文本、未经校验 URL

**附件资源**：
由用户、模型或页面批注产生，并位于 Zeus 已登记附件根或当前项目根内的本地文件会话资源。
_Avoid_: 任意本地路径、网页下载记录、页面批注本身

**待提交资源**：
用户在创建或编辑任务、发送会话消息时，暂存在当前输入表面中的图片、文件、文件夹或由超长文本物化的本机资源；创建任务与会话输入共享选择、粘贴、拖放、去重、失败反馈、移除和可恢复文本语义，提交后才分别成为任务附件或会话附件。任务详情复用粘贴、去重和失败反馈，资源完成校验后立即关联任务，文字草稿仍独立保存。
_Avoid_: 已持久化任务附件、已发送会话资源、仅图片预览

**执行轮次变更集**：
可明确归因到一次执行轮次的文件集合、统一 diff、变更前后哈希和可恢复材料；它与调用时整个工作区的 Git diff 不同。
_Avoid_: 项目当前 diff、任务历史全部改动、Git commit

**变更文件当前打开**：
用户从执行轮次变更审核中打开该变更所指文件的动作；它按变更集当前状态解析工作区现路径，打开的是现实文件而不是历史 diff 或恢复快照。
_Avoid_: 历史版本预览、恢复快照执行、直接信任 diff 路径、变更审核本身

**变更审核**：
用户查看一个执行轮次变更集的文件、增删统计和 hunk 的只读过程；打开审核不会自动暂存、撤销或应用文件修改。
_Avoid_: Git review 状态、hunk 已应用、撤销变更

**撤销变更**：
在当前文件仍匹配轮次变更后状态时，反向应用该执行轮次变更集的用户动作；它不修改 Git 历史，也不允许静默覆盖后续编辑。
_Avoid_: git reset、git revert、丢弃整个工作区

**重新应用变更**：
撤销成功后，在当前文件仍匹配轮次变更前状态时，重新应用同一执行轮次变更集的用户动作。
_Avoid_: 重跑模型、重复提交任务、Git cherry-pick

**变更冲突**：
当前文件状态不再满足撤销或重新应用的前置哈希与路径条件，因而必须停止写入并交由用户查看的安全状态。
_Avoid_: 自动覆盖、补丁失败后部分成功、普通构建错误

**命令定义**：
用户在 Zeus 中保存的可复用脚本执行契约，包含名称、别名、标题、命令文本、声明式参数、超时、风险和远程授权；它不是 Zeus 内置的 Git、微信或同步业务能力。
_Avoid_: 用户脚本文件、Runtime 会话、内置动作

**全局命令**：
不属于单个项目、可在用户当前选择项目的真实根目录中执行的命令定义；它的名称和别名不得与任何项目命令冲突。
_Avoid_: 在 Zeus 源码目录执行、系统级 shell、默认注入命令

**项目命令**：
只属于一个项目并只在该项目真实根目录中执行的命令定义；不同项目之间允许使用相同名称，但同一项目内不得与全局命令冲突。
_Avoid_: 全局命令副本、项目任务、项目配置脚本

**命令参数**：
命令定义声明并在单次执行时注入的字符串、数字或布尔环境变量；敏感参数值只驻留本次进程内，不进入执行快照、持久日志或审计。
_Avoid_: 任意环境继承、`ZEUS_*` 保留变量、命令文本拼接

**项目命令权限**：
项目安全配置中持久化的 Shell 与 Git 写入许可；点击运行时如缺少必要许可，Zeus 可以先询问用户是否只开启本命令所需权限，开启后仍必须进入本次命令确认且不得自动执行。
_Avoid_: 本次命令确认、静默放宽权限、开启后自动执行

**命令确认**：
用户通过桌面确认弹窗或 Telegram 明确动作创建的、绑定命令版本、项目、真实目录和完整参数摘要的一次性短期授权；任一绑定内容变化、过期或 Zeus 重启都会使确认失效，高风险分类只改变风险说明和操作语义，不要求额外口令。
_Avoid_: 项目永久授权、Telegram 命令开关、记住本次选择、高风险口令

**命令执行**：
命令确认后创建并关联 Runtime 会话的持久化执行快照；状态区分成功、失败、超时、取消和拒绝，删除命令定义不会删除历史。
_Avoid_: Runtime 会话本身、任务执行轮次、终端标签

**Runtime 实时进度行**：
运行中命令通过终端覆盖语义反复更新的同一条暂态输出；它不是多条独立日志，也不代表命令或业务阶段的最终结果。
_Avoid_: 百分比日志列表、命令完成状态、发布阶段结果

**命令执行入口**：
环境信息卡片中进入当前项目完整命令中心的导航动作；它只打开“代码 → 命令”，不在卡片内直接运行命令，也不代表自由 Shell 终端。
_Avoid_: 直接执行按钮、会话终端入口、命令确认

**命令实际耗时**：
命令从 Runtime 真正开始执行到当前时刻或结束时刻的持续时间；等待用户确认不计入，从未启动的命令执行没有实际耗时。
_Avoid_: 超时上限、执行记录存续时间、确认等待时间

**命令产物**：
用户脚本通过 `ZEUS_ARTIFACT_FILE` 声明、且经 Zeus 复验位于本次执行产物目录内的真实文件；目录外路径、目录或失效符号链接都不构成产物。
_Avoid_: 任意日志路径、用户脚本、项目文件浏览

**Telegram 命令授权**：
命令定义上的独立远程执行开关；只有命令已启用、项目权限满足且该开关开启时，Telegram 菜单才展示并允许逐次确认执行。
_Avoid_: Telegram Bot 总开关、项目 `allowShell`、永久高风险授权

## 多仓项目与推送工作区

**项目容器**：
承载一个业务项目相关目录、仓库和共享资料的本机根目录；它可以不是 Git 仓库，也不代表任何单一仓库。
_Avoid_: 项目仓库、主仓库、Git 根目录

**项目仓库**：
Zeus 在一次 worktree 模式推送前从项目容器中自动发现的真实 Git 根；它不是项目创建时需要维护的永久配置，一个项目也可以完全没有 Git 仓库。
_Avoid_: 项目容器、项目级仓库清单、必须手工勾选的仓库

**有远端仓库**：
至少配置了一个 Git remote 的项目仓库；该分类只表示远端配置存在，不表示远端当前可访问、凭据有效或刷新成功。
_Avoid_: 远端可用仓库、远端已刷新仓库、远端仓库本身

**纯本地仓库**：
没有配置任何 Git remote 的项目仓库；它使用本地分支完成任务开发和本地交付，并跳过远端拉取与远端推送。
_Avoid_: 非 Git 仓库、远端访问失败仓库、未发现仓库

**远端受阻仓库**：
已经配置 Git remote、但当前因网络或凭据问题无法刷新远端的项目仓库；它仍允许本地查看和本地提交，只阻止远端拉取、推送及依赖最新远端的合入。
_Avoid_: 纯本地仓库、无 Git 仓库、全局代码交付失败

**内部派生仓库**：
Zeus 为发布、任务隔离或临时处理生成的 Git 副本；它服务于产品内部流程，不属于用户业务项目的仓库集合，也不能进入任务推送的逐仓选择。
_Avoid_: 项目仓库、嵌套项目仓库、用户业务仓库

**嵌套项目仓库**：
目录位置位于另一项目仓库内部、但在任务分支、代码修改和交付上保持独立的项目仓库；父仓和子仓之间不存在 Zeus 自动提交或推送联动。
_Avoid_: 父子联动仓库、父仓子模块工作流、级联交付

**直接目录模式**：
任务会话直接以项目容器的真实路径作为工作目录；它不要求 Git，不创建分支或 worktree，存在 Git 时自然使用该目录当前检出的分支。
_Avoid_: 非 Git 降级、临时目录模式、隔离工作区

**Worktree 模式**：
任务推送时自动发现项目容器内全部 Git 仓库，并为本次开发线创建隔离分支和工作区的执行方式；没有发现仓库时不创建任务环境，而是提示用户先初始化仓库。
_Avoid_: 项目仓库配置、自动启用 Git、直接目录模式

**共享可写目录**：
经用户显式登记、允许任务直接持久写入的项目容器子目录；它不属于任务的 Git 隔离与回收边界。
_Avoid_: 仓库任务工作区、临时目录、全部非 Git 内容

**推送工作区**：
一次 worktree 模式任务推送独占的完整工作现场，包含本次发现的全部项目仓库各自的仓库任务工作区，以及项目登记的共享可写目录；直接目录模式不创建推送工作区。
_Avoid_: 可跨推送复用的任务环境、单个 worktree、项目容器

**仓库任务工作区**：
推送工作区中对应一个项目仓库的隔离开发现场；有远端时冻结本次刷新得到的远端来源基线，纯本地时冻结本地来源基线，只属于本次推送创建的任务开发线。
任务分支名只在同一个项目仓库内保持唯一；同一项目的不同仓库可以合法使用相同分支名。
_Avoid_: 推送工作区、原始仓库工作目录、跨推送共享目录

**逐仓来源确认**：
用户在 worktree 模式推送时可以分别调整全部项目仓库的来源分支和新任务分支；有远端的仓库只展示本次刷新成功后的远端分支，纯本地仓库展示本地分支，选择只确定本次开发线基线。
_Avoid_: 项目仓库配置、过期远端记录、沿用其他推送的分支

**多仓任务交付**：
逐个确认推送工作区内各项目仓库的提交、推送、合入、放弃或无变化结果，并在全部仓库收敛后回收整个推送工作区。
_Avoid_: 跨仓原子提交、一键远端事务、任务管理状态

## 交付证据

**自动化测试体系**：
Zeus 仓库内部已经退役的单元、组件、API、DOM/CSS 契约测试及其命令、框架、配置和生成产物；它不再构成交付门禁。
_Avoid_: 真实接口联调、浏览器验收、正式包健康检查

**测试专用接线**：
没有生产调用、只为已退役自动化测试替换依赖或改变时间、监听与执行条件的入口；真实运行所需的进程边界和组合端口不属于该概念。
_Avoid_: 生产组合接口、浏览器自动化端口、附件授权

**真实交付验收**：
针对正式构建产物和真实用户路径形成的可复现证据；静态检查与生产构建只证明对应阶段，不能代替正式打包、接口联调、浏览器或桌面运行结果。
_Avoid_: 单元测试通过、仅类型检查通过、仅构建成功

**发布阻断项**：
公开稳定版对外发布前必须满足的真实交付验收条件；任一阻断项没有通过时，版本不能进入公开发布，即使静态检查、构建或远端 CI 已经成功。
_Avoid_: 已知限制、发布后补验、仅构建成功

**发布例外**：
用户在了解具体影响后明确接受、不阻断本次公开稳定版的未覆盖项；它必须进入发布记录和最终结论，不能被改写为已经验收，也不能替代其他发布阻断项。
_Avoid_: 发布阻断项、默认放行、验收通过

**完整升级发布**：
把一个已确认范围交付为新的公开稳定版，完成公开安装渠道的一致性核对，并将本机日常正式应用切换到该稳定版后确认真实版本和服务健康的完整交付过程。
_Avoid_: 仅生成测试包、仅公开发布、仅本机替换、构建通过

## 任务推送错误边界

**任务推送失败**：
任务首发某一阶段未完成的可恢复状态；能力或账号就绪检查失败归属于推送弹窗，尚未建立真实执行轮次时不得生成会话。真实执行轮次已经建立后发生的发送失败归属于对应会话，并保留登录后重试入口；两者都不代表 Zeus 应用整体故障。
_Avoid_: 应用故障、全局错误、未登录占位会话、隐藏真实失败轮次

## 应用更新

**更新重启目标**：
本次更新已经确认安装成功、并且在用户点击“立即重启”后唯一允许启动的 Zeus 应用；它由安装位置、应用身份和目标版本共同确定。
_Avoid_: 任意 Zeus、系统选择的 Zeus、同名应用

**当前运行版本**：
用户此刻实际进入的 Zeus 应用版本，以正在运行的应用身份为准，不以下载记录或安装记录代替。
_Avoid_: 已安装版本、最新版本
