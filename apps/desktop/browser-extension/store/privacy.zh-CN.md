# Zeus Browser 隐私说明

Zeus Browser 只在用户启用扩展并授予站点或功能权限后处理浏览器数据。标签标题、URL、页面可访问性/DOM 摘要和工具结果只传给本机正在运行且已通过短期令牌认证的 Zeus 实例。扩展不提供云端遥测，不出售或共享用户数据，不在扩展内持久保存网页正文、凭据或一次性验证码。

截图和下载仅按用户或 Agent 的明确工具调用生成。Zeus 的敏感操作审批、制品生命周期与会话历史策略适用于这些结果。停用外部浏览器功能会使短期 rendezvous 失效；用户可以从浏览器设置中撤销站点和可选权限。
