# TASK_20260630_001：Zeus 新建对话空态样式优化

## 目标

按用户截图聚焦范围，只优化新建对话空态里的开放输入框、内部工具栏、项目条和下方三条建议入口；不改变真实动作，不新增假推荐。

## 本轮视觉返修

- 保留项目归属条 `▣ Zeus E2E`，不删除项目名、不改真实项目上下文。
- 移除输入区内层浅色轮廓线：新建对话 textarea 明确 `background: transparent`、`border: 0`、`box-shadow: none`，自身 focus 圈继续关闭，由外层 composer 承接焦点反馈。
- 移除建议入口之间的常驻横向分隔线：`.session-starter-suggestion-row` 不再设置默认 `border-block-start`，hover / focus 仅在交互态出现轻反馈。

## 依据

- `DESIGN.md:5-9`：Zeus 是克制、清晰、可信的 macOS 产品 UI，需要长期使用低疲劳。
- `DESIGN.md:20-33`：采用冷中性色，控件需要 hover / focus 等状态。
- `DESIGN.md:156-165`：composer 输入为主，操作收进输入框内部或底部 rail。
- `DESIGN.md:198-206`：必须具备 focus-visible / keyboard-navigation，不引入外部品牌 token，不用 CSS 隐藏旧结构冒充完成。

## 本轮改动

- `apps/desktop/src/renderer/styles.css`
  - 新建对话 composer 保持轻边框和容器级 `:focus-within`。
  - textarea 去掉内层控件背景、边框和阴影，消除截图红框标出的输入区内部浅线。
  - 三条建议入口继续使用 `.session-starter-suggestion-row`，但移除常驻分隔线。
  - `.conversation-new-chat-project-strip` 保留，项目名继续显示。
- `apps/desktop/test/app-shell-layout.test.tsx`
  - 回归测试锁定项目条保留、textarea 内层轮廓移除、suggestion rows 默认分隔线移除。

## 不变项

- `完成一次项目任务 / 查看代码图谱 / 打开运行环境` 仍绑定当前真实动作。
- `▣ Zeus E2E` 项目归属条完整保留。
- 不引入新依赖。
- 不改 API、schema、数据迁移或运行时行为。
- 不处理仓库内既有大量未提交变更，本轮只做局部样式、测试和任务文档。

## 验证记录

| 验证项 | 结果 | 说明 |
| --- | --- | --- |
| TDD RED | 通过预期失败 | 新断言先因 textarea 缺少 `background: transparent` 失败。 |
| 定向测试 | 通过 | `pnpm vitest run apps/desktop/test/app-shell-layout.test.tsx -t "softens the zero-state Codex composer chrome" --reporter=verbose`。 |
| 布局测试文件 | 通过 | `pnpm vitest run apps/desktop/test/app-shell-layout.test.tsx --reporter=verbose`，170 个测试通过。 |
| 全量门禁 | 通过 | `pnpm lint && pnpm typecheck && pnpm format:check && pnpm test && git diff --check`，66 个测试文件 / 995 个测试通过。 |
| 退出 Zeus 后打包 | 通过 | 已发现并退出运行中的 Zeus，随后 `pnpm package:mac` 成功生成 macOS arm64 zip / dmg。 |
| 文档后空白检查 | 通过 | 更新本任务文档与 HTML 后复跑 `git diff --check`。 |

## 风险与回滚

- 风险：样式通过静态测试和后续打包验证；真实 Electron 窗口截图仍建议人工复核红框区域。
- 回滚：撤回本轮 textarea / suggestion row 样式和 `app-shell-layout.test.tsx` 中对应断言即可；项目归属条无需回滚。

## 任务收口

- 任务编码：TASK_20260630_001
- 任务名称：Zeus 新建对话空态样式优化
- 本次使用的 skill：executing-plans、test-driven-development、frontend-skill、impeccable、accessibility、verification-before-completion
- 本次修改的影响点：新建对话空态 textarea 内层轮廓、三条建议入口常驻分隔线、布局回归测试和任务交付文档。
- 待用户重启服务或待执行脚本：本轮已退出运行中的 Zeus 并执行 `pnpm package:mac`；如正在使用旧构建，请重新打开最新打包产物。
