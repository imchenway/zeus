# TASK_20260701_002 Zeus 项目作战室页面设计稿

## 目标

把上一轮“项目作战室”设计方向画成可评审的单文件 HTML 页面设计稿。用户已指出旧截图不是最新 Zeus，因此本稿不引用旧截图作为事实依据。

## 设计判断

- 唯一核心目标：让用户在真实本地仓库中先理解代码图谱，再把选中对象转成任务、运行验证或 Git Diff 审查。
- 用户当前最需要完成的动作：选择真实项目 / 选中图谱节点 / 打开源码或创建任务。
- 首层必须展示：source-list、object-toolbar、graph-canvas、context-inspector、task-runway、主要状态。
- 应收纳内容：长日志、长证据、SQL/命令、危险动作、二级工具和边详情进入 inspector 或折叠区。
- 不发散候选的原因：用户要求“画出你的设计来”，上一轮已收敛到项目作战室方向；本轮交付单稿页面设计，不做 A/B/C 换色稿。

## 设计依据

- `PRODUCT.md:7`：Zeus 是本地优先的 macOS AI 研发工作台。
- `PRODUCT.md:17`：核心 Aha Moment 是选择真实仓库并完成扫描后看到真实文件生成的代码图谱。
- `PRODUCT.md:21-24`：当前边界包含本地 app-server、SQLite、真实代码扫描、任务、Runtime、Git Diff、Telegram；无真实数据时显示空/失败/未配置/等待。
- `PRODUCT.md:42-48`：本地优先、不默认上传代码、图谱和任务上下文可追溯。
- `DESIGN.md:5-9`：克制、清晰、可信的 macOS 产品 UI。
- `DESIGN.md:17`：Code Map 以真实扫描生成的图为核心，每个节点和边必须可追溯来源。
- `DESIGN.md:30-35`：空状态解释下一步；卡片只用于分组；控件覆盖 hover/focus/disabled/loading/empty/error。
- `DESIGN.md:39-45`：主要页面覆盖 loading、empty、error、permission denied、external wait。
- `DESIGN.md:65-69`：页面必须一眼看出当前对象、状态和主操作。
- `DESIGN.md:146-180`：source-list、object-toolbar、controls、composer、decision-rail、mode-rail、graph-canvas 组件语义。
- `DESIGN.md:198-213`：质量门禁和 must-not：无纯黑纯白、无卡片堆、无假数据、不可用 CSS 隐藏旧结构。
- `apps/desktop/src/renderer/App.tsx:9859-9961`：CodeMapView 入口和状态。
- `apps/desktop/src/renderer/App.tsx:10118-10214`：Code Map 主画布与右侧 inspector 结构。
- `apps/desktop/src/renderer/App.tsx:11200-11895`：GraphCanvas 与图谱画布逻辑。
- `apps/desktop/src/renderer/task/TaskWorkspace.tsx:177-234`：TaskWorkspace 表格/任务工作区入口。

## 交付物

- HTML：`docs/TASK_20260701_002_Zeus项目作战室页面设计稿.html`
- 本文档：`docs/TASK_20260701_002_Zeus项目作战室页面设计稿.md`

## 验证

已执行本轮设计稿自检：

- HTML 结构与自包含检查：`doctype`、内联 CSS/JS、无外部 URL/CDN、关键文案与证据锚点存在。
- 旧截图排除检查：保留“旧截图已排除”说明，不引用过期截图文件作为事实依据。
- 空白与文件格式检查：HTML/MD 无行尾空白。
- Chrome headless 渲染检查：桌面 1440×1100 与窄屏 390×1200 均无横向溢出，图谱节点数为 7，无控制台错误。

## 修改范围

- 新增设计稿 HTML 与说明文档。
- 未修改 Zeus 源码、测试或构建配置。
