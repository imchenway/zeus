# TASK_20260701_009 Zeus 逐页多方案设计稿

## 任务目标

基于用户指定前会话 `019f1b69-589f-7702-af7e-d6db02b9b8ff` 中的最新 Zeus appshot，按页面分别给出多个设计稿选项。本轮不修改 Zeus 源码，只生成可评审 HTML 和任务说明。

## 事实来源

- 已从前会话 JSONL 提取 8 张 appshot 到 `docs/artifacts/TASK_20260701_009_current_appshots/`。
- 覆盖页面：Settings、Sessions 新对话、Sessions 任务会话、Code Map、Tasks 空态、Tasks 列表、创建任务弹窗、任务详情抽屉。
- 截图只能证明视觉结构和当前静态状态，不能证明 hover、键盘、加载、错误和运行时动效。

## 设计原则

- 保留当前多项目任务执行、连续对话、代码图谱理解的主路径。
- 不做网页式 dashboard，不把所有功能堆到单页，不新增 Home。
- 每页都给 A/B/C 方向；默认推荐 A，因为它们最贴近当前动线且实现成本最低。
- 视觉语言遵守 `PRODUCT.md` 和 `DESIGN.md`：本地优先、真实来源、macOS 产品 UI、低疲劳、低边界、单一蓝紫强调色、无假数据。

## 页面推荐

- Settings：A 系统偏好式两栏。
- Sessions：A 连续对话工作台。
- Code Map：A 画布优先 Inspector。
- Tasks：A 执行队列表。

## 输出文件

- `docs/TASK_20260701_009_Zeus逐页多方案设计稿.html`
- `docs/TASK_20260701_009_Zeus逐页多方案设计稿.md`
- `docs/artifacts/TASK_20260701_009_current_appshots/`

## 验证

已执行静态检查：HTML doctype、内联 CSS/JS、无远程资源、页面 tab 可访问属性、关键文案和推荐项存在、旧版黑绿网格不存在；未执行浏览器截图渲染检查。
