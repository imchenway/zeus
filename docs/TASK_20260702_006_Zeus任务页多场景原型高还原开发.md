# TASK_20260702_006 Zeus 任务页多场景原型高还原开发

## 目标

基于 `docs/TASK_20260702_005_Zeus任务页多场景原型设计稿.html` 中“分组工具条优先 / 多场景 / 悬浮列设置 / 新任务弹窗”的页面方向，开发落地到 Zeus 任务页。

## 交付范围

- 分组工具条优先：将任务页旧单行工具条拆成主操作工具条与视图控制工具条。
- 默认任务列表：默认 7 列继续保留，不隐藏右侧列；默认不制造假横向滚动条。
- 宽列 / 扩展字段：列宽扩展时保留唯一横向滚动兜底。
- 空任务态：保持表头与下一步提示，避免大块空白或详情停车场。
- 自定义列：字段配置保持悬浮 popover，不顶开任务行。
- 新任务弹窗：保持独立 modal，并收敛尺寸、留白和遮罩。

## 关键文件与锚点

- `apps/desktop/src/renderer/task/TaskWorkspace.tsx:383-451`：任务页 DOM 拆成 `task-table-primary-toolbar`、`task-table-view-toolbar`、状态分段控件、字段设置 popover。
- `apps/desktop/src/renderer/styles.css:13689-14120`：最终覆盖规则，锁定分组工具条、空态、滚动策略、列设置浮层与新任务弹窗。
- `apps/desktop/test/app-task-controls-rendering.test.tsx:653-707`：覆盖桌面工具条密度、空任务工具条、loading/error 状态。
- `apps/desktop/test/app-task-controls-rendering.test.tsx:1522-1567`：覆盖空任务态紧凑布局。
- `apps/desktop/test/app-task-controls-rendering.test.tsx:1937-1959`：覆盖新任务弹窗字段顺序、辅助文案和间距。
- `apps/desktop/test/app-shell-layout.test.tsx:3796-3864`：覆盖中英文状态分段与筛选文案。
- `apps/desktop/test/renderer.test.tsx:128-153`：覆盖顶部隐藏标题栏拖拽层之上的任务工具条命中区。

## 验证记录

- `./node_modules/.bin/vitest run apps/desktop/test/renderer.test.tsx apps/desktop/test/app-task-controls-rendering.test.tsx apps/desktop/test/app-shell-layout.test.tsx`：305 tests passed。
- `./node_modules/.bin/vitest run apps/desktop/test/app-task-controls-rendering.test.tsx apps/desktop/test/task-workspace-model.test.ts apps/desktop/test/app-shell-layout.test.tsx`：252 tests passed。
- `npx --yes pnpm@10.31.0 lint`：passed。
- `npx --yes pnpm@10.31.0 typecheck`：passed。
- `npx --yes pnpm@10.31.0 format:check`：passed。
- `npx --yes pnpm@10.31.0 test`：66 test files / 1016 tests passed。
- `git diff --check`：passed。
- 已退出运行中的 Zeus 后执行 `npx --yes pnpm@10.31.0 package:mac`：passed，重新生成 `dist/mac-arm64/Zeus.app`、`dist/Zeus-0.1.0-arm64.dmg`、`dist/Zeus-0.1.0-arm64.zip`。

## 未覆盖点

- 已进行真实打包窗口与可访问性树复核：空任务表头、空任务块、自定义列悬浮层、新任务 modal 均已检查；未创建任务、未改用户数据。
- 打包日志仍显示 `skipped macOS code signing reason=identity explicitly is set to null`，这是当前 unsigned 打包边界，不影响本轮 UI 变更验证。

## 现场验收补充（2026-07-02 16:31）

用户授权“自己去验收”后，使用真实打包后的 Zeus 窗口进行验收，不只依赖截图：

- `Computer Use / Accessibility Tree`：确认任务页真实 URL 为 `#project-tasks`，默认 7 列 header 与两行数据均存在。
- 视觉验收：发现 980px 响应式规则让桌面宽度提前折行，导致搜索框与“新任务”按钮变成不符合原型的大横条。
- 修复：移除任务页最终覆盖块中的 980px 提前折行规则，并锁定 `.task-table-primary-toolbar .task-table-new-task-button` 为 `inline-size: auto`、`justify-self: end`。
- 交互验收：点击“自定义列”后弹层悬浮在表格上方，未把任务行往下撑；点击“新任务”后出现居中 modal，未直接创建任务。

补充验证：

- `./node_modules/.bin/vitest run apps/desktop/test/app-task-controls-rendering.test.tsx --testNamePattern "packaged desktop toolbar"`：RED 后 GREEN，1 test passed。
- `./node_modules/.bin/vitest run apps/desktop/test/app-task-controls-rendering.test.tsx apps/desktop/test/app-shell-layout.test.tsx apps/desktop/test/renderer.test.tsx`：305 tests passed。
- `npx --yes pnpm@10.31.0 lint`：passed。
- `npx --yes pnpm@10.31.0 typecheck`：passed。
- `npx --yes pnpm@10.31.0 format:check`：passed。
- `npx --yes pnpm@10.31.0 test`：66 test files / 1016 tests passed。
- `git diff --check`：passed。
- 已退出运行中的 Zeus 后执行 `npx --yes pnpm@10.31.0 package:mac`：passed，已重新生成打包产物。

## 浏览器评论返修补充（2026-07-02 17:00）

针对用户在 `TASK_20260702_005` 原型页上的 5 条批注，逐条返修并固化测试：

1. **空任务工具条是否对齐**：`TaskWorkspace.tsx:308-328` 区分 loading/error/empty 的顶部状态文案；`styles.css:13715-13771` 锁定主工具条为 `项目元信息 / 搜索 / 状态分段 / 新任务` 同一紧凑行，桌面不再提前折成大横条。
2. **空任务块是否存在**：`TaskWorkspace.tsx:652-673` 渲染 `还没有任务`、第一步说明、`新任务` 和 `查看项目代码`；不再保留旧的 outcome-grid 假结果预览。
3. **新任务弹窗边距与字段细节**：`App.tsx:4749-4808` 调整表单顺序为标题、任务要求、优先级/标签、上下文来源、AI Runtime 提示；`styles.css:14062-14120` 锁定 modal 尺寸、14px body padding、10px gap 和 runtime notice。
4. **读取失败状态**：`TaskWorkspace.tsx:633-651` 增加表格内容区 inline recovery，只有 `重试` 和 `项目设置`，不暴露 stack；`App.tsx:8351-8352` 接上真实重试和项目设置入口。
5. **加载态**：`TaskWorkspace.tsx:620-632` 在保留表头的前提下只替换表格内容为 skeleton；`styles.css:13961-14000` 固化加载骨架，不创建假任务行或整页停车场。

新增/调整测试锚点：

- `apps/desktop/test/app-task-controls-rendering.test.tsx:668-684`：空任务工具条顺序、空态文案、次级动作与旧 outcome-grid 反查。
- `apps/desktop/test/app-task-controls-rendering.test.tsx:686-707`：loading/error 状态、恢复按钮、无 stack 暴露。
- `apps/desktop/test/app-task-controls-rendering.test.tsx:1937-1959`：新任务弹窗字段顺序、辅助文案、modal body 间距、runtime notice。
- `apps/desktop/test/renderer.test.tsx`：CSS token 完整性回归，补齐 `--zeus-warning-bg` 后通过。

本轮最终验证：

- `./node_modules/.bin/vitest run apps/desktop/test/app-task-controls-rendering.test.tsx apps/desktop/test/task-workspace-model.test.ts apps/desktop/test/app-shell-layout.test.tsx`：252 tests passed。
- `./node_modules/.bin/vitest run apps/desktop/test/renderer.test.tsx apps/desktop/test/app-task-controls-rendering.test.tsx apps/desktop/test/app-shell-layout.test.tsx`：305 tests passed。
- `npx --yes pnpm@10.31.0 lint`：passed。
- `npx --yes pnpm@10.31.0 typecheck`：passed。
- `npx --yes pnpm@10.31.0 format:check`：passed。
- `npx --yes pnpm@10.31.0 test`：66 test files / 1016 tests passed。
- `npx --yes pnpm@10.31.0 package:mac`：passed，已重新生成 `dist/mac-arm64/Zeus.app`、DMG 与 ZIP。

现场复核：已使用真实打包后的 Zeus 窗口与可访问性树检查当前任务页、新任务 modal、列设置 popover；未创建任务、未改用户数据。

