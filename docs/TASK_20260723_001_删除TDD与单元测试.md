# TASK_20260723_001 删除 TDD 与单元测试

## 目标

用户明确要求删除 Zeus 项目现有 TDD 与单元测试体系，并要求后续不再恢复或新增这两类内容。本任务按用户最终指令直接执行，不再保留现有
Vitest 测试套件。

## 删除范围

- 删除 `apps/desktop/test`、`packages/*/test` 与 `scripts/*.test.*` 下的现有自动化测试文件及测试夹具。
- 删除 Vitest 配置、依赖、workspace 测试脚本和真实扫描测试脚本。
- 从 CI、发布脚本、贡献指南、README、PR 模板和当前设计/发布文档中移除测试门禁与 TDD 要求。
- 新增项目级 `AGENTS.md`，持久化“不采用 TDD、不恢复单元测试”的后续规则。

## 保留边界

- 历史任务文档与历史实施计划保留原始事实，不批量篡改过去已发生的 TDD/测试记录。
- 保留 lint、typecheck、build、package:mac、包内产物检查和真实运行检查；它们不作为单元测试替代品，也不得被描述为测试通过。
- 不修改本任务无关的用户工作区变更，不执行 git commit 或远端操作。

## 风险

- 删除后不再有细粒度自动回归保护，代码行为错误将主要依赖类型检查、构建、打包和真实运行检查暴露。
- 当前 25 个带未提交改动的测试文件（23 个已跟踪修改、2 个未跟踪新增）将随测试体系一并删除；用户已在了解该事实后要求继续删除。

## 执行状态

- 当前阶段：已完成。

## 实际删除与清理结果

- 删除 94 个测试代码、夹具、测试脚本与测试专用配置文件，其中包含删除前识别到的 91 个测试文件，以及 `vitest.config.ts`
  、真实扫描测试脚本和 Java/Spring 测试夹具校验脚本。
- 删除 `docs/testing.md`，并移除所有空的 `test/` 目录。
- 从根目录、桌面应用和 14 个 workspace 包中移除共 17 个 `test` / `test:*` 脚本入口。
- 移除 Vitest 依赖并刷新 `pnpm-lock.yaml`；16 个 package manifest 与锁文件中均不再包含 Vitest。
- 从 CI、发布脚本、README、贡献指南、PR 模板、发布文档、实现报告、验收矩阵和开发设计中移除测试门禁及失效证据引用。
- 退役“单元测试”内置任务模板；新数据库不再创建该模板，既有数据库打开时通过幂等迁移删除旧的内置模板记录。
- 新增项目级 `AGENTS.md` 与 ADR，固定后续不采用 TDD、不恢复单元测试的规则。

## 验证结果

- `pnpm install --lockfile-only --offline`：通过。
- 测试文件、测试目录、package 测试脚本和 Vitest 依赖反向扫描：通过，未发现残留。
- `pnpm verify:acceptance-matrix`：通过，12 个章节、139 个验收项一致。
- `pnpm lint`：通过。
- `pnpm typecheck`：通过。
- `pnpm format:check`：通过。
- `pnpm build`：通过；Vite 仅报告既有的大 chunk 警告。
- 内置模板迁移运行检查：通过；模拟旧库中的 `task_template_unit_test` 后重新打开数据库，旧模板已删除，剩余 7 个内置模板。
- `pnpm verify:release`：通过；已完成 AI CLI 探针、macOS 正式打包、包内 Electron 加载与本地服务健康检查。
- 产物：`dist/mac-arm64/Zeus.app`、`dist/Zeus-0.1.0-arm64.dmg`、`dist/Zeus-0.1.0-arm64.zip`。
- Apple 签名证书未配置，因此本次只验证 unsigned DMG/ZIP；未将其描述为签名或公证完成。
- `git diff --check`：通过。

## 未覆盖风险

- 按本任务决策，不再执行单元、组件、API 或 UI 自动化回归测试。未来行为回归主要依赖静态检查、构建、正式打包和针对改动范围的真实运行检查发现。
- 本次没有启动 GUI 做人工交互巡检；运行证据限于打包产物加载及包内本地服务健康检查。
- 被删除的已跟踪测试基线可从 Git 历史恢复；此前未提交的测试改动与未跟踪测试文件无法通过 Git 恢复。

## 2026-07-29 补充清理

### 用户确认边界

- 删除仅服务旧自动化测试的源码接线、实时流程要求和 Vitest 结果缓存。
- 保留 `qa:browser`、类型检查、生产构建、静态发布验收、正式打包、真实接口联调和浏览器验收。
- 保留 Codex manager、浏览器自动化、附件授权等真实运行组合边界，以及上游 Codex 完整源码缓存；不得把它们误删为 Zeus 测试资产。

### 可恢复备份

- 当前未提交源码、文档和 11 处 Vitest 缓存在修改前备份到
  `/Users/david/.Trash/zeus-test-cleanup-backup-20260729-2ZJS57`。
- 备份保留的是本轮开始时的工作区内容，不以 Git 历史覆盖用户原有未提交修改。

### 当前状态

- 已移除 Renderer 初始错误、Desktop 本地服务工厂、Keychain 执行器、本地服务客户端/进程/Git/时钟/TTL 替身和 Fastify 监听替身。
- 已删除无生产调用的 SQLite 表名枚举辅助，并将产品、路线图、Issue 模板和主开发设计中的当前测试要求改为真实验收口径。
- 补充清理已完成；生产组合边界、`qa:browser` 与发布验收链路均保留。

### 清理结果

- 11 处 `node_modules/.vite/vitest/results.json` 缓存目录已移出工作区，保存在上述 Trash 备份中，可恢复。
- 16 个 package manifest 均不存在测试脚本和测试框架依赖，`pnpm-lock.yaml` 不存在 Vitest、Jest、Mocha、Ava、Testing Library、Happy DOM 或 JSDOM 包记录。
- 已跟踪及未跟踪文件中不存在 Zeus 自有 `test/`、`tests/`、`__tests__/`、`coverage/`、`*.test.*`、`*.spec.*`、Vitest 或 Jest 配置路径。
- 已移除仅供旧自动化测试注入的错误初值、工厂、执行器、客户端、进程、Git、时钟、TTL 与监听替身；运行时继续直接组合真实实现。
- 静态浏览器验收夹具中的 Maven `-Dtest` 与“聚焦测试”示例已改为 `pnpm package:mac` 生产打包验收，代码图谱创建任务文案改为“建议验收范围”。
- 发布脚本已明确区分包内容完整性检查与正式 App 的真实 `/health` 联调，避免把静态门禁误报为运行态接口通过。
- 历史任务文档中的 TDD 与测试记录继续作为历史事实保留；Telegram“测试连接”等真实产品操作也不属于自动化测试体系，未误删。

### 补充验证证据

- `pnpm lint`：通过。
- `pnpm typecheck`：通过。
- `pnpm build`：通过；773 个 Renderer 模块完成生产构建，仅保留既有的大 chunk 警告。
- `pnpm verify:acceptance-matrix`：通过，12 个章节、139 个验收项一致。
- `git diff --check`：通过。
- `pnpm package:mac`：通过；`dist/mac-arm64/Zeus.app` 完成包内容检查及 ad-hoc codesign 校验。
- `pnpm verify:release`：通过；AI CLI 真实探针、生产构建、DMG/ZIP、SHA256、发布清单、安装脚本、包内 Electron 载入、Renderer/Main/Preload/Codex runtime 完整性均通过。
- 浏览器冷启动验收：`qa/session-styles.html` 完整渲染；审批“授权选项”可展开，菜单状态为 `aria-expanded=true`，控制台 warning/error 为 0，且页面不含旧测试示例。
- 正式包 GUI 验收：从 `dist/mac-arm64/Zeus.app` 启动单一正式实例，页面从“会话”成功导航到“命令”，真实项目与命令数据可见。
- 真实接口联调：正式包只在 `127.0.0.1:54524` 监听；`GET /health` 返回 `ok=true`、`database=ok`、`runtime=ok`，不带临时令牌访问 `GET /api/dashboard` 返回 401，鉴权边界生效。

### 明确限制

- 当前未配置 Apple 正式签名证书；本轮只完成本地 ad-hoc 签名及未公证 DMG/ZIP 验收，未声称正式签名或公证完成。
- `pnpm format:check` 在本轮开始后的脏工作区中报告 80 个既有格式差异；为避免覆盖用户的大量未提交修改，本轮未执行全仓格式化。它不影响上述 lint、typecheck、build、package 与运行态结论。
- 浏览器验收页引用的 3 张历史对照截图不在仓库中，因此本轮确认了 Zeus 实现区的渲染与交互，但没有把缺失参考图的对照区域报告为视觉对比完成。
- 按用户决策，本轮不恢复任何单元、组件或接口自动化测试；未来若恢复，需要重新安装并配置整套工具链。
