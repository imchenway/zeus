# TASK_20260803_001 查找并恢复 Codex 会话

## 任务目标

- 通过关键字 `6921555542811` 和 `6921555539668` 定位侧栏中不易找到的 Codex 会话。
- 用户确认找到后恢复；优先使用 Codex 线程管理能力，不直接修改本地状态数据库。

## 领域口径

- **目标会话**：两个关键字直接出现在原始用户消息中的顶层 Codex 线程，不把日报、汇总或其他会话中的引用命中当作目标。
- **恢复**：先核对线程是否仍可读取和是否归档；若已归档则取消归档，随后在 Codex 主窗口打开。

## 定位证据

- Thread ID：`019facef-ca21-7dd0-b9b3-47cff23ef300`
- 标题：`排查一品多码条码问题`
- 创建时间：`2026-07-29 16:13:43`（Asia/Shanghai）
- 工作目录：`/Users/david/cckg/tcapp/Back-End/pim`
- 原始记录：
  `/Users/david/.codex/sessions/2026/07/29/rollout-2026-07-29T16-13-43-019facef-ca21-7dd0-b9b3-47cff23ef300.jsonl`
- 原始首条消息同时包含两个关键字，并描述“一品多码”问题。

## 状态核对

- Codex `read_thread` 可以读取该线程，状态为 `idle`。
- Codex 最近线程列表仍包含该线程，且存在未读完成结果。
- 取消归档操作返回 `no archived rollout found`，证明该线程并未归档；侧栏不易找到更可能由当前项目或列表筛选造成，而不是会话数据丢失。

## 恢复结果

- 已调用 Codex 页面导航打开该线程，返回 `navigated: true`。
- 未直接修改 Codex 状态数据库，未删除或重写任何会话 JSONL。

## 验证边界

- 已验证：关键字原始命中、线程身份、标题、工作目录、可读状态、非归档状态和页面导航成功。
- 未验证：用户当前界面是否仍启用了额外的项目或列表筛选；如重新进入侧栏后仍不可见，应继续检查 UI 筛选状态。
