# Zeus 升级后宿主安全交接与正常启动

## 任务目标

修复 Zeus 从 `0.1.19` 手动升级到 `0.1.20` 后，新界面连接旧执行宿主并进入“界面已安全暂停”，随后应用退出的问题。修复后，执行宿主交接不得影响应用正常启动；旧会话继续运行时，新版主窗口和工作台仍需正常打开。

## 用户确认口径

1. 直接在 `main` 分支开发，不创建或切换任务分支。
2. 新版应用必须先正常打开；宿主交接只能限制相关局部操作，不能阻断整个应用启动。
3. 旧宿主已有执行轮次不能因升级被强制终止或自动重跑。
4. 不能启动第二个数据库写入者；无法安全切换时继续使用兼容旧宿主，并明确限制旧宿主不支持的新能力。
5. 修复完成后使用独立身份 `Zeus Test.app` 做真实运行验收，再提交、推送并发布下一补丁版本。
6. 不使用或修改日常正式 `/Applications/Zeus.app` 作为开发验收对象。

## 现场证据

- `/Applications/Zeus.app` 已更新为 `0.1.20`，bundle id 为 `dev.hypha.zeus`。
- 2026-08-05 10:29 的执行宿主日志显示，仍存活的宿主由 `0.1.19` 启动；`0.1.20` 界面三次附着后都很快退出。
- Renderer 恢复页来自 `apps/desktop/src/renderer/ErrorBoundary.tsx`，说明应用主进程和 Renderer 已经启动，实际失败是界面运行期异常，不是应用包完全无法执行。
- `0.1.20` 的 dashboard 新增 `conversationAttentionByProject`；旧 `0.1.19` 宿主返回的 snapshot 没有该字段，`App.tsx` 直接读取后触发顶层错误边界。
- 当前宿主发现只校验 `executionHostProtocolVersion = 1`；`0.1.19` 与 `0.1.20` 使用相同控制协议号，因此旧宿主被误判为完整界面 API 兼容。

## 修复设计

### 应用启动边界

Renderer 在消费本机 API 响应前统一补齐跨版本可选字段。上一正式版宿主缺少新增投影字段时，使用真实空状态或派生默认值，不允许因为属性缺失进入顶层崩溃页。

优点：新版窗口可以立即读取旧宿主中的真实数据，已有会话不受影响。

缺点：只适合兼容可安全默认的新增字段；语义不兼容的写接口仍需单独限制，不能用默认值掩盖。

### 宿主兼容事实

界面配置携带当前应用版本、宿主应用版本和交接状态。相同版本表示当前宿主；不同版本表示正在使用上一正式版宿主完成存量工作。该事实用于局部能力限制和用户提示，不用应用版本字符串冒充底层运行时世代。

优点：用户能区分“应用已经打开”和“部分新版能力等待宿主切换”。

缺点：版本只能说明宿主代码来源，真正的长期兼容仍需显式能力表与发布门禁。

### 安全交接

旧宿主仍有活动工作时继续附着，保证现有轮次、审批和回答可用；旧宿主空闲后才允许关闭并启动新版宿主。交接过程保持唯一 SQLite 写入者，不在旧宿主未释放锁时创建第二宿主。

优点：不丢任务，不产生双写。

缺点：交接期间旧宿主不支持的新增写能力必须明确受限；旧版本没有“进入排空”控制能力时，本次补丁只能保守等待安全窗口。

## 实施范围

- `apps/desktop/src/renderer/apiClient.ts`：统一规范化 dashboard snapshot，补齐跨版本可选字段。
- `apps/desktop/src/main/executionHostProtocol.ts`、`localServerRuntime.ts`：向 Renderer 提供宿主版本与交接事实，发现空闲旧宿主时执行唯一宿主切换。
- `apps/desktop/src/renderer/main.tsx`、`App.tsx` 与样式：应用正常渲染并展示低干扰交接提示；只限制旧宿主不支持的局部能力。
- 发布门禁：加入“上一正式版宿主 + 当前候选界面”的真实启动验收，防止新增字段再次让应用整体崩溃。

## 验收条件

1. 使用隔离用户数据启动上一正式版宿主，再启动当前 `Zeus Test.app`，主窗口正常出现且不进入界面错误页。
2. 旧宿主有活动工作时保持原宿主 PID 和执行轮次，不启动第二数据库写入者。
3. 交接状态只显示低干扰提示；项目、任务和会话真实数据仍可读取。
4. 旧宿主缺少的新版写能力显示局部不可用原因，不触发顶层错误边界。
5. 旧宿主空闲后安全退出，只启动一个新版宿主，Renderer 使用刷新后的本机端点继续工作。
6. SQLite `quick_check` 与应用 `/health` 均为真实成功。
7. `pnpm lint`、`pnpm typecheck`、`pnpm build`、`pnpm package:mac` 和 `pnpm verify:release` 按阶段通过；静态与构建结果不替代 GUI 验收。

## 当前进度

- 已完成现场日志、应用版本、宿主版本和 Renderer 崩溃字段的根因取证。
- 已确认直接在 `main` 开发并在真实测试包验收后发布。
- 已完成 dashboard 跨版本规范化，旧宿主缺少 `conversationAttentionByProject` 时使用空投影，不再进入顶层崩溃页。
- 已完成宿主版本事实下发、局部新执行入口限制、顶部低干扰提示与空闲稳定后的唯一宿主切换。
- 发布前门禁首轮暴露了 `main` 中已存在的合并残留：任务仓储事务缺少收口、任务工作台分支状态函数被错误嵌套、重复类型导入和未使用的迁移常量；已按现有业务语义做最小修复，否则任何正式构建都无法开始。

## 真实验收记录

- 2026-08-05 执行 `pnpm lint`、`pnpm typecheck`、`pnpm build` 和 `pnpm package:mac`，当前候选代码全部通过；打包产物为独立身份 `Zeus Test.app`。
- 使用独立 `ZEUS_USER_DATA_DIR` 直接启动当前 `Zeus Test.app`，真实窗口、项目导航和会话工作台正常渲染，未进入错误页。
- 从 `v0.1.19` 标签使用 `git archive` 生成临时旧版 `Zeus Test.app`，不创建或切换分支；直接启动其独立执行宿主。
- 当前候选界面成功附着 `0.1.19` 宿主，工作台先正常打开，界面明确显示“Zeus 已正常启动”和交接期局部限制，没有白屏、全屏等待或崩溃页。
- 旧宿主空闲稳定 10 秒后安全退出，新宿主启动并刷新真实 BrowserWindow；交接后工作台完整恢复，只存在一个执行宿主 PID。
- 交接后 `/health` 返回 `ok=true`、`database=ok`、`runtime=ok`，SQLite `PRAGMA quick_check` 返回 `ok`。
- 以上验收未启动、替换或修改 `/Applications/Zeus.app`。

## 待完成交付

- 升级下一补丁版本，执行完整 `verify:release` 与 `verify:publish`。
- 提交并推送 `main`，发布 GitHub Release 与 Homebrew Cask，再回下载核对公开制品。
