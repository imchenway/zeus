# 发布失败与高能耗诊断

## 任务背景

用户在 Zeus 正式应用的 AI Runtime 中执行 `sh -lc pnpm release`，遇到两个现象：

1. 首次发布失败。
2. 命令执行期间整机能耗异常升高，风扇高速运转，机器持续发热。

本轮先完成只读跟踪和隔离复现；用户随后明确授权修复，并允许在需要时重新执行 Zeus 发布命令。代码修复、静态检查、打包、测试包运行回归、v0.2.18 正式升级发布，以及用户日常安装的正式 Zeus 升级后能耗复验现已全部完成。

## 结论

### 发布结果

- 首次 Release Workflow `31291960474` 失败在 `actions/upload-artifact@v4` 创建 GitHub Actions 产物时。
- 打包和致命产物校验已经成功；失败日志是 GitHub 产物服务连续 5 次请求超时：
  `Request timeout: /twirp/github.actions.results.api.v1.ArtifactService/CreateArtifact`。
- 用户第二次触发发布后，脚本复用了同一个候选提交 `640b21d9ceaad7e62e8ee6566b0012eaa521a30f`，Release Workflow `31292396463` 完整成功。
- v0.2.17 已于 `2026-08-09T03:29:19Z` 正式公开，DMG 和发布清单均已上传，Homebrew 同步步骤成功。
- 因此首次失败属于 GitHub Actions 产物服务的瞬时故障，不是代码编译失败，也不是 DMG 打包失败。
- 修复提交为 `3dd4c5877db532e6f42e87aabf0b95529c471ca5`，正式发布提交为 `a26362fb17ccdb3efc021de2abcb04cb1cd9c572`。
- v0.2.18 Release Workflow `31298166095` 的 preflight、package-mac、typecheck、publish 四个作业全部成功；artifact 首次上传成功，因此本轮没有人为制造外部超时来触发重试分支。
- v0.2.18 已于 `2026-08-09T06:11:06Z` 正式公开，DMG 为 `Zeus-0.2.18-arm64.dmg`，大小 `157085507` 字节，SHA-256 为 `37ae3d19636f064727c3b2883aeb713125fa8a13ce88fb43cc3415dee85f0c24`；manifest 与 Homebrew Cask 均已同步。
- 用户日常安装的 `/Applications/Zeus.app` 已通过 Homebrew 从 0.2.17 升级到 0.2.18，bundle ID 为 `dev.hypha.zeus`。公开 manifest 仍明确记录 `signed=false`、`notarized=false`，不能描述为 Apple 已签名或已公证。

### 高能耗首要根因

高能耗发生在 Zeus 自身的命令详情读取与渲染链路，不是远端 GitHub 打包消耗本机 CPU。隔离复现已经证明：命令本身不需要是 `pnpm release`，任意命令完成后只要命令详情保持选中，就会触发持续循环。

直接证据：

- 第二次 Runtime 已在 `2026-08-09T03:29:34.762Z` 以退出码 0 结束，日志只有 66 行、约 31 KB。
- 命令结束数分钟后，正式 Zeus 的 execution host 仍约占 44% CPU，Renderer 仍约占 48% 至 52% CPU，Network Service 仍约占 18% 至 19% CPU。
- 本机回环 HTTP 连接在命令结束后仍持续传输，5 秒内继续增加约 14 MB；应用启动约 8 分钟时累计读取已超过 4.7 GB。
- 应用启动约 15 分钟、发布命令结束约 11 分钟后，三个进程仍分别约占 47.5%、46.7% 和 19.4% CPU，本机累计读取已增长到约 10.5 GB，证明异常循环不会随发布子进程退出而退出。
- execution host 的进程采样显示大量时间消耗在 HTTP 回调和 `node::sqlite::StatementSync::All`，与远端构建进程无关。
- Zeus 项目当前有 188 个未归档任务、24 个未归档会话、33838 个会话项，数据库约 483 MB。
- 使用正式数据库的一致性只读副本启动 `Zeus Test.app`，页面空闲时 execution host 与 Renderer 均约 2% 至 4%。
- 通过命令中心执行只输出一行 `ZEUS_ENERGY_PROBE_OK` 的 `printf` 命令后，execution host 立即升到约 46.9%，Renderer 升到约 49.8%，Network Service 升到约 18.0%。这排除了 pnpm、GitHub CLI、远端打包和大日志是必要条件。
- `CommandCenterPanel` 的命令详情 effect 依赖 `activeRuns` 数组；每次详情加载又无条件 `map` 出新的 `runs` 数组。即使选中执行已经结束、内容也完全没变，新的数组仍会让 effect 再次立即加载详情，形成“HTTP 详情请求 → SQLite 查询 → setRuns 新数组 → effect 重跑”的无限循环。
- 服务端每次详情请求还会查询 Runtime 会话和日志，因此该循环同时解释了 SQLite `StatementSync::All`、本机 HTTP 流量、Renderer 重绘和命令结束后仍不停止的现象。发布日志比一行探针更大，只会进一步放大每次循环的传输量。

同时存在但不是本次启动条件的放大链路：

1. `apps/desktop/src/renderer/App.tsx` 在进入“会话”或“任务”区域时，会对当前项目的每一个任务同时调用 `loadTaskConversationChoices`。Zeus 项目当前一次会创建 188 个任务请求，另加 1 个项目请求。
2. `packages/local-server/src/index.ts` 的 `listTaskConversationHistory` 没有使用已有的按任务查询，而是每个任务请求都分页读取整个项目的全部会话，然后在 JavaScript 中按 `taskId` 过滤。
3. `packages/storage/src/index.ts` 的 `listByProject` 还会为分页命中的每个会话读取消息。仓储层实际上已经存在 `listByTask(taskId)`，但上述接口没有使用。
4. 每个任务请求完成后，Renderer 又分别更新任务会话状态，带来大量独立 React 更新和重复渲染。

任务会话扇出会加重大项目的 SQLite 读取和 Renderer 更新，但隔离复现表明它不是“执行任意命令后立刻持续飙高”的首要根因。根据本机回环累计流量，当前问题也不能被简化为 `gh run watch` 输出过多；第二次发布 Runtime 的全部日志只有约 31 KB，远小于数 GB 的本机 HTTP 流量。

## 触发关系的校正

- 已确认：每次 Runtime 输出都会被 Zeus 写入 SQLite、终端日志文件并广播实时事件。
- 已确认：修复前的发布脚本等待 GitHub Workflow 时，每 10 秒执行一次 `gh run watch` 状态刷新，因此发布期间会稳定地产生 Runtime 输出事件。
- 已确认：第二次发布原始终端日志只有约 35 KB，且只有 2 个 ANSI ESC 控制字符，不支持“终端控制字符或日志体积直接把电脑压满”的解释。
- 已确认：异常 HTTP/SQLite/Renderer 循环在 Runtime 退出后仍持续至少 11 分钟。
- 已通过对照回归确认：使用同一份隔离数据库，命令中心空闲时 CPU 保持低位；执行只输出一行的无害命令后立即复现。触发器是命令详情状态更新，不是打开会话页，也不是发布脚本自身。
- 已完成：先切断命令详情自触发循环和重叠轮询，再收口项目会话批量读取、实时摘要刷新、Runtime 日志内存与持久化放大；发布侧低噪声轮询和上传重试作为可靠性补强一并完成。

## 实施阶段记录

- 命令详情轮询只依赖“所选执行是否仍在运行”这个稳定布尔值；详情未变化时保持原状态对象，请求进行中时禁止重叠。运行中最多每秒读取一次，进入终态后停止轮询。
- 终态存在日志积压时，不再无间隔补拉完整历史，而是只做一次受 2000 条和约 4 MB 限制的尾部重定位。完整日志仍保留在 Runtime 中，需要时可以分页查看或导出。
- Runtime 日志分页改为数据库持久序号游标，不再使用实时追加时会漏行或重复的偏移量分页；同一毫秒产生的日志也能保持稳定顺序。
- 日志正文查询先读取字节长度，再在数据库层按约 4 MB 预算取正文。即使出现单条 5 MB 日志，也只把轻量省略标记交给 Node 和 Renderer，避免先把巨块读进内存再截断。
- Runtime 内存日志设置单会话 2000 条、约 4 MB、缓存会话数和总会话数上限；SQLite 仍保留完整历史。数据库保存与日志文件写入按 100 毫秒合并，移除每块日志一个小文件的写放大。
- 每个 stdout/stderr 数据块不再复制成一条会话消息；绑定任务的 Runtime 结束时只写入一次有界终态摘要。实时事件按 100 毫秒真正批量合并，只更新 sessionId 匹配的会话，避免对全部会话和 4 MB 窗口反复编码、复制。
- Runtime 的完成边界从子进程 `exit` 改为标准输出、标准错误均排空后的 `close`。macOS 下按进程组发送 TERM/KILL 并确认整棵进程树退出；发现孤儿进程时保留管理权并允许再次强杀，不能把仍存活的子进程误报为已完成。
- Runtime 在 spawn 前生成随机进程身份，先持久化再注入子进程环境；spawn 后立即把真实 PID 单独提交到数据库，完成前不向调用方报告启动成功。这个内部身份不进入 Runtime API、命令详情、审计日志或终端输出，并自动纳入日志脱敏。
- Zeus 异常退出后恢复 Runtime 时，只有确认原进程组已不存在才记为 `lost`；进程组仍存在但身份无法核验时保留 `orphan_detected` 并拒绝发信号，避免 PID 复用导致误杀无关进程。身份可核验时才按进程组 TERM、限时等待、KILL 并再次确认。
- 未确认终态的 Runtime 不得归档或软删除，启动恢复会重新显示历史上被隐藏的活动会话。任务终态操作只发出停止请求并立即返回“待清理”冲突，用户在进程树确认结束后重试；不再让一个 HTTP 请求同步等待最长约 11 秒。
- local-server 关闭前先等待 Runtime 管理器完成；强制关闭时阻断晚到的日志回调，避免数据库关闭后仍有后台写入。如果进程树仍未确认退出，local-server 保留数据库并低频重试，execution host 也保留控制面；只有安全收口后才退出。非 Codex 图谱调用也改为等待真实完成或按配置超时后停止、强杀并再次确认。
- 项目会话选择改为一次 metadata-only 批量查询并按任务分组，Renderer 只合并一次状态；实时列表、执行主机状态、待处理请求和旧会话迁移也全部改为轻量记录或有界查询，不再把长会话消息历史读入内存后丢弃。
- 发布脚本改为每 10 秒读取 Workflow JSON，只在状态、作业或步骤变化时输出；连续三次状态读取失败才终止。Release Workflow 的 artifact 上传先正常执行，外部服务失败时再条件重试一次，重试仍失败会继续阻断发布。

上述边界的代价是：命令详情只保留最新约 2000 条、约 4 MB 的终端投影，完整历史需要进入 Runtime 分页查看；文件和数据库实时落盘最多增加约 100 毫秒延迟；无法核验身份的跨重启孤儿进程会保持可见并要求人工处理，而不是冒险强杀；GitHub artifact 只自动重试一次，不能掩盖持续性外部故障。换来的收益是命令结束后不再持续轮询、长日志不会无限占用 Renderer/Node 内存、发布等待日志更短，并且后台子进程不会因 PID 复用被误判或在终态前被隐藏。

## 建议修复方案

### 方案一：项目级批量加载任务会话摘要，推荐

一次查询项目会话的紧凑摘要，在服务端按 `taskId` 分组，再由一个接口返回所有任务的会话选择结果；Renderer 只进行一次请求和一次状态合并。

优点：

- 从根本上消除 188 个并发请求和 188 次独立渲染。
- 服务端只扫描一次，项目任务数量增加时性能仍可控。
- 最符合会话树一次展示整个项目数据的真实使用方式。

缺点：

- 需要新增批量接口和对应数据结构，改动范围比单点替换更大。
- 需要明确单次响应的大小上限，避免以后项目会话过多时形成另一个大响应。

### 方案二：先把任务接口改成已有的 `listByTask`

将 `listTaskConversationHistory` 改为直接调用仓储层已有的 `conversations.listByTask(taskId)`。

优点：

- 改动小，能立即消除“每个任务都扫描整个项目”的数据库浪费。
- 风险相对较低，现有接口形状不变。

缺点：

- Renderer 仍会同时发出 188 个 HTTP 请求，并产生大量独立状态更新。
- 只能缓解，不能彻底解决大项目下的能耗和渲染问题。

### 发布可靠性补强

可为 GitHub Actions 的产物上传增加工作流级重试，并把本机 `gh run watch` 改为只在状态变化时输出的低噪声轮询。

优点：

- GitHub 产物服务瞬时超时时，不必重新走完整打包。
- Runtime 日志更短，更容易看清真正失败节点。

缺点：

- 重试只能提高容错，无法保证 GitHub 外部服务永不失败。
- 低噪声轮询只能减少次要开销，不能代替会话列表数据链路修复。

## 验证边界

- 已验证：第二次 GitHub Actions 四个作业全部成功；公开 Release、DMG、manifest 和 Homebrew 同步步骤存在。
- 已验证：第二次本地 Runtime 退出码为 0。
- 已验证：命令退出后正式 Zeus 进程仍保持高 CPU 和持续本机 HTTP 流量。
- 已验证：188 请求扇出和逐任务扫描整个项目的代码路径真实存在。
- 已执行：隔离数据库下的 `Zeus Test.app` 基线与一行命令触发对照，首要根因可稳定复现。
- 已执行：命令详情循环、Runtime 日志游标与内存边界、进程组终结、会话 metadata-only 查询、低噪声发布轮询与 artifact 条件重试的全部代码修改。
- 已通过：`pnpm typecheck`、`pnpm lint`、`pnpm build`、`git diff --check`。
- 已通过：`pnpm package:mac`，生成的独立测试包是 `dist/test/mac-arm64/Zeus Test.app`，bundle ID 为 `dev.hypha.zeus.test`，版本为 0.2.17；深度严格签名校验通过。
- 已执行：最终测试包使用同一份约 483 MB 的隔离真实数据库启动，并通过命令中心真实执行只输出一行的“能耗探针”。执行 0 秒成功退出、退出码 0、终端日志完整。
- 已验证：最终测试包中，探针终态后连续 60 秒 CPU 采样为：main 平均 0.16%、峰值 0.90%；execution host 平均 0.20%、峰值 1.00%；Renderer 平均 2.34%、峰值 3.80%；Network Service 为 0%；GPU 平均 3.22%、峰值 4.60%。五个进程平均合计约 5.92%，与低位基线一致。
- 已验证：同一 60 秒窗口内，Zeus 相关进程的 loopback 双端口径合计 369706 字节，平均约 6162 字节/秒，单秒峰值 8744 字节；旧版本同条件下 5 秒增加约 14 MB，并持续至少 11 分钟，差异明确。
- 已验证：能耗探针会话已持久化 36 位内部身份、真实 PID、退出码和 `endedAt`；命令详情 API 返回 200，不包含身份字段名或身份值。探针 PGID 在终态后为空。
- 已验证：真实执行一个忽略 TERM 且包含 `sleep 300` 子进程的命令，点击停止后 Zeus 记录“未在宽限期内退出，已升级为强制终止进程组”，会话进入 `stopped` 并写入 `endedAt`；shell 和 sleep 所在 PGID 全部消失。
- 尚未执行：真实 GitHub artifact 超时分支只能在后续外部服务再次瞬时失败时现场验证；本轮只做工作流结构与静态检查。
- 已执行：使用修复后的 `pnpm release` 完成 v0.2.18 正式发布；Workflow `31298166095`、公开 Release、DMG、manifest、Homebrew Cask 和本机安装版本均已回验。
- 已执行：在升级后的正式 `/Applications/Zeus.app` 中从命令中心运行只输出一行的 `v0.2.18 能耗探针`。界面显示 0 秒成功；数据库记录 `status=exited`、退出码 0、真实 PID、36 位内部身份和 `endedAt`，未读取或输出身份内容，终态后 PID/PGID 查询为空。
- 已验证：正式 v0.2.18 探针终态后冷却约 139 秒，再连续采样 60 秒。main 平均 0.262%、峰值 3.0%；execution host 平均 0.315%、峰值 1.2%；Renderer 平均 3.312%、峰值 4.3%；Network Service 为 0%；GPU 平均 4.570%、峰值 5.1%；五进程平均合计 8.458%、合计峰值 11.0%。
- 已验证：同一正式版 60 秒窗口内，loopback 双端合计 367080 字节，平均 6118 字节/秒，单秒峰值 6118 字节；与测试包结果一致，未再出现旧版本 5 秒约 14 MB、持续增长到数 GB 的循环。
- 已清理：正式应用中为本轮创建的临时探针命令定义已删除，执行历史保留作为验收记录；正式 v0.2.18 应用保持运行，未影响用户其他会话。

## v0.2.18 升级后的发布分页器阻塞

- `2026-08-09 17:33`，用户从正式 v0.2.18 再次运行“一键升级发布 Zeus”后，界面持续显示运行中，终端停在 `(END)`。
- 现场进程组为：`pnpm release` → `node scripts/release-all.mjs` → `git diff --check v0.2.17^{commit} <candidate>` →
  `/usr/bin/less`。四个进程 CPU 均为 0%，说明不是高能耗循环，而是 Git 在 PTY 中自动启动分页器并等待 `q`。
- Runtime 只有启动信息和 `(END)` 共 4 条、134 字节日志，最后一条日志在启动后约 1 秒写入；等待 16 分钟没有继续执行。
- 已通过正式 Zeus 的“停止”按钮终止本次执行。界面状态为“已取消”，数据库记录 `run=cancelled`、`session=stopped`，PGID 下的
  `pnpm`、`release-all`、`git`、`less` 均已消失。
- 发布脚本所有直接继承终端的 Git 操作现统一通过 `git --no-pager` 执行。优点是即使 Runtime 提供 PTY，也不会再进入 `less`
  等交互分页器；代价是 Git 输出会直接写入日志，但当前发布脚本的 Git 输出量很小，并已受 Runtime 日志上限保护。
- 本轮只修复并验证脚本，不创建提交、不推送、不执行新一轮发布；由用户自行重新触发发布。
- 用户将修复提交为 `86c1a08b63e56d191826f61a97d8bed9fd2f7ce5` 后自行重试，发布已顺利越过原先的 Git 空白检查和分页器阻塞，证明
  `--no-pager` 修复生效。
- 第二次执行随后被候选 Prettier 门禁阻断。提交后的 `scripts/release-all.mjs` 额外包含 import 花括号空格删除和 7 处
  `runGit` 多缩进两格的格式漂移，不符合仓库 `prettier.config.js`；这不是 GitHub、打包或发布脚本运行时故障。
- 第二次失败仍发生在版本写入、发布提交、main 推送和远端发布之前。已使用仓库固定 Prettier
  恢复脚本格式，并重新执行候选范围格式检查、lint、typecheck 和 Git 空白检查；仍由用户自行提交后重新发布。
- 用户随后创建的 `abb893bb1545b9d5073ea79346de2f393ce20777` 只提交了本诊断文档，没有包含 `scripts/release-all.mjs`
  。因此第三次发布仍读取 `86c1a08` 中同一份格式错误的脚本，再次被同一 Prettier 门禁阻断，并不是修复后的脚本重新发生漂移。
- 第三次失败同样没有写入版本、创建发布提交、推送 main 或触发远端发布。脚本格式已再次恢复；下一步必须确认格式修复本身真实进入候选
  HEAD 后，才能由用户重新发布。
- 当前工作区已确认只包含脚本格式修复和本诊断文档更新。发布脚本实际检查的 19 个候选文件、诊断文档、脚本语法、`pnpm lint`、
  `pnpm typecheck` 与 `git diff --check` 均已通过；尚未创建新的本地提交，也未推送或发布。
- 用户要求发布门禁自动修复格式问题。发布编排调整为先对候选范围执行 `Prettier --write`，再执行 `Prettier --check`
  ；若产生变更，只把候选范围内的格式修复创建为独立本地提交，并重新绑定候选提交后继续发布。优点是格式错误不再要求人工修复和重跑；缺点是确有格式漂移时，发布历史会多一个可审计的格式提交。

## 2026-08-09 23:40 会话页高能耗复发现场

本次只读诊断确认，当前高能耗不是前述命令详情无限 HTTP / SQLite 循环原样复发，而是正式 Zeus 正在展示一个执行 Computer Use
GUI 验收的活动会话，形成“正式 Zeus 实时渲染过程 + 屏幕采集服务 + Zeus Test.app”嵌套工作负载。

现场证据：

- 正式应用版本为 `0.2.21`，停在项目会话页；选中会话为“远程为什么一定要会话全部完成？”，界面显示正在处理，并正在用 Computer
  Use 检查新启动的 `ZEUS-0151` 独立测试包。
- 正式 Zeus Renderer 连续 5 秒约为 `18%` 至 `28%` CPU，GPU 进程约为 `10%` 至 `14%`；正式主进程接近 `0%`，execution host
  后续回落到 `0%`，Codex app-server 约为 `0.6%` 至 `1%`。这与旧故障中 execution host 约 `45%` 的特征不一致。
- 屏幕采集服务 `SkyComputerUseService` 约为 `20.2%`，Codex GPU 服务约为 `17.2%`，WindowServer 一度达到 `79.8%`；这些进程与正式
  Zeus Renderer / GPU 同时活跃，足以解释风扇高速运转。
- 当前活动会话 5 秒内 JSONL 从 `1,615,385` 字节增长到 `1,869,133` 字节，新增约 `254 KB`；数据库中的 provider 序号已超过
  `7,000`，token 用量已超过 `300 万`，说明页面正在持续接收并投影密集的 GUI 工具过程，而不是静态空闲。
- execution host 的本机 TCP 发送量在 5 秒采样中每秒只增加约 `2.5 KB`，未出现旧故障每 5 秒约 `14 MB`
  的回环流量；因此不能把本次问题归因于命令详情请求循环。
- 新启动的 `ZEUS-0151` 测试包本身主进程约 `1%`，其 execution host、Renderer 和 GPU 接近 `0%`；其存在是嵌套 GUI
  验收的组成部分，但不是单独打满 CPU 的主进程。
- 系统和会话页均显示已启用“减少动态效果”，会话 CSS 也会关闭无限动画；因此当前 GPU 压力更符合持续屏幕采集和实时 DOM
  更新，而不是单纯的 spinner 动画漏关。

当前判断与取舍：

1. 立即降温的最短路径是停止或等待当前 GUI 验收会话结束；优点是立刻切断屏幕采集和实时过程渲染，缺点是会中断当前 `ZEUS-0151`
   验收现场，不能由诊断任务擅自执行。
2. 产品侧应把 Computer Use 截图频率、全量无障碍树回传和会话页实时渲染分别限流，并避免在正式 Zeus 内持续高频展示其自身测试包的完整
   GUI 过程；优点是保留自动验收能力，缺点是需要明确“实时程度”和能耗之间的取舍。
3. 会话 Renderer 还应补充长会话下的增量渲染剖析，重点核对 40 毫秒 delta 合并、`transcriptRevision`
   引发的列表重算和活动耗时每秒更新；当前已有强相关证据，但尚未用 DevTools 性能录制证明单一 React 函数是最终热点，不能把这一层写成已确认代码根因。

本阶段未停止任何会话、未退出应用、未修改运行数据，也未创建提交或发布。

## 2026-08-09 23:49 与 Codex App 的性能差异复核

用户指出 Codex App 使用同一 Computer Use 能力时不会出现同等高能耗。复核后，前一阶段把高占用主要解释为“嵌套 GUI
验收工作负载”并不完整：Computer Use 是负载来源之一，但 Zeus 会话页对大体积工具结果的处理才是已确认的产品侧放大器。

新增现场证据：

- 当前验收轮次已于 `23:45:14` 完成，JSONL 停止增长，最终约为 `2.75 MB`；但数分钟后正式 Zeus Renderer 仍为 `16.2%` CPU。
- 同一时刻 `ZEUS-0151` 独立测试包 Renderer 为 `0%`，旧测试包 Renderer 也为 `0%`；因此被验收应用不是持续高占用来源。
- 当前轮次共有 13 条 MCP 工具结果，载荷约 `890 KB`，其中单条最大约 `151 KB`；另有 21 条命令结果约 `349 KB`。Computer Use
  多次使用 `disableDiff: true` 获取完整无障碍树，产生了重复的大体积全量结果。
- `SessionActivityGroup` 在外层 `<details>` 关闭时仍遍历全部活动项并挂载 `ActivityItemRow`；每行又通过
  `activityItemDetail` 取出完整输出，并创建隐藏的 `<pre>`。浏览器只是不显示关闭内容，React 和 DOM 并没有延迟创建这些节点。
- 会话每次 item 增量都会复制 `state.items`、递增 `transcriptRevision`；Renderer 最多每 40
  毫秒应用一次合并增量，随后重新投影完整条目列表、活动分组和轮次索引。长会话和大工具输出会把这条链路显著放大。
- `SkyComputerUseService` 此时仍约为 `18.1%`，说明屏幕采集服务本身也有成本；但同一能力在 Codex App 中可正常使用，且 Zeus
  Renderer 在验收轮次结束后仍高占用，因此不能再把它当作 Zeus 侧持续高占用的充分解释。

修正后的根因排序：

1. 已确认的 Zeus 缺陷：折叠活动详情仍挂载全部原始工具输出，没有按展开状态懒加载，也没有对大输出做摘要或截断。
2. 已确认的输入放大：本次验收反复请求 Computer Use 全量无障碍树，而不是默认增量树，使单轮工具结果达到约 `1.2 MB`。
3. 已确认的实时放大：40 毫秒增量刷新会复制会话 items，并触发会话投影链路重算；活动组自身没有记忆化隔离。
4. 尚待性能录制确认的持续触发源：轮次完成后 Renderer 仍保持约 `16%` CPU，需用 Electron DevTools Performance 或 tracing 区分
   React 重渲染、无障碍树查询和布局/绘制各自占比。

建议修复顺序：先让关闭的活动详情不挂载子项，并对工具输出设置显示上限和按需读取；再限制 Computer Use
全量树请求；最后用虚拟列表和更粗的增量窗口处理超长会话。前两项改动小、收益直接，虚拟列表收益最大但会影响滚动定位和可访问性，实现风险更高。

## 2026-08-10 00:02 会话渲染修复与测试实例残留

会话 Renderer 首轮修复已实现：

- 工作活动组关闭时只保留摘要，不再创建活动列表和每条工具结果节点。
- 活动组打开后只创建各条标题；只有继续展开某一条活动时，才创建命令、目录和输出正文。
- 单条工具输出最多展示前 `40,000` 个字符，并明确提示已截断；完整结果仍保留在会话数据中，不因展示优化破坏追溯证据。
- 已完成活动组使用条目引用浅比较跳过无关重渲染，组内已完成条目也使用记忆化组件隔离后续活动增量。

上述方案优点是直接移除折叠状态下的大体积 DOM，改动集中且不改变会话数据协议；缺点是首次展开大活动组时仍需创建全部标题，且
Renderer 内存仍保存原始结果，后续仍需结合性能录制决定是否引入虚拟列表或后端按需读取。

用户截图中的多个 `Zeus Test` Dock 图标也已完成现场核对：

- 共发现 7 个真实 `Zeus Test` 主实例，不是最近使用图标；全部已脱离启动终端并由 PID 1 托管。
- 其中 5 个从 8 月 8 日晚持续到当前，1 个已运行约 13 小时，最新 `ZEUS-0151` 已运行约 19 分钟；`ZEUS-0129` 同一路径存在两个主实例。
- 这些旧实例当前大多为 `0%` CPU，不是本次风扇高速的首要来源，但会长期占用 Dock、内存以及部分 execution host 等后台资源。
- 真实操作复现确认：点击窗口关闭按钮后，本轮新测试包窗口消失，但主进程和 execution host 继续存在；使用 `Command-Q` 后两者才完整退出。

对应修复为：测试身份 `Zeus Test` 在最后一个窗口关闭时始终进入既有安全退出清理；正式 `Zeus.app`
仍按“后台模式”设置决定是否常驻。优点是普通验收人员点击关闭即可完成收尾；缺点是测试身份不再支持“关闭窗口后继续后台运行”，但活动任务仍会走既有退出决策与安全清理，不会直接绕过运行状态保护。

本轮没有终止上述 7 个其他任务残留实例；只启动并退出了当前主仓测试包，避免擅自破坏其他验收现场。

最终验证结果：

- `pnpm typecheck`、`pnpm lint`、`pnpm build`、Prettier 检查和 `git diff --check` 均通过。
- `pnpm package:mac` 通过，生成独立身份 `dist/test/mac-arm64/Zeus Test.app`；深度严格签名校验通过，测试包仍为 ad-hoc
  签名且未公证。
- 修复后的测试包真实启动成功，窗口和基础应用壳正常显示；只点击窗口关闭按钮后，再次查询本次 App 路径下的主进程和 execution
  host 均为空，测试身份自动退出修复通过。
- 会话渲染修复已进入真实测试包并通过构建，但该测试包的隔离数据库没有正式环境中的超长 Computer Use
  会话；因此本轮没有把空会话页冒充成同数据量性能回归完成，仍需在带有脱敏长会话数据的隔离环境中补做 CPU 对照。
