# Zeus 0.3.0 升级发布

## 当前阶段

- 阶段：发布完成
- 状态：Zeus 自带一键发布脚本执行成功
- 目标版本：`0.3.0`

## 用户确认范围

- 只通过脚本支持的 `RELEASE_VERSION=0.3.0` 指定目标版本。
- 只执行仓库自带的 `pnpm release`，不追加深度公开 DMG 下载验证或其他发布动作。
- 脚本按自身编排创建发布提交、推送 `main`、触发 Release Workflow、创建 GitHub Release 并同步 Homebrew Cask。

## 执行前事实

- 最新公开稳定版：`v0.2.41`。
- 本地 `main` 工作区干净，相对 `origin/main` 领先 4 个提交。
- 发布范围：`v0.2.41..d409ddf1634a`。

## 执行结果

- 执行命令：`RELEASE_VERSION=0.3.0 pnpm release`。
- 发布提交：`c27b092`，提交说明为 `chore(release): v0.3.0`。
- 版本文件：根 `package.json` 与 `apps/desktop/package.json` 已同步为 `0.3.0`。
- Release notes：`docs/releases/v0.3.0.md` 已由脚本写入发布提交。
- 发布说明模型缺少 Zeus 发布说明能力令牌，脚本按既定降级规则使用确定性模板继续。
- 候选格式检查通过，没有创建额外格式提交。
- 本地快速发布前置检查通过。
- `main` 已安全推送到远端发布提交。
- Release Workflow：`31585034399`，最终状态 `completed/success`。
- 工作流的 `preflight`、`typecheck`、`package-mac` 与 `publish` 作业全部成功。
- GitHub Release、公开资产与 Homebrew Cask 已由脚本创建并完成轻量对账。
- 脚本最终退出码：`0`。

## 验证边界

- 本次优点：完全沿用 Zeus 自带一键发布编排，版本、提交、远端发布和 Homebrew 状态由同一恢复状态串联，避免手工旁路。
- 本次代价：按用户要求保留脚本默认轻量回验，没有追加下载完整公开 DMG、重新计算摘要或执行 `hdiutil verify`。
- 本次没有启动、升级或操作本机日常正式 `Zeus.app`，也没有启动任何测试应用。
