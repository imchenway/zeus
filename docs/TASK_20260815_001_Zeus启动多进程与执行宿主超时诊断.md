# Zeus 启动多进程与执行宿主超时诊断

## 任务状态

- 日期：2026-08-15
- 用户决策：明确要求移除第四个同名 Zeus execution-host 进程，接受关闭或升级界面时后台执行不能继续的代价。
- 实施范围：正式启动链改为主进程直接持有 Local Server、SQLite 和 Agent 调度；同时停止已经完成的 Agent 身份迁移在每次启动重复全量回填。
- 当前结论：独立 execution-host 不再由正式启动链拉起；Electron 主进程及其 GPU、网络、渲染 Helper 仍保留。`0.3.14` 已公开发布并安装到本机，正式数据日常重启端到端完整界面就绪为 1,096 ms。

## 用户现场

活动监视器显示：

- PID `60926`：Zeus 主进程；
- PID `60927`、`60928`：两个 `Zeus Helper`，对应 Chromium/Electron 的渲染、GPU 等隔离子进程；
- PID `60929`：Zeus 独立执行宿主。该宿主通过同一个 Zeus 可执行文件配合 `ELECTRON_RUN_AS_NODE=1` 启动，因此活动监视器仍显示为“Zeus”；
- PID `60929` 瞬时 CPU 为 `102.6%`，表示占用约一个 CPU 核执行同步启动工作，不是四份界面应用同时启动。

四个进程本身不是故障。Electron 使用多进程隔离界面、渲染和 GPU；Zeus 额外把 Local Server、SQLite 和 Agent 调度放进可脱离界面的 execution-host，以便界面重启时长任务仍可继续。优点是界面与长任务解耦；代价是活动监视器会出现多个同名进程，并且宿主启动门禁必须正确处理慢启动和并发拉起。

## 现场证据

### 正式应用

- `/Applications/Zeus.app` 版本、构建版本均为 `0.3.10`，bundle ID 为 `dev.hypha.zeus`；仓库当前 `HEAD` 与 tag 均为 `v0.3.10`。
- `codesign --verify --deep --strict` 通过，说明应用包没有被破坏。
- 应用是 ad-hoc 签名，没有 Team ID；`spctl --assess` 拒绝。它是独立的发布质量风险，但不是本次弹窗的直接原因：应用已经实际创建主进程、Helper 和执行宿主，而且弹窗文字来自 Zeus 自己的 fatal startup 处理，不是 Gatekeeper 文案。
- 没有发现对应的 macOS Crash Report；`ELECTRON_RUN_AS_NODE`、`ZEUS_EXECUTION_HOST_BOOTSTRAP_PATH` 和 `ZEUS_USER_DATA_DIR` 也没有被写入当前 `launchd` 环境。

### 执行宿主时间线

`~/.zeus/runtime/execution-host/host-startup-error.log` 在本地时间 `00:05:34` 写入：

```text
Error: Zeus execution-host is already running with PID 60929.
```

`~/.zeus/runtime/execution-host/host.log` 随后显示：

```text
00:06:34  PID 60929 才写出 execution_host.ready，版本为 0.3.10
00:07:14  PID 60929 因 detached_idle 关闭
```

因此，在 `00:05:34` 发生下一次宿主拉起时，PID `60929` 已经持有单实例锁，但至少又过了约 60 秒才发布可连接的 rendezvous。界面侧只等待 20 秒；宿主未就绪时 `connectOrLaunchExecutionHost()` 直接抛出 `Zeus execution-host did not become ready within 20 seconds.`。该异常进入 `handleFatalStartupError()` 后显示用户截图中的“Zeus 无法启动，请重新打开应用”，随后界面退出。失去界面租约且没有活动工作后，已经晚到的宿主按 30 秒空闲规则自动退出。

日志中的“already running”是慢启动之后重复拉起产生的二次错误，不应误判为同时正常运行了两套数据库写入者。真正需要先解决的是为什么 PID `60929` 在 ready 之前耗时超过 20 秒，以及为什么发现活锁但尚无 rendezvous 时仍尝试创建新宿主。

## 慢启动的已确认代码热点

当前 `~/.zeus/data/zeus.db` 的只读检查结果：

| 指标 | 现场值 |
| --- | ---: |
| 数据库逻辑大小 | 1470.0 MiB |
| freelist 空闲页 | 547.9 MiB |
| `conversation_items` 数据 | 674.6 MiB |
| `conversation_items` 行数 | 92,102 |
| Codex 原生会话数 | 352 |
| 启动时需要联机恢复的非归档会话 | 121 |
| 每次启动 UPDATE 命中的会话明细 | 92,093 |
| 已经完成同样回填的命中行 | 92,093 |
| `20260803_0001_agent_runtime_framework` 迁移记录 | 已存在 |
| `PRAGMA quick_check` | `ok` |

`createZeusDatabase()` 每次打开数据库都会调用 `migrateAgentRuntimeSchema()`。该函数虽然写入了迁移记录，但没有先检查迁移是否已经完成，并且每次都执行：

```sql
UPDATE conversation_items
SET agent_kind = COALESCE(agent_kind, 'codex'),
    native_item_id = COALESCE(native_item_id, provider_item_id)
WHERE conversation_id IN (
  SELECT id FROM conversations WHERE agent_kind = 'codex'
);
```

现场 92,093 条目标记录已经全部符合目标值，但仍会在每次启动进入大范围 UPDATE。由于 `conversation_items` 同时携带较大的正文与 JSON 负载，这会重写大量 SQLite 页面、产生 WAL 和检查点开销，符合 PID `60929` 单核满载、数据库在宿主退出时仍被写入以及 ready 明显晚于 20 秒的现场表现。

除此之外，Local Server 启动还会对全部原生会话逐项读取 `conversation_items`、解析 `payload_json` 并重新归一化会话资源；该循环没有“历史回填已完成”的迁移门禁。随后 `codexNativeCoordinator.recover()` 会在 execution-host 写出 ready 之前恢复 Codex 世代，并逐个续接、读取非归档线程。现场有 352 条 Codex 原生会话，其中 121 条属于非归档恢复候选。

所以，本次可以确认的是“执行宿主被启动前的历史数据处理拖过了 20 秒”，并能确认无效全量 UPDATE 是确定存在的高开销缺陷；由于当前启动代码没有阶段耗时日志，未启动隔离测试包做计时前，不能把 PID `60929` 的全部耗时只归因于这一条 SQL。全量资源归一和 121 条会话恢复也需要分别计时后收口。

数据库本身通过完整性检查，当前证据不支持“数据损坏”判断。freelist 达到约 548 MiB 说明文件还存在明显膨胀，但压缩数据库不是首要修复；如果启动代码继续全量 UPDATE，单纯 `VACUUM` 只能暂时缩小文件，后续仍会重复产生写放大。

## 建议修复顺序与利弊

### 1. 先停止重复全量回填与全量资源归一

迁移完成后直接跳过一次性字段回填；同时把兼容修复限制为字段缺失的记录，例如只处理 `agent_kind IS NULL OR native_item_id IS NULL`。会话资源历史归一也需要用迁移记录或待处理集合驱动，不能每次启动重新解析全部历史消息。

- 优点：直接消除 92,093 条无效重写，是本次启动超时的根因修复；保留对部分迁移现场的修复能力。
- 缺点：仍需明确“迁移记录存在但数据部分缺失”的恢复策略，不能只依赖一个迁移标记后无条件跳过所有检查；资源归一规则升级时还需要新的迁移编号。

### 2. 修正“活锁存在、rendezvous 尚未写出”的并发语义

发现 execution-host 锁对应进程仍存活时，把它识别为“宿主正在启动”，在同一受限等待流程中观察该进程或 rendezvous；不要立刻再 spawn 一个必然撞锁的宿主。

- 优点：消除二次错误，避免重试把真实慢启动污染成“宿主已存在”；也能保护 SQLite 单写入者边界。
- 缺点：需要处理持锁进程卡死、PID 复用和最大等待时间，不能无限等待。

### 3. 把 20 秒硬超时改为带阶段反馈的安全上限

数据迁移、资源归一、历史会话恢复和 Provider 恢复应分别记录开始、结束和数量；界面应区分“宿主正在迁移/恢复”与“宿主已经失败”。修复无效 UPDATE 后再根据真实基线设置上限。

- 优点：大数据现场不会被一个固定数字误判为失败，用户能看到真实进度。
- 缺点：只把 20 秒改成更长时间会掩盖性能退化，不能作为单独修复。

### 4. 根因修复后再评估数据库回收空间

在数据库副本上验证 `VACUUM` 或受控重建，再决定是否对正式数据执行。

- 优点：可回收约 548 MiB 的空闲页并改善后续 I/O。
- 缺点：需要额外磁盘空间和较长独占时间，属于数据维护动作；未修复重复 UPDATE 前收益不持久。

## 2026-08-15 实施记录

### 移除第四个同名 Zeus 进程

`apps/desktop/src/main/main.ts` 的正式启动入口由 `startDesktopLocalServer()` 切换为主进程内嵌的 `startEmbeddedDesktopLocalServer()`：

- BrowserHost、Local Server、SQLite 和 Agent 调度直接位于 Electron 主进程；
- 正式启动链不再执行 `spawn(process.execPath, executionHost.js)`，因此不会再出现同名的第四个 Zeus execution-host；
- Renderer、GPU 与网络 Helper 仍由 Electron 按安全隔离模型创建，不在本次删除范围内；
- `/api/execution-host/status` 等内部接口名称暂时保留，它们表达运行状态协议，不再代表一定存在独立操作系统进程。
- 升级时如果发现旧版留下的独立宿主，先停止其活动工作并等待完全退出，再由主进程打开 SQLite，避免出现两个写入者。
- 对“旧宿主已持有锁、但尚未发布就绪文件”的慢启动窗口也做了保护：最多等待 45 秒进入可控状态；仍不可控则取消启动，不冒险并发打开数据库。

优点：消除 execution-host 锁、rendezvous、20 秒就绪超时和重复拉起竞争；活动监视器不再出现第二个同名 Zeus。缺点：Local Server 与界面重新绑定，主进程退出或升级时，数据库服务和 Agent 调度也会结束。

### 收紧退出语义

有活动工作时，退出弹窗不再提供“退出界面，任务继续运行”；只允许“停止活动工作并退出”或“取消”。如果运行状态无法读取，退出会被取消并保持应用打开，不再伪造后台继续执行。

优点：界面承诺与真实进程模型一致，不会让用户误以为第四个进程仍在后台托管任务。缺点：用户不能关闭界面后继续长任务，升级也需要先结束当前执行。

### 停止重复迁移写放大

`packages/storage/src/index.ts` 现在只在 `20260803_0001_agent_runtime_framework` 迁移尚未登记时执行 Agent 身份历史回填，并且 UPDATE 只命中缺失字段的记录。当前正式数据库已经登记该迁移，升级后的启动不会再重写 92,093 条已完成记录。

优点：直接消除当前现场已经确认的无效大表 UPDATE、WAL 和检查点写放大。缺点：首次处理真正的旧数据库时仍需执行一次必要回填；会话资源归一与 Provider 历史恢复仍有独立的性能优化空间。

## 验证边界

- 静态与构建：`pnpm lint`、`pnpm typecheck`、`pnpm build` 和 `pnpm package:mac` 均通过；构建仅有既有的大 chunk 警告。
- 包体：产物为 `dist/test/mac-arm64/Zeus Test.app`，bundle ID 为 `dev.hypha.zeus.test`；`codesign --verify --deep --strict` 通过。该测试包是 ad-hoc 签名且未公证，不代表正式发布已完成。
- 隔离运行：使用全新 `ZEUS_USER_DATA_DIR` 启动测试包，首启成功并创建主窗口；未出现“Zeus 无法启动”弹窗。
- 进程现场：只有一个 `Zeus Test` 主进程，另有 GPU、网络、Renderer 三个 Electron Helper；未出现 `executionHost.js`、`ELECTRON_RUN_AS_NODE=1` 或第二个同名 Zeus 主进程。
- 内嵌服务现场：主进程 PID `99692` 在 `127.0.0.1:62562` 监听，并由同一 PID 持有 `zeus.db`、`zeus.db-wal` 和 `zeus.db-shm`；隔离目录未生成 execution-host 运行文件。
- 退出后：测试进程全部结束，隔离数据库 `PRAGMA quick_check` 为 `ok`。
- 未执行：未启动或替换正式 `/Applications/Zeus.app`，未用 1.47 GiB 正式数据做修复后耗时对比，未提交、推送、发布、清理数据、删除锁文件或压缩正式数据库。

## 发布授权

- 2026-08-15，用户明确要求“升级发布”，授权提交本次修复、推送 `main` 并执行仓库自带的正式发布编排。
- 发布目标由 `scripts/release-all.mjs` 根据当前公开稳定版自动选择下一个补丁版本；不手工预改 `package.json`。
- 上述“未提交、推送、发布”是实现验证结束时的阶段边界；后续发布事实以版本化 Release notes、远程标签、Workflow 和公开资产回验为准。

## 2026-08-15 0.3.12 正式数据启动阻塞续诊

### 现场结论

- `/Applications/Zeus.app` 已是 `0.3.12`，包体深度签名校验通过，不是旧版本未覆盖或应用包损坏。
- 正式主进程在 `14:37:04` 创建，但直到 `14:40:44` 才创建 Renderer，用户实际等待约 3 分 40 秒；其间只有主进程、GPU Helper 和网络 Helper，所以界面表现为“启动不了”。
- 主进程采样显示绝大多数执行时间位于同步 SQLite `StatementSync.all()` 和磁盘 `pread`；正式数据库此时约 1.54 GB，包含 100,963 条 `conversation_items`。
- Local Server 在 Provider 恢复之前还会无条件枚举全部原生会话、读取 100,963 条历史消息、解析正文与 `payload_json` 并重算会话资源。正式库已经有 5,740 条资源投影，但旧代码没有完成标记，因此每次启动仍会重复执行整段历史回填。
- 同一次启动生成了 58 份 Codex shell snapshot，并逐项更新多个历史任务日志。这与 `migrateLegacyCodexThreads()` 对已经完成迁移的旧线程再次执行 `resumeThread()`、`readThread()` 和完整快照导入一致。
- 当前有 126 个未归档 Codex 原生会话，其中 96 个为 `paused`、26 个为 `ready`。旧恢复器在窗口创建前仍对这些会话逐个续接 Provider、读取线程并投影历史；这不是启动所必需的最小集合。
- 正式应用最终能够打开，且主进程开始监听回环端口，证明当前数据可读、启动链没有永久死锁；问题是同步历史恢复把窗口创建推迟了数分钟。

### 根因修复

`packages/local-server/src/legacyCodexThreadMigration.ts` 现在先识别已落库的 `legacy_source_conversation_id + session_id` 迁移身份；命中后直接复用，不再联机恢复 Provider 或重放快照。已归档的旧来源也不再进入迁移候选。

`packages/local-server/src/codexNativeConversationCoordinator.ts` 将启动恢复收敛为真正需要保持执行连续性的会话：

- `binding`、`active`、`waiting` 会话仍在启动时恢复；
- `dispatching`、`active` 提交仍会触发所属会话恢复；
- `ready` 与 `paused` 历史会话不再阻塞窗口创建，用户点开并执行相关操作时只恢复当前会话；
- 同一 app-server 世代记录已恢复的会话身份，后续操作不重复恢复；世代变化后自动清空并重新核验；
- 启动时计划操作与自动回答定时器只枚举轻量会话记录，不再为全部会话加载完整消息正文。

`packages/local-server/src/index.ts` 为会话资源历史投影增加明确的回填修订标记：

- 从未产生资源投影的旧库仍会完成一次全量历史回填，结束后写入修订标记；
- 已经存在资源投影的现有库只登记当前修订，不再为了证明同一事实而重新读取全部历史正文；
- 新消息仍沿用实时资源归一流程，打开资源时仍校验真实路径；后续如果投影规则发生变化，需要提升修订号并提供对应的受控回填，不能再次恢复为每次启动全量扫描。

优点：正式数据规模增大时，启动耗时不再与全部历史会话数和历史正文体积线性增长；真正活动中的工作仍保留恢复能力。缺点：长时间未访问的 `ready` 或 `paused` 会话首次执行操作时会承担一次单会话恢复等待；资源投影规则升级也必须显式提升修订并安排回填，但影响都不再拖住每次应用启动。

### 验证结果

- 静态与构建：`pnpm typecheck`、`pnpm lint`、`pnpm build`、`pnpm package:mac` 和 `git diff --check` 均通过；构建只有既有的大 chunk 提示。
- 包体：`dist/test/mac-arm64/Zeus Test.app` 版本为 `0.3.12`，bundle ID 为 `dev.hypha.zeus.test`；`codesign --verify --deep --strict` 通过，`dist/test/Zeus-Test-0.3.12-arm64.dmg` 经 `hdiutil verify` 校验有效。测试包仍是 ad-hoc 签名且未公证，不代表正式发布完成。
- 正式数据副本：通过 SQLite 在线备份复制正式数据库，仅在副本中关闭远程控制并把 Codex 命令替换为不可用占位，避免访问正式凭据或 Provider。修复后首次核心服务启动为 5,147 ms，再次启动为 1,103 ms，`/health` 均返回 200；对比当前正式 `0.3.12` 的窗口创建实测约 220 秒，启动阻塞已从分钟级收敛到秒级。
- 资源投影：副本保留 5,740 条资源并登记 `20260815_resource_projection`，属于采用既有投影；全新空库路径用 25 ms 启动，登记同一修订且资源数为 0，覆盖了真正从未回填的分支。
- 数据完整性：正式数据副本与全新空库退出后 `PRAGMA quick_check` 均为 `ok`；两个临时目录已经移入废纸篓。正式数据库没有被修改、清理或压缩。
- GUI 边界：没有启动测试包做界面验收，因为当前窗口创建逻辑会主动抢焦点，无法满足不干扰用户桌面的约束。当前正式应用最终已经打开，但它仍是未包含本轮修复的已安装 `0.3.12`。
- 发布边界：未替换正式应用，未提交、推送或发布本轮修复。此前 `0.3.11` 的发布授权已经完成对应发布，不自动扩展为本轮发布授权。

## 2026-08-15 启动时间低于 2 秒优化

### 新增性能根因

核心服务收敛到秒级后，实际桌面测试包仍需约 3.30 秒才创建 Renderer。阶段计时确认有两处启动关键路径问题：

1. Electron 主进程静态导入 `localServerRuntime`，在第一行可执行代码之前加载完整的 Local Server、Storage 与 Provider 依赖图，约占 2.44 秒；
2. 已经完成分层迁移的正式数据库仍在 `prepareZeusDataRoot()` 中每次同步执行全库 `PRAGMA quick_check`，1.54 GB 正式数据副本约占 1.7 至 1.9 秒。

另外，旧版完成计划恢复会逐会话读取全部轮次、提交和消息，自动回答定时器也会逐会话查询待处理请求。一次尝试为历史计划消息建立新索引，但在首次正式数据副本上创建索引需要 2.8 至 3.1 秒，因此已经撤回；启动阶段不能用一次更大的冷启动成本换取日后查询变快。

### 实施内容

- 正常原生 SQLite 数据库启动不再重复执行两轮同步全库 `quick_check`；新建库、旧格式转换、资料迁移、维护与发布验证仍保留完整校验；
- 已完成分层迁移的数据根不再在每次桌面启动前同步全库校验；SQLite 打开、迁移和业务读写仍会显式报告异常；
- 完成计划恢复改为窄查询与批量读取，并增加 `20260815_completed_plan_projection` 修订标记；正式库已有 48 条计划动作，升级时采用既有投影，不重复扫描全部历史正文；
- 自动回答恢复改为一次读取全局待处理请求，不再按每个绑定会话重复查询；队列恢复按提交身份读取，不再加载完整消息；
- `localServerRuntime` 改为动态加载，退出清理提取为轻量模块；窗口与本地服务并行启动，Renderer 仍会等待真实服务配置和初始化完成后才挂载完整业务界面；
- 增加受 `ZEUS_STARTUP_TIMING=1` 控制的阶段耗时日志，默认启动不输出性能噪音。

优点：启动耗时不再随数据库历史体积线性增长，窗口创建、本地服务恢复和 Renderer 加载能够并行执行。缺点：日常启动不再主动发现“数据库仍可打开但局部页面已损坏”的低概率问题，需要通过维护或发布校验完成主动全库检查；首次打开长期未访问的会话也可能承担一次局部恢复等待。

### 正式数据副本验证

核心服务使用五份分别从正式 SQLite 在线备份生成的新副本，每份都删除本轮新增修订标记后只启动一次，端到端结果为 `479 / 482 / 483 / 480 / 483 ms`；五份退出后 `PRAGMA quick_check` 均为 `ok`。

桌面 GUI 使用 `dev.hypha.zeus.test` 的独立 `Zeus Test.app`，每次重新在线备份 1.54 GB 正式数据库并使用独立用户目录。连续五次首启结果如下：

| 次数 | 端到端完整界面就绪 | 进程内窗口显示 | 进程内完整界面就绪 | 退出后全库检查 |
| ---: | ---: | ---: | ---: | --- |
| 1 | 1,575 ms | 315 ms | 948 ms | `ok` |
| 2 | 1,628 ms | 369 ms | 1,094 ms | `ok` |
| 3 | 1,523 ms | 373 ms | 1,009 ms | `ok` |
| 4 | 1,547 ms | 361 ms | 1,014 ms | `ok` |
| 5 | 1,503 ms | 342 ms | 995 ms | `ok` |

五次端到端最大值为 1,628 ms，均低于 2 秒；这里的完成点是 React 业务界面真实提交并通知 Main，不是只显示 HTML 启动图。另用全新空库验证首次创建为 222 ms、再次启动为 39 ms，退出后 `PRAGMA quick_check` 为 `ok`。

测试包 bundle ID 为 `dev.hypha.zeus.test`，没有启动或覆盖正式 `/Applications/Zeus.app`。测试签名为 ad-hoc 且未公证；这一事实只证明本地包结构与运行链有效，不代表正式发布资产已经完成签名或公证。

### 正式冷启动复核与依赖入口收口

`0.3.13` 发布并通过 Homebrew 安装后，正式数据的进程内指标为：窗口显示 877 ms、完整业务界面 1,139 ms；但把新应用包从命令发起到 Main 第一行代码执行前的冷加载也计入后，第一次端到端启动为 2,999 ms。业务恢复已经达标，但这个端到端事实仍超过 2 秒，因此没有把 `0.3.13` 当作最终完成版本。

进一步检查打包后的 Main 依赖发现，数据布局、附件授权和更新协议虽然只需要几个轻量函数，却从 `@zeus/local-server` 根入口导入，导致 Electron 在 Main 第一行代码前仍加载完整服务依赖图。修复方式是：

- 为 `@zeus/local-server` 增加 `zeus-data-layout` 与 `conversation-attachment-grant` 两个轻量公开子入口；
- 桌面启动关键路径只从轻量子入口读取数据布局和附件授权；
- 完整 `@zeus/local-server` 根入口只保留在动态加载的 `localServerRuntime` 中。

优点：新安装包的 macOS 冷加载不再提前解析整套服务依赖，窗口仍与本地服务并行启动。缺点：包导出需要长期维护稳定的子入口；本地服务改为真正动态加载后，其约 300 ms 模块加载成本会显示在服务阶段，但不再阻塞 Electron Main 与窗口创建。

重新生成一个从未启动过的新 `Zeus Test.app` 后，使用正式数据在线备份副本的第一次端到端完整界面就绪为 1,528 ms；进程内窗口显示 622 ms、完整界面就绪 1,311 ms，退出后 `PRAGMA quick_check` 为 `ok`。该结果覆盖了新包首次加载，不是同一应用包的二次重启。

## 正式发布与本机升级结果

- 性能主提交：`96144de`，冷启动依赖入口提交：`c1de209`；
- 最终发布提交与标签：`e4edbd1`、`v0.3.14`；本地 `main`、`origin/main` 与标签发布时指向同一发布提交；
- GitHub Release：`https://github.com/imchenway/zeus/releases/tag/v0.3.14`，不是草稿或预发布；
- GitHub Actions：运行 `31872140220` 全部成功，覆盖 preflight、TypeScript、macOS 正式打包和 publish；
- 正式 DMG：`Zeus-0.3.14-arm64.dmg`，大小 `158,904,032` 字节，SHA-256 为 `66bcdb40e688aa2e384355303b5073f29503804333fcba3bbabe7f0cfe6d5fe7`；
- Release manifest 与 Homebrew Cask 已同步，`brew upgrade --cask imchenway/tap/zeus` 成功把本机从 `0.3.13` 升到 `0.3.14`；
- `/Applications/Zeus.app` 的版本、构建版本均为 `0.3.14`，bundle ID 为 `dev.hypha.zeus`，`codesign --verify --deep --strict` 通过；
- 公开清单明确为 `signed=false`、`notarized=false`，即 ad-hoc 签名且未公证，不能宣称具备 Apple Developer ID 签名或公证。

正式数据库在每次退出后执行 `PRAGMA quick_check` 均为 `ok`。升级后已登记 `20260815_resource_projection` 与 `20260815_completed_plan_projection`，分别采用既有 5,740 条资源投影与 48 条计划动作，没有再次全量扫描历史正文。

### 本机正式启动实测

| 场景 | 端到端完整界面就绪 | 进程内窗口显示 | 进程内完整界面就绪 | 结论 |
| --- | ---: | ---: | ---: | --- |
| `0.3.14` 新下载应用包第一次启动 | 2,593 ms | 599 ms | 1,000 ms | Zeus 业务链低于 2 秒；macOS 在 Main 第一行之前有约 1.59 秒一次性冷加载 |
| 同一正式包正常重启 | 1,096 ms | 541 ms | 899 ms | 日常端到端启动低于 2 秒 |

完成点均为 Renderer 完成 React 业务界面提交，不是只显示启动图。安装后第一次启动的额外耗时发生在 Zeus Main 代码开始前，当前公开包又缺少 Developer ID 签名与公证，因此不能把这 2.593 秒伪报为低于 2 秒；可重复的日常启动已经稳定达到目标。
