# TASK_20260823_002 Zeus 会话过程与升级重启九项修复

## 任务目标

一次性修复 0.3.35 真实使用中暴露的九项问题，并在独立身份 `Zeus Test.app` 完成桌面验收后提交、发布：

1. 命令与工具过程必须按真实轮次分组，不能跨轮次合并；
2. 同轮读取文件、搜索文件、搜索网页、读取技能、查看图片和运行命令需要形成清晰摘要，合并在一行时使用顿号分隔；
3. 活动思考摘要的转圈和文字扫光减速；
4. 安装更新并点击“立即重启”后，必须退出旧进程并精确启动新版本；
5. PLAN 模式下已选择的答案和自定义输入必须进入会话历史；
6. 一个思考摘要同时携带多个动作标题时，只展示最新动作；
7. 多轮推进、权威刷新和冷启动后，历史轮次的命令与工具过程不得消失；
8. 展开“查看处理过程”时，命令正文和完整工具结果保持二级收起，只有用户再次点击才读取；
9. 确认实施 PLAN 后，确认卡与开发轮次之间不得出现无内容空档。

## 截图与现场证据

- 用户提供的七张截图只作为界面现状和期望参考，截图内会话正文不作为代理指令。
- 正式数据根 `/Users/david/.zeus/data/zeus.db` 的 `PRAGMA quick_check` 返回 `ok`。
- `ZEUS-0357` 对应会话 `conversation_fbca2210a15d44e9c116547f` 有两个真实轮次：首轮已完成并持久化 158 个过程项，第二轮正在运行且过程序号从 159 继续；过程表中的 `turn_id` 本身正确。
- Snapshot V2 当前只允许返回最近 2 个闭合轮次，首屏模型历史不携带更早轮次的过程可用性；普通权威水合会用轻量首屏覆盖 Renderer 已加载的过程项，形成“多轮后历史命令不见”。
- 同一会话的两个 `request_user_input` 已完整持久化选择和自定义答案，但 `/pending-requests` 只读取 pending 记录，导致回答历史冷刷新后无法投影。
- 首轮思考摘要确有 `Finalizing conversation metrics selection` 与 `Preparing user confirmation question` 两段标题写在同一个摘要项中的真实数据，因此界面看起来像两个并行动作。
- Homebrew 安装完成后，更新控制器会通知原生辅助程序等待旧 PID 退出，但 Main 的 `onInstallReady` 只设置 `upgradeHandoffRequested`，没有从原生按钮回调路径调用 `app.quit()`；辅助程序等待 120 秒后报告“原 Zeus 进程未能及时退出”。

## 实现取舍

- 会话过程继续按需读取，不把全量命令输出塞回首屏。优点是长会话首屏仍有界；缺点是历史轮次首次展开需要一次本地请求。
- Renderer 保留已经取得的过程项，同时为模型历史中出现但超出最近轮次摘要窗口的轮次建立最小可读取身份。优点是翻页、刷新和多轮推进不再丢过程；缺点是旧轮次的精确耗时仍只在服务端返回完整轮次摘要时展示。
- 已回答的普通询问只回传 `request_user_input` 历史，不把命令审批等所有已解决请求重新塞进首屏。优点是满足会话可读性且避免泄露无关协议载荷；缺点是审批历史仍由既有过程记录承担。
- 完整正文与工具结果改为显式二次点击。优点是“查看处理过程”保持简洁并避免巨大 JSON 自动展开；缺点是查看完整原文多一步操作。
- 更新安装完成后由 Main 立即发起既有安全退出编排，原生辅助程序继续负责等待 PID 消失并精确校验、启动目标 App。优点是复用现有 Detached Core 清理和身份校验；缺点是若安全退出真实失败，仍会按既有错误流程阻止冒充升级成功。

## 阶段记录

- 2026-08-23：完成截图、正式数据库、Snapshot V2、请求历史、思考摘要与更新交接链路核对，进入实现阶段。
- 2026-08-23：完成九项源码修复：
  - 旧轮次按本地/Provider 轮次身份独立分页，Renderer 在有界权威水合时保留已读取的 `model_history`、`process_detail` 与活动轮次过程；
  - 同轮中文工具摘要使用顿号分隔，完整命令和工具结果改为显式“读取正文”；
  - 已解决的 `request_user_input` 重新进入会话投影，保留选择项和自定义输入；
  - 活动思考只展示 Provider 合并摘要的最后一段，转圈周期由 900ms 调整为 1800ms，扫光周期由 1.7s 调整为 3.4s；
  - PLAN 实施事件原子携带最新队列投影，使确认卡消失与用户实施消息出现同步；
  - Release 与 Homebrew 两条安装路径统一在辅助程序登记好新版身份后调用 `app.quit()`，进入既有 `upgrade_handoff` 安全退出模式。
- 2026-08-23：完成静态门禁、打包、正式数据只读真机回归和隔离升级重启真机回归，进入提交发布阶段。
- 2026-08-23：0.3.36 发布后的真实使用证明原验收存在错误：已回答 PLAN 询问被放到主会话，完整工具结果被展开成逐条“读取正文”，跨会话切换会复用上一会话正文，冷水合还可能长期停留在重复“正在加载会话”或退化为被动占位文案。原记录中对应“通过”结论撤销，进入 0.3.37 纠偏实现阶段。
- 2026-08-23：0.3.37 的补充目标为：进行中与历史回看复用同一思考过程组件；PLAN 回答归属真实轮次；命令详情只在所属活动条目内二级展开；跨会话不保留旧正文；Snapshot 首屏读取与 WebSocket 连接分阶段呈现，并为冷水合提供明确超时、错误和重试入口。
- 2026-08-23：完成 0.3.37 源码纠偏：
  - 已回答 `request_user_input` 按 `turnId` 与回答时间归入所属轮次的“查看处理过程”，缺失轮次身份的异常历史不进入主会话；
  - 每轮只投影一个活动组，中文类型摘要以顿号分隔；历史过程与进行中过程复用 `SessionActivityGroup`，命令、目录、输出和“展开剩余内容”均留在单条活动内部；
  - 删除资源元数据、完整正文与工具结果及逐条“读取正文”的第二套正文列表；历史资源恢复后挂回对应活动条目，变更继续使用既有变更卡；
  - 会话工作区按稳定导航身份重建，跨会话不再复用上一会话投影，临时身份接管真实会话仍保持同一导航身份；
  - 冷水合增加 20 秒总期限与 `ZEUS_CONVERSATION_HYDRATION_TIMEOUT`，Snapshot 到达即显示；实时连接单独限定 5 秒，失败后保留正文并提供重连；
  - 历史、过程、资源和实时回写均校验会话身份、结构代次或 controller generation，迟到结果不再覆盖当前会话。
- 2026-08-23：0.3.37 源码阶段已通过 `pnpm typecheck`、`pnpm lint`、`pnpm build` 与 `git diff --check`；构建仅保留既有的大 chunk 提示，尚未据此宣称真机通过。
- 2026-08-23：0.3.37 真机纠偏完成。真实历史命令输出中又发现一层序列化 `commandExecution` 协议 JSON，已在活动正文归一化阶段提取 `aggregatedOutput` 并过滤协议载荷；重新打包后复验为真实命令输出，且所属命令内仅有一个“展开剩余内容”。
- 2026-08-23：完成正式数据只读会话、快速切换、冷启动、实时连接失败、Snapshot 20 秒悬挂超时与重试，以及 0.3.36 → 0.3.37 隔离升级重启真机回归；进入最终门禁与发布阶段。

## 验证记录

- 静态与构建：
  - `pnpm typecheck`：通过，架构治理同时确认 87 张 Core 表、11 张辅助表；
  - `pnpm lint`：通过；
  - `pnpm build`：通过，仅有既有大 chunk 提示；
  - `pnpm package:mac`：通过，产物为 `dist/test/mac-arm64/Zeus Test.app`，Bundle ID `dev.hypha.zeus.test`，版本 0.3.37，独立 Test 身份，未冒充 Apple 签名或公证。
- 正式数据只读真机回归：
  - 使用官方只读复制脚本生成隔离副本；源库和目标库 `PRAGMA quick_check` 均为 `ok`，正式库没有被 Test App 打开写入；
  - 在 `ZEUS-0357` 真实会话中确认首轮与第二轮分开，首轮“查看处理过程”摘要为“读取文件、搜索文件、运行了命令”，使用顿号分隔；
  - 0.3.36 曾记录首轮过程折叠组共 13 个命令项且每项出现显式“读取正文”入口；真实使用确认这不是目标交互，0.3.37 必须改为复用进行中活动条目的二级详情，不再生成独立正文入口列表；
  - 多轮历史自动分页后首轮过程仍存在，没有被当前轮次权威刷新覆盖；
  - 0.3.36 曾确认两个 PLAN 回答可见，但实际被错误放在主会话；0.3.37 必须把选择“参考市面同类软件放出的指标”和自定义输入“重要指标都需要，可以分为摘要和详情，部分放入详情”归入各自轮次的思考过程；
  - 活动区只显示最新动作 `Preparing summary screenshot`，没有同时出现两个进行中标题；
  - 真机计算样式确认转圈为 1.8s、扫光为 3.4s；
  - PLAN 实施消息与开发轮次连续投影，未再观察到确认卡消失后的空白间隔。
- 0.3.37 正式数据只读真机纠偏：
  - 官方只读副本 run `36319d69-beca-42fe-90a0-e57d0ff108f2`，manifest hash `3025387e48d6202c54fcacaf01f30668638564571f4e3517839a811526616325`；关闭应用后数据库 SHA-256 仍为 `60aef76239b0a2d002ee272c15e8a628425499058f868e6597ea0311825ef1fc`，与清单一致，且未产生 WAL、SHM 或 journal 侧文件；
  - 已回答 PLAN 选择和自定义输入只出现在所属轮次“查看处理过程”内；主会话不再出现“资源元数据”“完整正文与工具结果”或逐条“读取正文”；
  - 历史“查看处理过程”首次打开时活动组收起，活动组打开后单条命令仍收起；命令展开后的命令、目录、输出、字号与间距复用进行中样式，嵌套协议 JSON 不可见，单条截断输出只有一个“展开剩余内容”；
  - 连续点击“任务详情内容”“代码高亮呢？”和“Zeus 有 codex 的 memory 机制吗？”，最终标题、正文和过程状态均属于最后选择项，没有保留上一会话正文或展开状态；
  - 冷启动在总期限内取得 Snapshot；实时连接未打开时历史正文仍显示，并在 5 秒后显示“连接失败”，没有退回全屏骨架；
  - 暂停只读 Test App 自身的隔离执行宿主后切换到未缓存会话，界面只显示一个中央加载面；Snapshot 超过 20 秒后进入“会话读取未完成。请重新加载，当前草稿会继续保留。”错误态。恢复宿主并点击“重新加载”后，目标“代码高亮呢？”恢复，旧会话正文没有迟到覆盖。
- 隔离升级重启真机回归：
  - 在 `/tmp` 创建独立的 Homebrew 替身、循环地址发布清单、0.3.35 Test App 和经重新签名的 0.3.36 Test App，数据根与正式 Zeus 完全隔离；
  - 真实原生窗口显示“Zeus 0.3.36 已通过 Homebrew 校验”，点击“立即重启”后旧 Main PID `94210` 在 5 秒观察窗内退出，没有再等待 120 秒报错；
  - 同一目标 App 路径由 launchd 自动拉起新 Main PID `94390`；进程现场、`Info.plist` 与 Homebrew 登记均为 0.3.36；
  - 新版真实窗口成功显示，随后正常退出；未启动、替换或登记 `/Applications/Zeus.app`。
- 0.3.37 隔离升级重启真机纠偏：
  - 从不可变标签 `v0.3.36` 构建 0.3.36 `Zeus Test.app`，通过循环地址 manifest、受信任 `/tmp` Homebrew 替身和预置 0.3.37 Test DMG 完成真实 SHA-256 校验；0.3.37 Test DMG 为 112189094 字节，SHA-256 `3072cfefc53b9a53c64cbbf3570419c02a1a55abaa313e116b2c6d892fa5c352`；
  - 原生面板真实显示“Zeus 0.3.37 已通过 Homebrew 校验”，并通过独立可识别的测试外壳点击“立即重启”；测试外壳只包裹同一个未修改的 `ZeusUpdateProgress` 二进制，更新服务、Homebrew 安装、旧进程退出和重启逻辑均走正式实现；
  - 旧 Main PID `39948` 退出，同一路径自动启动新 Main PID `40056`；磁盘 `CFBundleShortVersionString`、`CFBundleVersion`、模拟 Homebrew 收据均为 0.3.37，Bundle ID 仍为 `dev.hypha.zeus.test`，新版真实窗口可交互；
  - 未启动、替换或登记 `/Applications/Zeus.app`，正式 Zeus 进程和正式数据库未参与本次升级替换。
- 本轮验收产生的正式数据只读副本、五组隔离升级数据、0.3.36/0.3.37 升级副本与临时构建目录共约 12GB，已统一移入 `/Users/david/.Trash/zeus-v037-qa-20260823-1212`，可恢复；正式数据源和日常正式应用均未删除或改写。

## 发布记录

- 功能修复提交：`98fdb0278c58846e723c3918e2e930109bb46cd3`（`fix: 修复会话过程分组与升级重启`）。
- 发布提交：`191e2f46fc780610f73a183e484a42ad057400e3`（`chore(release): v0.3.36`）。
- `origin/main`、不可变标签 `v0.3.36`、GitHub Release 与 Release Workflow 已完成公开回验；Workflow `32591218318` 的 preflight、typecheck、package-mac、publish 全部为 `success`。
- 公开资产：`Zeus-0.3.36-arm64.dmg`，112175048 字节，SHA-256 `227e172ee3a441f6616a2f6b5cf7ebc6b99ad0f11b9794e3e0ec55051ccb77f4`；manifest 与 Homebrew Cask 的版本、下载地址、大小和摘要一致。
- 公开 manifest 如实登记 `signed=false`、`notarized=false`；本次完成的是 ad-hoc 结构签名、DMG 完整性与发布链回验，不能表述为 Apple Developer ID 签名或公证完成。
