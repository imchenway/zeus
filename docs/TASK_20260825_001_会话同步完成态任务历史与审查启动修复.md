# 会话同步、完成态、任务历史与审查启动修复

## 目标

本任务一次性修复并发布以下现场问题：

1. Remote Control 移动端发送的新消息不能及时出现在 Zeus。
2. 已完成轮次仍显示 `Defining runtime paths and execution contracts` 一类思考摘要。
3. `account/read` 超时会阻断继续会话或创建代码审查。
4. “已处理 X”没有位于本轮正文和交付文件卡片之后。
5. 代码审查弹窗长期停留在“正在启动”。
6. 已完成任务不能查看关联会话，恢复到待验收后又只显示一条。
7. 点击任务关联会话后历史消息为空。
8. 历史会话的环境卡片经常不显示。
9. 创建失败后仍同时显示“正在思考”，且错误文本被挤成逐词换行。
10. Provider 已经结束或会话创建已经失败时，侧栏和正文仍长期显示转圈、停止按钮与未结束过程。
11. `account/read` 与 `thread/turns/list` 在共享 stdout 背压时连续超时。
12. 助手最终答复引用的临时截图在历史会话里退化成“图片不可用”。

## 现场结论

- `account/read` 是账号状态探测，不是发送消息本身；普通创建和恢复路径却强制使用 `refreshToken: true`，网络刷新超时会把本可继续的会话一并阻断。
- Zeus 只在恢复、发送和 Provider 事件到达时补齐远端轮次；已打开的 Remote Control 会话缺少持续的增量拉取，因此移动端消息可能停留在 Provider 侧而不进入 Zeus 投影。
- 会话正文投影会在每轮末尾重新追加最新 reasoning；没有限制为活动轮次，导致完成后仍保留内部英文摘要。
- 轮次时长以最后一条普通消息为锚点，交付物卡片另行插入，所以时长会出现在正文或交付物之前。
- 代码审查启动会同步等待账号刷新和首次 Provider 派发；任何慢调用都会锁住弹窗。
- 任务详情在非终态过滤归档会话；手工从完成态改回非终态时，服务端又会自动恢复排序靠前的任意归档会话。现场因此恢复了一个没有 Provider 线程、首次提交已取消的代码审查壳，会话为空且其他真实历史仍被隐藏。
- 历史快照模式直接关闭了环境快捷卡片，而不是只把操作置为只读。
- 创建失败卡片沿用了三列网格，但失败分支只渲染两项，错误内容落入 20px 窄列；失败态也没有抑制独立“正在思考”状态。
- 正式 v0.3.51 的代码审查线程 `01a0367e-253e-7392-8be7-21f33ebee2af` 已于 09:38:22 写入 `task_complete`，数字员工线程 `01a034ab-3587-7ca1-9c55-ca50ac4f5f8a` 已于 09:59:51 写入 `task_complete`；正式 SQLite 却仍把对应轮次保留为 `running`，侧栏分别持续显示 55 分钟和 2 小时以上。点击前者后才补齐终态，证明是 Provider 终态通知遗漏后的被动对账缺口，不是模型仍在执行。
- “侧边栏会话数量优化”创建失败发生在服务端接受会话之前，正式 SQLite 没有该任务的会话记录；Renderer 本地待处理工作面仍保留 `active_prework`、活动 turn 和 `connecting` choice，因此即使错误已经显示，侧栏、正文和停止按钮仍冒充运行中。

## 实施原则与取舍

- 普通执行只读取本地账号快照，并允许在 app-server 瞬时传输失败时回退到同一运行世代的最后成功账号快照；显式登录或用户主动刷新仍走严格刷新。优点是网络波动不再阻断会话，缺点是极短时间内可能使用稍旧的登录状态，因此真正派发失败仍必须保留可见审计。
- 对当前打开且已绑定 Provider 的会话做低频水位补齐，不依赖 Remote Control 开关；只有 Provider 出现新轮次或本地活动态与 Provider 终态不一致时才做完整对账。优点是移动端消息与遗漏终态都能回流且不会扫描全部历史，缺点是上游 Provider 从未收到的消息无法由 Zeus 凭空恢复。
- 任务历史显示所有有意义的真实会话；未绑定 Provider 且首次提交已取消或删除的壳不作为关联会话展示，但数据库审计记录保留。
- 手工修改任务状态不再猜测要恢复哪条会话；只有用户明确点击某条历史会话继续时，才恢复该精确会话。
- 代码审查在本地持久化成功后立即切入新会话，首次 Provider 派发异步进入统一队列。优点是弹窗不再等待网络，缺点是后续 Provider 失败会在新会话中展示并允许重试。
- 会话创建失败会原子地把本地 choice、活动 turn、队列和会话状态收口为失败态，同时保留原始输入用于重试；优点是不会再出现失败、转圈和停止按钮并存，代价是失败后必须显式重试，不能假定首次请求已被 Provider 接受。

## 验收分层

- 源码：逐项核对状态机、投影顺序、任务历史和同步边界。
- 工程门禁：`pnpm lint`、`pnpm typecheck`、`pnpm build`、`pnpm package:mac`。
- 真实运行：仅启动独立身份 `Zeus Test.app`，使用独立用户数据目录验证会话、任务历史、错误态、环境卡片和审查启动。
- 发布：升级版本、提交并推送；核对远端主分支、标签、Workflow、Release、DMG、清单和 Homebrew。签名或公证只按公开产物和发布检查的真实结果报告。

## 当前阶段

0.3.52 已包含首轮 10 项修复。用户随后补充了共享 RPC 连续超时和历史答复截图丢失两个问题；传输层、资源归档和升级迁移已经完成定向验收，当前改动尚未提交或发布。

## 实施结果

- 当前打开会话每 5 秒检查一次 Provider 最新轮次；当前项目的未归档 Codex 会话在既有 10 秒列表刷新中触发 30 秒节流水位检查，每批最多并发 4 条。只有出现新轮次，或 Provider 终态与本地活动态不一致时，才读取完整增量并持久化。这样未打开会话的侧栏假死和移动端新增轮次也会自动收敛，不会调用模型或消耗推理 Token。
- 普通会话创建和恢复优先复用当前 app-server 世代内的账号快照，并允许 `account/read` 传输瞬断时回退；用户主动刷新登录态仍严格读取 Provider。
- 完成轮次不再把最后一条内部 reasoning 摘要追加到正文；失败创建态也会抑制独立“正在思考”。
- “已处理 X”与交付文件共用最终产物锚点，顺序固定为正文、显式交付资源、交付卡片、时长。
- 代码审查在本地会话与只读约束持久化成功后立即关闭启动弹窗并切入新会话，首次 Provider 派发进入统一异步队列。
- 任务详情同时返回归档与未归档历史，剔除未绑定 Provider 且首次提交已取消或删除的空壳；状态从终态改回非终态时不再擅自恢复任意历史会话。
- 历史快照继续隐藏写操作，但不再隐藏环境入口。
- 创建失败原子收口 choice、任务运行态、活动 turn、队列和乐观消息；错误行使用剩余宽度，重试按钮独占尾列。

## 验收证据

### 正式现场只读对账

- 正式 Zeus 0.3.51、正式 SQLite 和 Provider JSONL 三方一致证明两条假死线程已经在 Provider 结束，但本地仍为 `running`；没有向正式会话发送消息、停止任务或修改数据库。
- “侧边栏会话数量优化”在正式 SQLite 中没有会话记录，证明它失败于服务端接纳之前；旧 Renderer 却仍显示 Stop 与转圈，创建失败收口修复覆盖了这一分支。
- ZEUS-0315 的正式数据中存在 3 条有消息的归档任务会话和 1 条未绑定 Provider、首次提交取消的空壳，修复后的查询精确返回 3 条真实历史。

### 定向行为探针

- Provider 最新轮次为 `completed`、本地为 `running` 时，触发一次完整对账和一次持久化；同一会话并发检查只执行一次。
- 项目批量追赶对两条会话各检查一次，重复 ID 自动去重；30 秒内再次刷新不重复访问 Provider。
- 创建失败后 choice=`error`、任务运行态=`failed`、会话=`turn_failed`、活动 turn 为空、队列=`idle`、原始乐观消息=`failed`，同时保留显式重试。

### 独立 GUI

- 使用 `dev.hypha.zeus.test` 的 `Zeus Test.app` 和正式数据库只读副本验收；正式应用与正式数据库未被修改。
- ZEUS-0315 任务详情显示 3 条关联会话，打开第一条后可见完整历史消息，不再出现空白会话。
- 完成会话只显示最终正文，处理过程默认收起；“已处理 1分44秒”位于该轮正文最下方。
- 历史会话顶部重新出现“环境”入口；只读验收模式按设计阻止进一步连接 Runtime、Provider、Git 或 Keychain。
- 浏览器渲染夹具确认：创建失败只显示单行自适应错误与“重试”，不再同时显示“正在思考”；完成态内部英文 reasoning 不再回显，时长位于正文和交付资源之后。

### 工程门禁

- `pnpm lint`：通过。
- `pnpm typecheck`：通过；架构门禁确认 88 张 Core 表、11 张可重建辅助表，以及源码行数、依赖和包循环均符合约束。
- `pnpm build`：通过；仅保留既有的大 chunk 提示。
- `pnpm package:mac`：通过；生成独立身份 `Zeus Test.app`，严格代码签名结构校验通过。测试包为 ad-hoc 签名且未公证，不将其冒充正式签名证据。
- `git diff --check`：通过。

验收结束后已关闭 `Zeus Test.app` 和本地 QA 服务；两份合计约 11.4 GB 的临时只读数据库副本已移入 macOS 废纸篓，可恢复，未触碰正式数据。

## 2026-08-25 共享 RPC 通道补强与全量复核

### 新增现场

用户补充截图显示同一次继续会话先后出现：

1. `ZEUS_CODEX_RPC_TIMEOUT: Codex app-server request timed out: account/read`；
2. 再次继续后出现 `ZEUS_CODEX_RPC_TIMEOUT: Codex app-server request timed out: thread/turns/list`。

两者是同一类根因，不是两个独立 Provider 故障。Codex App Server 的 RPC 回包与过程通知共用一条 JSONL stdout；旧实现只为少数标记为 `priorityRead` 的读取临时解除事件投影背压，普通 `account/read` 或未标记的 `thread/turns/list` 仍可能和异步事件投影互相等待。

### 补强实现

- 所有已经写出的 RPC 都在回包、拒绝或超时前保持 stdout 可读；事件投影仍有积压时，只在当前没有未决 RPC 后重新施加背压。该规则同时覆盖 `account/read`、`thread/read`、`thread/turns/list`、中断和其他控制 RPC，不再维护容易漏标的优先方法白名单。
- 同一 app-server 世代内的普通 `account/read` 按“本地/显式刷新”身份合并；普通读取使用 30 秒短缓存，额度与用量各使用 15 秒短缓存，避免多个会话和顶部统计重复挤占控制通道。
- 普通账户、额度和用量读取使用最长 8 秒的控制读取窗口；普通账户读取在传输失败时可以回退同一世代最后一次成功快照，用户显式刷新仍严格返回失败，不伪造成功。
- 会话创建、继续和代码审查热路径只消费已有账户快照；没有快照时直接让真实 `thread/turn` RPC 负责权威认证，账号探测不再先于实际派发成为硬门禁。
- 任务关联历史进一步剔除“未绑定 Provider 且根本没有首次提交”的本地空壳；真实 Provider 会话、已有提交和归档历史继续完整显示。

### 定向行为证据

- 3 个并发普通 `account/read` 只产生 1 次 Provider RPC；随后读取命中同世代 TTL 缓存。
- 两个并发额度读取与两个并发用量读取分别只产生 1 次 Provider RPC。
- 事件投影 Promise 故意保持未完成、stdout 已进入背压时，普通 `thread/read` 仍能在 50ms 验收窗口内正常返回。
- 同一背压现场下，新增截图对应的 `thread/turns/list` 也能在 50ms 验收窗口内正常返回，且只调用一次。
- 普通账户读取超时时回退同世代成功快照；`refreshToken: true` 的显式刷新仍返回 `ZEUS_CODEX_RPC_TIMEOUT`，没有吞错。

### 可见界面证据

- 应用内浏览器打开 `http://127.0.0.1:4179/qa/session-styles.html?motion=1`，页面标题、非空 DOM 和目标夹具正确，没有 Vite/React 错误遮罩，控制台无警告或错误。
- 创建失败夹具中 `ZEUS_CODEX_RPC_TIMEOUT: Codex app-server request timed out: account/read` 使用正常换词规则和 768px 可用宽度；没有逐词换行，没有独立“正在思考”，重试按钮可点击。
- 切换为完成态后，`Defining runtime paths and execution contracts` 与最新活动 reasoning 均不在可见文本；处理过程默认收起，最终正文索引早于“已处理 1分0秒”。

### 独立 Test.app 证据

- `pnpm lint`、`pnpm typecheck`、`pnpm build`、`pnpm package:mac` 与 `git diff --check` 通过；架构门禁仍为 88 张 Core 表和 11 张可重建辅助表。
- 生成包为 `dev.hypha.zeus.test` / 0.3.52，严格代码签名结构检查通过；测试包为 ad-hoc 签名且未公证。
- 使用全新 `/Users/david/.zeus-test/rpc-final-de3zamtT` 数据根启动当前 `Zeus Test.app`：主窗口真实出现，任务首屏可读，点击“设置”后路由与设置内容正常；Execution Host 写入 `execution_host.ready` 和 `execution_host.ui_attached`，SQLite `PRAGMA quick_check` 返回 `ok`。
- 测试包日志只有“无法注册开机启动项”的沙箱权限提示；不影响窗口、Renderer、Execution Host 或本任务功能链。

### 11 项复核矩阵

| # | 用户问题 | 当前处理 | 证据层 |
|---|---|---|---|
| 1 | 移动端新消息在 Zeus 不见 | 已有实时远端轮次接纳、5 秒打开会话水位补齐、30 秒项目批量补齐与重连增量对账 | 源码、既有定向探针、隔离 GUI；本轮未重新执行真实 iOS 发送 |
| 2 | 完成后仍显示英文思考摘要 | 只投影活动轮次当前 reasoning，终态不追加末尾 reasoning | 源码、浏览器完成态 |
| 3 | 会话无法继续 | 移除热路径账户探测硬门禁，并对所有未决 RPC 打开全双工读取窗口 | 源码、RPC 行为探针 |
| 4 | “已处理”位置错误 | 时长锚定最终正文后的显式交付资源/交付卡片末尾 | 源码、浏览器完成态、既有隔离 GUI |
| 5 | 代码审查卡在“正在启动” | 本地持久化后立即切会话，首次派发异步入统一队列；账户读取不再阻断启动 | 源码、既有隔离 GUI |
| 6 | 已完成任务不能查看关联会话 | 任务详情返回归档与未归档全部真实历史，不按任务终态隐藏 | 源码、既有隔离 GUI |
| 7 | 改回待验收只显示一条且打开空白 | 状态变更不再自动恢复任意会话；剔除无 Provider/无有效首次提交的空壳 | 源码、正式数据只读对账、既有隔离 GUI |
| 8 | 历史会话环境卡片不显示 | 历史模式只限制写操作，不再隐藏环境快捷入口 | 源码、既有隔离 GUI |
| 9 | 报错与“正在思考”并存、错误逐词换行 | 失败态抑制独立思考；错误网格使用完整剩余宽度与正常换词 | 源码、浏览器错误态 |
| 10 | 已结束/失败会话仍长期转圈 | 打开会话与项目列表持续对账 Provider 终态；创建失败原子收口本地活动态 | 源码、正式现场只读证据、既有行为探针 |
| 11 | `account/read` 后又 `thread/turns/list` 超时 | 两个方法都由统一未决 RPC 全双工窗口覆盖；账户读取另有合并与短缓存 | 源码、两条背压行为探针 |
| 12 | 历史最终答复里的截图显示“图片不可用” | 完成态最终答复的本地 Markdown 图片在投影时归档到 Zeus 自管资源目录；升级时只补扫含图片语法的完成答复 | 源码、正式数据只读对账、精确迁移探针、浏览器视觉验收 |

## 2026-08-25 历史最终答复截图持久化

### 正式现场根因

- 正式会话 `01a03708-cc62-7921-ace2-464786652244` 的最终答复完整保留了两条 Markdown 图片引用，来源分别为 `/private/tmp/zeus-0365-after-show-more.png` 与 `/private/tmp/zeus-0365-after-reexpand.png`；两份源文件当前仍存在且签名为 PNG。
- 对应 Provider item 已是 `completed/final_answer`，正文没有丢失；但 `conversation_resources` 只有两个代码/文档文件链接，没有两张图片资源。
- 原因是旧资源规范化只把 Markdown 引用当普通链接处理，而普通本地链接受项目根目录授权约束，`/private/tmp` 图片被安全过滤。Renderer 找不到受信图片资源后按设计显示“图片不可用”。

### 实现与取舍

- 仅对已完成的助手最终答复处理本地 Markdown 图片；用户输入、活动过程、远程 URL 与普通本地链接保持原规则。
- PNG、JPEG、GIF、WebP 在投影时校验扩展名、文件类型、真实路径和文件签名后，原子复制到 `conversation-attachments/assistant-markdown-images`。来源只允许项目根、Zeus 已授权附件/生成图根与系统临时目录；符号链接解析后逃出这些根目录会被拒绝。单图上限 16 MiB、单条答复本轮新增上限 64 MiB，权限固定为 `0600`。
- 归档目标由会话、Provider item 和原始引用稳定派生；来源被清理后，重复水合仍复用同一归档文件，不会把既有资源删除。
- 内联截图只标记为 `inline` 和 `assistant_markdown_image` 来源，不标记成独立 `assistant` 交付物；否则整条最终正文会被误分类成图片卡片。视觉验收在发布前发现并纠正了这一语义回归。
- 升级迁移不全表载入任意历史条目，只查询 `completed/final_answer` 且正文含 Markdown 图片语法的记录；迁移只追加新图片资源，保留原有文件和网站资源。归档单图失败不阻断正文、其他资源或应用启动。

### 精确现场迁移与回放证据

- 直接归档探针：首次和删除临时来源后的重复投影均返回 2 个资源，目标完全稳定，两个归档文件存在且 PNG 头均为 `89504e470d0a1a0a`；资源均为 `inline`，没有独立交付标记。授权根外的图片和指向授权根外的符号链接均投影为 0 条资源。
- 对正式问题 item 做只读取数、在隔离目录执行当前迁移：资源由 2 条变为 4 条，新增 2 张图片、原有 2 个文件链接完整保留；归档都落在隔离根内且存在，没有修改正式 SQLite 或正式资源目录。
- 浏览器真实渲染夹具同时包含最终正文和归档图片：正文可见，图片 alt 为“持久化验收截图”，`图片不可用` 与加载占位均为 0。
- 最终源码重新通过 `lint`、`typecheck`、`build`、`package:mac` 与 `git diff --check`；新打包的独立 `Zeus Test.app` 以全新数据根启动，真实窗口、任务工作台和设置入口可见，Execution Host 写入 `ready/ui_attached`，SQLite `quick_check=ok`，资源迁移 revision 正确登记为 `20260825_durable_assistant_markdown_images`。
- 一个误建的约 9.7 GB 临时正式库副本已经移入 macOS 废纸篓，可恢复；正式数据库始终只读，未改动。

### 仍需如实保留的边界

- 本轮没有向真实 ChatGPT iOS 或正式 Provider 会话发送新消息，因此不把行为探针冒充真实双端端到端回归；修复前已漏掉的 iOS 历史消息仍按 ZEUS-0193 的用户确认边界不追溯补写。
- 升级迁移只能归档当前仍可读取的历史临时图片；已经被系统清理且没有其他持久副本的旧图片无法凭空恢复。本次用户截图对应的两张源图仍存在，精确迁移已证明可以恢复。
- 当前补强尚未提交、push、打标签或发布；正式安装版不会自动获得这些新改动。
