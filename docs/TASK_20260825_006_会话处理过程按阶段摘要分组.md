# TASK_20260825_006 会话处理过程按阶段摘要分组

## 用户问题

普通会话和子智能体会话把整轮命令、工具、文件操作和思考摘要压成一个处理过程。用户实际需要按每次可见的
中间摘要分段：A 摘要之后到 B 摘要之前是一组，B 到 C 是一组，C 之后是下一组；子智能体不能再维护另一套
扁平展示算法。

## 根因

- `projectTranscriptRows()` 只使用 `turnId` 建立 `activity:${turnId}`，因此整轮所有操作在第一次操作位置就被合并。
- `projectTranscriptTurnRows()` 再把整轮所有过程行包成单个 `turn_work`，中间 `agentMessage/commentary`
  也只是该大组中的普通行，不能形成阶段边界。
- 子智能体虽然已经复用 `ConversationTranscript`，旧投影算法仍会把其大量 reasoning 和文件操作整轮展开或整轮折叠。

## 实现

1. 阶段边界只认用户可见、非最终答复的 assistant/commentary 摘要；内部 reasoning、计划、交付物和最终正文
   不会误切新阶段。
2. 命令、工具、文件、网页和上下文活动按“当前阶段身份”聚合，活动 key 同时包含 turn 与阶段，不再按整轮聚合。
3. 每个阶段生成独立 `turn_work`：摘要始终可见，摘要后的 reasoning 和操作放入该阶段的“查看处理过程”。
4. 活动轮次只让最后阶段实时展开；前面已经结束的阶段折叠。最后活动阶段仍只显示最新 reasoning，避免恢复旧的
   英文思考列表；完成轮次展开后可以逐阶段查看完整过程。
5. 只有最后阶段负责 Snapshot V2 后续分页，避免三个阶段同时请求相同尾页。
6. `SubagentWorkspace` 不增加分组逻辑，继续把只读线程投影为 `NativeSessionState` 后直接复用
   `ConversationTranscript`。

## 取舍

- 优点：阶段语义来自真实可见摘要，普通会话和子智能体永久一致；完成态默认简洁，用户仍可逐段审计命令和思考。
- 缺点：没有中间摘要的旧轮次仍只有一个过程组；这是忠于历史事实，不能靠时间间隔臆造阶段。

## 验收记录

- 纯行为探针构造 A/B/C 三条摘要、三类操作和一条 reasoning，得到 3 个独立阶段；A 与 B 之间的 reasoning
  只属于 A，每个阶段各有一个活动组，只有 C 标记为后续分页入口。
- 浏览器组件真实 DOM：运行态显示 A/B 两个可折叠阶段和实时 C 阶段；完成态显示 3 个独立“查看处理过程”。
  逐一展开后得到 3 个 reasoning、3 个活动组，处理时长仍位于最终正文下方。
- 子智能体 Zeno 使用同一组件显示 A/B/C 三条摘要和 3 个独立过程入口；任务指令位于转录上方，最终审查正文和
  处理时长保持在本轮末尾。
- 组件现场无 console error；窄分栏中摘要、折叠入口和活动组均无横向溢出。

## 当前阶段

源码、行为探针、普通会话/子智能体组件现场和全仓构建验收已完成。`pnpm lint`、`pnpm typecheck`、
`pnpm build`、`pnpm package:mac` 与 `git diff --check` 通过；独立 `Zeus Test.app 0.3.58` 首次窗口、
`health.status=ok`、`database/runtime=ok`、SQLite `quick_check=ok` 和测试包严格签名结构均已核验。

`pnpm verify:zarch-gates` 仍有 4 项与当前 `HEAD` 隔离快照完全一致的既有失败，未冒充为通过。正式发布与
安装版回归进入发布阶段。

## 正式安装版 0.3.58 回归

- 正式应用通过自身菜单退出后由 Homebrew 从 0.3.57 升级到 0.3.58；执行宿主、`/health` 和正式 SQLite
  `quick_check` 均正常。
- 同一条历史长轮次不再只有一个整轮“处理过程”：每条可见摘要后都有独立“查看处理过程”。
- 展开 A 与 B 之间的阶段，只显示该区间的一组命令和四条思考摘要；B、C 分别从新的过程入口开始。
- 完成正文仍位于最后一个过程阶段之后，处理时长仍位于正文末尾。
- 正式现场同时暴露了 0.3.58 之前遗留的 `paused/interrupted` 队列气泡与 Provider 接管气泡双显；该存量兼容修复
  已纳入下一版本，不把 0.3.58 记录为所有历史消息问题均已收口。

## 正式安装版 0.3.59 回归

- 正式应用已由 Homebrew 升级到 0.3.59；生产 bundle ID、严格代码签名、执行宿主、`/health` 和正式 SQLite
  `quick_check` 均正常。
- 但这次 UI 验收结论错误：正式历史轮次在 A 摘要之前仍显示了一个没有摘要的“查看处理过程”；把首条摘要前的
  Provider reasoning 并入 A 后，A 阶段内又会残留两个活动组。因此 0.3.59 只能算前一轮提示词、排队消息和存量
  接管兼容修复发布，不能算阶段分组完整验收。
- 错误结论的修复提交为 `26bde24`，发布提交为 `7eb16b3`；GitHub Release Workflow `32843854619` 成功不等于
  这项 UI 行为已经正确。

## 0.3.59 后续修正

1. 首条可见摘要接管同一轮在它之前产生的 reasoning、命令与工具过程，不再生成 `summary=null` 的孤立过程入口。
2. 同一摘要到下一摘要之间即使投影阶段身份发生变化，也会把命令、工具、文件、网页和上下文活动归并成唯一活动组；
   reasoning 仍保留在同一个摘要阶段中。
3. 行为探针增加“摘要前准备过程 + A 到 B 区间过程”的回归数据，要求只有 A/B/C 三个阶段、没有无摘要阶段，且每个
   阶段恰好一个活动组。
4. 24GB 正式历史副本还暴露了只读验收启动窗口不足：Main 与 Host 都需做完整摘要、schema 和 `quick_check`，健康
   Host 会在普通 120 秒上限之后才就绪。只读验证身份的等待上限单独改为 300 秒；普通可写身份仍为 120 秒，且
   owner、lock、rootId、数据库摘要和唯一写入者门禁均未放宽。Host ready 后等待 Main 最终复验并建立 UI 租约的
   detached idle 同样只对只读验证身份延长为 300 秒；普通可写 Host 仍在 30 秒无租约且无活动后退出。

## 正式历史只读副本复验

- 使用 SQLite Backup API 将正式库复制为独立 `dev.hypha.zeus.test` 只读验收身份；源库与副本
  `quick_check=ok`，副本 6,213,105 页、25,448,878,080 字节，SHA-256 为
  `0022d23ad9e2613194ec4cb09b0bacbf25ef3e5900b428a00e82d34a67302d32`。全程未调用 Provider、未写正式库。
- 目标会话 `conversation_19f0fc80ca9e3456dd587612`、目标轮次
  `conversation_turn_626d69e1f89deede0de31024` 的真实分页数据为 251 条模型历史和 164 条过程记录；过程序号
  1095–1258，最前面两条 reasoning 和一条命令确实早于第一条可见摘要，完整复现 0.3.59 的孤立分组条件。
- 将上述真实 V2 历史页和过程页送入正式 `adaptConversationSnapshotV2()`、`projectTranscriptRows()` 与
  `projectTranscriptTurnRows()`，得到 16 个用户可见摘要阶段、0 个无摘要阶段；16 个阶段各自恰好 1 个活动组，
  只有最后阶段保留后续分页入口。第一阶段同时接管摘要前 3 条活动与后续 reasoning，不再生成额外过程入口。
- 24GB 副本上的 `Zeus Test.app` 在新的只读 300 秒窗口内完成 Main/Host 双重验证，执行宿主保持存活，
  `/health` 返回 `status=read_only_validation`、`database=read_only_validation`；普通可写启动与空闲回收参数未变。

## v0.3.60 发布与正式安装版回归

- 修复提交 `304078f`，发布提交 `a83acde`，标签 `v0.3.60`；Release Workflow
  `32850366084` 的 preflight、typecheck、package-mac、publish 全部成功。
- 公开 DMG `Zeus-0.3.60-arm64.dmg` 为 112,347,938 字节，SHA-256
  `e7427285c8926bc840301d782f76f99d916a50b697be81a8d3a18bdd0a8c5921`；GitHub Release、manifest、Release
  notes 与 Homebrew Cask 已对账。manifest 明确 `signed=false`、`notarized=false`，未冒充 Developer ID 签名或 Apple 公证。
- `/Applications/Zeus.app` 已由 Homebrew 从 0.3.59 升级到 0.3.60；bundle ID 为 `dev.hypha.zeus`，
  `codesign --verify --deep --strict` 通过。正式 Main 与 execution host 启动后 `/health` 为 HTTP 200，
  `version=0.3.60`、`database=ok`、`runtime=ok`、正式存储保持 writable；正式 24GB SQLite 库的只读
  `PRAGMA quick_check` 返回 `ok`。
- 自动窗口验收被 macOS 锁屏阻止，Computer Use 明确返回 “The Mac is locked”；因此本记录不把正式窗口点击写成已通过。
  源码使用真实历史数据完成的正式投影验收、独立 Test 身份启动、公开发布和正式后台/数据库回归均已通过，待解锁后只补同一
  历史会话的最终窗口截图与点击复核。
