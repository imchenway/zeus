# TASK_20260903_001 v0.3.98 发布类型检查恢复

## 当前阶段

- 状态：已定位并修复本地发布门禁中的 TypeScript 失败，`pnpm typecheck` 已通过，等待从既有发布状态继续。
- 发布状态：`v0.3.98` 的格式提交 `f2ca8c98` 与发布提交 `0b83cc96` 已形成；恢复状态停在 `release_committed`，尚无 main
  推送、标签或公开 Release 完成证据。
- 操作边界：只恢复缺失导入，不修改会话业务逻辑，不手工编辑 `.git/zeus-release/v0.3.98/state.json`。

## 根因

`ce52d7aa` 重排 `packages/local-server/src/index.ts` 时，从 `@zeus/storage` 导入列表中误删了仍在使用的
`ProjectRepository`。`new ProjectRepository(db)` 因而无法解析，依赖该实例推断类型的回调继续产生 6 个隐式 `any`
，均为同一缺失导入的级联错误。

## 修复

恢复既有 `ProjectRepository` 导入。没有新增类型声明、适配层或重复注解；仓库已经提供并导出该类，一行导入即可恢复原有类型推断。

## 验收

- [已通过] `pnpm typecheck`，包含架构治理与 TypeScript 全量检查。
- [待验证] `pnpm release` 从 `release_committed` 恢复并完成本地门禁、远端 main、标签、Workflow、GitHub Release、DMG、manifest
  与 Homebrew 核验。
- [待验证] 分开报告 ad-hoc 签名与 Apple Developer ID 签名、公证事实。
