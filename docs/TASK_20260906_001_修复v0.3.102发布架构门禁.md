# TASK_20260906_001 修复 v0.3.102 发布架构门禁

## 现场结论

2026-09-06，`pnpm release` 已写入本地发布提交 `de2fb52a`（`chore(release): v0.3.102`），随后在架构治理阶段失败：

- `packages/local-server/src/index.ts`：4005 行，超过 4000 行上限 5 行。
- `packages/local-server/src/localServerPlatformRoutes.ts`：4010 行，超过 4000 行上限 10 行。

失败发生在公开 Release 创建前，不能视为 v0.3.102 已发布。

## 本次修复

新增 `packages/local-server/src/localServerPlatformSupport.ts`，收纳本地平台边界的共用能力：

- IM 内部命令封装；
- 本地 API 来源校验与 CORS 响应头；
- 请求头归一化；
- Runtime adapter 与工作目录边界校验。

修复后文件规模：

- `index.ts`：3964 行；
- `localServerPlatformRoutes.ts`：3986 行。

未放宽架构治理阈值，未修改发布脚本，未执行提交、推送、打标签或公开发布。

## 验证证据

- 3 个相关文件的 Prettier 检查通过。
- `pnpm exec tsc -p packages/local-server/tsconfig.json --noEmit` 通过。
- `git diff --check` 通过。
- 全仓 `pnpm exec tsc -b` 仍被既有 Renderer 错误阻断，错误位于 `useProjectRepositoryDiscovery.ts`、`SessionWorkspace.tsx`、
  `ModelConnectionsSettingsPane.tsx`，不在本次改动文件中。
- `pnpm verify:architecture` 在当前受限运行环境中未能执行脚本逻辑：`tsx` 创建本地 IPC 管道时返回 `listen EPERM`；直接使用
  Node 类型剥离又因仓库 `.js` 扩展名映射无法解析源码模块。因此架构门禁结果仍需在正常发布运行环境复核。

## 第二轮发布阻塞修复

随后重新运行 `pnpm release` 时，架构治理已通过，TypeScript 暴露 3 组 Renderer 错误：

- 为仓库发现错误转换补齐当前应用语言参数；
- 为会话顶部 Computer Use 停止按钮补齐忙碌/错误状态，并携带 `conversationId` 与 `nativeSession.id`；
- 补齐模型供应商设置使用的 `formatVisibleApplicationError` 导入。

lint 继续发现并清理了 3 个拆分后的无效 local-server 导入，以及已由统一错误解释器替代的未使用错误文案表。

第二轮验证结果：

- Prettier 通过；
- `pnpm lint` 通过；
- `pnpm exec tsc -b` 通过；
- `git diff --check` 通过。

## 当前状态

第二轮修复保留在工作区，未提交、未推送、未发布。架构门禁在最新发布日志中已通过；要完成 v0.3.102，仍需将本轮修复纳入发布候选并重新执行发布流程。
