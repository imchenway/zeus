# ZEUS-0088 关于对话信息的整理

## 需求与确认口径

- 所有共享原生会话渲染器的会话统一采用 Codex 风格。
- 进行中的轮次展开显示过程；轮次完成后默认折叠过程，但用户可以重新展开查看完整记录。
- 每个轮次都有自己的“已处理”耗时，位置在用户消息之后、过程入口处；轮次完成后耗时固定，不再继续读秒。
- 最终回复依据 provider item 的 `phase=final_answer` 识别，不根据“结论”等正文关键字截断。
- 变更卡片、失败卡片和用户确认历史保持独立，不被过程折叠隐藏。

## 根因

渲染端原本只在每轮最后一个可见 item 后追加 `SessionTurnDuration`。实时完成事件只更新 `activeTurnId` 和 `terminalTurnIds`，没有同步更新 `turnsByProviderId`，导致已完成轮次仍可能用当前时间计算耗时。

Pi 运行内核在完成持久化时还会丢失原轮次的 `startedAt`，重载后的历史轮次因此无法恢复真实耗时。

## 实现

- `conversation.turn.started/completed` 事件携带后端产生的 `startedAt`、`completedAt`。
- Reducer 在实时事件中创建或更新 `NativeTurnSnapshot`，保持轮次状态、起止时间和已有计划、错误信息。
- Codex 与 Pi 的持久化起止时间作为耗时事实源；Pi 完成时保留已有 `startedAt`。
- transcript 先按原始时间线投影，再按轮次把 reasoning、plan、commentary、工具活动等合并到一个过程入口；最终回复保持独立可见。
- 当前轮次的“正在思考”状态放在当前轮次入口内；不再渲染置底的全局耗时或思考状态。
- 计时刷新限制在活动的 `SessionTurnDuration` 组件内，使用现有等待确认扣除规则。
- 样式只作用于 `.session-codex-parity-v1`，支持折叠摘要、键盘焦点、窄窗口和 reduced-motion。

## 取舍

过程默认折叠可以让最终回复成为主要阅读内容，同时保留展开后的审计能力；代价是 transcript 需要额外的轮次投影和嵌套布局。失败和中断轮次继续保留失败证据，不伪装成普通成功回复。

## 验证记录

- 依赖：使用 `pnpm install --offline --frozen-lockfile` 恢复，锁文件未改变。
- 已完成：`pnpm typecheck`、`pnpm lint`、`pnpm build`、`git diff --check`。
- 已完成：`pnpm package:mac` 生成 `dist/test/mac-arm64/Zeus Test.app` 和测试 DMG；Bundle ID 为 `dev.hypha.zeus.test`，`codesign --verify --deep --strict` 通过，包体健康检查通过。
- 已完成真实桌面检查：使用 `/tmp/zeus-0088-gui-user-data-2` 独立数据目录打开历史会话；每轮都有独立耗时，首轮过程默认折叠，点击和 Space 均可展开/收起，最终回复保持可见；等待 2.5 秒后已完成轮次仍为 `已处理 21s`。
- 未完成真实活动轮次检查：当前隔离数据没有安全可用的进行中模型轮次；为避免无关的真实模型调用，本轮未新建或提交模型请求。
- 内置 Zeus Browser 已尝试连接，但标签列表为空，打开测试包内页面的调用无响应；该 Browser 缺口不以静态构建结果替代，真实桌面证据来自本 worktree 的 `Zeus Test.app`。
- 真实运行只使用独立身份 `Zeus Test.app`（Bundle ID `dev.hypha.zeus.test`）；没有启动、修改或退出正式 `/Applications/Zeus.app`。

## 提交与合入

- 实现提交：`150656f66a2f835fd3ec047f5b41a1e1a7332a4d`。
- 已合入 `/Users/david/hypha/zeus` 的 `main`，合并提交：`ce5de2467556311a30c3bc32fced352f8fabb223`。
- 合并时处理了 `ConversationTranscript.tsx`、`SessionActivity.tsx`、`session.css` 和 `piNativeConversationCoordinator.ts` 的内容冲突；保留 main 的滚动跟随、活动项细节、失败诊断和队列刷新行为，同时接入本任务的轮次过程折叠与固定耗时。
- 合入后的 `main` 工作区干净；未执行远端 push。

## 2026-08-10 二次复验与定点返修

### 本轮范围

- 当前主线已保留按轮折叠、`phase=final_answer` 独立展示、每轮起止时间和完成后停止读秒，本轮不重复重构这些链路。
- 中文界面的耗时单位改为“时 / 分 / 秒”；秒始终显示，小时存在时保留中间的零分钟，英文界面继续使用 `h / m / s`。
- 不新增接口、数据库迁移、状态枚举或测试体系；不按正文关键词截取最终回复。

### 实施记录

- `SessionTurnDuration` 将当前界面语言传给耗时格式化函数。
- 中文格式示例为 `已处理 28秒`、`已处理 17分37秒`、`已处理 1时0分3秒`；英文格式保持 `Processed in 17m 37s`。
- 真实 Codex 轮次暴露出阶段映射缺口：Provider 载荷为 `phase=commentary` 的 `agentMessage` 被持久化成了 `final_answer`，导致过程回复留在折叠区外。
- 新事件入库和旧 Codex 线程迁移现在优先采用 Provider 明示阶段；只有缺失阶段的兼容记录才继续把 `agentMessage` 兜底视为最终回复。
- Renderer 会优先读取项目载荷中的原始阶段，因此无需数据库迁移，也能让既有误分类记录恢复为过程信息；没有按“结论”等正文关键词切割。

### 本轮验证记录

- 已通过：`pnpm lint`、`pnpm typecheck`、`pnpm build`、`pnpm package:mac`、目标文件 Prettier 检查和 `git diff --check`。构建仅有既存的大分块提示，没有失败。
- 测试产物仅有 `dist/test/mac-arm64/Zeus Test.app`，Bundle ID 为 `dev.hypha.zeus.test`，代码签名校验通过；没有生成 `Zeus.app`。
- 使用实例隔离数据根启动本任务的单一测试包，并发起一轮已授权的只读 Codex 请求。运行中可见展开的“已处理 0秒”和思考状态；完成后显示“已处理 16秒”，过程自动收起，最终回复独立位于折叠区外。
- 完成后等待超过 4 秒，“已处理 16秒”保持不变；点击可展开过程回复和一条命令活动，Space 可再次收起，最终回复始终保留在外部。
- 返修后重新载入同一条历史记录，原先误分类的 `commentary` 过程回复已进入折叠区，说明既有载荷兼容投影生效。
- 浅色和深色主题均完成目视检查；测试环境明确启用了减少动态效果，折叠交互和键盘焦点正常。
- 运行轮次仅覆盖“秒”格式；“分秒 / 时分秒”、英文单位和 Pi 兼容性完成代码路径核对，没有用伪造时间制造运行证据。
- 隔离历史只有一个安全可用的已完成轮次，因此“多个完成轮次各自独立入口”和窄窗口没有形成真实运行证据，保留为验收缺口。
