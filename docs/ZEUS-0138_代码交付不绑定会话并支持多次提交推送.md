# ZEUS-0138 代码交付不绑定会话并支持多次提交推送

## 任务结论

代码交付操作不再绑定活动会话，也不把任务分支推送当作合入闸门。用户可以在同一会话和同一任务工作区内按需要多次提交、多次推送、再次提交新变化；界面的 `busy` 只防止同一请求重复点击，不代表提交或推送次数限制。

## 行为规则

- 提交、推送、合入、回收和丢弃不因活动会话数量而被服务端拒绝。
- 活动会话继续写入时，界面显示非阻断提示：本次提交只包含当前已经落盘的内容，后续变化可以再次提交。
- 停止活动会话保留为独立操作，不再是提交、推送或合入前置步骤。
- 任务分支推送是协作和备份动作。任务分支只有本地提交时仍可创建合入候选；合入固定使用创建任务时记录的来源分支，完成本地合入后再按需推送来源分支，不读取旧远端记录，也不强制覆盖远端。
- 回收、丢弃以及可能在来源分支交付后回收 worktree 的合入操作，在活动会话存在时先提示风险；用户确认后才执行。
- 冲突、脏工作区、Git 锁、目标分支并发变化、远端非快进、远端刷新失败和远端 SHA 校验失败继续显示真实错误，不自动停止会话、不自动强推、不伪造成功。

## 实现范围

- 桌面端 `TaskGitReviewModal` 和 `TaskGitMergeModal` 移除活动会话与任务分支远端校验对按钮和 readiness 的阻断。
- 本地服务端移除提交、推送、合入、回收和丢弃路由的活动会话写入校验；合入开始和最终确认只保留本地任务 HEAD、来源分支 HEAD、集成工作区和真实 Git 安全检查，来源分支推送继续执行刷新、非快进和远端 SHA 校验。
- `activeConversationCount`、`remoteVerified` 和 `remoteRefreshError` 仍作为事实状态返回和展示，不新增提交次数、推送次数、会话绑定或迁移字段。
- 任务生命周期终态清理、嵌套 worktree 先子后父、通用高风险 Git 操作策略保持不变。

## 优点与代价

优点：提交和推送回到真实 Git 功能，适合边写边提交、分阶段推送和多人协作；远端临时不可用时仍能保留本地提交现场。

代价：活动会话可能在提交后继续写入，用户需要再次提交；回收、丢弃或来源分支交付可能影响仍在写入的 worktree，因此必须认真阅读风险提示。远端刷新失败时，不能使用缓存状态代替权威远端结果。

## 验证记录

本任务不新增单元测试。实现完成后记录以下验证：

- `pnpm lint`
- `pnpm typecheck`
- `pnpm build`
- `pnpm package:mac`
- `git diff --check`
- 隔离 Git 工作区中的活动会话、多次提交、多次推送、未推送任务分支合入、真实 Git 错误和远端刷新失败场景。

### 2026-08-08 实施结果

- `pnpm lint`：通过。
- `pnpm typecheck`：通过。
- `pnpm build`：通过。
- `pnpm package:mac`：通过，生成 `dist/test/mac-arm64/Zeus Test.app`，bundle ID 为 `dev.hypha.zeus.test`，签名结构校验通过。
- 隔离 Git 验收：通过。同一 worktree 完成两次本地提交并重复推送；未推送任务分支可以合入记录的来源分支，来源分支再按需推送；远端分叉返回 `ZEUS_TASK_REMOTE_DIVERGED`；远端刷新失败返回 `ZEUS_GIT_REMOTE_REFRESH_FAILED`。
- GUI 点击验收：未执行。本机已有 ZEUS-0137 的 `Zeus Test.app` 使用同一测试 bundle 和用户数据目录运行，继续启动会命中单实例锁；按项目约束不停止或干扰已有测试实例。
- `git diff --check`：通过。

### 2026-08-11 活动会话误导与提交锁死修正

真实页面仍在代码交付和提交弹窗中提供“停止活动会话”。该操作复用了 Git
提交的全局忙碌状态；遇到无法收口的孤儿会话时，停止请求会连带禁用提交按钮。分支摘要还优先显示“会话写入中”，覆盖了“未提交文件”这一真实
Git 状态，造成必须先停会话才能提交的错误表达。

本次修正：

- 移除代码交付和提交弹窗中的“停止活动会话”操作；停止会话回到会话自身入口处理。
- 活动会话卡片改成纯信息提示，明确它不参与提交或推送门禁。
- 分支摘要始终先显示未提交、已提交、冲突或合入状态，再把活动会话数量作为后缀补充，不再用“会话写入中”覆盖 Git 状态。
- 保留来源分支合入可能回收 worktree 时的风险确认。

优点：孤儿会话或停止失败不再锁死 Git 提交；页面直接表达真实交付状态。代价：确实需要停止会话时，用户要回到会话入口操作，代码交付页不再提供快捷停止按钮。
