# ZEUS-0139：`.zeus` 目录内容审计

审计日期：2026-08-08

审计目标：只读检查 `/Users/david/.zeus` 中是否存在可确认无用的遗留备份、缓存或孤儿文件。

本次没有删除、移动、清空缓存、修改 SQLite、停止 Zeus 进程，也没有执行提交、推送或发布。

## 结论

当前 `.zeus` 使用空间约 `3.0G`。目前能够明确列为“无业务用途候选”的只有 macOS 生成的 `.DS_Store`：共 29 个、约 254 KiB。它们不属于 Zeus 业务数据，但删除后可能重置 Finder 的目录显示状态；本次仍未删除。

占空间最大的内容并不能直接视为无用：

- `profile/electron/Cache` 约 863 MiB，是可重建的 Electron 网络缓存，但正式应用正在使用，只能在应用关闭后通过应用级缓存清理处理。
- `backups/database/zeus.db.pre-layered-...bak` 约 432 MiB，是最新分层迁移留下的唯一数据库回滚点，应继续保留。
- `artifacts/turn-change-sets` 中有约 131 MiB 文件未被当前 `turn_change_files.pre_blob_ref/post_blob_ref` 直接引用，但这些文件分布在 `applied`、`unavailable` 和 `conflicted` 变更集下，不能按“未引用”直接删除。
- 任务附件、会话附件和浏览器批注附件均能找到业务引用，没有发现附件孤儿。

## 运行边界

审计时正式 `/Applications/Zeus.app` 及其执行宿主正在运行，测试身份 `Zeus Test.app` 及其执行宿主也正在运行。审计只读取文件系统、进程列表和 SQLite，不触碰活动锁文件或会合信息中的敏感内容。

文件系统共发现 14 个符号链接，均属于 Node 命令入口、Provider 配置投影、Codex 临时执行入口或 Electron 单实例运行链接；没有发现指向已废弃 `.zeus` 根目录的孤儿链接。

## 顶层目录盘点

以下空间来自 `du -sh`；SQLite 的 `-wal` 和 `-shm` 文件属于当前运行数据库的一部分。

| 路径 | 占用 | 只读判断 |
| --- | ---: | --- |
| `artifacts/` | 639 MiB | 受管运行产物，按下表分别判断 |
| `backups/` | 437 MiB | 包含唯一回滚点，不能整体清理 |
| `data/` | 428 MiB | 当前数据库、WAL、日志和密钥，保留 |
| `profile/` | 1.0 GiB | Electron 缓存与浏览器状态，分开处理 |
| `providers/` | 566 MiB | Provider 会话、SQLite、插件和运行时内容，保留 |
| `runtime/` | 112 KiB | 执行宿主、迁移、更新和隔离状态，保留 |

### `artifacts/`

| 子目录 | 占用 | 证据 |
| --- | ---: | --- |
| `turn-change-sets/` | 463 MiB | 变更恢复快照，存在大量历史状态，不能按文件孤立性清理 |
| `task-attachments/` | 121 MiB | 142 个文件、111 个不同引用路径，全部现存 |
| `command-scripts/` | 27 MiB | 全局命令脚本及 `wx-remote-debug` 运行依赖 |
| `runtime-sessions/` | 23 MiB | 48 条数据库会话记录对应的运行日志 |
| `browser-comments/` | 2.8 MiB | 5 个文件、3 个引用路径，全部现存 |
| `conversation-attachments/` | 2.6 MiB | 14 个文件、3 个引用路径，全部现存 |
| `command-runs/` | 276 KiB | 37 个运行目录、54 个已登记产物 |

### `backups/`

- `database/`：当前文件为 `437,735,424` 字节。两个迁移清单均为 `committed`；最新清单记录它是本次分层迁移的数据库备份，上一份迁移备份已按规则清理。
- `imports/codex/`：两批 Codex 配置导入内容约 4.6 MiB，共 199 个文件；当前目录没有 `manifest.json`，不能确认每个内容都仍可恢复，因此列为“待确认”，不自动删除。
- `imports/codex-legacy/`：2 个旧会话 JSONL，约 8 KiB；`codex_legacy_imports` 表当前为 0 行，但这不足以证明用户不再需要这些兼容导入材料。
- `legacy-roots/`：仅 1 个约 325 字节的旧根回收记录，属于迁移审计凭证，应保留。

### `data/` 与 `profile/`

- `data/zeus.db` 约 417 MiB，`zeus.db-wal` 约 8.5 MiB，`zeus.db-shm` 约 32 KiB。数据库当前处于 WAL 运行态，不能直接复制覆盖或手工删除其中任一文件。
- `data/logs/` 约 2 MiB、461 个文件。Runtime 日志保留策略为 30 天，但当前没有满足“无任务归属、未收藏、已归档或已删除且到期”的会话候选。
- `profile/electron/Cache` 约 863 MiB；`GPUCache` 约 4.6 MiB，两个 Dawn 缓存各约 548 KiB，`Code Cache` 约 108 KiB。这些属于可重建缓存候选。
- `profile/electron/Partitions/zeus-browser` 约 136 MiB，连同 Cookies、Local Storage、Session Storage 等属于浏览器状态或站点授权，不列为缓存垃圾。

## SQLite 与引用核对

### 数据库完整性

- 当前 `/Users/david/.zeus/data/zeus.db`：`PRAGMA quick_check` 返回 `ok`。
- 最新迁移回滚快照：通过 SQLite `.restore` 读入内存后执行 `PRAGMA quick_check` 返回 `ok`；没有写回备份文件。

### 变更恢复快照

当前数据库中的变更集状态和文件记录如下：

| 状态 | 变更集 | 数据库文件记录 |
| --- | ---: | ---: |
| `applied` | 54 | 194 |
| `capturing` | 104 | 0 |
| `conflicted` | 2 | 2 |
| `unavailable` | 44 | 731 |
| `undone` | 1 | 1 |

文件系统共有 2,474 个 `.blob`，约 480,207,568 字节：

- 1,694 个、约 342,804,003 字节能在当前 `pre_blob_ref/post_blob_ref` 中找到；
- 780 个、约 137,403,565 字节未被这些字段直接引用；
- 未引用部分包括 `applied` 变更集约 30.3 MiB、`unavailable` 变更集约 107.1 MiB、`conflicted` 变更集约 24 字节；
- 变更集顶层没有发现不对应数据库记录的目录。

这些未引用文件可能是重复快照、失败捕获留下的现场或仍需人工审计的恢复证据。项目已有规则明确禁止仅按 `unavailable` 或文件引用状态推断可删除。

### Runtime 会话

- 数据库共 48 条 Runtime 会话：45 条 `exited`、3 条 `lost`，当前没有 `running` 会话；其中 9 条关联任务。
- 48 条会话均存在日志或终端事件，数据库中没有收藏、归档或删除标记。
- 文件系统有 49 个一级目录，额外的 1 个是空的受管 `.retention-quarantine`，不是孤儿会话目录。
- 按当前 30 天保留规则计算，可进入隔离清理的候选为 0 条。

### 命令运行与产物

- `command_runs` 有 37 条记录，全部为 `failed` 或 `succeeded`，没有活动运行。
- `command_artifacts` 有 54 条登记记录，总大小约 96 KiB。
- `command-runs/` 中另有 3 个未登记文件、共约 17.8 KiB：根目录 `.DS_Store`，以及两个旧版本发布结果 Markdown。它们是后续可人工确认的轻量候选，不在本次删除。
- `command-scripts/` 的脚本目录没有用数据库表直接登记；其中包含全局 Git/微信脚本和 `wx-remote-debug` 的 `node_modules`，不能因“数据库无引用”判定无用。

### 附件引用

只读交叉核对任务、会话资源、会话 item、消息元数据和命令产物路径后：

- `task-attachments/`：142 个文件全部落在至少一个现存托管引用下；
- `conversation-attachments/`：14 个文件全部被现存引用覆盖；
- `browser-comments/`：5 个文件全部被现存引用覆盖；
- 没有发现可以直接列为附件孤儿的文件。

## Provider 内容

`providers/codex/` 约 565 MiB，其中 Codex 会话 JSONL 39 个、约 86.5 MiB；数据库中的 32 个 Provider 路径均可读取，当前会话目录中另有 12 个 JSONL 未被 Zeus 当前 conversation 路径直接匹配，约 5.7 MiB。

这些文件由 Provider 自己管理，未被 Zeus conversation 表直接引用不等于无用；尤其当前 Provider SQLite、插件、shell snapshot 和会话 JSONL 正被运行中的正式或测试宿主使用，本次全部保留。

## 分类结果与后续建议

| 分类 | 内容 | 本次结论 |
| --- | --- | --- |
| 明确无用候选 | 29 个 `.DS_Store`，约 254 KiB | 后续可统一清理；本次不删除 |
| 可重建 | Electron `Cache` 及 GPU/Code/Dawn 缓存 | 只能在应用关闭后通过应用级清理处理 |
| 待确认 | 未引用变更 blob、旧 Codex 导入备份、未登记发布结果、未被 Zeus 当前表匹配的 Provider JSONL | 需要业务或 Provider 生命周期确认 |
| 必须保留 | 当前回滚备份、数据库/WAL/SHM、任务和会话附件、浏览器分区、Provider SQLite/JSONL/插件、迁移与运行审计记录 | 不按年龄或占用直接删除 |

当前没有执行清理。若后续需要释放空间，安全顺序应是：先处理 `.DS_Store`，再在关闭应用后使用应用级 Electron 缓存清理，最后针对导入备份、命令产物和变更快照设计带预览、隔离宽限期和审计记录的专用回收流程。

## 验证边界

本次是只读数据审计，没有代码或 API 变更，因此未执行 `pnpm lint`、`pnpm typecheck`、`pnpm build` 或 `pnpm package:mac`。这些命令不能替代本次数据库、文件引用和运行中进程的审计结论。
