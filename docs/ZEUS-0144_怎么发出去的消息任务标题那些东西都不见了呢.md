# ZEUS-0144 怎么发出去的消息任务标题那些东西都不见了呢

## 任务目标

修复 Codex 原生会话中，首条用户消息已经发送后，任务标题、任务类型和类型字段被提供方异步回放覆盖的问题。任务提示词本身继续由现有任务正文构造器生成，不改变发送内容和结构化附件。

## 根因证据

- 任务推送提交记录中保存的是完整用户正文，包含任务标题、任务类型以及优化任务的“现状”和“预期”。
- `item/started` 与 `item/completed` 的 `userMessage` 事件可能只带消息元数据，文本字段为空或比本地提交正文短。
- 旧逻辑在完成事件中直接使用提供方事件文本生成项目正文，并用同一正文更新 `conversation_messages`；因此晚到的空文本覆盖了此前完整的乐观消息和本地记录。
- 会话快照又把 `conversation_messages` 作为历史用户消息来源，所以切换或重新打开会话后会继续显示被覆盖的正文。

## 实现口径

在 `packages/local-server/src/codexNativeUserMessageProjection.ts` 集中处理 Codex 原生用户消息的关联和正文选择：

1. 明确的 `displayText` 优先，保留“是，实施此计划”等短展示文案。
2. 精确关联的提交正文其次，保留任务字段、换行和空字符串输入语义。
3. 没有关联提交时保留同一提供方消息已经保存的非空正文。
4. 只有没有本地正文可用时才使用提供方事件正文。

实时 `item/started`、`item/completed`、本地消息项目、持久化用户消息和广播事件使用同一结果。原生会话快照对已经存在的 Codex 用户消息按 `clientMessageId` 或唯一 `providerTurnId` 做读取兜底，不执行模糊全库迁移。

不修改任务提示词构造器、发送接口、事件字段、附件或客户端消息 ID 语义。Renderer 的乐观消息收敛逻辑保持不变。

## 方案取舍

共享投影层能同时修复任务首条消息和普通 Codex 用户消息，避免只在一个页面上保留文本；代价是必须持续保留 `displayText` 的特殊展示语义。历史消息采用精确关联的读取兜底，可以立即恢复会话气泡，同时避免没有可靠关联时误改其他历史消息。

## 验证记录

以下结果只记录本任务实际执行过的检查；静态检查、构建和打包不等同于真实 GUI 或真实 provider 验收：

- `pnpm lint`：通过。
- `pnpm typecheck`：通过。
- `pnpm build`：通过；15 个工作区项目和桌面 Renderer 均完成构建。
- `pnpm package:mac`：通过；生成 `dist/test/mac-arm64/Zeus Test.app` 和测试 DMG，`codesign --verify --deep --strict` 通过，bundle ID 为 `dev.hypha.zeus.test`。
- 合并暂存区 `git diff --cached --check`：通过；未发现冲突标记。
- 一次性隔离投影契约探针：通过；覆盖空提供方正文、完整提交正文、显式 `displayText`、已有正文、无本地提交时的提供方正文回退，以及客户端 ID 冲突隔离。
- 隔离 coordinator 事件回放：通过；任务消息、普通用户消息和显式 `displayText` 消息在 `item/started`、`item/completed` 后均保持预期正文，提交记录中的实际正文不变。
- 隔离 local-server 快照恢复：通过；预置空历史消息后通过 HTTP 快照重新打开，会话首条正文完整、消息不重复且 `providerItemId` 保留。
- 全仓 `pnpm format:check`：未通过；检查发现 36 个既有格式文件，未执行无关文件的批量格式化。新增投影文件及本任务触及的既有源码文件已单独通过 Prettier 检查。
- 尚未执行真实 app-server 或桌面 GUI 交互验收，因此不能把本次静态、构建和打包结果表述为真实 provider 或 GUI 验收。

本次合并只在本地 `main` 完成，不执行 fetch、push、分支删除或历史重写；合并提交前以当前验证结果作为门禁。
