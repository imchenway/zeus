# ZEUS-0165 会话环境卡片、代码页按钮与计划展示修复

## 任务目标

修复宽屏环境卡片定位错误、任务详情与环境卡片重叠、浏览器打开后环境按钮无法展开、代码页模式按钮无法点击，以及计划正文没有直接显示的问题。

## 根因与取舍

### 环境卡片

- 常驻卡片原先绝对定位在头部操作按钮的零宽锚点上，位置依赖头部内边距，无法稳定表达正文右上留白。
- `forceCollapsed` 同时承担“取消常驻”和“禁止展开”，导致浏览器打开后虽然出现环境按钮，点击仍无法显示卡片。
- 任务详情抽屉由应用层管理，会话卡片此前不知道详情状态，因此没有互斥关系。

本次把“强制收起”和“完全隐藏”拆开：浏览器、计划、源码预览和变更审查只取消常驻，仍允许用户主动弹出；任务详情打开及关闭动画期间完全隐藏卡片。优点是状态语义明确并保留主动查看能力，代价是应用层需要向会话工作区传递一个内部布尔状态。

### 代码页模式按钮

代码模式工具栏位于窗口顶部 `56px` 透明拖拽层范围内，却没有加入顶部交互控件的层级规则，点击被拖拽层截获。本次只提升工具栏交互层，不改源码、图谱和命令的状态逻辑。优点是改动小且保留窗口拖拽区域，代价是新增顶部工具栏仍需遵守同一层级契约。

### 计划正文

`plan` 条目已经持久化，“实施此计划？”也能引用对应条目，但会话投影把所有非用户、非最终回答条目都归入“已处理”折叠区。计划是交给用户审核的最终产物，不属于过程信息，因此本次将它从处理过程分组中排除，继续复用现有计划摘要组件独立展示。

## 实现范围

- 环境卡片保持 `300px` 宽，距会话头部下缘和右边缘各 `16px`，只允许纵向滚动；长来源名称使用省略显示。
- 任务详情状态通过内部属性传给会话工作区；隐藏期间不清空已加载的工作区快照。
- 浏览器等右侧工作面打开时，环境按钮可以正常展开弹出卡片。
- 代码页模式工具栏加入顶部交互层规则。
- 实时和历史快照共用的会话投影不再把计划条目折叠进处理过程。

## 验证记录

### 静态检查与构建

- `git diff --check`：通过。
- `pnpm lint`：通过。
- `pnpm typecheck`：通过。
- `pnpm build`：通过；Vite 保留既有的代码编辑器分包体积提示，没有构建错误。
- `pnpm package:mac`：通过，生成 `dist/test/mac-arm64/Zeus Test.app` 和测试身份 DMG。
- 测试应用包身份：`dev.hypha.zeus.test`；`codesign --verify --deep --strict` 通过。

当前 worktree 初始没有依赖目录，先使用 `pnpm install --frozen-lockfile --offline` 恢复锁文件对应依赖；没有修改锁文件。

### Electron GUI

使用 `Zeus Test.app` 和独立临时数据目录 `/tmp/zeus-0165-qa.UZ0Zqu` 启动验收；运行进程的 Chromium `user-data-dir` 和 `zeus.db` 均位于该目录，数据库 `PRAGMA quick_check` 返回 `ok`。没有启动或生成生产身份 `Zeus.app`。

- 代码页“图谱”：鼠标点击后进入 `#project-code-graph`，真实显示代码图谱，共扫描到 16 个节点和 21 条关系。
- 代码页“命令”：从图谱按钮使用键盘 `Tab` 和 `Return` 后进入 `#project-commands`，真实显示命令中心。
- 代码页“源码”：从命令按钮使用键盘反向切换并确认后进入 `#project-code-source`，鼠标和键盘切换均已覆盖。
- 独立测试数据没有 Zeus 专属 Codex 登录信息。尝试从计划模式创建真实会话时，应用明确提示“Zeus 专属 Codex 尚未登录。请先完成登录，再创建会话。”，因此本次没有导入正式环境凭据，也不能把计划正文、环境卡片弹出、任务详情互斥和历史会话重载声明为真实 Provider 或 Electron GUI 已验收。

### 结论边界

计划投影、环境卡片状态规则和定位通过代码检查、类型检查与构建链路；代码页三个模式额外完成了真实 Electron 鼠标和键盘切换验收。由于独立测试数据的登录前置条件未满足，其余需要真实会话的 GUI 场景仍需在具备测试账号的隔离数据目录中补验。静态检查、构建、打包、Electron GUI 和真实 Provider 结果分别记录，不互相替代。
