# ZEUS-0171 思考过程

## 任务信息

- 任务标题：思考过程。
- 任务类型：优化。
- 现状：同一活动轮次出现两条“正在思考”；思考摘要和过程活动字号偏小；会话页与任务列表打开的会话抽屉字体和字号不一致。
- 预期：同一活动轮次只展示一个可信过程状态；会话页与任务会话抽屉使用同一套清晰、可读的会话排版。

## 现场与代码事实

1. 两条“正在思考”来自同一个 `showThinking` 派生状态，不是 Provider 重复发送。
2. 当前活动轮次已经在 `SessionTurnDuration` 内渲染一条 `TranscriptThinking`，会话记录底部仍根据同一状态无条件补画第二条。
3. 可读思考摘要使用独立的 `13px`，工作活动标题使用 `13px`，活动中的最新一项和展开项使用 `12px`，与会话工作区 `14px` 基准形成过细字号层级。
4. 系统字体与 `14px` 基准只绑定 `.session-codex-parity-v1 .ai-workspace`。普通会话页满足该祖先关系；任务会话抽屉通过 Portal 挂载，内部 `session-workspace-root` 绕过该规则并继承根页面字号。
5. 历史任务 ZEUS-0124 已确认：只移动活动轮次的最新思考摘要，历史轮次继续保留原时间线位置。本任务不推翻该规则。

## 已确认领域口径

- **思考摘要**是运行内核提供的阶段性可读说明，保留原文和原始语言。
- **思考占位状态**只在活动轮次尚未出现新思考摘要、工作活动或回答时提供临时反馈。
- “思考过程”是任务标题，不把不可见的完整模型推理冒充成可展示内容。

## 用户确认

1. 会话页与任务会话抽屉统一使用系统字体。
2. 普通正文、思考摘要和过程活动统一为 `14px`；只有耗时等元信息保留 `11–12px`。
3. 同一活动轮次最多显示一条“正在思考”，位置在当前轮次末尾。
4. 一旦出现真实思考摘要、工具活动或回答，由真实内容接替占位，不再并排保留“正在思考”。
5. 确认进入实现与验证，不扩展为其他页面的全局字体重做。

## 实施方案

### 单一思考占位

- 继续从当前会话状态派生是否需要占位，不新增 React 同步状态。
- 活动轮次已经进入可见轮次结构时，只在该轮次内部渲染占位。
- 活动轮次尚无可见行时，才在会话记录底部使用兜底占位。
- 真实反馈继续由现有 `feedbackEpoch / visibleFeedbackEpoch` 收敛占位，不用 CSS 隐藏重复节点。

优点：展示数量与活动轮次事实一致，屏幕阅读器也不会重复播报同一状态；不增加状态同步和恢复负担。

代价：需要同时覆盖“已有轮次行”和“轮次尚无可见行”两条渲染路径，不能简单删除底部兜底。

### 统一会话排版根

- 把系统字体与 `14px` 基准同时绑定到 `ai-workspace` 和 `session-workspace-root`，使普通会话页与 Portal 抽屉消费同一规则。
- 思考摘要、过程说明、工作活动标题、活动最新项和活动展开项统一为 `14px`。
- 轮次耗时、技能标签和技术详情继续作为 `11–12px` 元信息，不放大为正文层级。

优点：两个入口的可读内容不再随 DOM 挂载位置变化，正文与过程反馈具有稳定阅读节奏。

代价：工作活动区域会比当前占用更多垂直空间，长任务的信息密度略有下降。

## 验证范围

- 不新增或恢复 Vitest、组件测试、DOM/CSS 契约测试。
- 执行定向格式检查、`git diff --check`、`pnpm lint`、`pnpm typecheck`、`pnpm build` 和 `pnpm package:mac`。
- 测试包只允许为 `Zeus Test.app`，bundle ID 必须为 `dev.hypha.zeus.test`，并使用独立数据目录。
- 真实界面目标流程：会话页活动轮次和任务列表会话抽屉分别打开同一类会话状态，确认只有一条“正在思考”，并核对系统字体、`14px` 正文、浅色、深色、跟随系统、窄窗口和减少动态效果。
- 静态检查、构建、测试包和真实界面证据分别记录，不互相替代。

## 当前阶段

- 已完成需求追问、用户确认、历史文档、当前实现和 Git 引入点核对。
- 已完成单一思考占位与统一会话排版根的实现。
- 未执行 Git commit、push、merge、revert 或发布动作。

## 验证结果

### 静态与构建

- `pnpm exec prettier --check CONTEXT.md docs/ZEUS-0171_思考过程.md apps/desktop/src/renderer/session/ConversationTranscript.tsx apps/desktop/src/renderer/session/session.css`：通过。
- `git diff --check`：通过。
- `pnpm lint`：通过。
- `pnpm typecheck`：通过。
- `pnpm build`：通过；仅保留现有的大分块提示。
- 构建产物的会话样式已包含 `ai-workspace` 与 `session-workspace-root` 共用的排版根、`14px` 思考占位和 `14px` 思考摘要规则。

### 测试身份打包

- `pnpm package:mac`：通过，生成 `dist/test/mac-arm64/Zeus Test.app` 和测试 DMG。
- `CFBundleIdentifier`：`dev.hypha.zeus.test`。
- `CFBundleDisplayName`：`Zeus Test`。
- `codesign --verify --deep --strict --verbose=2`：通过。

### 真实应用检查

- 从本任务打包产物复制出可单独定位的临时 `Zeus Test.app`，使用独立数据目录启动；没有关闭、复用或改写其他测试实例。
- 为避免复制账号凭据或触发外部 Provider，使用项目正式存储仓储在隔离数据库中创建一个项目、一个任务以及普通会话和任务会话两份活动快照。两份快照都包含运行中轮次、已完成命令活动、思考摘要和待继续处理状态，覆盖原问题中“轮次内已经有内容，底部又补画占位”的精确渲染条件。
- 普通会话页真实打开后，同时显示工作活动、思考摘要和一条“正在思考”；辅助功能树中“正在思考”计数为 `1`，没有第二条底部兜底。
- 从任务列表运行状态入口打开任务会话抽屉后，同样显示工作活动、思考摘要和一条“正在思考”；辅助功能树中“正在思考”计数为 `1`。
- 两个入口都加载本任务包内 `app.asar`，截图中的正文、思考摘要和过程活动层级一致；打包后的 CSS 同时命中 `ai-workspace` 与 `session-workspace-root`，对应正文和过程字号为 `14px`、元信息为 `11px`。
- 本次验收不依赖 Zeus 专属 Codex 登录，也没有把其他实例的账号状态复制进隔离目录。检查结束后已停止本轮启动的测试进程；临时应用副本、隔离数据和验收脚本随后移入废纸篓。

## 剩余风险与验收建议

- 本次已完成真实 Electron 渲染验收，但活动快照由隔离数据库构造，没有发送外部 Provider 请求；因此验证的是持久快照、水合、会话投影和最终 DOM/视觉链路，不覆盖 Provider 账号登录及实时事件传输本身。
- 本任务缺陷来自渲染器对同一 `showThinking` 状态的重复消费，当前验收快照覆盖了该缺陷的精确状态组合。若后续要做发布级全链路回归，可再使用专用测试账号发送一轮真实请求，但不作为本次 UI 优化完成的阻塞项。
