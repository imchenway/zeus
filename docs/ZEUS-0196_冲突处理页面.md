# ZEUS-0196 冲突处理页面

## 任务类型

缺陷。

## 用户反馈

1. 冲突处理页面信息层级混乱、视觉体验差。
2. 任务分支落后 `main` 时，页面表现为不允许继续处理冲突。

## 已确认实施范围

用户在 2026-08-12 补充说明：IDEA 截图不是任务级代码交付窗口的视觉参考，而是项目侧边栏“代码”入口的目标能力范围。点进“代码”后需要具备完整 Git 工作区，包括动作入口、分支操作、Repository Diff、Git Log，以及合入触发的冲突处理。

这项口径覆盖此前只重构任务级代码交付窗口的范围判断。任务代码交付仍是任务开发线的受控流程；项目代码页则提供独立的仓库级 Git 工作能力，动作直接作用于用户选中的项目仓库，而不是把任务弹窗放大后冒充完整 Git 页面。

优点：Git 查看、比较、分支操作、提交、合入和冲突处理拥有统一入口，用户不需要从任务详情或会话快捷键才能进入。

代价：范围从单个缺陷扩大为仓库级 Git 客户端能力；它会与现有“代码＝源码 / 图谱 / 命令”的信息架构冲突，并引入 checkout、pull、merge、rebase、reset、delete 等高风险写操作，必须重新确认模式归属和授权边界。

## 两个 Git 入口必须分开

用户进一步明确：项目侧边栏“代码 → Git”和从会话环境信息打开的“代码交付”是两个不同工作面，不能把能力混在一起。

### 项目侧边栏“代码 → Git”

这是当前项目全部仓库的通用 Git 工作台，范围以本任务确认的 IDEA 截图工作面为准。

### 会话环境信息“代码交付”

这是当前会话所在任务 worktree 和当前任务分支的交付页。它不再要求用户选择开发分支，也不展示通用 Git 菜单，改造后只保留三个业务动作：

1. 提交当前任务分支的代码。
2. 推送当前任务分支到远端。
3. 将当前任务分支合入其来源分支。

页面布局参考 IDEA Repository Diff：顶部左侧是全部变更文件，顶部右侧放紧凑的当前分支事实与三个动作；现有提交、推送、合入大卡片压缩为上下文工具栏。下半区用于当前文件的便捷双栏对比。文件可双击打开独立 Diff 窗口，支持最大化或全屏审查，主交付页保留原文件选择和滚动位置。

优点：环境入口天然知道当前 worktree 与当前分支，用户不再重复选择对象；代码内容成为视觉主角，主窗口和独立窗口可以分别承担概览与深度审查。

代价：交付页不再承载 checkout、rebase、reset、远端管理等通用动作；这些动作只能去项目“代码 → Git”执行。主窗口和独立 Diff 窗口还需要同步文件状态、选中项和刷新结果。

### 交付冲突的两种处理方式

- “魔法棒”只处理可由确定性规则安全解决的简单冲突，行为参考 IDEA 的 Accept Left、Accept Right 和自动合并；无法唯一判断时必须保留为待人工处理，不能猜测业务语义。
- “交给 AI”继续使用隔离区解决复杂冲突，但处理结果需要形成真实提交，并让 AI 会话后续进入当前任务 worktree 的当前分支，使用户从该会话再次打开代码交付时能看到冲突处理后的真实已提交代码，再自行选择推送或合入等后续动作。

AI 冲突处理延续用户已经发起的“当前任务分支 → 来源分支”动作：隔离候选解决全部冲突后由 Zeus 生成合并提交，并安全更新该任务 worktree 记录的真实来源分支。来源分支来自 `workspace.sourceBranch`，不假定也不写死为 `main`。成功后，AI 会话的执行现场迁移到实际承载该来源分支的本机 worktree；用户从该会话再次打开代码交付时，可以看到冲突处理后的真实已提交结果，再决定是否推送等后续 Git 操作。

优点：简单冲突无需启动 AI，复杂冲突又保留可追问、可审查的真实会话。

代价：隔离区结果写回真实分支属于修改 Git 历史的动作；必须把分支方向、提交 SHA 和当前工作目录显示清楚，不能继续使用模糊的“来源/目标”提示。

## 与现有代码页的冲突

- 当前最新实现由 `ZEUS-0163` 定义：点击“代码”默认进入源码编辑器，右上角切换“源码 / 图谱 / 命令”。
- 更早的代码图谱文档曾把代码页定义为图谱首屏，后来已经被 `ZEUS-0163` 的源码默认模式覆盖；不能只依据旧稿恢复图谱或 Git 页面。
- 当前源码工作台已经提供真实目录树、CodeMirror 编辑、保存冲突保护和未保存门禁；图谱和命令也有独立真实能力。直接替换“代码”会造成这些能力不可达。
- 当前任务级代码交付只针对任务开发线，并继续保留自己的工作区、目标分支和合入记录归属校验；项目 Git 工作台不复用这套任务门禁，两种入口必须在接口和界面上明确分开。

已确认入口结构：项目名称默认进入该项目任务列表；项目工作区顶部提供“任务 / Git / 源码 / 图谱 / 命令”，用户点击 Git 后进入项目 Git 工作台。

优点：用户进入代码页即可获得仓库级 Git 能力，既有源码编辑、图谱和命令中心仍保持可达。

代价：代码页从三种模式扩展为四种模式，需要迁移每项目模式偏好并处理旧地址；Git 模式不能把仓库级写操作伪装成任务代码交付完成。

## 已确认 Git 操作边界

### 当前项目全部仓库

Git 工作台覆盖当前项目容器内 Zeus 已发现的全部项目仓库，包括根仓库、并列仓库和嵌套仓库；用户先选择仓库，再查看和执行对应 Git 动作。内部派生仓库与当前项目外的任意路径不进入候选。

优点：覆盖 Zeus 的真实多仓项目场景，同时保持项目侧边栏的对象语义。

代价：仓库发现、排序和切换必须稳定；父仓与嵌套仓库不能自动级联提交、拉取、合入或推送。

### 直接 Git 语义

用户明确选择取消任务、开发线、代码交付状态、HEAD 并发、脏状态保护和审计门禁。项目 Git 工作台中的动作直接交给底层 Git，以 Git 自身成功、冲突或失败结果为准；不增加 Zeus 自创的权限或确认层。

唯一保留的入口边界是操作对象必须属于当前项目已发现的项目仓库，不能提交任意本机路径。该边界用于维持项目 Git 工作台的对象身份，不承担任务工作流保护。参数格式和 Git 引用合法性校验仍是执行命令所需的输入边界，不属于产品操作门禁。

优点：行为接近 IDEA 和终端，用户不会被任务状态或 Zeus 候选过期逻辑阻断。

代价：多个窗口同时操作、目标分支前进、脏工作区、历史改写、分支删除和远端写入可能造成现场丢失；Zeus 不再保留可审计的保护证据或产品级恢复承诺，风险由执行动作的用户承担。

## 已核实现场

- 截图中的红色提示来自 `ZEUS_TARGET_HEAD_CHANGED`，实际含义是隔离合入候选创建后，本机来源分支又前进了，不是任务分支落后某个固定 `main` 被禁止。
- 代码内部把合入落点记为 `targetBranch`，但在环境代码交付的产品语义中它就是该任务开发线的来源分支。界面必须展示真实来源分支名称和新旧提交，不能只显示抽象错误码。
- 当前实现会在保存冲突或收尾时发现候选过期，再占用整页忙碌状态重建；安全校验成立，但反馈时机和交互方式会让用户感觉处理权被收走。
- 权限确认层叠在冲突编辑器之上，背景再叠加整页交付层；文件、冲突块、三栏编辑、权限与底部错误分散在多个区域，当前对象、真实状态和下一步操作不够集中。

## 已确认领域口径

### 最新本地来源基线

冲突候选以该任务开发线真实来源分支的最新本机提交为准。冲突处理不自动访问远端；远端刷新和推送继续由用户单独触发。

优点：本地冲突处理不受网络和凭据故障阻断，符合本地优先的代码交付边界。

代价：用户未主动刷新远端时，本机来源分支可能不是服务器上的最新提交，界面必须如实表达这是本地基线。

### 过期合入候选

本机来源分支在冲突处理期间前进后，Zeus 自动基于最新本地来源基线重建候选；冲突指纹一致的草稿继续保留，只让真正变化的冲突重新确认。

优点：保留来源分支并发安全，同时不中断用户的冲突处理主路径。

代价：重建期间需要明确表示正在校准；冲突结构变化时，无法安全复用的草稿必须失效。

### 页面核心目标

冲突页默认采用“人工审查优先”：首屏围绕当前冲突和可编辑合并结果，确定性自动合并与 AI 处理作为辅助动作，不代替用户确认最终结果。

优点：当前对象、真实差异和保存边界保持可见；AI 等待、权限或失败不会遮断人工处理能力。

代价：复杂冲突仍要求用户理解三方差异；页面需要把辅助动作做得容易发现，但不能再次形成多个主操作。

### AI 权限确认

点击“AI 处理”后，在触发入口附近展开就地轻量确认。每次默认自动权限，用户需要显式改选完全访问；权限不跨会话记忆，也不使用覆盖冲突工作台的居中遮罩。

优点：安全确认仍然明确，当前冲突和人工处理能力保持可见。

代价：轻量确认的空间较小，权限说明必须精简，并保证键盘焦点、Escape 关闭和窄窗口定位可靠。

### 页面结构

页面采用“文件列表＋单块审查”。左侧只展示冲突文件、每个文件的已处理数量和完成状态；右侧一次聚焦当前文件的一个冲突块，通过上一处、下一处切换。完整文件三栏按需打开，不与默认冲突块视图同时常驻。

优点：文件层与冲突层职责分开，首屏能直接看懂当前处理对象；大文件默认只渲染必要上下文。

代价：跨冲突对照需要切换；完整文件中的远距离上下文需要额外打开完整视图。

### AI 会话窗口协同

AI 冲突会话创建成功后，主窗口立即进入该真实会话；代码交付窗口继续保留当前冲突位置、人工草稿和处理状态，并显示会话正在处理或已经结束。用户可以并排查看两个窗口。

优点：审批、追问和过程信息留在会话中，冲突审查现场不被跳转替换。

代价：需要可靠同步两个窗口的会话身份和处理状态；窗口重开后仍要从服务端真实记录恢复，不能依赖临时前端状态。

### 整窗信息架构

代码交付采用阶段式单工作台，按真实 Git 事实呈现“审查本机变化、准备提交、合入、处理冲突、推送”。同一时刻只有一个主工作区和一个主操作，已完成阶段可以回看，但不会用总览卡片或不可逆向导把用户困住。

优点：页面始终能回答“现在处理什么、为什么、下一步是什么”，状态与操作不会分散在多个面板。

代价：需要把当前组件中并列的文件树、差异范围、提交、合入和推送状态重新划分所有权；跨阶段切换必须保留真实选择和未保存草稿。

## IDEA Git 界面学习记录（2026-08-12）

用户补充了 IDEA 的 Git 主菜单、Repository Diff、分支上下文菜单和 Git Log 工作面截图。本轮同时对照 JetBrains 官方的 Diff Viewer、Manage Git branches 与 Log tab 文档核实可见行为。

### 动作菜单

- Git 主菜单按普通本地动作、远端同步、合入与变基、分支管理、历史或差异查看、远端管理分组，不把全部动作做成一个同权重按钮区。
- 分支上下文菜单始终围绕“选中分支”和“当前分支”组织，比较、显示工作区差异、合入和变基的方向直接写进文案；危险删除独立放在末尾。
- Zeus 采用对象和方向明确、动作分组的方式；Checkout、Rebase、Reset、Pull、Delete 等通用 Git 能力属于新项目 Git 工作台，但不能反向混入任务代码交付流程。

优点：用户不需要先理解内部 `source`、`target` 或 integration 术语，就能知道谁与谁比较、谁合入谁。

代价：完整分支名会增加文案长度，需要软换行、复制和窄窗口布局，不能用省略号隐藏关键身份。

### Repository Diff

- 差异内容占据绝大多数窗口；标题、文件序号、两侧提交身份和差异总数靠近窗口边缘，避免重复卡片。
- 顶部紧凑工具栏只承载前后文件、前后差异、双栏或统一视图、空白忽略策略、差异粒度、设置和帮助。
- 双栏各自展示真实内容，使用共同滚动、对齐行号、行级和词级高亮以及滚动条差异标记；只读一侧明确显示提交身份。
- 普通代码审查适合双栏；只有真正的冲突合并才需要目标、结果、任务三方语义，不能把三栏扩散到所有差异场景。

优点：视觉主角始终是代码，审查路径短，页面不会退化为状态卡片集合。

代价：工具栏图标依赖可靠提示、键盘名称和命中区；长文件仍需要可见区域渲染和位置同步。

### Git Log

- 左侧分支树、中间提交图、右侧变更文件与提交详情各自拥有稳定职责；选择提交后才改变右侧上下文。
- 本地分支、远端分支、提交标签、作者、时间和 SHA 均使用真实事实，颜色只辅助识别，不替代文字身份。
- Log 是独立历史调查工作面，不与提交表单、冲突编辑或推送确认同时成为主操作。

优点：分支拓扑、提交顺序和文件结果可以直接对账。

代价：完整 Log 属于独立大型能力；本次如果直接复制到代码交付，会扩大为仓库级 Git 客户端并偏离任务交付目标。

### 对 ZEUS-0196 的修正启示

- 保留单一主工作区，但把“阶段式单工作台”进一步修正为“对象驱动的上下文工作台”：用户可以围绕工作区差异、提交、分支、标签、远端或冲突自由切换，不被不可逆步骤条限制。
- Git 模式默认子页仍待确认；上下文工具栏根据当前对象展示相关动作，不能把全部动作铺成同权重按钮墙。
- 所有合入、比较和过期提示必须显示真实来源分支名称、任务分支名称、方向和新旧提交；不能把来源分支假定成 `main`，也不能只写“分支变化”。
- Git Log 是本任务的正式能力，不再作为任务代码交付中的按需附属面板。

优点：兼顾 IDEA 的高效上下文切换与 Zeus 的项目多仓语义，也能避免线性向导在分支变化或冲突重建时把用户卡住。

代价：需要撤回先前过强的线性阶段表达，改为由真实对象和能力状态驱动工具栏、主区和底部动作。

## 现有能力差距核查（2026-08-12）

### 已有可复用基础

- `git-core` 已能发现项目容器内的根仓库、并列仓库和嵌套仓库，并读取当前分支、工作区状态、远端分支、最近 5 条提交、未暂存与已暂存差异。
- 已有统一差异解析、补丁导出、任务级分支比较、任务级合入冲突读取与保存能力。
- 项目代码页已有“源码 / 图谱 / 命令”三种保活模式和每项目偏好，可扩展为“Git / 源码 / 图谱 / 命令”。

优点：仓库发现、基础状态解析和代码编辑器不用从零重写。

代价：这些能力分散在项目根仓入口、任务代码交付和通用确认流程中，不能直接拼接成完整 Git 客户端。

### 当前阻断与缺口

- 项目 Git 状态、差异和写操作当前只接受项目 ID，并通过 `resolveProjectGitScope` 绑定单一项目根仓；只要登记了多仓或嵌套仓库就直接返回“不支持”。
- 项目写操作必须先创建、确认并消费高风险确认单，执行目录固定为服务启动时的 `projectRoot`，并不是当前项目中用户选中的仓库。
- 通用写操作目前只有提交、stash、应用 stash、恢复文件、新建或切换分支、`pull --ff-only` 和 push；不具备 fetch、任意 merge、rebase、reset、标签、远端管理、分支删除或重命名等 IDEA 动作。
- 状态页只有扁平文件状态和合并后的 unified diff；没有逐文件暂存或取消暂存、任意引用比较、双栏/统一视图切换、空白策略、差异粒度和差异导航。
- Log 只返回最近 5 条提交；没有分页提交图、父提交、引用、路径/作者/日期过滤、选中提交文件和详情。
- Stash 只能创建和按手填引用应用；没有列表、内容比较、pop、drop 或 clear。
- 尚无项目级通用冲突会话。现有冲突编辑器绑定任务 integration worktree，不能处理项目 Git 工作台发起的 merge、rebase、pull 或 checkout 冲突。
- 尚无 Git Console；IDEA 的 Shelf 也不是 Git 原生命令，需要 Zeus 自己定义补丁存储、恢复和生命周期。

优点：缺口已经能按仓库模型、读取模型、操作模型和冲突模型拆开实施，不会只完成一张外观相似的空页面。

代价：这不是一次局部视觉修补；完整能力会跨 `git-core`、本地服务、Renderer API、项目代码页状态管理、差异编辑器和冲突恢复。

### Shelf 与 Git Console

用户确认两者都纳入正式范围。

- `Shelf` 作为 Zeus 管理的项目仓库补丁搁置区，至少记录来源仓库、补丁内容、标题或备注、创建时间、应用状态，并支持查看差异、应用、重命名和删除。
- `Git Console` 记录从项目 Git 工作台发起的每次 Git 调用，包括仓库、参数、开始与结束时间、退出码、标准输出和标准错误；它用于还原真实执行结果，不重新引入操作审批或审计门禁。

优点：IDEA 截图中的 Git、Local Changes、Shelf、Stash、Log、Console 不会出现只有外壳没有能力的占位页。

代价：Shelf 需要新的持久化实体和补丁应用冲突处理；Console 会产生持续增长的本机数据，需要分页、大小上限和清理策略。

### Clone 归属

用户确认 Clone 同时支持两种落点：

1. 克隆到当前项目容器内，成功后重新发现仓库并纳入当前项目 Git 工作台。
2. 克隆到用户选择的新目录，并用该仓库创建新的 Zeus 项目。

优点：既能补充多仓项目，也能从 Git 工作台直接开始一个新项目。

代价：Clone 入口必须先明确“加入当前项目”或“创建新项目”；前者要限制在当前项目容器内，后者要复用项目创建与重名处理，任何一步失败都不能留下已登记但不可用的项目记录。

### IDEA 式多仓操作

用户明确要求按 IDEA 的多仓交互实现，而不是提供一个抽象的“批量同步”按钮。

- Push 打开独立弹窗，列出当前项目所有仓库及各仓库当前分支尚未推送的提交；未启用同步分支控制时只默认勾选当前仓库，用户可以改选仓库、检查提交和文件差异、修改目标远端分支，并选择是否推送标签。
- Force Push 使用 `--force-with-lease`；Push 被拒绝后展示受影响仓库，并允许用户选择 merge 或 rebase 更新，不静默伪装为成功。
- 分支动作提供 IDEA 式“对所有仓库同步执行”项目设置；关闭时先选仓库再执行，开启时 checkout、merge、delete 等同名分支动作作用于全部项目仓库。
- 多仓同步动作必须逐仓返回成功、冲突或失败。只要出现部分成功，就如实列出已执行仓库，并提供对已成功仓库的反向操作建议；不能把它压成一个整体成功状态。

优点：与 IDEA 的 Push Commits 和同步分支控制心智一致，既支持当前仓精确操作，也支持真实多仓项目协同。

代价：跨仓操作不具备数据库事务式原子性；反向操作本身也可能失败，因此页面必须保留每个仓库的命令与结果，不能承诺一键绝对恢复。

### IDEA 多仓工作面规则

根据 JetBrains 当前的分支管理、Log 和提交或推送文档，IDEA 不使用一个覆盖全部 Git 页面语义的“当前仓库”下拉，而是由具体工作面表达仓库范围。本任务按相同原则设计：

- `日志` 默认聚合当前 Zeus 项目内所有已发现仓库；提交行通过仓库颜色条和可选仓库根列标明归属，`Paths / Roots` 可筛选一个或多个仓库。
- `本地变更` 按仓库根组织变更，用户勾选实际文件；提交对象由所选文件所属仓库决定，不继承全局仓库选择。
- `Stash` 按仓库根分组，因为 Git stash 是单仓库对象；查看、应用、弹出和删除始终作用于条目所属仓库。
- `Shelf` 是 Zeus 项目级补丁列表，但每个条目必须记录并显示涉及的仓库；应用时按条目中的仓库映射执行。
- `控制台` 聚合当前项目全部 Git 调用，每条记录显示仓库、参数、时间和退出结果。
- 分支弹窗在同步分支控制开启时展示共同分支并对全部仓库执行；关闭时先进入具体仓库再操作其分支。
- Push 弹窗始终列出全部项目仓库；同步控制关闭时只默认勾选动作来源仓库，用户仍可修改勾选范围。

优点：日志可以调查跨仓历史，写操作又能在动作现场明确落点，符合 IDEA 多根项目的真实行为。

代价：不同子页的仓库选择方式并不完全相同，必须在分支、stash、提交和 push 等写操作文案中重复显示真实仓库身份，不能依赖一个全局下拉替代。

### Git 默认子页

用户确认：项目按项目记住上次打开的 Git 子页；首次进入项目 Git 工作台时默认打开 `Log`。

优点：首次呈现用户截图中的分支树、提交图、变更文件和提交详情，后续又能恢复真实工作位置。

代价：存在未提交改动时不会自动跳去 `Local Changes`，因此工具栏必须常驻显示当前分支、改动数量和冲突数量，并允许一键进入本地变更。

### 截图工作面完整边界

用户在总确认时修正范围：不做 IDEA Git 插件全量复刻，聚焦本次截图直接覆盖的整套工作面及其关联闭环。正式范围包括：

- Git 动作菜单：Commit、Push、Update Project、Pull、Fetch、Merge、Rebase、Branches、New Branch、New Tag、Reset HEAD、Show Git Log、Patch、Uncommitted Changes、Current File、Manage Remotes、Clone 和 VCS Operations Popup。
- Git 工具页：Git、Local Changes、Shelf、Stash、Log、Console。
- Local Changes 的文件选择、暂存与取消暂存、逐文件和逐块差异、提交消息、Commit 与 Commit and Push。
- Repository Diff 的文件导航、差异导航、双栏或统一视图、空白忽略策略、差异粒度、同步滚动、行级和词级高亮。
- 分支树和分支上下文动作，所有 Compare、Merge、Rebase 文案必须显示真实当前分支、选中分支和方向。
- Git Log 的分支树、提交拓扑、分页与过滤、提交文件、提交详情，以及从已显示提交进入 Diff 或 Push up to here 的关联路径。
- Push Commits 弹窗、多仓勾选、待推送提交和文件预览、目标远端分支、标签、普通 Push 与 `--force-with-lease`。
- Clone、远端管理、补丁创建或应用、Stash 完整生命周期、Shelf 完整生命周期、Git Console。
- 上述 checkout、pull、merge、rebase、stash、shelf 或 patch apply 触发冲突后的项目级冲突处理闭环。

Annotate / Blame、Bisect、Submodule、Worktree、交互式 Rebase 编辑提交序列等未在本批截图工作面中出现的高级能力不属于本次完成条件。

优点：每个截图入口都有真实数据和可走通的操作结果，不用空菜单冒充 IDEA。

代价：不是 IDEA Git 插件全量能力；用户需要上述高级功能时仍要使用 IDEA 或终端，后续再独立扩展。

### 不包含代码托管平台插件

本次全量对齐只覆盖本机 Git 客户端能力，不包含 GitHub、GitLab 等平台插件提供的账号登录、PR、MR、评论、审查和平台通知页面。

优点：本地仓库、引用、工作区和命令结果保持为唯一事实源，不把第三方平台鉴权与 API 生命周期混入 Git 核心。

代价：远端代码审查与平台协作仍需使用浏览器或后续独立能力，不能在本任务中当作已经交付。

## 尚待确认

- 按修正后的截图工作面范围进入设计与实现前的总确认。

## 已实施：AI 冲突会话的来源分支语义（2026-08-12）

- 冲突会话标题由内部“目标分支”表达改为“当前任务分支合入来源分支”，同时兼容识别已经持久化的旧标题。
- 提示词明确本次任务是完成“当前任务分支 → `workspace.sourceBranch`”的代码交付，不允许只处理入口文件或只留下工作区、暂存区修改。
- AI 必须处理仓库内全部冲突、逐项 `git add`，并在 detached 隔离候选中生成 merge 或 squash 提交；结束前复验未解决冲突为空并报告候选提交 SHA。
- AI 不 checkout、reset、rebase、不直接移动真实来源分支，也不 push。Zeus 在 AI 提交后继续复验任务分支和来源分支 HEAD，再把候选提交安全同步到动态来源分支。
- 专用冲突会话开启提交能力；界面权限说明同步表达“修改、暂存并生成合入提交”，不再把暂存描述成终点。

优点：AI 与用户看到的是同一个“合入来源分支”任务，候选提交成为明确交接物，后端现有安全落地流程仍保留。

代价：来源分支最终更新仍依赖 AI 会话完成后的 Zeus 收尾；若来源分支已经前进或实际 worktree 无法安全更新，必须保留候选并如实显示失败或待同步，不能把候选提交冒充已合入。

静态验证已通过：`pnpm lint`、`pnpm typecheck`、`pnpm build`、`git diff --check`。构建成功不代表 Electron GUI 和真实冲突仓库流程已经验收。

## 整窗视觉方案（2026-08-12）

### 视觉风格硬边界

用户明确修正：IDEA 截图只用于对齐 Git 能力、信息布局和交互关系，不是 Zeus 的视觉风格参考。

- 新 Git 工作面必须继续使用当前 Zeus 类 Codex App / Apple 的设计语言，复用现有颜色、字体、间距、圆角、阴影、按钮、图标和选中态。
- 不复制 IDEA 的高密度工具窗视觉、重蓝色强调、彩色提交图谱、字号与控件风格。
- 弹窗是 Zeus 窗口内的标准模态层，不伪装成带红黄绿窗口按钮的独立 macOS 窗口。
- 高频代码工作面不使用网页式居中大弹窗；返回使用 macOS 统一标题工具栏图标，窗口关闭由红色窗口按钮负责。
- 确认型弹窗使用底部“取消 / 确认”收口，不再叠加右上角 `×`；普通浮层菜单通过点击外部或 `Esc` 关闭，不显示 `×`。
- 概念图只用于确认区域划分、信息层级和操作落点；图中颜色、提交图线和视觉装饰不进入实现验收。

优点：新 Git 能力属于 Zeus 原有产品，用户不需要在同一应用中学习第二套视觉语言。

代价：IDEA 的信息密度不能原样搬进来，需要通过渐进展开、可调栏宽和独立 Diff 面板保留功能完整性。

已使用用户提供的 IDEA Git Log、Git 动作菜单、分支菜单和 Repository Diff 对齐功能布局，再以当前 Zeus 页面对齐视觉语言。当前视觉基线资产为：

- `docs/assets/ZEUS-0196/project-task-list-zeus-style.png`
- `docs/assets/ZEUS-0196/project-git-log-zeus-style.png`
- `docs/assets/ZEUS-0196/project-git-local-changes-zeus-style.png`
- `docs/assets/ZEUS-0196/project-git-branches-zeus-style.png`
- `docs/assets/ZEUS-0196/project-git-push-commits-zeus-style.png`
- `docs/assets/ZEUS-0196/project-conversation-zeus-style.png`
- `docs/assets/ZEUS-0196/task-conflict-workspace-layout.png`

`project-task-list-concept.png`、`project-git-workbench-navigation-corrected.png`、`project-git-local-changes-concept.png`、`project-git-branches-multi-repository-concept.png`、`project-git-push-commits-layout.png`、`project-conversation-concept.png` 和 `task-code-delivery-concept.png` 仅保留早期布局推演，其颜色、控件和窗口质感不进入实现验收。更早的 `project-git-workbench-concept.png` 已完全废弃。

### 项目任务列表

- 点击项目名称默认进入任务列表，顶部“任务”激活；项目行只做较弱的归属强调，不选中任何会话。
- 任务页保持当前真实全宽表格能力：搜索、状态筛选、层级或平铺、批量处理、列配置、更多动作和新建任务。
- 列表继续使用连续滚动，不新增分页；任务详情仍按当前方式通过右侧浮层打开。

优点：延续 Zeus 任务优先心智，不因新增 Git 工作台改变项目默认落点。

代价：项目行归属强调和会话当前选中需要两套不同视觉强度，不能复用同一种蓝色选中底。

### 项目 Git 工作台

- 保留 Zeus 多项目侧边栏；项目名称点击后默认进入该项目任务列表，项目前独立箭头只控制会话展开或收起。
- 项目下直接显示该项目现有会话投影，不再保留“任务 / 代码 / 会话”二级入口。
- 项目工作区顶部采用“任务 / Git / 源码 / 图谱 / 命令”；项目名称进入时默认激活任务，点击 Git 后才进入项目 Git 工作台。
- Git 内部采用“本地变更 / Shelf / Stash / 日志 / 控制台”稳定标签，删除与顶部模式重复的二级“Git”；首次进入打开日志。
- 日志页采用左侧仓库与分支树、中间提交拓扑与提交列表、右侧变更文件与提交详情的三栏结构。
- 顶部只保留仓库、当前分支、变更或冲突数量、搜索、Fetch、Pull、Push 和分组动作入口；不使用大卡片。
- 分支动作围绕选中分支与当前分支展开，比较、合入和变基方向写进文案。

优点：与 IDEA 的对象驱动心智一致，代码历史和真实 Git 事实占据主要空间。

代价：信息密度较高，需要可调栏宽、键盘导航、虚拟列表和窄窗口降级，不能依赖固定宽度。

### 多仓分支弹窗

- 当前视觉基线：`docs/assets/ZEUS-0196/project-git-branches-zeus-style.png`。
- Git 工具栏显示仓库数量以及分支是一致还是已经分歧；展开后先列出每个仓库的真实当前分支。
- “共同本地分支”中的动作直接写明作用范围，例如“在全部 2 个仓库中签出 release/2.3”；“按仓库操作”进入具体仓库后，只作用于该仓库。
- 不在首次进入多仓项目时询问抽象的同步模式，也不使用一个全局当前仓库下拉替代动作范围。

优点：用户在点击写操作前就能看到真实仓库数量、方向和范围，不需要先理解同步分支设置。

代价：多仓分支弹窗比单仓菜单更宽；共同分支和单仓分支入口都要保留，不能压成一份混合列表。

### 本地变更

- 用户确认业务布局方向；当前视觉基线为 `docs/assets/ZEUS-0196/project-git-local-changes-zeus-style.png`。
- 文件先按仓库根分组，再按“未暂存 / 已暂存”分组；仓库归属必须由真实 Git 根决定，不能按路径外观猜测。
- 未暂存文件只能执行“暂存”，已暂存文件只能执行“取消暂存”；不再增加复选框作为第二套提交范围。
- 当前文件行高亮只表示 Diff 预览对象，不改变提交范围。提交只消费真实 Git 暂存区，外部命令产生的 stage 或 unstage 结果也按刷新事实呈现。
- Diff 支持整文件、差异块和精细暂存；差异块旁提供“暂存此块”，精细暂存按 IDEA 的 HEAD / Staged / Local 三栏语义进入独立工作面。
- 底部提交区逐仓显示将创建的提交及已暂存文件数量；多个仓库都有已暂存文件时分别创建各仓提交，不伪装成单个跨仓 Git 提交。

优点：提交范围与 Git 索引完全一致，逐块提交和终端外部操作都能准确投影。

代价：用户提交前必须理解暂存步骤；多仓提交可能部分成功，结果必须逐仓显示并保留重试入口。

### 推送提交

- 当前视觉基线：`docs/assets/ZEUS-0196/project-git-push-commits-zeus-style.png`。
- Push 弹窗始终列出当前 Zeus 项目内的全部 Git 仓库；仓库行复选框决定本次是否推送，提交行只用于查看提交和文件差异。
- 每个仓库独立显示本地分支、目标远端分支和待推送提交数；目标可逐仓编辑，也可从“编辑全部目标”集中检查。
- 底部状态句子必须明确写出本次将推送的仓库和提交数，未勾选仓库同时标明。
- 普通 Push 是默认主操作；强制推送只提供 `--force-with-lease`，不提供原始 `--force`。
- “推送标签”默认关闭，开启后选择当前分支可达标签或全部标签；不包含 PR 或 MR 选项。

优点：推送范围、方向和待推送内容在执行前同时可见，不会被抽象的“当前仓库”状态隐藏。

代价：仓库数较多时弹窗需要纵向滚动；目标编辑和逐仓结果不能被压缩成一个总成功状态。

### 项目会话

- 点击会话后，侧边栏仅该会话使用当前页面蓝色选中；项目行只表达父级归属。
- 用户确认：顶部“任务 / Git / 源码 / 图谱 / 命令”全部保持未激活，因为当前对象是会话，不冒充任何项目工具页，也不记住进入会话前的工具激活态。
- 旧会话页的独立会话列表列被删除；会话正文直接使用侧边栏右侧全部空间，消息流、实时状态、工具活动、批准或输入、环境信息和输入框能力保持不变。

优点：当前页面对象只有一个，项目工具和会话不会同时显示为当前页面。

代价：顶部工具栏存在合法的“无激活项”状态，需要会话标题和侧栏当前行共同说明位置。

### 会话环境“代码交付”

- 当前视觉基线：`docs/assets/ZEUS-0196/task-code-delivery-zeus-style.png`。
- 顶栏固定展示当前任务分支、动态来源分支和本机状态，右侧只保留“提交当前分支 / 推送当前分支 / 合入来源分支”。
- 上半区左侧为全部变更或冲突文件及进度，右侧为紧凑提交信息和当前事实；不再使用提交、推送、合入大卡片。
- 下半区以同步滚动双栏 Diff 为主，提供差异导航、视图模式、空白策略和粒度设置；双击文件打开独立 Diff 窗口。
- 合入冲突时用窄状态条显示真实方向和进度，并提供“解决简单冲突 / 交给 AI”；AI 权限在按钮附近就地展开，默认自动权限。
- 概念稿迭代时已删除误生成的平台合并请求选项，并修正 AI 权限默认值；GitHub 或 GitLab 平台能力不进入实现。

优点：文件审查和代码对比成为页面主体，三个交付动作始终与当前任务分支和来源分支绑定。

代价：上半区和 Diff 区需要在不同窗口尺寸下动态分配空间；独立 Diff 窗口必须与主窗口同步文件状态和刷新结果。

### 冲突处理工作台

- 布局资产：`docs/assets/ZEUS-0196/task-conflict-workspace-layout.png`。冲突处理是铺满独立 macOS 窗口的工作面，不是应用内悬浮大弹窗。
- 来源分支前进时的自动重建过渡态：`docs/assets/ZEUS-0196/task-conflict-stale-source-rebuild-zeus-style.png`。
- 窗口内容从原生标题栏下方开始贴边铺满，不存在灰色外围背景、大留白、圆角页面容器、网页模态阴影或内容右上角 `×`。关闭始终由 macOS 红色窗口按钮负责。
- 返回代码交付只使用 macOS 统一标题工具栏中红黄绿按钮之后的紧凑无文字左箭头；内容区不显示“返回代码交付”大按钮、独立返回列、面包屑或常驻快捷键提示。
- 顶部事实行始终写明真实方向：`当前任务分支 → 来源分支 · 本地合入`；来源分支来自当前 workspace，不假定为 `main`。
- 左栏只承载冲突文件、处理进度和每个文件的剩余冲突数；当前文件用 Zeus 现有低对比选中态，不使用 IDEA 式强蓝色。
- 右侧主区固定为三个等宽代码面板：左侧“来源分支（只读）”、中间“合并结果（可编辑）”、右侧“当前任务分支（只读）”；不再用模糊的左右两栏代替三方合并语义。
- 当前冲突与完整文件在工具栏切换；上一个或下一个冲突、合并简单冲突、AI 处理和撤销都是同一层工具，不用大卡片或页面遮罩表达。
- AI 处理使用紧凑的分裂按钮；主按钮直接以已选默认权限开始，下拉菜单才用于更换本次权限，不再先用居中大弹窗阻断代码视线。
- 底部左侧返回代码交付，中间告知未保存事实，右侧主按钮按真实剩余数禁用或允许“保存该文件并继续”；全部文件暂存完成后才切换为“完成合入来源分支”。
- 项目 Git 中的 checkout、pull、merge、rebase、stash、shelf 或 patch apply 产生冲突时复用同一工作台。三栏的真实名称由操作类型提供，任务合入才使用“来源分支 / 当前任务分支”语义。
- 来源分支在处理期间前进时，工作台原地进入重建态：保留最后已知代码但暂时只读，基于最新本机来源分支重建候选，然后按冲突指纹复用仍匹配的草稿；发生变化的冲突必须重新确认。
- 重建不自动 fetch，不强迫用户在过期候选和新候选之间做选择，也不承诺所有草稿都能保留。

优点：用户的主视线始终落在真实代码和可编辑结果上，AI 是辅助动作而不是页面主体；同时完整保留 Zeus 现有 Codex App / Apple 风格。

代价：独立窗口需要真实管理返回导航和窗口生命周期；三栏代码在窄窗口下仍可能过窄，应先收起冲突文件栏，再允许水平滚动，不能将中间结果栏隐藏。

### Stash

- 当前视觉基线：`docs/assets/ZEUS-0196/project-git-stash-zeus-style.png`。
- Stash 按真实 Git 仓库根分组；每个 `stash@{n}` 只展示所属仓库的分支、基线、文件和差异，嵌套仓库不能混入父仓 Stash。
- 应用、弹出、删除和清空都在动作现场重复显示所属仓库；“应用”默认保留条目，“弹出”仅在应用成功后删除。
- 应用或弹出产生冲突时，条目保留并进入项目级冲突工作台，不伪装成成功。

优点：界面与 Git Stash 的单仓库事实一致，用户不会误以为一个 Stash 可以原子覆盖多仓。

代价：多仓项目需要分别创建和管理 Stash，不能提供一个伪跨仓条目。

### Shelf

- 当前视觉基线：`docs/assets/ZEUS-0196/project-git-shelf-zeus-style.png`。
- Shelf 是 Zeus 项目级补丁条目，一条可以记录多个仓库映射；详情先按仓库根分组，再展示该仓文件和补丁差异。
- “应用到原仓库”逐仓执行并保存结果；成功仓库标记完成，失败或冲突仓库继续保留在条目中，不承诺跨仓原子性。
- 条目支持差异查看、应用、导出补丁、重命名和删除；应用冲突复用同一项目级冲突工作台。

优点：可以真实承载一次跨仓工作的补丁快照，同时不歪曲 Git Stash 的单仓语义。

代价：Zeus 需要自行管理补丁存储、仓库映射、部分应用状态和导出文件。

### Git 控制台

- 当前视觉基线：`docs/assets/ZEUS-0196/project-git-console-zeus-style.png`。
- 控制台是 Zeus 发起的真实 Git 调用记录，不提供命令输入、交互式 shell 或任意执行按钮。
- 每条记录显示仓库、人类可读动作、参数摘要、开始与结束时间、耗时、退出码和真实输出或错误；多仓动作仍拆成逐仓记录。
- “重试”只重放已定义的 Zeus Git 动作，不把旧字符串当作新的任意命令；导出和展示前必须隐藏凭据与敏感参数。
- 本机记录默认按最近 30 天或 10,000 条中先到者限制，支持按仓库、结果和文本搜索及显式清理。

优点：用户可以还原每次 Git 写操作的真实范围和失败原因，无需从总错误提示猜测。

代价：本机日志需要大小上限、过期清理、脱敏和导出边界；它不能替代通用终端。

### Repository Diff

- 当前视觉基线：`docs/assets/ZEUS-0196/repository-diff-zeus-style.png`。
- Repository Diff 是从提交、分支比较、本地变更、Stash 或 Shelf 打开的独立只读深度审查窗口，不是另一个 Git 主页。
- 左栏承载文件导航，右侧只读 Diff 支持并排或统一视图、空白策略、行级或词级高亮、差异导航、同步滚动和外部编辑器打开。
- 正常差异始终为两栏只读对比；只有冲突处理才出现“两侧 + 中间可编辑结果”三栏。

优点：主 Git 页保留提交或文件上下文，独立窗口可以最大化用于长时间审查。

代价：独立窗口必须与主工作面同步文件选择和最新 Git 事实，过期引用不能继续显示为当前。

项目任务、Git、会话、代码交付和冲突处理的主要工作面已按最新多项目导航和 Zeus 视觉语言分开。设计对齐期间不开始整窗 UI 编码。

## Zeus 多项目布局修正（已被后续导航方案替代）

用户所说的“多项目”是 Zeus App 同时管理多个项目，不是单个项目内部存在多个 Git 仓库。第一张概念稿错误地把 Zeus 现有项目优先侧栏画成 IDEA 式全局工具图标栏，也错误地在 Git 顶栏重复放了项目选择器。

这一阶段曾计划保留“任务 / 代码 / 会话”三个二级入口。用户随后进一步明确：二级入口不再保留，项目下面直接迁移现有会话列表；任务、Git、源码、图谱和命令改由项目工作区顶部切换。Git 工作台仍然只属于当前点击的 Zeus 项目，不是跨项目全局页面，也不在顶栏重复选择项目。

- 项目 A 的“代码”只读取和操作项目 A 的 Git 现场；项目 B 的运行请求或缓存不能出现在项目 A。
- 切换项目时，取消或隔离旧项目尚未完成的只读请求；已经发出的 Git 写命令不能伪装取消，完成结果仍归属原项目并在返回后刷新原项目状态。
- 每个项目分别记忆 Git 子页、筛选条件、选中提交或文件、栏宽和滚动位置；切回项目时恢复自己的上下文。
- 项目行或“代码”子项可以显示该项目的变更数和冲突数，但不把多个项目的 Git Log、分支或写操作合并成一份全局视图。
- 左侧项目栏继续支持折叠和拖拽调宽；需要最大化代码区域时可收窄项目栏，但不能用一排全局工具图标取代项目身份。

优点：Git 与任务、会话共享同一个项目上下文，用户从项目 A 的任务切到代码时不会丢失归属，也不会误操作项目 B。

代价：Git 主区可用宽度少于 IDEA 单项目窗口，需要更严格的可调栏宽、窄屏折叠和独立 Diff 窗口；跨项目 Git 总览不属于这个页面。

## 用户提出的项目与会话导航结构（2026-08-12）

用户进一步修正多项目信息架构：侧边栏保留每个项目名称，但项目下不再显示“任务 / 代码 / 会话”三个二级入口。

- 点击项目名称，右侧默认进入该项目的任务列表。
- 项目前设置独立的展开箭头，只负责展开或收起该项目会话；项目名称本身不再兼任折叠按钮。
- 项目名称下面直接展示该项目的会话；会话行完整复用现有 `ProjectConversationTree` 的投影与交互：项目级会话和各任务会话合成一条列表，按会话阶段更新时间倒序排列，同时保留运行中、待批准、待输入、错误、未读、归档边界和键盘导航。不能只复制外观，也不制作一套 Git 页面专用排序。
- 用户确认不在项目行或会话列表末尾放置新建入口，只保留侧边栏顶部现有“新对话”；没有当前项目时该入口禁用，设置页不显示此项目侧栏入口。
- 用户确认顶部“新对话”继续创建当前项目的项目级会话，不弹出任务选择器；任务级会话仍从具体任务入口发起，不能根据当前项目工具页或最近任务偷偷改变归属。
- 点击项目下的某条会话，右侧进入该真实会话；再次点击项目名称，返回该项目任务列表。
- 用户确认：查看会话时可以收起其所属项目；折叠只改变侧栏可见性，不关闭、不暂停也不切走当前会话。项目行使用现有轻量注意力提示表达当前会话仍在该项目中，重新展开后恢复会话选中行。
- 用户确认侧栏顶部搜索同时覆盖项目名称和会话标题。命中项目名时显示项目及其当前会话；仅命中会话时保留所属项目并只显示匹配会话。搜索期间临时展开命中项目但不写入折叠偏好，清空后恢复；结果仍按现有会话阶段更新时间排序，不把归档会话混入普通列表。
- 项目工作区顶部增加任务图标，并与 `Git / 源码 / 图谱 / 命令` 并列；点击任务图标进入当前项目原有任务页。
- 点击顶部 Git 入口返回当前项目 Git 工作台，其他代码模式继续使用当前项目自己的上下文。
- 项目设置、置顶、重命名、Finder 定位和删除继续由项目行的设置或更多菜单承载，不混入 Git 工具栏。

顶部顺序为 `任务 / Git / 源码 / 图谱 / 命令`，项目名称进入时默认激活任务；会话页不冒充其中一个项目工具模式。

优点：侧边栏只承担“项目与会话”这两个长期对象，任务和代码工具留在项目主区，减少一层重复导航；会话的实时状态和排序也能保持全应用一致。

代价：项目名称点击已经承担“打开任务列表”，不能同时继续承担展开或折叠会话列表；需要独立披露箭头。顶部工具栏还必须在任务、Git、源码、图谱和命令之间保留每项目上下文，不能因切换工具丢失会话选择或未保存源码草稿。

## 当前阶段

AI 冲突提示词、候选提交权限、项目与会话导航、项目 Git 工作台、环境代码交付页和冲突工作台均已进入实现。未执行提交、推送、合入、回退或远端操作。

## 2026-08-12 Git 工作台实现与真实包验收

### 本轮落地

- 分支列表按 `/` 组成可折叠目录；在具体分支上右键直接出现 IDEA 语义的上下文菜单，不再弹出居中的“选择分支”窗口。
- 上下文菜单中的签出、从远端新建、签出并变基、当前分支比较、工作区比较、合入、变基、拉取和本地安全删除均绑定白名单 Git 动作；比较和删除不会冒充执行完成。
- Git 日志按拓扑顺序读取真实父提交，Canvas 依据 `parentHashes` 绘制轨道、分叉和合流；共同父提交复用已有轨道并在合流后去重收束，提交行移除横向分隔线。
- 本地变更按“仓库 → 暂存状态 → 目录 → 文件”排列；提交详情中的变更文件也按目录树排列。
- 文件单击选择当前 Diff，双击通过受信 IPC 打开独立 macOS 窗口；窗口默认左右双栏，可切换统一视图，多文件差异左侧保留文件导航。
- 分支比较使用所选分支与当前 `HEAD` 的两点差异；工作区比较包含所选分支到当前工作树的差异。中文路径通过 `core.quotePath=false` 保持原文，不再显示 Git 转义字符串。
- 项目 Git API 仍然先用 `projectId + repositoryId` 复验仓库属于当前项目，Renderer 不能传入任意本机路径或任意 Git 子命令。

### 运行证据

- 参考：用户提供的 IDEA 分支右键菜单与 Git 日志截图。
- 右键菜单、分支目录与真实图谱：`docs/assets/ZEUS-0196/implementation-branch-context-and-graph.png`。
- 提交详情目录树：`docs/assets/ZEUS-0196/implementation-log-file-tree.png`。
- 本地变更双击后的独立双栏 Diff：`docs/assets/ZEUS-0196/implementation-native-side-by-side-diff.png`。
- `main` 与当前任务分支的多文件比较窗口：`docs/assets/ZEUS-0196/implementation-branch-comparison-diff.png`。
- 运行包：`dist/test/mac-arm64/Zeus Test.app`，`CFBundleIdentifier=dev.hypha.zeus.test`，隔离数据目录 `/tmp/zeus-0196-gui-3CiTUA`。
- 首个主窗口按保存状态恢复到外接显示器 `displayId=5`，日志明确记录 `targetDisplayId=5`、`actualDisplayId=5`、`corrected=false`。

### 验证结果

- `pnpm lint`：通过。
- `pnpm typecheck`：通过。
- `pnpm build`：通过；仅保留既有大 chunk 提示。
- `pnpm package:mac`：通过；生成并签名独立 `Zeus Test.app` 与测试 DMG。
- `codesign` 打包验证：`valid on disk` 且满足 Designated Requirement。
- 原生运行：项目默认进入任务列表；Git 日志、真实图谱、分支右键菜单、变更目录树、文件双击独立窗口、默认左右双栏和分支多文件比较均已观察。
- 验收全程只进行导航、右键、双击和只读比较；没有点击签出、合入、变基、删除、暂存、提交、推送或远端刷新动作。

### 取舍

- 优点：Git 对象和动作方向与 IDEA 一致，但视觉继续使用 Zeus 当前的类 Codex App / Apple 语言；所有内容来自真实仓库数据。
- 代价：Canvas 图谱当前基于最近 200 条拓扑提交，超长历史需要后续按滚动窗口增量加载；独立 Diff 目前提供行级左右对齐，词级高亮和空白策略仍属于后续增强。

## 2026-08-12 本地合入冲突处理记录

- 本次在 Zeus 准备的隔离合并工作区中，以 merge 方式组合 `main` 与 `zeus/ZEUS-0196-task-01`；不创建合并提交，不更新 `main`，不推送远端。
- `App.tsx` 的目标分支增加了会话 attention 确认协议、项目未读数和对应提醒；任务分支增加了项目 Git 工作台客户端能力，并把项目下二级导航替换为真实会话树。
- 合并结果保留 Git 工作台接口、会话树、会话搜索与归档，同时保留新的 attention 协议和项目未读数；不恢复已被替代的 completion 确认接口。

优点：项目 Git 与项目会话导航能按任务设计落地，且不会回退后续主分支已收口的未读提醒语义。

代价：项目级提醒与会话行级状态同时存在，Renderer 必须继续以同一份服务端 attention 与未读数事实同步两个层级，避免局部状态假清零。
