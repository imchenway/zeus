# ZEUS-0257 设计 QA

## 对照对象

- 设计真值：`docs/ZEUS-0257_样式候选_1.png`
- 真实实现截图：
  - `docs/ZEUS-0257_真实界面_常规248px.png`
  - `docs/ZEUS-0257_真实界面_最窄200px.png`
- 同画面对照：`docs/ZEUS-0257_设计实现同画面对照.png`
- 运行环境：本任务 `Zeus Test.app`，bundle ID 为 `dev.hypha.zeus.test`，独立数据目录为 `/tmp/zeus-0257-qa-external.y2Rkdl`。
- 窗口首次创建位置：`x=-1240, y=31, 1000×900`，位于非主外接竖屏，显示器横向范围为 `-1296…0`。

## 视口与归一化

- App CSS 窗口：`1000×900`。
- Computer Use 截图：`854×768`，常规与最窄状态使用相同捕获密度。
- 侧边栏 CSS 宽度：常规 `248px`，最窄 `200px`；辅助功能树分别确认分隔控件值为 `248` 与 `200`。
- 设计稿像素：`1254×1254`，是同时表达常规和最窄状态的组合稿，不代表 App 完整窗口像素。
- 同画面对照把设计稿缩放为 `650×650`，并把真实截图左侧约 `212px` 与 `170px` 的捕获区域按同高缩放；只比较侧边栏状态表达，不把 App 现有字体和工作区内容差异误判为偏差。
- 状态：浅色主题、项目展开、减少动态效果开启；隔离测试数据库包含无状态、连接中、失败、完成和普通未读五种会话投影。

## 完整视图与局部证据

- 完整视图：两张 `854×768` 真实 App 截图确认侧边栏没有横向溢出，工作区没有被状态图标挤压。
- 局部证据：同画面对照直接并列选定设计稿、常规侧边栏和最窄侧边栏；会话行状态槽、标题省略空间与纵向节奏均可读，因此不再追加第二组裁切。
- 主要交互：通过键盘聚焦侧边栏分隔控件，`Home` 切换到 `200px`，六次 `ArrowRight` 恢复到 `248px`；两档均重新读取辅助功能树并截图。
- 可访问性：状态图标在会话按钮名称中提供“有未读回复”“已完成”“运行失败”“正在连接”等完整状态文字；颜色不是唯一识别方式。
- 控制台：本次使用真实 Electron App 的 Computer Use 捕获，没有浏览器控制台通道；`pnpm lint`、`pnpm typecheck`、`pnpm build` 和真实打包均通过，App 未出现渲染崩溃。该缺口不影响本次纯布局对照。

## Findings

- 未发现 P0、P1 或 P2 偏差。
- 字体与排版：实现保留 Zeus 现有 source-list 字体、字重和省略规则；状态文字退出可见布局后，标题重新成为主内容。
- 间距与布局节奏：有状态与无状态会话均使用 `28px` 总行高；`18px` 状态槽和 `16px` 图形没有改变行高。常规与最窄截图中的相邻行基线一致。
- 颜色与视觉 token：连接使用低饱和蓝色，失败使用危险色，完成使用低饱和绿色；没有胶囊背景或描边，符合选稿的低噪音方向。
- 图像与图标：全部状态使用 Phosphor 图标库，普通未读使用既有圆点；没有自绘 SVG、字符图标或位图占位。
- 文案与内容：可见标题来自隔离验收数据；状态完整文案保留在 `title` 与可访问名称中。

## Comparison History

- 第一轮对照：没有发现可执行的 P0、P1 或 P2 问题，因此未触发修复后重截循环。
- 内置浏览器打开静态对照页时连接未返回，已停止等待；最终同画面对照由三张已验证 PNG 静态拼接，不使用外部 Playwright，也不把该连接失败计作真实 App 渲染失败。

## Open Questions

- 无阻断问题。

## Implementation Checklist

- [x] 去除侧边栏状态胶囊背景、描边和文字占位。
- [x] 统一有状态与无状态会话行高。
- [x] 保留运行、等待、授权、暂停、失败、完成、只读和普通未读的语义区别。
- [x] 验证 `248px` 与 `200px` 两档侧边栏。
- [x] 验证状态可访问名称。

## Follow-up Polish

- P3：如果后续真实用户反馈图标首次识别成本仍高，可增加统一的自定义 tooltip；当前原生悬停提示和可访问名称已满足本任务范围。

final result: passed
