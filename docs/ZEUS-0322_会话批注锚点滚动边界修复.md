# ZEUS-0322 会话批注锚点滚动边界修复

## 问题

会话回答存在多个文字批注时，滚动正文会出现两类位置错觉：

- 已经滚出对话记录顶部的文字，其编号锚点仍浮在会话标题区域；
- 可见文字的编号正常跟随，导致同一屏内同时出现“脱离正文的编号”和“正确编号”。

用户截图中的编号 1 已经越过对话记录可见边界，编号 2 仍位于可见正文中。

## 根因

回答批注编号、评论框和选区工具条使用 `position: fixed`，并通过 portal 提升到会话根层。滚动时组件会重新读取文字选区的视口坐标，但原实现只判断文字范围是否存在，没有判断编号的完整盒子是否仍位于 `.session-transcript` 的可见区域。

因此，正文虽然受滚动容器裁剪，提升后的浮层却不受同一裁剪边界约束，编号会越过顶部边界进入标题区。

用户补充复测表明，仅增加边界判断仍不充分：编号继续位于滚动容器外的固定定位层，它的移动依赖 React 收到滚动事件后重新渲染。在真实会话的 memo 与虚拟窗口更新链中，只要这次几何刷新延后或缺失，文字已经滚动，编号仍会保留上一次视口坐标。

## 修复契约

- 浮层可见边界取对话记录滚动容器与窗口视口的交集，不使用整个应用窗口冒充正文可见区域。
- 批注编号 portal 回实际 `.session-transcript`，使用滚动内容坐标和 `position: absolute`；正文滚动由浏览器原生带动编号，不再依赖逐帧 React 重算来制造跟随效果。
- 可见性刷新直接监听对应 `.session-transcript` 的 `scroll`，不再通过 `window` 捕获阶段间接监听。
- 批注编号只有在自身完整盒子仍位于可见边界内时才显示；文字离开顶部、底部或横向安全边界后隐藏，滚回时按真实文字坐标恢复。
- 评论框只在对应文字末端完整可见时显示，并在对话记录可用空间内选择上方或下方。
- 选区工具条在滚动开始时关闭；新选区再次按当前可见边界定位，避免沿用滚动前的固定坐标。
- 不修改批注数据、文字偏移、评论内容和发送协议。

## 取舍

优点：编号与正文共享同一个滚动坐标系，不再出现正文移动而编号钉死；编号不再穿过会话标题或底部交互区；编号、评论框与选区工具条遵守同一可见区域。

代价：锚点接近边缘、编号盒子无法完整显示时会提前隐藏，而不是显示一个被截断的半圆；编号也明确受正文滚动容器裁剪。用户滚回少量距离即可恢复完整编号。

## 初版边界修复验证记录

- `pnpm exec prettier --check apps/desktop/src/renderer/session/ResponseSelectionActions.tsx docs/ZEUS-0322_会话批注锚点滚动边界修复.md`：通过。
- `git diff --check`：通过。
- `pnpm typecheck`：通过。
- `pnpm lint`：通过。
- `pnpm build`：通过；Renderer、Main、preload 和全部工作区产物完成构建，只有既有大分块提示。
- `pnpm package:mac`：通过；只生成独立身份 `dist/test/mac-arm64/Zeus Test.app`，`CFBundleIdentifier=dev.hypha.zeus.test`，版本为 `0.3.20`。
- `codesign --verify --deep --strict`：通过；测试包为 ad-hoc 签名，不是 Developer ID 签名，也未公证。
- `hdiutil verify dist/test/Zeus-Test-0.3.20-arm64.dmg`：通过。
- 浏览器入口只读检查：本任务独立使用 `http://127.0.0.1:4180/`，页面身份为 Zeus；Vite 页面明确显示 `Electron 本地桥接未就绪`，无法进入真实会话。默认 `4179` 端口已被未知进程占用，本轮没有终止或复用该进程。
- 真实组件滚动验收：通过。使用产品实际 `ResponseSelectionActions`、`session.css`、真实文字 `Range` 和 `.session-transcript` 滚动容器，在 `1129 × 568px` 正文可见区内渲染两个回答批注，并执行顶部、中段、继续下滚、返回顶部的往返检查。
- 顶部 `scrollTop=0px`：只显示编号 1，盒子坐标为 `x=505.30, y=215.63, 26 × 26px`；编号 2 未显示。
- 中段 `scrollTop=760px`：编号 1 已完全隐藏；只显示编号 2，盒子坐标为 `x=337.01, y=265.63, 26 × 26px`，完整位于正文可见边界 `y=72…640px` 内。
- 继续下滚至 `scrollTop=1100px`：编号 1、2 均已离开正文上边界，页面不再渲染任何编号，没有进入会话标题栏或固定停留在窗口上。
- 返回 `scrollTop=0px`：编号 1 按原文字坐标恢复，编号 2 再次隐藏；滚动往返状态通过。
- 验收使用的临时页面和数据已在检查结束后删除，不进入交付。由于隔离测试数据中没有用户截图对应的持久化批注会话，本轮没有把“打包后的 Electron 应用读取真实持久化会话”冒充为已验证，也没有读取或复制正式应用、其他 worktree 或既有会话数据，没有执行任何 AI Adapter。

## 补充复测记录（2026-09-02）

- 用户复测确认初版边界修复仍会出现“文字移动、图钉钉死”，因此前述真实组件夹具只证明边界过滤，不能继续作为最终滚动跟随的充分证据。
- 最终改动把编号 portal 放回 `.session-transcript`，用滚动内容坐标定位，并把编号从 `position: fixed` 改为 `position: absolute`；同时将刷新监听从 `window` 捕获改为直接监听对应滚动容器。
- `pnpm exec prettier --check apps/desktop/src/renderer/session/ResponseSelectionActions.tsx apps/desktop/src/renderer/session/session.css docs/ZEUS-0322_会话批注锚点滚动边界修复.md`：通过。
- `git diff --check`：通过。
- `pnpm lint`：通过。
- `pnpm typecheck`：通过，包含架构治理检查和全部 TypeScript 工程。
- `pnpm build`：通过；只有既有 `markstream-react` pure 注释和大分块提示。
- `pnpm package:mac`：通过；生成独立测试身份 `dist/test/mac-arm64/Zeus Test.app`，`CFBundleIdentifier=dev.hypha.zeus.test`，版本 `0.3.95`。
- `codesign --verify --deep --strict` 与 `hdiutil verify dist/test/Zeus-Test-0.3.95-arm64.dmg`：通过。
- Zeus 内置浏览器曾成功加载本任务 `4180` 端口的临时真实组件夹具并读取页面身份及 DOM；在校准夹具宽度后，后续 `navigate`、`list_tabs` 和 `snapshot` 均连续超时，BrowserHost 未恢复，因此最终改动的完整滚动往返未在本轮冒充为已验收。没有改用外部 Playwright、直接 CDP 或其他浏览器替代指定通道。
- 临时夹具与端口已删除并关闭，不进入交付；没有读取或复制正式应用、其他 worktree 或既有会话数据，没有执行任何 AI Adapter。

## 会话切换复测返修（2026-09-02）

- 用户补充确认，切换会话后批注编号仍会出现位置错乱。
- 根因是 `ResponseSelectionActions` 在 React render 阶段读取文字 `Range`；会话身份变化的那次 render 早于新正文提交到 DOM，可能读到旧会话的 `articleRef.current` 和旧坐标。原 `useLayoutEffect` 只注册后续滚动/缩放监听，既不在本次 DOM 提交后立即刷新，也不随 `itemId` 重新绑定，因此旧坐标可能持续到下一次滚动事件。
- 返修改为在 `useLayoutEffect` 绑定新会话正文与滚动容器后立即触发一次几何刷新，并把 `itemId` 纳入 effect 生命周期；不修改批注数据、文字偏移、虚拟窗口或会话滚动控制器。
- 取舍：每个已完成回答在首次挂载或身份变化后增加一次提交前的轻量重渲染；优点是新会话首帧不会保留旧 DOM 几何，代价是一次局部 state 更新。
- `pnpm exec prettier --check apps/desktop/src/renderer/session/ResponseSelectionActions.tsx docs/ZEUS-0322_会话批注锚点滚动边界修复.md`：通过。
- `git diff --check`、`pnpm lint`：通过。
- `pnpm typecheck`：通过，包含架构治理检查和全部 TypeScript 工程。
- `pnpm build`：通过；只有既有 `markstream-react` pure 注释和大分块提示。
- `pnpm package:mac`：通过；只生成独立测试身份 `dist/test/mac-arm64/Zeus Test.app`，`CFBundleIdentifier=dev.hypha.zeus.test`，版本 `0.3.96`。
- `codesign --verify --deep --strict` 与 `hdiutil verify dist/test/Zeus-Test-0.3.96-arm64.dmg`：通过。
- 为覆盖会话切换，临时夹具复用了真实 `ResponseSelectionActions`、`session.css` 和同一个 `articleRef`，编排 A → B → B 滚动 → A 的坐标检查。Zeus 内置浏览器首次 `open` 返回了独立标签页，但一次误传的非 CSS selector 令等待调用挂起；此后该标签页的 `snapshot`、`reload` 与新标签页 `open` 均不再返回，夹具结果回传端口也没有收到请求。因此这项真实组件切换检查没有计为通过，也没有改用外部 Playwright、直接 CDP 或其他浏览器替代指定通道。
- 临时夹具、结果监听和 `4180`、`4181` 端口均已删除或关闭，不进入交付；没有启动打包应用，没有读取或复制正式应用、其他 worktree 或既有会话数据，没有执行任何 AI Adapter。

## 批注标签与评论框视觉返修（2026-09-02）

- 用户补充反馈批注编号小标签与评论框视觉过重、样式难看。
- 编号标签复用会话浏览器批注的紧凑尺寸语言，从高饱和 26px 蓝色圆徽章收为 24px 浅色描边标签；默认不抢正文注意力，仅在 hover、键盘聚焦或评论框打开时强调。同步校准编号盒子尺寸和锚点偏移，保留完整边界判断。
- 评论框继续使用现有 DOM 和交互，只通过既有会话 token 收敛为弱边框、轻阴影、分区式紧凑浮层；引用文字降为辅助层级，输入区去除内嵌卡片边框，完成按钮保留明确主操作，删除按钮仅在 hover 时显示危险色。
- 增加编号 `aria-expanded`、编号与编辑器按钮的键盘焦点样式；不修改批注内容、评论保存、删除或发送协议。
- 取舍：默认视觉更克制，编号不再持续使用实心强调色；打开状态仍用完整强调色维持当前位置辨识。
- `pnpm exec prettier --check apps/desktop/src/renderer/session/ResponseSelectionActions.tsx apps/desktop/src/renderer/session/session.css docs/ZEUS-0322_会话批注锚点滚动边界修复.md`、`git diff --check`、`pnpm lint`：通过。
- `pnpm typecheck`：通过，包含架构治理检查和全部 TypeScript 工程。
- `pnpm build`：通过；只有既有 `markstream-react` pure 注释和大分块提示。
- `pnpm package:mac`：通过；只生成独立测试身份 `dist/test/mac-arm64/Zeus Test.app`，`CFBundleIdentifier=dev.hypha.zeus.test`，版本 `0.3.96`。
- `codesign --verify --deep --strict` 与 `hdiutil verify dist/test/Zeus-Test-0.3.96-arm64.dmg`：通过。
- 为覆盖用户指定的批注 `msg_0427eec3dc27d349016a9796e5e62087d08e55efaa136be008`，临时夹具复用了真实 `ResponseSelectionActions`、`session.css`、选中文字“DOM 提交阶段”和评论“123”。Zeus 内置浏览器能读取夹具正文，但首次几何检查时夹具尚未跨过 Markdown settled 门禁；补齐门禁后，对未生成节点的元素检查令 BrowserHost 再次挂起，后续导航未返回，因此本轮没有把评论框视觉冒充为已验收，也没有改用外部 Playwright、直接 CDP 或其他浏览器替代指定通道。
- 临时夹具和 `4180` 端口已删除或关闭，不进入交付；没有启动打包应用，没有读取或复制正式应用、其他 worktree 或既有会话数据，没有执行任何 AI Adapter。
