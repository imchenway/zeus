# ZEUS-0327 环境按钮遮挡修复

## 任务目标

修复会话右侧浏览器工作面打开时，点击顶部“环境”按钮后环境信息卡片被浏览器网页遮挡的问题。

## 根因

- 环境卡片属于 Renderer DOM，普通 CSS 层级只能在 Renderer 内部排序。
- 浏览器网页由 Electron `WebContentsView` 承载，是独立于 Renderer 的原生子视图，天然显示在 Renderer DOM 上方。
- 因此继续提高环境卡片的 `z-index` 无法解决遮挡。

## 方案与取舍

环境弹层打开时，通过现有 `BrowserWorkspace.suspended` 通道暂时隐藏原生网页视图；弹层关闭后恢复原生网页视图。浏览器标签、网址、页面进程和页面状态不销毁。

优点：

- 直接处理真实层级边界，卡片不会再被原生网页遮挡。
- 不改变环境卡片的位置、尺寸、键盘关闭和外部点击关闭行为。
- 不关闭浏览器标签，不触发页面重新加载，也不新增第二套 BrowserHost 协议。

缺点：

- 环境弹层打开期间，网页内容暂时不可见，网页占位区显示 Zeus 自身表面。
- 原生视图的隐藏与恢复需要经过现有 IPC，极慢设备上可能存在极短的切换延迟。

## 实现范围

- `SessionQuickActionsCard.tsx`
  - 向会话工作区同步环境弹层的真实可见状态。
  - 常驻环境卡不触发浏览器暂停，仅顶部按钮唤起的弹层触发。
- `SessionWorkspace.tsx`
  - 记录环境弹层可见状态。
  - 浏览器拖动调整尺寸或环境弹层打开时，共用 `BrowserWorkspace.suspended` 暂停原生网页视图。

## 验收路径

会话页打开浏览器工作面 → 点击顶部“环境” → 环境卡片完整显示在浏览器工作面之上 → 点击卡片外部或按 `Escape` 关闭 → 原网页恢复且标签、网址与页面状态保持不变。

## 验证记录

### 已通过

- `pnpm install --frozen-lockfile --offline`：使用锁文件与本机缓存恢复工具链，未修改 lockfile。
- 目标文件 `prettier`：通过。
- `pnpm lint`：通过。
- `pnpm typecheck`：通过。
- `pnpm build`：通过；Vite 的大分块提示是既有警告，不是构建失败。
- `pnpm package:mac`：通过，只生成独立测试身份 `Zeus Test.app` 与测试 DMG。
- 测试包 bundle ID：`dev.hypha.zeus.test`。
- `codesign --verify --deep --strict --verbose=2`：测试包磁盘签名有效并满足指定要求。
- `git diff --check`：通过。

### 真实渲染边界

- 当前 `4179` 端口已有其他本地质检服务占用，本任务未终止或替换该进程。
- 通过 Zeus 当前会话内置浏览器打开 `http://127.0.0.1:4179/`，页面标题为 `Zeus`，但在进入会话工作区前被真实错误 `Electron 本地桥接未就绪` 阻断。
- 因此，本轮内置浏览器检查不能证明环境卡片与原生 `WebContentsView` 的隐藏、恢复往返已经通过。
- 本轮没有启动测试包，也没有复用其他任务实例或正式用户数据；真实 Electron GUI 点击验收仍需在拥有目标会话的隔离测试数据中补充。

## 约束

- 不新增或恢复单元测试体系。
- 不执行 Git 提交、推送、合入或远端刷新。
- 不生成或启动生产身份 `Zeus.app`。
