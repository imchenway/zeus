# ZEUS-0338 会话全生命周期流畅输出优化

## 用户现场

- 从侧边栏点击会话开始，标题、历史消息、输入区和状态反馈存在卡顿、闪现与分段出现。
- 会话消息加载后的逐步输出仍有明显的停顿感，当前动效没有形成连续、稳定的阅读节奏。
- 用户要求覆盖“点击会话、消息加载、进行中输出、完成收口”的完整生命周期，而不是只调整某一个加载图标。

## 当前诊断

1. `App.tsx` 仍以会话身份作为 `ConnectedSessionWorkspace` 的 React `key`。每次切换都会卸载并重建标题、正文、输入区、快捷操作和监听器，已有常驻工作区能力没有真正生效。
2. `SessionWorkspace` 的标题在每次重挂载时播放整块入场；若取消重挂载，现有实现又会先保留旧标题 180ms 再换成新标题。两条路径都违背“目标身份立即出现”的契约。
3. `ConnectedSessionWorkspace` 在每个会话状态增量上重新创建完整 `actions` 对象和资源回调。`ThreadItemView` 即使使用 `memo`，也会因函数属性和批注数组身份变化而让历史消息重新渲染。
4. 流式正文每次可见增量都同步读取滚动高度、标记位置并执行 `elementFromPoint`，在主线程上形成强制布局；长会话和 Markdown 重算会与该布局读取竞争同一帧。
5. 冷加载骨架位于正文之外，快照到达时骨架直接卸载、正文直接挂载，缺少稳定的同一内容舞台，容易形成高度突变和闪现。
6. 历史消息没有使用浏览器的离屏渲染隔离，长会话首次切入仍会为屏幕外的大量消息支付布局和绘制成本。

## 本次方案

### 常驻工作区，只重建会话控制器与正文身份

- 移除普通会话工作区外层的会话 `key`，保留标题、输入区和主布局 DOM。
- 会话变化时由 `useSessionController` 按新身份同步创建控制器；正文组件按会话身份重建，确保滚动、实时新增标记和读屏追踪不会串线。
- 会话级右侧工作面、目标输入和临时错误在绘制前重置，避免上一会话状态闪入下一会话。

优点：切换不再经历整页卸载与空白首帧，输入区和主布局保持稳定。  
代价：必须显式管理会话级临时状态，不能继续依赖 React 卸载自动清空。

### 目标标题立即接管，动效只表达新身份

- 标题直接从当前会话事实派生，不再保存旧标题等待退出计时器。
- 会话身份变化时只让新标题内容做轻量透明度与短距离入场，不延迟目标标题和状态出现。

优点：点击后的身份反馈即时，同时保留连贯的方向感。  
代价：不再展示旧标题的完整退出过程；快速连续切换时优先保证事实与响应速度。

### 稳定回调与批注投影

- 连接动作按控制器身份缓存，执行时读取控制器最新状态，避免闭包迫使每个增量重建函数。
- 资源、目标、智能体和会话操作按真实会话身份缓存。
- 回答批注一次按消息分组，未变化的历史消息获得稳定数组引用。

优点：流式增量主要更新当前增长消息，历史 Markdown 不再陪同重算。  
代价：回调必须只依赖明确的控制器和会话身份，新增动作时要维护依赖边界。

### 异步可见性观察与离屏渲染隔离

- 使用 `IntersectionObserver` 异步观察最新内容标记，滚动处理只读取滚动距离，不再在每个增量上执行命中检测。
- 历史消息与轮次块使用 `content-visibility: auto` 和保守的固有高度占位，让屏幕外内容跳过布局与绘制。
- 当前活动内容、包含焦点的内容和正在入场的内容关闭离屏跳过，保证输入、展开和流式反馈可靠。

优点：减少主线程同步布局，长历史切换和持续输出更稳。  
代价：浏览器使用估算高度维持离屏滚动范围；极端超高消息首次滚入时仍可能发生小幅滚动校准。

### 同一舞台完成冷加载接管

- 冷加载反馈放入会话正文舞台，使用稳定最小高度和不抢焦点的状态提示；快照到达后由真实内容原位接管，运行图标使用纯旋转而不是呼吸缩放。
- 不对整份历史消息播放逐项入场，也不让正文容器整体做大幅位移。

优点：加载到正文不再像两个页面互相替换。  
代价：首次且无热缓存的会话仍需等待权威快照，只能改善真实等待期间的连续性，不能伪造已有消息。

## 验证计划

- 定向格式检查、`pnpm lint`、`pnpm typecheck`、`pnpm build`、`git diff --check`。
- 使用内置浏览器打开会话样式页，核对页面身份、错误覆盖层、控制台、浅色/深色、窄窗口和减少动态效果。
- 若测试应用与独立资料允许，打包并启动 `Zeus Test.app`，使用独立 `ZEUS_USER_DATA_DIR` 核对真实会话切换；静态、浏览器样式页、测试包和真实 Provider 结果分别记录。
- 不新增或恢复单元测试体系，不生成或启动生产身份 `Zeus.app`，不执行 git commit、push、merge 或 revert。

## 阶段记录

- 2026-08-18：完成历史任务文档、设计契约和当前实现的只读诊断，进入代码调整阶段。
- 2026-08-18：移除 `ConnectedSessionWorkspace` 的会话级重挂载，标题改为目标事实立即接管；消息正文仍按会话身份重置滚动、新增项和读屏追踪，主布局与输入区保持常驻。
- 2026-08-18：会话切换前置清理右侧工作面、目标面板、临时错误和中断确认；能力、智能体预读及未完成异步操作均绑定项目或会话身份，旧结果不会回写新会话。
- 2026-08-18：连接动作、资源动作和正文事件回调改为稳定引用；回答批注按消息一次分组，历史 `ThreadItemView` 不再因当前消息增长而获得新的函数和空数组属性。
- 2026-08-18：消息增量的滚动跟随与最新内容可见性合并到动画帧；移除每次增量上的 `getBoundingClientRect` 与 `elementFromPoint`，最新标记改由 `IntersectionObserver` 异步观察。
- 2026-08-18：冷加载由真实 `ConversationTranscript` 内部覆盖层原位接管；长历史使用 `content-visibility: auto`，活动、入场、展开和包含焦点的内容保持完整渲染。
- 2026-08-18：更新既有会话样式验收页，直接渲染真实冷加载组件，并复用热更新根节点，避免开发态重复 `createRoot` 产生无关控制台错误。
- 2026-08-19：修复冷切换仍然闪空的问题。`ConnectedSessionWorkspace` 暂留上一条已水合正文，目标会话的输入区与加载状态独立前进；目标快照就绪后才切换正文身份，暂留正文期间不向目标会话错误派发旧消息操作。
- 2026-08-19：`useSessionController` 同时启动 WebSocket 建连和权威快照读取；`ConversationTranscript` 用即时 `scrollTop` 加 3 帧高度收敛处理首次定位，避免懒加载内容把目标会话重新推离底部。
- 2026-08-19：移除活动行包含 `updatedAt` 的 React key，降低过程增量造成的 DOM 重挂载；思考、活动和计划图标改为稳定纯旋转，流式正文的 700ms 结构化尾部等待收紧为 180ms。
- 2026-08-19：根容器无障碍标签从“已遵循减少动态效果”改为“支持减少动态效果”，明确表示能力支持而不是当前系统状态。
- 2026-08-19：思考摘要之间仍保留同一工作阶段边界，但执行记录改为按命令、工具、文件变更和上下文整理分类聚合；折叠态每类各有摘要，展开后查看该类别内的原始明细，不再把不同语义压成一个混合数量。
- 2026-08-19：排队消息点击“引导”后先保留在队列并标记“正在引导当前回复”，HTTP acceptance 不再冒充消息已进入当前轮次；收到 `conversation.submission.steering` 才按正常 steer 语义转入会话底部，等待期间不会消失，失败保留为需恢复的队列消息且不自动重发。
- 2026-08-19：会话工作态的呼吸、连接提示和加载脉冲统一收紧节奏，正式动效焦点的计算周期降为 `0.9s`，并保留减少动态效果分支。
- 2026-08-19：发送后的自动到底路径同时覆盖 awaiting-reply 和快速 accepted/completed 两条路径；滚动容器关闭全局 smooth 行为，使用即时定位和有限帧高度收敛，验收页改为复用正式 `session-codex-parity-v1` 滚动样式。

## 验证结果

- `pnpm install --frozen-lockfile --offline`：通过；只建立本 worktree 依赖，未修改锁文件。安装阶段提示两个尚未构建的本地 CLI 软链接缺失，后续完整构建已生成对应产物。
- Prettier 定向检查、`pnpm lint`、`pnpm typecheck`、`pnpm build`、`git diff --check`：通过。构建仅保留仓库既有的大分块提示，没有新增编译错误。
- Zeus 内置浏览器打开 `http://127.0.0.1:4179/qa/session-styles.html?motion=1`：页面身份正确，无错误遮罩；浅色和深色现场均包含真实会话记录、进行中输出与冷加载覆盖层。冷加载记录区和状态层在快照中拥有相同起始坐标，历史过程可真实点击展开。
- 开发态热更新复核：修改验收页后触发 HMR 和页面重载，不再出现重复 `createRoot` 错误。Vite 仍报告该老验收入口的导出不兼容快速刷新并自动整页重载，不影响生产构建，也不是会话运行路径。
- 早期内置浏览器连接曾报告视口为 `0×0`，当时只把结果作为 DOM、可访问角色和接线证据；本轮重载已报告 `1129×1074`，并完成滚动距离和交互状态的 DOM 验证，仍不把它夸大为真实 Provider 主观流畅度验收。
- `pnpm package:mac`：通过，生成 `dist/test/mac-arm64/Zeus Test.app` 与测试 DMG；`CFBundleIdentifier` 为 `dev.hypha.zeus.test`，`codesign --verify --deep --strict` 通过。
- `node scripts/verify-packaged-app-health.mjs 'dist/test/mac-arm64/Zeus Test.app'`：通过；Renderer、Main、Preload、Browser Page Preload 和 Codex 资源完整。
- 2026-08-19 最终源码复核：在分类聚合、排队引导保留和动效节奏修改完成后再次执行 `pnpm package:mac`、打包健康检查和深度签名校验，均通过。
- 使用 `/private/tmp/zeus-e2e-0338-final.pdlVw6` 作为独立 `ZEUS_USER_DATA_DIR` 启动当前 worktree 的 `Zeus Test.app`：Electron Renderer URL 指向当前 `ZEUS-0338` 包，Bundle ID 为 `dev.hypha.zeus.test`，空资料启动正常；无障碍树显示“支持减少动态效果”，未复用正式应用、既有登录或其他 worktree 会话。测试进程已关闭。
- 当前独立资料没有项目、历史会话和 Provider 登录；真实桌面首帧因此只证明包身份、桥接和空态，不能证明真实会话切换、长消息流式输出或底部定位已经主观通过。
- 2026-08-19 追加验证：内置浏览器验收页的执行段出现独立的命令类别和工具类别；命令类别摘要为“正在搜索文件并运行 3 个命令”，展开后可见 3 条命令明细，工具类别摘要为“已使用 1 个工具”。
- 2026-08-19 追加验证：点击“引导”后的确认空窗中，队列仍显示原消息并标注“正在引导当前回复，等待当前轮次确认”，正文不会提前伪造 steer 消息；模拟 `steering` 事件到达后，队列收口、消息进入正文并显示“引导中”。
- 2026-08-19 追加验证：真实 DOM 动效诊断报告 `focusAnimationDurations=0.9s、0.9s`，发送夹具在新增消息后报告 `距底部 0px`；浏览器夹具只验证 renderer 状态与 DOM 投影，不替代真实 Provider 网络链路。
- 2026-08-19 追加静态验证：本轮修改后的 `pnpm lint`、`pnpm typecheck`、`pnpm build` 与 `git diff --check` 均通过。
- 2026-08-19：重新启动测试包时发现另一 worktree 已占用同一测试 Bundle ID 的单实例锁；未关闭或干扰该实例，因此本轮没有把这次启动尝试冒充为新的桌面动态会话验收。

## 尚需真实数据确认

- 独立测试资料没有 Provider 登录和历史会话，因此本次没有伪造“点击多个真实会话并持续输出”的动态证据。代码、DOM、构建、测试包和真实桌面首帧已经闭环，但最终主观流畅度仍应在用户自己的已登录测试资料中，用长会话、快速连续切换和长 Markdown 输出做一次现场确认。
- 优化没有改变首次冷加载必须等待权威快照的事实，也没有把历史内容伪装成已经到达；收益集中在消除整页重建、同步布局争用和无关历史重渲染。
