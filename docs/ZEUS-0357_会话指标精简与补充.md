# ZEUS-0357 会话指标精简与补充

## 任务目标

- 精简会话运行信息，移除当前轮次、最后请求用量、最后请求缓存明细与目标能力展示。
- 摘要展示会话累计 Token、上下文占用、缓存命中率、最近请求输出速率和 API 等价费用估算。
- 详情按使用与费用、性能与活动、环境分组；代码变更作为活动产出指标展示。
- 缺少真实 Provider 请求边界、时序、价格或变更集时显示不可用或不完整，不使用轮次耗时反推模型速率。

## 已确认口径

1. 最近请求输出速率只统计最近一个边界完整的纯文本模型请求：`非推理输出 Token / 首个文本增量到请求完成的时间`。
2. 首段可见响应延迟按轮次开始到首个实际可见助手内容计算；累计处理耗时排除审批和用户提问等待。
3. 费用是按请求发生时价格快照计算的 API 等价美元估算，不是 ChatGPT、Codex 订阅或最终账单。
4. 费用不完整时只显示已覆盖部分、覆盖率和来源；未知模型不借用相似模型价格。
5. 代码增删行是稳定轮次变更集的累计工作量，不等于当前工作区净差异。
6. 删除项只退出界面展示，底层数据继续服务上下文、缓存、费用和能力判断。

## 2026-08-24 补充需求

- 真实会话现场确认当前 Codex app-server 只产生 `item/agentMessage/delta`、`item/started|completed` 与 `thread/tokenUsage/updated`，没有实现原先依赖的 `rawResponse/completed`；因此请求虽有精确 Token，用量记录仍是 `measurementComplete=false`，界面也因主会话隐藏不可用指标而完全看不到输出速率。
- 保留“最后一次模型请求”口径：有完整纯文本边界时显示如 `750 tokens / s`，最后请求包含工具、命令、文件变更或缺少时序时显示 `-`，不回溯较早请求，也不使用轮次耗时估算。
- 摘要中的五项指标固定存在；主会话与 Subagent 的缺失值统一显示 `-`，缺失原因继续通过标题和无障碍文本提供。
- 展开详情采用分区数据表。顶部拆成“会话”和“费用”两栏；模型、推理强度、服务层级分别展示。“性能与活动”和“环境”继续通栏展示，窄屏纵向堆叠。
- 线程 ID 与 JSONL 路径增加复制图标，展示可截断但复制完整原值，成功后短暂显示已复制反馈。

本轮补充阶段：代码实现、静态验证和最终测试身份隔离启动完成；GUI 内容读取与真实 Provider 仍受验收工具和独立身份认证边界阻断。既有结论不自动覆盖本轮改动，以下均记录 0.3.45 最终包的重新验证结果。

### 补充实现取舍

- 以 `thread/tokenUsage/updated` 作为当前 app-server 兼容链路的请求完成边界。优点是使用真实事件时间与精确请求 Token，能够恢复纯文本速率；缺点是只有能排除工具、命令、文件变更等非文本输出时才可信，混合请求必须显示 `-`。
- 摘要固定展示五项指标。优点是会话间位置稳定、便于横向比较；缺点是数据缺失时仍占用一项空间，因此用低噪音 `-` 配合标题和无障碍说明，而不是隐藏整项。
- 详情顶部采用“会话 / 费用”双栏，活动和环境通栏。优点是把高频身份、用量与费用拆开，扫描路径更短；缺点是窄屏不能维持双栏，因此在 720px 以下纵向堆叠。
- 线程 ID 与 JSONL 路径只在存在完整原值时显示复制图标。优点是视觉上可截断而复制不丢失；缺点是复制失败时不显示成功勾选，仍需依赖原生剪贴板、Web Clipboard 或选区兜底之一可用。

### 2026-08-24 补充验证

- `pnpm lint`、`pnpm typecheck`、`pnpm build` 与 `pnpm package:mac` 均通过；类型检查中的架构治理确认 88 张 Core 表和 11 张可重建辅助表符合政策。Vite 仅保留既有的大分块提示。
- 新产物为 `dist/test/mac-arm64/Zeus Test.app` 0.3.45 和 `dist/test/Zeus-Test-0.3.45-arm64.dmg`；反读 bundle ID 为 `dev.hypha.zeus.test`，`codesign --verify --deep --strict` 通过。该包为 ad-hoc 签名、未公证，只是测试身份证据。
- 一次性隔离 SQLite 领域探针使用真实存储仓库和 Codex timing tracker 验证：纯文本请求得到 `75 tokens / s`；最后请求为混合响应时得到 `null`，不会回退到较早请求；兼容请求后补 Provider response ID 时保留已经记录的时序与完整性。
- 独立测试身份队列释放后，使用全新数据根 `/private/tmp/zeus-0357-gui.W4Z8lZ` 和 `ZEUS_TEST_DISPLAY_ID=3` 启动最终 0.3.45 `Zeus Test.app`。Main 日志为 `matchKind=first-launch`、`targetDisplayId=3`、`actualDisplayId=3`、`corrected=false`，首窗从创建时就在非主外接屏，边界为 `x=-1268, y=268, width=1240, height=820`。
- Computer Use 在重置后可以初始化，但读取应用状态和列出应用均返回插件内部错误 `process is not defined`，没有取得 Electron 无障碍树或截图。未使用 AppleScript、外部 Playwright或坐标脚本绕过，因此不宣称真实窗口中的指标布局、复制反馈或 Provider 展示目视通过。
- QA 默认页可由 Zeus 内置浏览器打开并读取页面标题和 DOM；运行详情夹具实际位于 `?motion`，转到正确路由时导航与新标签均挂起，截图接口还返回空图片。没有降级到外部浏览器，Vite 编译证据不冒充布局和交互证据。
- 本任务测试实例已退出，当前没有 `Zeus Test.app` 进程；隔离数据根已移入废纸篓 `/Users/david/.Trash/zeus-0357-gui-20260824-2010`，仍可恢复。没有启动、修改或退出正式 `Zeus.app`。

## 设计依据

- [OpenAI 官方模型与价格资料](https://developers.openai.com/api/docs/models/compare)按输入、缓存输入、输出等 Token 类别公布 API 单价；历史请求继续使用落库时的价格快照。
- Claude Code 状态栏公开成本、上下文、耗时和代码改动；[LM Studio](https://lmstudio.ai/docs/developer/rest/endpoints) 与 [OpenRouter](https://openrouter.ai/docs/faq) 提供吞吐和首响应指标；[OpenCode](https://opencode.ai/docs/cli/) 提供 Token、成本、模型与工具统计。
- Zeus Snapshot V2 首屏是有界投影，所有会话级聚合必须由存储层计算，Renderer 不得从已加载的局部历史推断。

## 实现记录

- `conversation_model_requests` 幂等补充 `provider_request_id`、首次可见输出、首次文本输出、完成时间和测量完整性字段；旧记录保留空值，不回填推测时间。
- Codex 以单次 `rawResponse/completed` 的 `responseId` 和精确用量记账；可见文本、推理摘要和非文本响应项只在同一连续请求边界内参与测量。工具型、混合型、错误或缺事件请求的速率保持不可用。
- Pi 以 `message_start`、`message_update`、`message_end` 形成请求边界，保存 `responseId` 和文本增量时序；缺少开始事件、工具调用、错误/中止或用量拆分缺失时不生成速率。Pi 的完整消息已经证明不存在 thinking/tool 内容时，可把 Provider 未提供的 reasoning 拆分精确记为 0；存在 thinking 内容却无拆分时保持不可用。
- `sessionMetrics` 由存储层一次性投影 usage、cost、performance、activity、changeSummary 和完整性。Snapshot V2 直接携带该投影，Renderer 不再从分页历史汇总。
- `conversation.sessionMetrics.changed` 登记为可合并的低频过程事件，只在模型请求完成、工具/命令/重试完成、轮次终态等稳定点更新；流式增量不广播会话聚合。
- 费用继续读取请求发生时固化的费率账本：Codex 官方价格目录日期为 `2026-08-10`，DeepSeek 官方价格目录日期为 `2026-08-15`。推理 Token 是输出 Token 子集，不重复计费；未知模型覆盖率为 0 且费用不可用。
- 累计处理耗时只汇总已有起止时间的终态轮次，并合并扣除审批、提问等服务端请求等待区间；任一必要时间缺失时返回不可用。
- 代码工作量只累计状态稳定的变更集；存在捕获中或不可用变更集时保留已覆盖数值并标注部分统计，无变更集时不展示该行。
- 展开环境区只保留目录、分支、会话 ID、JSONL 和 MCP 启动状态；旧的推理级别证据、缓存请求证据不再作为目标能力或缓存状态的同义展示，底层配置证据未删除。

## 兼容边界

- 旧库打开时只增量补列和索引，不改变既有 schema generation；历史请求没有精确时序，输出速率与首段延迟显示不可用。
- 旧会话已有的 Provider Token 快照仍作为累计 Token、上下文、缓存和费用的兼容来源；不会用当前目录价格重算旧请求。
- 部分价格覆盖时摘要显示“估算不完整”，详情展示已覆盖金额、覆盖率、目录日期与来源；无可信价格时显示“不可用”。
- Snapshot V2 的 `sessionMetrics` 是新增稳定字段；实时事件只替换该整体投影，避免 token 增量引发工作区重渲染。

## 实施阶段

当前阶段：实施完成；静态、领域、响应式与测试身份启动验证完成，真实 Provider 请求和 GUI 内容读取存在下述环境缺口。

- [x] 模型请求时序字段与旧库幂等升级
- [x] Codex/Pi 精确请求观测与不完整性保护
- [x] Snapshot V2 `sessionMetrics` 与低频变更事件
- [x] 会话摘要、详情分组和响应式布局
- [x] 静态检查、构建与测试身份打包
- [x] 聚合口径、旧库兼容、重启一致性与响应式验证
- [x] 独立测试身份启动和非主外接屏首窗落位
- [ ] Codex/Pi 真实请求与 GUI 内容读取（受独立身份认证和 Computer Use 服务阻断）

## 验证结果

### 静态、构建与打包

- `pnpm lint`：通过。
- `pnpm typecheck`：通过；架构治理同时确认 88 张 Core 表和 11 张可重建辅助表的 owner、源码规模、导入与包循环均在政策内。
- `pnpm build`：通过；Vite 只报告既有的 500 kB 大分块提示。
- `pnpm package:mac`：通过；当前补充实现生成 `dist/test/mac-arm64/Zeus Test.app` 与 `dist/test/Zeus-Test-0.3.45-arm64.dmg`。
- `CFBundleIdentifier` 反读为 `dev.hypha.zeus.test`；`codesign --verify --deep --strict` 通过。测试包为 ad-hoc 签名且明确未公证，不是正式发布证据。
- `git diff --check`：通过；按项目规则未新增、恢复或执行单元测试体系。

### 聚合与兼容领域探针

使用一次性隔离 SQLite 领域探针验证存储层和 Snapshot V2，不把该探针冒充真实 Provider 或 GUI：

- 两轮总处理区间为 20 秒，其中审批/提问等待 3 秒，`cumulativeProcessedDurationMs=17000`。
- 完整纯文本请求以 300 个非推理输出 Token、4 秒文本区间得到 `75 tokens/s`；把最新请求改为混合响应后速率为 `null`。
- 首段可见响应延迟为 1000 ms；累计 Token 为 500。
- 已覆盖费用为 `$1.25`、覆盖率 50%、目录日期 `2026-08-10`，完整性为 false；摘要因此必须显示“估算不完整”。
- 活动投影为 2 轮、2 次模型请求、1 次工具、1 次重试、0 个失败轮次；代码变更为 2 个文件、`+3/-1` 且完整。
- 直接插入升级前形态的历史请求后，新时序字段保持 `null`、`measurementComplete=false`；关闭并重新打开同一数据库后结果一致，幂等补列与 Snapshot V2 投影保持稳定。

### 响应式与字段收口

- 使用当前 `session.css` 和与 Renderer 相同的指标 DOM 结构进行受控页面检查：1440px 摘要高度 46px，390px 自然换行为 88px；两种宽度均得到 `overflow none`。
- 390px 下折叠后详情文本从可见树消失，再次展开后恢复；长目录、分支、会话 ID 与 JSONL 不产生横向溢出。
- 会话运行信息源码不再渲染当前轮次、最后一次真实请求、缓存状态、最后请求缓存读写/普通输入、本轮用量/费用、目标能力、推理级别证据、缓存请求证据或 Credits；底层用量、缓存和配置证据仍保留。

### 真实测试身份启动

- 启动前确认当前主屏为外接显示器 ID 5，另一个非主外接显示器为 ID 3；测试身份队列释放后才启动本任务应用，没有关闭、复用或接管其他任务实例。
- 使用全新隔离数据根 `/private/tmp/zeus-0357-gui.arWCCG` 和 `ZEUS_TEST_DISPLAY_ID=3` 启动最终 `Zeus Test.app`。Main 日志为 `matchKind=first-launch`、`targetDisplayId=3`、`actualDisplayId=3`、`corrected=false`，窗口从首次创建起位于非主外接屏，边界为 `x=-1268, y=268, width=1240, height=820`。
- DevTools 只读元数据确认 Renderer URL 来自本 worktree 最终包的 `app.asar/dist/renderer/index.html`；主进程、Renderer 与执行宿主均使用上述隔离数据根。
- 本任务实例已退出；隔离数据根已移入废纸篓 `/Users/david/.Trash/zeus-0357-gui-20260823-0226`，仍可恢复。未启动、修改或退出正式 `Zeus.app`。

### 动态验收缺口

- Computer Use 初始化先后返回 `process is not defined` 与 `Computer Use requires nodeRepl.createElicitation`，无法读取本任务 Electron 窗口的可访问性树或截图。因此真实包启动、首窗落位和 Renderer 来源已验证，但不宣称会话指标的 Electron GUI 内容已目视通过。
- 全新的隔离测试身份没有可复用的 Codex/Pi Provider 登录与模型连接；在 GUI 控制不可用时也不能安全完成独立登录、新建隔离会话和真实发送。因此 Codex/Pi 纯文本、工具续接、推理、重试、失败、未知价格、重连和应用重启的真实 Provider 展示仍未完成。本轮只确认对应代码边界、领域投影和不完整性保护，不用合成事件冒充真实 Provider。

## 来源分支合入记录（2026-08-23）

- 在持久分支 `zeus/ZEUS-0357-task-03-merge` 中保留现有 merge 流程，将任务提交 `26a0238` 合入来源分支基线 `cd33824`；共同祖先为 `4c5b755`。
- 全仓未合并清单只有 `apps/desktop/src/renderer/session/session.css`。冲突来自来源分支把工作态扫光从 `1.7s` 放慢到 `3.4s`，而任务分支在同一区域统一格式；最终保留来源分支的 `3.4s`，同时保留任务分支新增的指标摘要换行与详情分组样式。
- 额外审计双方共同修改的四个文件，确认来源分支的只读历史加载、稳定过程项保留、计划确认队列同步与工作态节奏均存在，任务分支的会话指标投影、请求时序和指标界面也均存在，没有把合入范围缩减为冲突标记处理。
- 合并态 `pnpm lint`、`pnpm typecheck`、`pnpm build` 与 `pnpm package:mac` 均通过；测试包为 `Zeus Test.app` 0.3.36。本阶段只完成构建和打包，没有启动应用，不新增 GUI 或真实 Provider 验收结论。
- 所有解决结果已暂存，未创建 merge commit；`MERGE_HEAD` 保留给统一代码交付流程。

## 验收边界

- 静态与构建：`pnpm lint`、`pnpm typecheck`、`pnpm build`、`pnpm package:mac`。
- GUI 只允许使用 `Zeus Test.app`（`dev.hypha.zeus.test`）和独立用户数据目录。
- 若存在外接屏，测试窗口必须从首次创建起位于非主外接屏；无法保证时停止 GUI 验收并记录缺口。
- 不执行 commit、push、merge、revert 等 Git 历史或远端操作。
