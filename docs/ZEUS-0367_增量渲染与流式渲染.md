# ZEUS-0367 会话 Markdown 增量与流式渲染

## 目标与边界

- 将会话回复、思考、用户消息、任务首发内容、计划、侧聊、子智能体指令及旧会话正文统一迁移到 `markstream-react@2.0.6`。
- 文件源码、Markdown 文件预览及其他非会话 Markdown 表面维持现状。
- 不修改 Provider、WebSocket、数据库、会话事件协议或现有 16ms Renderer 事件合并。
- 不增加公开 API、持久化字段、迁移或用户设置，不引入 `stream-diffs`、Mermaid、KaTeX 或代码高亮。

## 基线

- 分支：`zeus/ZEUS-0367-task-01`
- 工作目录：`/Users/david/hypha/.zeus-worktrees/zeus-e2e-oBLuqT/dfc4ed10330e361a2bb4/ZEUS-0367`
- 开始状态：工作树干净。
- 原会话渲染：`react-markdown` + `remark-gfm`，流式正文由本地稳定块拆分和骨架占位处理；文件预览共用静态渲染器。
- 原事件节流：Renderer 会话事件保持 16ms 合并，本任务不改动。
- 原预算：正文 200,000 字符、单代码块 50,000 字符、顶层块 512、节点 4,096。

## 固定版本与参数

- 运行时：精确固定 `markstream-react@2.0.6`。
- 增量渲染：`maxLiveNodes=0`、`batchRendering=true`、首批 24 节点、后续每批 16 节点、批次间隔 8ms、帧预算 4ms、idle 上限 64ms。
- 平滑流式：首批不等待、60fps、目标延迟 80ms、32ms 进入追赶、单次最多 2,048 字符、完成时立即 flush。
- 动效：关闭框架淡入和尾部光标；系统减少动态效果时关闭占位动画。
- HTML：固定 `htmlPolicy="escape"`。
- 虚拟化：不启用框架内层虚拟窗口，保留完整完成态 DOM，保证批注文本偏移可靠。

## 安全与兼容取舍

- 链接、图片和代码块由 Zeus 自定义节点接管。链接只开放页内锚点或已审计会话资源；图片只加载权威会话资源；代码块保留复制按钮、纯文本展示和长度限制。
- `streaming` 与 `final` 复用同一渲染实例，由框架原位收口。`final` 且批次占位节点清零后才开放回答批注。
- 使用 `ResizeObserver` 合并通知外层滚动控制器，不增加嵌套滚动或内层虚拟窗口。
- 优点：稳定前缀节点复用、未闭合结构解析和分批挂载交由专用运行时管理。缺点：增加第三方运行时、构建体积与后续升级验证成本；单条超长完成消息仍保留完整 DOM。

## 实施记录

- 2026-08-28：完成代码入口、依赖边界、会话表面、批注 DOM 偏移和 Vite 分块基线调查，开始实施。
- 2026-08-28：精确锁定 `markstream-react@2.0.6`；新增 `ConversationMarkdown`，接管安全链接、权威图片、纯文本代码块、HTML 转义、四项内容预算、平滑流式、分批挂载、ResizeObserver 通知和 final 收口回调。
- 2026-08-28：会话回复、思考正文、用户消息、任务首发、计划摘要与工作区、侧聊回复、子智能体指令/继承上下文、旧会话正文完成迁移；`SourceWorkspace` 文件预览继续使用原静态渲染器。
- 2026-08-28：回答批注改为仅在 `final` 且 `.node-placeholder` 清零后开放；Vite Markdown 分组纳入 Markstream、stream parser、markdown-it 与相关依赖，组内上限保持 2 MiB。
- 2026-08-28：在既有 `session-styles` QA 页增加 `?markdown-stream` 现场，覆盖 20KB/30Hz、100KB 突发、节点身份、收口、帧/长任务、输入、选择、滚动、资源安全和复制按钮。
- 2026-08-28：`completed`、`failed` 和 `interrupted` 统一映射为 Markdown `final`，避免中断后的历史正文继续保留 loading 节点。
- 2026-08-28：侧聊用户与助手消息均进入统一适配层；`mermaid`、`infographic`、`d2` 代码语言和数学节点显式降为 Zeus 纯文本节点，lockfile 未引入 `stream-diffs`、Mermaid 或 KaTeX 包。
- 2026-08-28：进入验证阶段；固定端口 4179 已被其他进程占用，任务专用 Vite 服务改在 4187 正常启动；Zeus 内置浏览器调用层多次无输出挂起，尚未取得页面证据，未改用外部 Playwright。
- 2026-08-28：用户明确授权提交本任务代码并本地合入 `test`，再从 `test` 分支打包、启动独立 `Zeus Test.app` 验收；未授权 push。已核准目标 worktree 为 `/Users/david/hypha/.zeus-worktrees/zeus-e2e-oBLuqT/ZEUS-0352-test`，目标分支工作树与索引干净且无未完成 merge。

## 验证证据

### 静态检查

- 定向 Prettier：通过，覆盖本任务修改的 TypeScript、TSX、CSS、JSON、Markdown 与 lockfile。
- `pnpm lint`：通过。
- `pnpm typecheck`：通过；架构治理同时确认 97 张 Core 表和 11 张可重建辅助表的 owner、源码大小、导入、公开端口与包循环均符合策略。
- `git diff --check`：通过。

### 构建与资产

- `pnpm build`：通过，Renderer 共转换 1,300 个模块。
- 冻结 `HEAD` 基线 Renderer 总资产为 5,317,623 字节；改造后为 6,031,173 字节，增加 713,550 字节。
- 基线 Markdown 分块为 153,783 字节；改造后 Markdown JS 为 794,104 字节、CSS 为 63,469 字节，合计 857,573 字节，增加 703,790 字节。
- 当前最大 Renderer 单文件仍为 `code-editor-runtime` 1,106,599 字节，小于 2 MiB（2,097,152 字节）门禁；`markdown-runtime` JS 也小于该门禁。
- 构建有两类非阻断提示：项目既有的 500KB 分块提示，以及 `markstream-react@2.0.6` 发布产物中 `/* @__PURE__ */` 注解位置被 Rolldown 忽略；构建退出码为 0。

### 测试身份打包

- `pnpm package:mac`：通过。
- 应用：`dist/test/mac-arm64/Zeus Test.app`；DMG：`dist/test/Zeus-Test-0.3.69-arm64.dmg`。
- `CFBundleIdentifier` 为 `dev.hypha.zeus.test`；`codesign --verify --deep --strict` 显示应用在磁盘上有效并满足 Designated Requirement。
- 本任务 `dist` 中未生成生产身份 `Zeus.app`。测试包为临时 ad-hoc 签名且未公证，因此 Gatekeeper `spctl` 评估为 rejected；这不影响上述代码签名完整性结论，也不冒充正式发布验证。

### QA 夹具

- 既有 QA 页新增 `?markdown-stream`，20KB 夹具精确按 20 字符、30Hz 追加，100KB 夹具一次突发；内容覆盖段落、嵌套列表、GFM 表格、代码围栏、链接、图片、中英文、原始 HTML 与未闭合链接。
- 现场通过条件包含首节点 DOM 身份、占位节点清零、正文复制逐字一致、代码复制按钮、未授权链接不可点击、未授权图片零网络请求、选择/滚动/输入响应及计划中的性能阈值。
- Vite 在 4187 端口成功完成页面和 TSX 模块转换，HTTP 返回正常。
- **运行指标未验**：Zeus 内置浏览器在打开本地 QA 页时连续等待 60 秒仍无返回，调用已终止；未改用 Playwright。因此首段 100ms、final 150ms、长任务、P95 帧间隔、100KB 两秒收口及 DOM/网络运行断言均不能报告为通过。

### 真实桌面与 Provider

- **首次真实桌面尝试未验**：检查时另一个任务的 `/Users/david/hypha/.zeus-worktrees/zeus-e2e-oBLuqT/ZEUS-0352-test/dist/test/mac-arm64/Zeus Test.app` 仍在运行。该阶段按串行隔离理解未启动本任务测试包；后续用户确认允许不同资料根并行启动，续验结果见下文。
- 首次尝试阶段的浅色、深色、窄窗、减少动态效果、批注、资源打开和历史恢复均未验，也未尝试 Provider 登录。
- **真实 Provider 未验**：未新建真实 Provider 长 Markdown 会话，QA 夹具、静态检查、构建和打包均未被用来替代该证据。

## `test` 分支合入与续验

- 2026-08-28：功能分支提交为 `7ff39bb`（`feat(desktop): 实现会话 Markdown 增量流式渲染`）。用户授权后，本地合入 `test`，merge 提交为 `a70e382`；`git merge-base --is-ancestor 7ff39bb test` 通过，未执行 push。
- 合入时 `test` 已新增 ZEUS-0373 等提交，三方预检与真实 merge 在 `apps/desktop/qa/session-styles.tsx`、`apps/desktop/src/renderer/session/ThreadItemView.tsx` 产生内容冲突。解决结果同时保留既有 `plan-preview` QA 与新增 `markdown-stream` QA，并让 `test` 新增的速度提示在保留 `role="status"` 和视觉容器的同时使用 `ConversationMarkdown`；未用整文件 ours/theirs 覆盖。
- 合入后的 `test` 分支定向 Prettier、`pnpm lint`、`pnpm typecheck`、`git diff --check`、`pnpm build`、`pnpm package:mac` 均通过。首次 typecheck 因目标 worktree 尚未安装新依赖而找不到 `markstream-react`，执行 `pnpm install --frozen-lockfile --offline` 后通过，lockfile 未变化。
- `test` 产物位于 `dist/test/mac-arm64/Zeus Test.app`，版本 0.3.69，`CFBundleIdentifier=dev.hypha.zeus.test`；严格 codesign 验证通过，未生成生产身份 `Zeus.app`。最大 Renderer 单文件为 1,106,599 字节，仍小于 2 MiB 门禁。
- 合入后再次从 `test` 启动 4187 端口 QA 服务；Zeus 内置浏览器打开页面累计约 90 秒仍无返回，调用已终止且服务已关闭。因此性能和页面运行断言仍未验，未改用 Playwright。
- 当前只检测到内置主屏，不存在外接显示器，首窗外接屏门禁本次不触发。最初因另一任务实例仍在运行而没有并行启动；该判断已被用户后续确认和下述独立资料根续验取代。

### 2026-08-28：独立资料根并行启动续验

- 用户确认同一分支允许使用不同 `ZEUS_USER_DATA_DIR` 并行启动。当前 `test` 在 `e01c0b8` 重新执行 `pnpm package:mac` 并通过签名验证；随后 `test` 前进到 `a8abf0b`，差异仅为 ZEUS-0373 文档，不包含应用代码，因此已启动二进制与当前 `test` 应用代码一致。
- 本任务以 `ZEUS_USER_DATA_DIR=/private/tmp/zeus-0367-test-acceptance.gtiGSM` 启动 `dist/test/mac-arm64/Zeus Test.app`，主进程 PID 62173、执行宿主 PID 62214。另一任务实例同时运行，双方 profile、数据库、Provider Home、Runtime 租约和单实例锁均按资料根隔离，未关闭或复用其他实例。
- 执行宿主达到 `control_ready` 并记录 `ui_attached`；`/health` 返回 `ok=true`、`database=ok`、`runtime=ok`、`storage.state=writable`，SQLite `PRAGMA integrity_check` 返回 `ok`。这证明从 `test` 分支独立启动、Renderer 附着、数据库和 Runtime 健康通过。
- Computer Use 和 Browser 标准连接均被当前会话运行环境拒绝，错误分别为缺少受信 Computer Use 服务和受信 Browser 服务；Zeus 会话原生浏览器接口再次等待 30 秒无返回。因此无法读取指定窗口的可访问性树、截图或 DOM，也不能执行 Markdown、主题、窄窗、减少动态效果、批注、资源打开和 Provider 交互。
- 测试实例保持运行，未复制任何正式或其他测试实例的认证数据。真实 Markdown 视觉/交互、性能门槛和 Provider 发送仍明确为未验，不能用健康检查或 `ui_attached` 冒充。

## 最终取舍

- 已完成计划内代码与 QA 现场实施，未修改 Provider、WebSocket、数据库、会话事件协议或 16ms Renderer 合并链路。
- 运行时与升级治理成本已经量化：Renderer 总资产增加约 696.83 KiB，其中 Markdown 分组增加约 687.29 KiB；换取稳定节点复用、未闭合结构解析、平滑追赶与原位 final 收口。
- 浏览器性能门槛、真实桌面交互和真实 Provider 仍是明确的验收缺口；需要恢复本会话受信的 Computer Use/Browser 服务后补跑，不再要求等待其他资料根的测试实例退出。
