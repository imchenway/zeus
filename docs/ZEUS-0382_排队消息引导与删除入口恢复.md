# ZEUS-0382 排队消息引导与删除入口恢复

## 用户现场

- 当前回复仍在运行时，用户在底部输入框按 Enter 发送“还在继续没？”。
- 消息以“排队中”气泡进入会话时间线，但气泡没有删除入口，也不能显式引导给当前回复。
- 用户提供的现场截图中，输入区仍显示停止按钮，说明当前轮次尚在执行；排队气泡本身只显示状态和正文。

## 已确认根因

- 服务端仍保留按稳定 `submission.id` 删除排队消息和把队首消息引导给当前活动轮次的能力。
- Renderer 控制器仍保留 `deleteQueuedSubmission` 与 `sendQueuedNow`，`SessionWorkspace` 也仍创建对应 action。
- 2026-08-23 为修复历史会话与计划持久化回归时，旧 `QueuedConversationMessages` 管理组件被删除，排队正文改为时间线中的单一用户气泡。
- 正常 `queued` 气泡只获得普通消息的复制操作；`ConversationTranscript` 仅在失败或恢复场景传递“重试/取消”，没有把正常排队消息的引导、删除等操作迁入气泡。
- 因此本次不是 Provider 不支持引导，也不是消息没有落库，而是新投影表面丢失了正常队列操作入口。

## 产品决定与取舍

### 发送语义

- 当前轮次运行时，Composer 的 Enter 继续默认保存为下一轮排队消息。
- 不把整个消息气泡设置为单击即引导。这样可以避免用户选择文本、打开附件或误触气泡时直接改变 Provider 执行。
- 在唯一的排队气泡上提供明确的“引导”与“删除”操作；消息不再复制到第二个队列面板。

优点：保留“下一轮再处理”和“补充给当前轮”两种不同意图；操作对象与可见消息身份一致；不会恢复重复投影。

缺点：用户需要点击明确按钮，不能通过点击气泡任意位置快速引导；这是为降低误发送风险保留的一次显式确认。

### 引导边界

- 仅普通 `queued` 消息允许引导。
- 仅真实队首允许引导，不能绕过更早的 `queued`、`paused` 或 `failed` 提交。
- 仅当前轮次处于活动状态、连接就绪，且 `startedTurnId === activeTurnId` 时允许引导。
- 计划确认等不可变控制提交不允许编辑、删除、排序或引导。
- 引导请求以原 `submission.id` 调用现有 `sendQueuedNow`，不按正文模糊匹配，也不先创建第二条 `steer_now` 消息。

### 删除边界

- 仅尚未交给 Provider 的普通排队或暂停消息允许删除。
- `dispatching`、`steering` 和不可变控制提交不提供删除，以免把已经进入外部副作用窗口的消息伪装成本地可撤销。

## 验收目标

1. 活动回复期间按 Enter 后，消息只出现为一个“排队中”用户气泡。
2. 排队气泡清楚提供“引导”和“删除”入口，键盘也能聚焦并触发。
3. 点击“引导”后，同一消息身份进入“引导中”，不会复制出第二条排队消息；最终以 Provider 权威事件进入当前轮次。
4. 点击“删除”后，未发送消息从时间线和权威队列中同时消失，不触发 Provider 发送。
5. 非队首、当前轮次尚未就绪、正在引导和不可变控制消息展示与真实协议一致的不可用原因。
6. 静态检查、构建或打包不能替代真实 Provider 轮次中的图形界面验收。

## 阶段记录

- 2026-08-29：完成任务边界、历史文档、当前分支和代码链路核对；确认工作树初始干净，分支为 `zeus/ZEUS-0382-task-01`，基线为 `441b7ad`。
- 2026-08-29：进入实现阶段，计划把正常队列操作迁入时间线中的唯一排队气泡，不恢复已删除的第二队列面板。
- 2026-08-29：`ConversationTranscript` 通过稳定 `submission.id` 将权威队列项绑定到唯一气泡，并按真实队首与 `canSteerActiveTurn` 计算引导可用性；`ThreadItemView` 常驻展示“引导 / 删除”，保留键盘焦点、不可用原因、忙碌态与统一错误弹窗。
- 2026-08-29：删除 action 改为向气泡传播失败；历史恢复卡片同时补齐 rejection 捕获，避免正常删除可见后让旧取消入口产生未处理 Promise。
- 2026-08-29：`pnpm install --frozen-lockfile --offline` 通过，仅恢复本机锁文件依赖；安装期两个内部 CLI 尚未构建的软链接提示没有阻断后续完整构建，lockfile 未变化。
- 2026-08-29：定向 Prettier、`git diff --check`、`pnpm typecheck`、`pnpm lint`、`pnpm build` 全部通过。构建只保留既有 `markstream-react` 注释位置与大 chunk 警告。
- 2026-08-29：`pnpm package:mac` 通过，只生成 `dist/test/mac-arm64/Zeus Test.app` 与测试 DMG；包体为 `Zeus Test`、`dev.hypha.zeus.test`、版本 `0.3.72`，严格签名、Designated Requirement、包体健康检查与 DMG 校验均通过。
- 2026-08-29：用户明确授权完成代码提交、合入共享 `test`、从 `test` 打包并真实验收、退出本任务测试实例、将本任务精确提交链合入 `main`，以及执行正式升级发布。
- 2026-08-29：提交前重新核对三个工作树：任务分支基于 `main@441b7ad`；`main` 干净并与 `origin/main` 一致；共享 `test@0b4409c` 干净但领先 `origin/test` 118 个提交，且与 `main` 双向分叉，包含多项未进入主线的其他任务。因此本任务会合入 `test` 做集成验收，但验收后只把本任务提交链合入 `main`，不会把共享 `test` 的其他待验收内容一并带入正式发布。

## 当前真实验收边界

- 当前机器有主外接屏、内建屏和非主外接竖屏；本任务真实窗口必须从首次创建起位于非主外接屏。
- 打包后曾检查到 ZEUS-0380 的 `Zeus Test.app` 占用同一测试 bundle 身份，主 PID 为 `63245`，资料根为 `/private/tmp/zeus-0380-acceptance.cnZ0iH`；本任务没有关闭、接管、复用或修改该实例。
- 2026-08-29 提交前复查时，该实例已经自然退出，共享测试身份当前空闲。后续仍须从共享 `test` 分支重新打包，使用本任务独立 `ZEUS_USER_DATA_DIR`，并在非主外接屏首次创建窗口后，完成真实活动轮次中的排队、引导、删除和 Provider 权威事件闭环。

## `test` 分支真实验收结果

### 集成与包体

- 功能提交为 `a5ba5ce`，共享 `test` 集成提交为 `e467f0f`。合入时保留 `test` 的计划预览 QA 与计划对象语义，并叠加本任务的 `onSendQueuedNow`；没有整文件覆盖共享分支内容。
- `test` 集成态的 `pnpm typecheck`、`pnpm lint`、`pnpm build`、`pnpm package:mac` 全部通过；构建仅保留既有第三方纯注释位置和大 chunk 警告。
- 最终测试包位于 `test/dist/test/mac-arm64/Zeus Test.app`，身份为 `Zeus Test`、`dev.hypha.zeus.test`、版本 `0.3.72`。严格签名、Designated Requirement、包体健康检查与测试 DMG 校验均通过。

### 隔离与落屏

- 使用独立资料根 `/private/tmp/zeus-0382-acceptance.6515dF` 和独立项目根 `/private/tmp/zeus-0382-project.AEKz1x`，没有读取、复制或覆盖正式 Zeus、日常 Codex 或其他任务实例的数据。
- 当前主屏为外接显示器 ID `5`，ID `1` 为内建屏，ID `3` 为非主外接屏。测试包每次启动日志均为 `matchKind=first-launch`、`targetDisplayId=3`、`actualDisplayId=3`、`corrected=false`，首窗边界为 `x=-1268,y=268,width=1240,height=820`。
- 独立资料根最初未登录 Provider；只在该根的 `providers/codex` 通过官方 `codex login` 完成临时 ChatGPT 登录，没有导入其他身份凭据。验收结束后执行官方 `codex logout`，状态恢复为 `Not logged in`，临时 `auth.json` 已移除。

### 排队与删除

- 真实 Codex 会话 `conversation_519578b70c37a65c4f4c27ea` 运行 90 秒只读命令期间，从可见 Composer 发送 `ZEUS0382_DELETE_ME`。界面只出现一个“排队中”气泡，并在同一气泡常驻显示“引导 / 删除”。
- “删除”按钮通过 Electron Renderer 的真实 `isTrusted=true` pointer/mouse/click 事件触发。submission `conversation_submission_Yye5Wvl1tb2K` 随后从时间线和权威队列消失，数据库为 `status=deleted`、`submission_outcome=terminal`、`provider_turn_id=null`，没有触发 Provider 发送。
- 可见证据为 `/Users/david/.zeus/artifacts/ZEUS-0382/queued-delete-actions.png`。

### 活动轮次引导

- 第二个真实活动回合先以 submission `conversation_submission_ZJF9aP0Ppg8X` 启动 120 秒只读命令，再发送队首 `ZEUS0382_STEER_NOW`，随后点击同一气泡的“引导”。
- 引导 submission `conversation_submission_PSj0dY6Xk0SX` 最终为 `completed/terminal`，与首发提交共享 Provider turn `01a04b77-137d-7f21-a006-65c61aa6dc3b`，没有新建第二个 Provider turn。
- 隔离 Provider JSONL 只出现一次该用户消息，并在同一 turn 下记录 `response_item user` 与 `event_msg user_message`；Agent 收到引导后中止原长命令并回复 `ZEUS0382_STEER_NOW`。界面最终只保留一个对应用户气泡，不存在第二条排队副本。
- 完成态可见证据为 `/Users/david/.zeus/artifacts/ZEUS-0382/steer-completed.png`。

### 双队列与键盘边界

- 第三个真实活动回合中连续排入 `ZEUS0382_QUEUE_HEAD` 与 `ZEUS0382_QUEUE_SECOND`。两个气泡的“引导 / 删除”均为原生 `<button>` 且 `tabIndex=0`。
- 队首“引导”为 `aria-disabled=false`；第二条仍可聚焦，但为 `aria-disabled=true`，`aria-label` 与 `title` 都明确提示“请先处理更早的排队消息”。
- 删除队首后，第二条在同一活动轮次立即变为 `aria-disabled=false`，提示恢复为“补充给当前回复，不中断当前执行”。随后删除第二条；两个 submission `conversation_submission_LXqITlEJVBfE`、`conversation_submission_SX8vv1n3Vy90` 均为 `deleted/terminal` 且 `provider_turn_id=null`。
- 双队列可见证据为 `/Users/david/.zeus/artifacts/ZEUS-0382/two-queued-messages.png`。

### 退出与结论

- 验收结束后通过界面停止本任务构造的最后一个只读长回合，状态回到 `native_idle`，再正常退出本任务 `Zeus Test`；没有结束或操作任何其他测试/正式实例。
- 退出后本任务 Main、Renderer、Execution Host 与 Provider 子进程均不存在，资料根无打开文件，`host.lock` 与 `rendezvous.json` 已清理；SQLite `PRAGMA quick_check` 与 `PRAGMA integrity_check` 均为 `ok`。
- 结论：正常活动回复期间，Enter 仍按产品决定默认排队；唯一排队气泡已经恢复可见、可键盘访问且符合权威队首边界的“引导 / 删除”入口。删除不触发 Provider，显式引导进入当前 Provider turn，现场缺陷闭环通过。

## 主线合入与发布

- 2026-08-29：正式合入前重新拉取并核对 `origin/main`；本地 `main` 干净、无 `MERGE_HEAD` 或未解决文件，且与远端同为 `441b7ad`。
- 2026-08-29：仅将任务分支的功能提交 `a5ba5ce` 与验收记录提交 `05c6776` 以 `--no-ff` 合入 `main`，生成合并提交 `70a4d03`。共享 `test` 分支上的 ZEUS-0381 等其他未发布任务没有进入主线。
- 2026-08-29：主线 `pnpm typecheck`、`pnpm lint`、`pnpm build` 全部通过；仅有既有第三方 pure annotation 与大 chunk 警告。
- 2026-08-29：正式发布 `v0.3.73` 完成，发布提交为 `259d3f4`，Release Workflow `33230676714` 的 `preflight`、`typecheck`、`package-mac`、`publish` 全部成功；GitHub Release、manifest 与 Homebrew Cask 一致性对账通过。
- `Zeus-0.3.73-arm64.dmg` 大小为 `113468598` 字节，SHA-256 为 `bda25ec09577de23f564fcc6c1aa78499157c52088f0d130e78dbe6c1565c817`。当前发布凭据不含 Developer ID 签名与 Apple 公证，manifest 明确记录 `signed=false`、`notarized=false`。
- 2026-08-29 收口复核时，并发任务已将 `v0.3.74` 发布提交 `8853411` 推入主线，但对应 Release Workflow 仍在运行，远端尚无 `v0.3.74` 标签或 Release，因此不将它记录为已发布。该主线提交以 `v0.3.73` 为祖先；ZEUS-0382 已证实的不可变发布证据仍以 `v0.3.73`、提交 `259d3f4` 和上述成功工作流为准。
