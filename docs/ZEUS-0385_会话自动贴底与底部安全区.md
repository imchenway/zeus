# ZEUS-0385 会话自动贴底与底部安全区

## 任务目标

- 打开或切换会话后直接显示最新消息。
- 用户停留在底部时，消息增量、异步正文布局和窗口尺寸变化都持续贴底。
- 只有用户主动上滚查阅历史时暂停跟随；用户自己提交新消息或点击“返回最新消息”后无条件恢复贴底。
- 会话末尾保留总计约三行正文高度的可滚动留白安全区。

## 已确认产品口径

- 滚动状态只有“跟随最新内容”和“查阅历史”两种；普通程序滚动、虚拟列表测量及锚点补偿不能进入历史阅读态。
- 用户滚轮、触控、滚动条拖动或滚动按键使会话离底部超过 `24px` 后进入历史阅读态；主动滚回容差内恢复跟随。
- 每次重新切入会话都放弃上次阅读位置，在绘制前即时定位到底部，不播放平滑滚动动画。
- 当前会话内本地提交新消息时，即使用户此前在查阅历史，也立即恢复跟随并定位到底部。
- 会话底部安全区总高度为 `72px`，不在现有 `24px` 基础上继续叠加。

## 实施方案

- 收口 `ConversationTranscript` 与滚动控制器：原始 `scroll` 事件只同步视口和可见性，只有已登记的真实用户滚动意图才允许改写跟随状态。
- 移除新轮次按距底部 `300px` 自动接管的例外；新轮次只沿用当前状态，本地提交由显式提交信号恢复跟随。
- 在 `SessionWorkspace` 维护会话内单调递增的本地提交序号并传入转录区，覆盖普通发送与编辑重发；保留权威用户消息投影作为异步兜底。
- 把虚拟行重新测量、滚动容器尺寸变化与既有 Markdown/图片可见内容变化接入同一个按帧合并的贴底调度器。跟随态定位到底部，历史阅读态继续使用稳定行锚点补偿。
- 用户首次离开底部时保存变更前的稳定行身份与顶部偏移；后续行测量优先使用这份阅读锚点，避免浏览器自动滚动锚定先于虚拟器测量后造成误采样。
- 调度器以 `requestAnimationFrame` 合并高频几何通知；仅当 Electron 窗口被遮挡、帧回调暂停时启用 `80ms` 低频后备，并以最多十二次、连续两帧几何稳定为单轮收敛上限。
- 调整会话正文末尾可滚动内边距为 `72px`。
- 扩展真实 `ConversationTranscript` QA 夹具，并先补齐现有 runtime fixture 缺少的 `serviceTier` 事实字段；不修改 `RuntimeDetails` 产品逻辑。

## 取舍

- 优点：切换、发送、流式增量和延迟布局共享同一状态语义，不再依赖固定三帧或把程序滚动误判为用户操作。
- 代价：转录区需要维护短生命周期的用户滚动意图；尺寸观察会增加少量回调，但统一按动画帧合并，不让 React state 承接逐像素事件。
- 优点：`72px` 留白让末尾消息与 composer 之间更舒适。
- 代价：同一屏可见正文略少。

## 验证边界

- 不新增或恢复 Vitest、组件测试、DOM/CSS 契约测试。
- 执行 Prettier 检查、`pnpm lint`、`pnpm typecheck`、`pnpm build`、`pnpm package:mac` 和必要的真实运行检查。
- GUI 验收只使用本任务生成的 `Zeus Test.app`（`dev.hypha.zeus.test`）与独立 `ZEUS_USER_DATA_DIR`；不读取或覆盖其他任务实例、正式会话与既有正式 Provider 线程。用户后续明确授权把正式环境的指定模型连接元数据与 Keychain 密钥复制到本任务隔离资料根；该例外不扩大到正式会话数据。
- 2026-08-29 用户追加明确授权：本任务代码可自行提交，先合入 `test` 并从该分支构建、启动独立 `Zeus Test.app` 完成 Computer Use 验收；验收通过且退出本任务实例后，再将同一任务提交合入 `main` 并执行正式发布升级。
- 2026-08-29 用户进一步要求：Codex 实际模型验收通过后，还要分别使用 DeepSeek Flash 与 1xm 或 apikey 的低价模型完成实际会话验收；全部通过前不得退出本任务 Test、合入 `main` 或发布。
- `test` 当前包含大量尚未进入 `main` 的其他任务提交，因此正式合入不能直接把整个 `test` 分支并入 `main`；本任务提交分别合入两条分支，以避免夹带无关变更。

## 阶段记录

- 2026-08-29：完成现状只读诊断和实施方案确认，进入实现阶段。
- 2026-08-29：独立 worktree 初次启动既有会话 QA 页面时，确认 runtime fixture 缺少当前必需的 `serviceTier` 事实而导致页面崩溃；该问题仅在 QA 数据中补齐。
- 2026-08-29：完成两态滚动控制链路、本地提交序号、虚拟行/内容窗/容器尺寸通知、变更前阅读锚点和 `72px` 安全区实现，进入验证阶段。
- 2026-08-29：Zeus 内置浏览器 QA 已取得首次打开 `距底部 0px`、延迟增高后 `距底部 0px`、安全区 `72px`，以及上滚后新增回答仍保持 `距底部 180px` 且显示“返回最新消息”的动态证据。验收标签页在后续高负载交互中失去响应，稳定行、会话切换和窄窗口证据继续由重启后的 QA 页与独立 `Zeus Test.app` 复核，不把该阶段结果冒充完整 GUI 验收。
- 2026-08-29：Prettier、`pnpm lint`、`pnpm typecheck`、`pnpm build` 与最终 `pnpm package:mac` 均通过。最终产物为 `dist/test/mac-arm64/Zeus Test.app`，显示名 `Zeus Test`、bundle ID `dev.hypha.zeus.test`，深度签名结构有效；构建仅有既存第三方 `markstream-react` pure annotation 与大 chunk 警告。
- 2026-08-29：包体健康检查通过（Renderer/Main/两个 Preload 入口完整），测试 DMG 的 `hdiutil verify` 通过。启动前检测到 ZEUS-0384 的另一独立 `Zeus Test.app` 正在运行，本任务持续排队，没有关闭、复用或读取其测试资料根。当前显示器为主外屏 ID `5`、内屏 ID `1`、非主外屏 ID `3`；本任务后续首窗目标固定为 ID `3`。
- 2026-08-29：持续排队多轮后 ZEUS-0384 实例仍未自然退出，本任务未启动自己的 Test 窗口。因而“独立 Test.app 长会话切换/上滚”和“全新隔离会话真实 Provider 发送及回复增长”仍是明确的 GUI/Provider 验收缺口；实现、真实组件 QA、构建、包体和签名结论与该缺口分级记录。
- 2026-08-29：用户授权提交、test 验收、main 合入和正式发布后重新检查现场；当前占用 `Zeus Test` 身份的是 ZEUS-0383，ZEUS-0388 已在其后排队。本任务继续排队，不关闭、借用或复用其他任务实例及数据根。
- 2026-08-29：任务实现提交为 `f7edcfb`；真实长会话首次切换暴露 Markdown 正文可在稳定行 key 和 revision 之后水合，补充 DOM 正文观察的提交为 `18fca5e`。两次提交均已分别合入本地 `test`。
- 2026-08-29：使用独立资料根 `/private/tmp/z385.dUQtzq`、外接屏 ID `3` 和远程调试端口 `9385` 启动 test 分支包；隔离安装 Codex CLI `0.150.1` 并通过官方 device auth 登录。真实 GPT-5.6-Sol 完成 120 行、500 行及短回复会话；冷打开和同运行期往返切换长会话均稳定为 `scrollTop=max`、距底 `0px`，`72px` 安全区可见。
- 2026-08-29：真实新会话 500 行回答完成后的延迟布局一度出现 `max=12219`、`scrollTop=12142`、距底 `77px`。根因是活动状态收起等纯几何变化可能只移动底部标记，既不改变固定高度容器 border box，也不保证产生新的正文 DOM 变更；补充“跟随态底部标记离屏即唤醒统一调度器”，提交为 `b451d2f`，已合入本地 `test`。
- 2026-08-29：`b451d2f` 后重新执行 Prettier、`pnpm lint`、`pnpm typecheck`、`pnpm build` 均通过。默认 test 包路径被 ZEUS-0384 的独立实例占用，未关闭该实例；改用受支持的 `ZEUS_PACKAGE_OUTPUT_DIR=/private/tmp/zeus-0385-package.oTtYc1` 从同一 test checkout 生成任务专属 `Zeus Test.app`，签名结构有效。构建仍只有既存第三方 pure annotation 与大 chunk 警告。
- 2026-08-29：更新包真实长会话连续采样为 `scrollTop=12211/max=12211/距底=0`；真实键盘上滚后为距底 `1050px` 且“返回最新消息”显示。历史态展开处理过程使最大高度增加 `67px`，稳定行 identity 与顶部偏移连续八次采样均不变；Computer Use 激活“返回最新消息”后距底 `0.5px`（设备像素取整后的最大可达位置）。
- 2026-08-29：真实 `ConversationTranscript` motion QA 夹具经 Computer Use 完成桌面与窄窗口验收。桌面初始距底 `0px`；上滚后距底 `180px`、稳定行 `motion:send-history-3@6px`；无 revision 延迟增高使内容窗 `493→741px`、最大位置 `367→615px` 时稳定行和偏移不变；历史态本地发送在 `105ms` 内回到距底 `0px`；底部新增回答和延迟增高均保持距底 `0px`；会话往返均距底 `0px`；窄夹具宽 `343px` 时到达浏览器可实现的 `0.5px` 亚像素误差并保留 `72px` 安全区。
- 2026-08-29：历史分页夹具只有在 Computer Use 明确上读到顶部后才连续请求；最终请求 `2` 次、提示只挂载 `1` 次、游标推进至无更多历史，稳定锚点漂移 `0.1px`（亚像素取整）。打开和切回没有自动请求。
- 2026-08-29：Computer Use 的旧助手会间歇抢占并触发版本不匹配，鼠标/滚轮路径还会命中 macOS Window Bounds Observer 崩溃；验收只精确终止旧助手、启动 ChatGPT 自带助手，并使用只负责聚焦/读数的 CDP 桥接，所有按钮激活、键盘滚动与发送动作仍由 Computer Use 完成。Zeus 设置页另有异步读取 `event.currentTarget.value` 导致错误边界的既有问题，不属于本任务改动。
- 2026-08-29：隔离数据根原始的 `models.connections` 不存在，运行环境也没有 DeepSeek、1xm、apikey 或通用 API Key 变量；在用户额外授权前，DeepSeek Flash 与 1xm/apikey 实际模型验收因没有独立测试凭据而保持未执行。
- 2026-08-29：用户明确授权“直接拿正式的过去”后，仅把 DeepSeek `model_connection_c98cb29a9ebe4121980abfe2dc2a3bcb`、apikey `model_connection_4b7e6ee4a632480fb53485d5a44b81ae` 和后续备选的 1xm `model_connection_bce8a480bf884f87ac881b75f876b53d` 连接元数据与对应 Keychain 项复制到隔离服务 `Zeus Test 3398af359b9b0d07`；没有打印密钥，也没有读取正式会话。离线修改前备份为 `/private/tmp/z385.dUQtzq/backups/task-zeus-0385/zeus.before-model-copy.db` 和 `zeus.before-1xm-copy.db`，修改后 `pragma integrity_check=ok`，三个连接在隔离 API 均显示 `apiKeyConfigured=true`。
- 2026-08-29：DeepSeek V4 Flash 在隔离项目级会话真实调用通过：会话 `conversation_ff4492a4035b01980a449db9`，连接 `model_connection_c98cb29a9ebe4121980abfe2dc2a3bcb`，模型 `deepseek-v4-flash`，submission 在约 `3.8s` 后进入 `completed/terminal`，持久化助手正文精确为 `DEEPSEEK FLASH PASS`。
- 2026-08-29：项目级首发对 apikey Luna 与 1xm Fable 的拒绝经 Computer Use 复现并由网络响应定性：`POST /api/projects/.../conversations` 返回 `ZEUS_INVALID_CONVERSATION_SETTINGS`，产品口径为“项目首发当前只接受 Codex App Server 模型”。因此改从支持非 Codex 运行时的任务级“推送到新会话”进行后续验收。
- 2026-08-29：1xm Claude Fable 5 任务级会话成功进入真实 Provider，但供应商返回 `403 ZEUS_PI_MODEL_REQUEST_FAILED`：剩余额度约 `$2.37`，需要预扣约 `$3.56`，因而该备选不记为通过。随后 apikey GPT-5.6 Luna 任务级真实调用通过：会话 `conversation_a58bfeeade223e60b60425d7`，连接 `model_connection_4b7e6ee4a632480fb53485d5a44b81ae`，模型 `gpt-5.6-luna`，submission 在 `3.129s` 后进入 `completed/terminal`，持久化助手正文精确为 `APIKEY LUNA PASS`。
- 2026-08-29：多模型新会话在完成后仍可出现 UI 泛化错误或旧的“排队中”投影，但隔离数据库中的 submission 终态、Provider 连接/模型归属和助手正文已完整持久化；该投影刷新问题不是 ZEUS-0385 滚动改动引入。排查中误用平铺路径的首次 SQLite 只读查询曾自动生成一个 `0` 字节 `/private/tmp/z385.dUQtzq/zeus.db`；已在校验为空后移入备份目录为 `accidental-empty-flat-zeus.db`，重启后 Execution Host 仅使用正确的 `data/zeus.db`。
- 2026-08-29：全部隔离 GUI 与多模型验收收口后，已只精确退出本任务 `/private/tmp/zeus-0385-package.oTtYc1/mac-arm64/Zeus Test.app` 的主进程和 Execution Host，没有停止其他任务的 Test 实例。验收证据提交 `efe9cbf` 已合入本地 `test`（merge `b75e6a1`）；任务代码已合入 `main`（merge `51f6154`）。
- 2026-08-29：仓库发布编排生成 `chore(release): v0.3.76` 提交 `6c31c0164cb23b52408535aa65a6229bbc06f73c`。首次本地 TypeScript 门禁因 ZEUS-0391 新增的 `@modelcontextprotocol/client` 已在 lockfile 但当前 checkout 尚未安装而失败；执行 `pnpm install --frozen-lockfile --offline` 后未下载、未改 lockfile，恢复发布时 TypeScript 门禁通过。
- 2026-08-29：v0.3.76 已公开发布并回验通过：`main` 与 `origin/main` 均指向发布提交 `6c31c01`，Release Workflow `33243065596` 为 `completed/success`，GitHub Release 非 draft/非 prerelease，资产为 `Zeus-0.3.76-arm64.dmg` 与 `zeus-release-manifest.json`，Homebrew Cask 与资产元数完成一致性对账。正式 DMG 在上传前已通过 `hdiutil verify`；本次快速发布不包含 Developer ID 签名与 Apple 公证，也没有启动生产 `Zeus.app`。
