# ZEUS-0420 自动化的下拉框

## 当前状态

- 阶段：用户补充需求已实现、验收并提交；本地 `test` 基线严重分叉，定向合入已阻断且未执行。
- 工作区：`/Users/david/hypha/.zeus-worktrees/zeus-e2e-oBLuqT/efc365a1207ba4eb648c/ZEUS-0420`。
- 分支：`zeus/ZEUS-0420-task-04`；开始处理补充需求时工作树干净。
- 交付授权：开发完成后提交当前任务分支，并本地合入 `test`；不推送远端。

## 缺陷证据

- 用户截图显示自动化编辑器的“触发方式、精确模型、推理强度、权限、会话模式、阻塞策略”仍使用浏览器／macOS 原生 `select` 菜单。
- 展开后的系统蓝色选中面、字号、圆角、阴影和勾选样式与 Zeus 产品内统一下拉框明显脱节；菜单也无法复用 Zeus 已有的视口定位、搜索、键盘和焦点恢复行为。
- 根因位于 `apps/desktop/src/renderer/features/automations/AutomationsWorkspace.tsx`：自动化表单直接渲染原生 `<select>`，没有使用仓库共享的 `ZeusSelect`。
- 用户补充截图确认：“目标项目”仍是平铺复选块，“Skill ID”和“Plugin IDs”仍是要求输入内部稳定身份的文本框；三者均需改为产品下拉框，项目还需支持全选。

## 实现边界

1. 自动化编辑器内六个选择控件全部改用现有 `ZeusSelect`，不只修截图中已展开的“阻塞策略”。
2. 保留现有值域、双语文案和状态更新语义；精确模型继续固定来源与模型身份，选择模型时继续刷新默认推理强度。
3. 模型列表保留搜索；短枚举列表不显示搜索框。无可用模型时禁用控件及保存动作，避免替换原生 `required` 后放宽提交边界。
4. 目标项目改为可搜索的多选 `ZeusSelect`，在同一菜单内提供“全选项目”；至少选择一个项目仍是保存门禁。
5. Skill 改用既有 `SkillSelector` 单选，Plugin 改用多选 `ZeusSelect`；只展示所有已选项目都可用的权威目录交集，已失效的历史选择可见但不能保存。
6. 不新增下拉框组件、依赖、测试文件或与本缺陷无关的自动化表单重构；只为共享 `ZeusSelect` 增加可选的多选契约。

## 方案权衡

- 优点：复用全局 `ZeusSelect` 后，视觉、上下展开、视口边界、键盘导航、焦点恢复和选中勾选一次对齐；改动集中且后续维护只有一个控件权威。
- 缺点：自定义组合框不再获得系统原生菜单外观；其正确性依赖共享控件契约。该契约已被 Zeus 其他模型、权限和状态选择场景采用，适合本页复用。
- 补充方案优点：不再暴露 Skill/Plugin 内部 ID；多项目时取目录交集，可在保存前阻止“某个目标项目运行时才发现不可用”。
- 补充方案缺点：共享 `ZeusSelect` 契约增加了多选状态；多项目目录需并行读取每个项目，选择项目越多，首次展示 Skill/Plugin 的等待时间越受最慢项目影响。

## 此前六项下拉框验证证据

- `git diff --check`、`pnpm lint`、`pnpm typecheck`、`pnpm build`、`pnpm package:mac` 均通过；构建仅保留依赖 `markstream-react` 的既有 PURE 注释警告。
- 打包产物为 `Zeus Test.app`，bundle ID 为 `dev.hypha.zeus.test`，`codesign --verify --deep --strict` 通过。
- 使用独立数据根 `/private/tmp/zeus-0420-final-gui.XpsngG` 启动最终包；首个窗口按 `ZEUS_TEST_DISPLAY_ID=3` 创建在非主外接屏，日志确认 `actualDisplayId=3`。
- 真实渲染现场确认自动化编辑器原生 `<select>` 数量为 0，共有 6 个统一组合框。空能力现场中，“精确模型”显示“暂无可用模型”，“推理强度”同步禁用，“保存并启用”保持禁用，不再暴露内部模型身份。
- 通过真实鼠标事件打开“阻塞策略”，弹层完整位于窗口内并显示三项；通过方向键与 Enter 从“串行排队”切换到“丢弃新触发”，弹层关闭且焦点返回触发器，随后恢复原值。另逐项展开并核对“触发方式”7 项、“权限”3 项、“会话模式”2 项和“阻塞策略”3 项。
- 最终 GUI 截图保存在 `/private/tmp/zeus-0420-final-blocking-policy.png`。独立应用正常退出后，任务数据根无残留应用进程，`zeus.db` 的 `PRAGMA quick_check` 返回 `ok`。
- 独立空数据根没有项目和模型能力，因此“精确模型”的带数据搜索弹层未做本次 GUI 实测；代码继续直接复用已在产品中使用的 `ZeusSelect` 搜索路径，本次已验证其空态与禁用边界。静态检查、构建或打包结果未替代上述 GUI 证据。

## 补充需求验证证据

- 定向 Prettier、`git diff --check`、`pnpm lint`、`pnpm typecheck`、`pnpm build` 和 `pnpm package:mac` 全部通过；架构治理确认 126 张 Core 表、11 张可重建辅助表及源码大小均在门禁内。构建仅保留依赖 `markstream-react` 的既有 PURE 注释和大 chunk 警告。
- 测试产物为 `dist/test/mac-arm64/Zeus Test.app`，显示名为 `Zeus Test`，bundle ID 为 `dev.hypha.zeus.test`；`codesign --verify --deep --strict` 通过，`hdiutil verify dist/test/Zeus-Test-0.3.95-arm64.dmg` 通过。
- 最终包使用独立数据根 `/private/tmp/zeus-0420-task04-gui.aFcpUW` 启动，首窗日志确认 `targetDisplayId=3`、`actualDisplayId=3`、`corrected=false`；包内服务健康检查为 `status=ok`、`version=0.3.95`、`database=ok`、`runtime=ok`。
- 包内 Renderer 产物反读确认包含 `aria-multiselectable`、“全选项目”和“选择自动化 Plugin”契约；共享 `ZeusSelect` 的既有单选路径仅在未提供 `selectedValues` 时保持选中即关闭，多选路径保持弹层以连续勾选。
- 本轮 Composer 没有明确启用 Computer Use，官方桌面控制通道拒绝读取 `Zeus Test`；同时独立根的权威现场为 0 个项目、0 条自动化。本轮没有使用 AppleScript、CDP 或外部自动化替代，因此“目标项目全选”、Skill 选择和 Plugin 连续多选尚未取得真实 GUI 点击证据，不用静态或打包证据冒充。
- 验收结束后只退出本任务精确路径的 `Zeus Test.app`，任务测试进程数为 0，`zeus.db` 的 `PRAGMA quick_check` 返回 `ok`；未停止、复用或修改正在运行的正式 `/Applications/Zeus.app`。

## 交付与合入阻断

- 补充需求已在任务分支提交为 `0447d731b709329bd0c4b9f5d9cf3680374bcf22`（`fix(automations): 统一项目与能力下拉框`），未推送远端。
- 本地 `test` 工作区 `/Users/david/hypha/.zeus-worktrees/zeus-e2e-oBLuqT/ZEUS-0352-test` 工作树干净，但停在 `056c48c19cfaab0c71e713bc034bc181a9c1f971`；它已比实时 `origin/test=ee21c81925bed0469e52d5378de8874029dea311` 领先 207 个提交，不是可向远端基线快进的落后分支。
- `test` 与任务分支无互为祖先关系，共同基线为 `eac84bc329c4f10df1b7ceb9b9f5057c53403433`；任务侧独有 209 个提交，`test` 侧独有 72 个提交。普通 merge 会同时引入 0.3.75–0.3.95 期间的大量无关变更，不再是本任务的四文件定向交付。
- `git merge-tree --write-tree test 0447d731` 预演返回冲突，除本任务的 `AutomationsWorkspace.tsx` 和任务文档外，还包含 `session-styles.tsx`、`SessionWorkspace.tsx`、`sessionReducer.ts`、ZEUS-0387 文档、三个 Local Server 文件及 `verify-renderer-event-flow.ts`，共 10 个冲突文件。预演未改动 `test` 工作树或分支历史。
- 未经用户选择“升级 `test` 整体基线并做全量语义合并”或“例外授权将单个任务提交窄化移植到旧 `test`”前，不执行任何目标分支写入。
