# ZEUS-0456 新用户 Worktree 分支发现修复

## 根因与已确认目标

任务推送能力在查询只读改造后仅消费已登记仓库；项目创建不再登记仓库，也没有接入替代的后台发现入口。新用户及未登记仓库的旧项目因此拿到空清单，即使真实目录已有 Git 分支。

项目添加后立即在后台发现本地仓库；进入项目、修改项目目录及主动刷新共用同一发现流程。推送弹窗优先展示已登记仓库的真实分支，查询本身保持只读。

优点：项目创建不等待全目录扫描，新用户无需手动登记仓库；代价：需要持久维护发现状态，处理失败、目录变化及退出后的恢复。

## 实施约定

- 发现只读取本地目录和 Git；不拉取远端、不初始化仓库、不创建任务分支。
- 成功后完整替换仓库清单，保留同一路径的仓库身份；失败不清空旧清单。
- 未开始、进行中、完成和失败分别显示；只有完成后的空清单才表示没有仓库。
- 只恢复未完成的发现请求，旧项目进入时补扫；不增加目录监听或新的任务调度框架。
- 后台更新保留模型、工作方式、补充信息和仍有效的分支选择；创建前复验清单与实际引用。
- 仅在当前任务分支实施；不执行 Git 提交、推送或合入。

## 实施与验证记录

### 实施结果

- 项目创建与目录修改在原有命令事务中保存发现请求，事务提交后启动后台扫描；返回创建成功不等待仓库遍历。
- 新增 `POST /api/projects/:projectId/git/repositories/refresh`，沿用命令身份、幂等接纳与事务机制。进入项目和主动刷新共用此入口，同项目同目录的进行中请求合并。
- 发现记录保存在项目设置中，包含项目、对应目录、请求身份、状态、上次完整完成时间和错误。启动仅恢复进行中的记录，旧项目进入时补扫。
- 复用 `discoverGitRepositories` 及既有排除目录、六层扫描范围。目录或仓库读取失败会明确失败，不再返回不完整的成功清单；Git 状态读取关闭可选索引写锁，不产生扫描带来的索引刷新。
- 成功后在一个事务内替换仓库登记清单并保存完成状态，同一路径保留身份；失败保留上次清单。写回前同时核对项目当前目录和请求身份，退出中止遍历，防止旧目录及已退出实例写回。
- 能力查询增加 `repositoryDiscovery`，依旧只读。已登记仓库独立读取真实本地引用和本机已知远端引用；单仓不可读时显示不可用原因，其他仓库继续展示。项目目录已移走时，附件读取排除不可用根目录，使发现错误仍能显示，且不扩大附件访问范围。
- 推送弹窗监听既有实时事件，连接恢复后补读权威能力；只更新仓库相关信息并合并现有选择。仓库新增、移除或来源失效要求复核，失效来源要求重选，自定义新分支名、模型、补充信息、附件及上下文选择继续保留。
- 首次没有工作方式偏好时，发现 Git 后默认 Worktree；空项目后来发现 Git 同样适用。用户已选方式不会被覆盖，已默认 Worktree 的草稿也不会因仓库暂时为空而切回直接目录。
- 模型目录未就绪时仍显示仓库表单；模型后来就绪后，界面与提交入口使用相同模型来源。首次完整清单就绪前仅限制创建新 Worktree；既有环境和直接目录不受发现状态限制。
- 创建新环境继续验证清单修订和真实来源引用；清单变化会拒绝旧请求。没有新增远端拉取、目录监听、调度框架、数据库表或依赖，也没有修改已有任务环境。

后台扫描当前按项目串行执行，优点是限制磁盘竞争，代价是同时添加多个大项目时后面的发现需要排队。复用现有六层范围及排除规则，范围之外的仓库不会纳入清单。

### 静态、构建与安装包

截至 2026-09-05，最终源代码已完成以下检查：

| 检查 | 结果 |
| --- | --- |
| `pnpm lint` | 通过 |
| `pnpm typecheck` | 通过，含架构治理 |
| `pnpm build` | 通过；现有依赖的纯注释位置及大块体积警告仍存在 |
| `pnpm package:mac` | 通过，生成当前任务 `dist/test/mac-arm64/Zeus Test.app` 和测试 DMG |
| `git diff --check` | 通过 |
| HTTP 只读审计 | 163 个 GET/HEAD，未知外部调用为零，覆盖完整 |
| 命令副作用接纳审计 | 207 个变更入口，覆盖完整，无违规 |
| 安装包身份与结构 | bundle ID 为 `dev.hypha.zeus.test`；严格签名检查及 DMG 校验通过 |

最后补齐首次空项目默认值及模型晚到边界后，以上检查与打包已全部重新通过，退出码均为零。最终包身份复核、严格签名检查及 DMG 校验通过；DMG 校验和为 `E76D93A4`。日志位于临时现场的 `lint.log`、`typecheck.log`、`build.log`、`package-mac.log`、`final-build-status.json` 和 `dmg-verify.log`。

### 本地服务真实运行检查

在独立临时数据目录运行生产本地服务、真实 SQLite 和实际 Git 克隆，通过正式 HTTP 命令检查；Provider 使用不允许启动请求的被动端口，远端配置为不可达本机地址。

已通过：旧项目查询不隐式扫描、显式补扫、未登录时读取分支、命令重放不重复扫描、相同路径身份稳定、嵌套多仓与排除目录、成功空清单、新增和移除仓库修订、目录移走后失败保留清单、快速切换目录拒绝过期结果、查询不写 SQLite、不调用 Provider、不拉取远端、退出后恢复持久请求，以及创建前拒绝过期清单。此次多仓与空项目创建请求均约 4 毫秒返回；该时间只描述本机临时现场，不代表性能承诺。

直接执行真实表单合并函数的状态检查通过：无模型初始化、首次默认 Worktree、保留正文和附件及上下文、保留来源和自定义新分支名、失效来源要求重选、仓库变化要求复核、手动直接目录不被覆盖，以及连续刷新为空时不静默切换工作方式。

最终构建后的本地服务检查再次通过，独立现场为 `/private/tmp/zeus-0456-qa.hrFreF/run-hAIds3`，记录于 `runtime-check-final.log`；该轮多仓项目创建约 6 毫秒、空项目约 4 毫秒返回。最终表单检查还覆盖空项目后来发现 Git 的默认选择，以及失效来源撤销旧的带入本地改动勾选，结果均通过。

临时现场与日志位于 `/tmp/zeus-0456-qa.hrFreF`。可从当前任务根目录执行 `node /tmp/zeus-0456-qa.hrFreF/runtime-check.mjs`，每次使用新的 `run-*` 独立目录；脚本只克隆当前已有历史，不创建 Git 提交。表单状态检查使用 `node --import tsx /tmp/zeus-0456-qa.hrFreF/form-check.mjs`。未向项目新增验证框架或检查脚本。

### 真实界面验收：尚未完成

- 当前任务的应用尚未启动，没有界面点击或截图证据。本地服务检查、构建和打包不能替代真实界面验收。
- Computer Use 返回：`Computer Use 仅在 Composer 为本轮明确启用后可调用。` 需要会话开启工具后才能操作当前任务应用。
- 初次检查发现 ZEUS-0452 和另一测试实例运行，最终复核时 ZEUS-0452 已退出，另一实例仍在运行。按既有排队约定未干扰其他实例；已提出是否允许独立并行验收的选择，尚无答复时继续排队。
- 显示器现场已确认：外接屏 5 为主屏，外接屏 3 为非主屏。开始验收前需复核显示器，并从首次创建窗口起使用 `ZEUS_TEST_DISPLAY_ID=3` 与本任务独立 `ZEUS_USER_DATA_DIR`。
- 待补真实交互：添加后立即推送、三种发现状态及重试、模型与草稿保留、来源分支选择、进入旧项目、连接恢复后的清单更新以及应用退出重启恢复。

当前分支为 `zeus/ZEUS-0456-worktree-01`。未提交、推送、合入或修改其他 worktree；本任务目前不能记为全部验收完成。
