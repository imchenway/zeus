# ZEUS-0457 全项目用户提示语重构

## 目标与约定

- 当前阶段：`zeus/ZEUS-0457-task-02` 已完成最新“配置直接展示与接入前置”实现、工程检查和独立启动，真实界面交互仍待验；以文末最新记录为准。前一阶段 `zeus/ZEUS-0457-task-01` 及本轮早期方案的记录保留，不替代最新验收。
- 用户已确认：全项目面向用户的提示语，中英文同步。重点解释为什么操作受阻，不能以“消息已保存”“操作未完成”或无依据的“重试”代替原因。
- 覆盖桌面页面、原生弹窗、通知、Telegram，以及到达这些界面的服务端错误。用户内容、AI 正文、第三方网页、诊断日志和模型指令保持原意。
- 文案遵循“具体情况、实际原因、影响、有效操作”；内容按需要出现，不强制每条提示包含四句话。
- 按场景解释必要名称；保留 Codex、Git、API 等准确身份，去掉内部实现过程与自我辩护。
- 没有证据时不得猜测登录、网络、权限或服务状态。按钮只调用场景已有能力，不能凭文案决定重发、取消或归档。
- 不修改历史数据，不执行提交、合并、推送或发布。

## 已确认的代码问题

1. `formatVisibleApplicationError` 只识别少量错误，其他已知错误也统一丢弃原因；`errorToLocalUiMessage` 固定传入中文。
2. 队首失败包装仅保留通用错误码和消息；部分命令、接口与会话投影继续丢失底层错误身份。
3. 通知、原生对话框和 Telegram 存在固定中文；模型失败直接展示英文服务原文，缺少与应用语言一致的解释。
4. 设置、插件、自动化和会话提示混入“权威、派发、运行世代、真实、不伪造”等实现用语。

## 实施取舍

- 复用现有模块文案和错误展示；只对跨入口共享的原因和脱敏逻辑建立共用函数，不引入翻译框架或在线翻译服务。
- 原始错误信息通过可选原因字段保留；摘要与详情分开。优点是原因可读且可追溯；代价是需要同步检查传输与展示两端。
- 原生窗口、通知和 Telegram 跟随现有应用语言；设置未就绪时默认中文。
- 全量清单记录来源、原文、最终文案、触发入口、操作与处理结论；明确区分改写、合理保留和非产品文案。

## 验证要求

- 执行 lint、typecheck、build、package:mac 与差异检查。
- 使用现有 QA 页面及真实组件核验已知原因、未知原因、再次失败、中英文切换、长文换行和按钮行为。
- 桌面运行只用本任务独立 `Zeus Test.app`、`dev.hypha.zeus.test` 和独立资料根；有外接屏时首窗须位于非主外接屏。
- 新用户原始故障发生在其他电脑，目前缺少实际错误记录。发送和归档原因未复现，文案重构不能被记录成该故障已解决。

## 实施与验证记录

- 已完成工作树、项目约束和历史主文档核对；随后安装锁定依赖并建立提示语清单。

### 实现阶段记录

- 已完成会话错误链、归档原因、模型配置、共享中英文错误解释与脱敏详情；发送结果未知时保留检查状态入口，不新增自动重发或强制归档。
- 已逐模块检查会话、项目、任务、Git、源码、模型、设置、插件、技能、自动化、数字员工、记忆、Telegram；清楚的按钮和表单标签保留。原生菜单、登录窗口、授权窗口、系统通知、更新窗口和浏览器扩展语言随应用设置传递，初始默认中文。
- 错误对象只增加可选原因与诊断信息；接口地址、原错误码、业务状态、权限确认和删除保护保持兼容。查询缓存继续保留原错误字符串，额外携带原因对象，避免展示前丢失错误身份。
- 模型检查成功仅说明读到了模型列表，单独注明图片、工具等能力仍需检查。失败使用实际 HTTP、认证、证书或连接原因。API Key 在设置首次说明为服务访问密钥。
- 更新只对已有短暂下载失败判断或明确允许重新下载的场景显示重试；安装失败不提供无依据的再次安装按钮。
- 原生与 Telegram 的原始技术日志不作翻译；界面正文使用原因解释，错误详情移除密钥、个人路径、认证信息和堆栈。

### 当前验收证据

- 依赖：`pnpm install --frozen-lockfile --offline` 成功，未改变锁文件。
- 过程中多轮 `pnpm typecheck` 已通过，包括架构归属检查；最终检查仍在执行。
- 现有 `apps/desktop/qa/session-core-qa.tsx` 增加提示语场景，使用真实 `MessageDeliveryOutcomeFeedback`、`VisibleApplicationError`、错误弹窗。覆盖未登录、权限、连接、用量限制、模型拒绝、配置、归档、未知原因、结果未确认及恢复未发消息；可驱动等待、完成、恢复和再次失败，记录回调以区分核对与重发。
- 本任务 QA 服务位于 `http://127.0.0.1:4179/qa/session-styles.html?copy`。Zeus 内置浏览器 `open` 与 `list_tabs` 均返回原生工具调用超时，尚未取得 DOM 或截图，因此不能认定布局与交互验收通过。未换用其他浏览器或外部自动化工具。

## 最终实现与覆盖清单

### 清单阅读方式

清单覆盖 `apps`、`packages` 中 565 个源码、原生界面、HTML、样式和配置文件，按语法节点收集 18,941 条字符串候选，而非仅按技术词搜索。产品候选出现 10,799 次，将同一词典键的中英文合并，并按函数或词典键整理为 3,202 个入口；同一入口里的每条文字保留行号、条件和现有按钮回调。数量包含双语和重复出现，不代表不同提示的数量，也不代表运行场景全部通过。

- [用户提示语入口清单](./ZEUS-0457_用户提示语入口清单.csv)：逐项列出原文列表、当前中文、英文、动态表达式、触发条件、操作和处理结论。同一个语言分支里的字符串按源码顺序列出；动态表达式保留插值，未编造运行时内容。
- [错误原因清单](./ZEUS-0457_错误原因清单.csv)：457 条明确错误身份或精确已知原文匹配，列出原始来源、双语解释、解决方向与按钮限制。包括本地表单校验的完整中英文原文，避免已知校验原因再次被通用错误吞掉。
- [文案改写对照](./ZEUS-0457_文案改写对照.csv)：664 条成对改写记录。后续结构调整或文字变动已标记，最终值以入口清单为准。
- [原提示退出记录](./ZEUS-0457_原提示退出记录.csv)：记录原文退出直接展示的入口，包括改写、合并和删除；不将原文退出等同于删除业务能力。
- [检查文件索引](./ZEUS-0457_检查文件索引.csv)：全部扫描文件、候选数量与产品入口分布。
- [非产品文案清单](./ZEUS-0457_非产品文案清单.csv)：记录模型指令、接口枚举、原始诊断、任务记录、第三方工具结果等保留理由。服务端事件保存值由前端按事件类型解释，用户标题和 AI 内容保持原文。

### 模块覆盖结论

| 入口 | 本次处理 | 保留与限制 |
| --- | --- | --- |
| 新用户配置、模型连接 | 登录、访问密钥、地址、证书、HTTP 状态、模型不可用分别解释；模型目录成功仅说明读取到列表 | 不将模型目录检查成功当成图片、工具等能力验证通过 |
| 发送、等待、恢复 | 保留包装前的错误原因；发送结果未确认时只核对状态；明确未发消息才由用户选择发送或取消 | 原发送身份、队列状态与安全重发条件继续生效，无自动重复发送 |
| 归档 | 区分等待回答、尚有工作、待发送内容等阻止原因 | 不绕过归档检查，不新增强制归档 |
| 任务、看板、数字员工 | 创建失败按原因控制操作；任务事件按类型显示双语标题；补齐附件、缺省字段和决策说明 | 用户描述、AI 交付正文、事件原始记录不改写 |
| 项目、文件、源码、Git | 目录与权限错误解释；冲突编辑器按钮和辅助阅读文字双语；确认保留操作对象和内容影响 | 删除、停止、提交、推送、放弃工作目录等原确认流程保留 |
| 插件、技能、自动化、记忆、远程连接、禅道 | 表单、空白页、状态和失败原因改写；查询缓存额外携带原因；禅道认证与连接问题分开 | 技能文件、外部网页、机器人用户输入不作为产品提示改写 |
| 会话详情、资源、上下文和进度 | 状态描述当前用户工作；删除反复解释缺省数据的内部过程说明；缺省数据统一用破折号及辅助阅读名称 | 不把恢复历史伪装成模型已开始思考；普通排队语义保留 |
| 原生菜单、弹窗、通知、权限、更新 | 跟随当前语言，启动设置未就绪默认中文；停止与退出说明具体影响；更新重试由失败类别限制 | 不新增授权动作；安装失败无默认再次安装按钮 |
| Zeus 浏览器与扩展 | 浏览器授权、登录、下载、保存说明双语；扩展弹窗跟随 Zeus，安装说明采用浏览器内置语言文件 | 不增加扩展权限；第三方网页内容和原始工具诊断保持原文 |
| Telegram | 命令帮助、状态、确认、失败双语，读取应用语言；错误正文用可读解释 | 未向任何 Telegram 用户发送验收消息 |

### 错误链与取舍

- 现有错误对象只增加可选 `cause`。命令包装、HTTP 返回、前端接口、查询缓存与会话投影保留原错误码和底层原因；原因对象限制深度和长度，补充上下文只保留有界标量。
- 历史会话从现有错误 JSON 中只读提取可选原因，无数据库迁移、历史重写或业务状态调整。历史记录本身没保存的原因无法补造。
- 详情隐藏访问密钥、登录令牌、密码、认证与 Cookie 头、URL 查询参数、个人目录和堆栈。未知原因如实说明，不用网络问题代替所有错误。
- 普通模块继续使用原有双语组织；共用错误原因集中维护。优点是不同入口对同一原因表达一致；代价是错误码新增时也要更新原因目录。没有引入翻译框架、在线翻译、测试依赖或新的测试体系。
- 删除 AI 连接状态旁重复的内部检查说明；终端按已有能力说明“命令输出”或“交互式终端”。缺省数据不再向普通用户解释内部事件为什么尚未同步。

## 最终验证记录

### 已取得的运行证据

1. 使用生产 `MessageDeliveryOutcomeFeedback` 等真实组件进行静态渲染检查，中英文原因、登录入口、未知原因不显示重试、结果未确认显示核对、明确未发显示发送均符合当前业务条件。此检查不执行浏览器点击，也不证明布局。
2. 检查嵌套错误、发送结果未确认、任务创建重试限制及原生菜单双语；校验 Bearer、JSON 密钥、带空格密码、Cookie 多值、客户端密钥、令牌、个人路径、URL 查询和诊断对象的脱敏。
3. 执行生产历史会话摘要 SQL：无效 JSON、缺少错误、嵌套原因和对象形式的模型错误码均能读取，未修改任何历史数据库。
4. 独立测试应用 `dev.hypha.zeus.test` 使用 `/tmp/zeus-0457-copy-CMhPL7/gui-user-data`。首窗参数 `ZEUS_TEST_DISPLAY_ID=3`；启动日志记录 `targetDisplayId=3`、`actualDisplayId=3`、`corrected=false`，首窗坐标 `x=-1268, y=268, width=1240, height=820`。显示器 3 为非主外接屏，满足首窗约束。
5. 测试应用日志依次记录窗口显示、本地服务就绪、应用设置就绪和界面启动完成。首次命令受工具环境 `ELECTRON_RUN_AS_NODE=1` 影响未创建窗口；清除该变量后正常启动。仅关闭本任务实例，未操作其他应用实例或复制登录凭据。macOS 记录登录启动项设置被拒绝，主窗、本地服务和界面仍完成启动；该系统设置未纳入通过结论。

### 运行验收缺口

- Zeus 内置浏览器打开 QA 页及列出标签多次返回原生调用超时，未取得 DOM、截图或点击结果。未使用外部 Playwright、CDP 或其他浏览器替代。
- 本轮 Computer Use 未启用，应用状态工具直接拒绝调用。桌面仅取得独立启动与首窗位置日志，**未完成中英文布局、点击、原生弹窗与通知的目视验收**。
- QA 页已经准备正常、等待、失败、恢复和再次失败；这些切换的真实点击结果尚未验证，不能用静态渲染检查代替。
- 未验证真实账号登录、模型服务拒绝、网络中断后恢复、外部插件服务、Telegram 收发、更新安装与再次失败。未触发真实付费请求或安装更新。
- 新用户原始发送和无法归档故障发生在另一台电脑，未取得该次原始错误或复现现场。**本任务改进原因传递和展示，不将原故障记为已修复。**

最终测试包再次启动后，界面于 1,404 毫秒就绪，首窗仍直接位于显示器 3；检查后仅关闭本任务应用。

### 最终命令结果

| 检查 | 结果与边界 |
| --- | --- |
| `pnpm lint` | 通过 |
| `pnpm typecheck` | 通过，含架构检查 |
| `pnpm build` | 通过；构建器仍输出第三方依赖的纯函数标注位置警告，不影响本轮构建完成 |
| `pnpm package:mac` | 通过；生成本任务 `dist/test/mac-arm64/Zeus Test.app`，身份为 `dev.hypha.zeus.test`，签名结构检查通过 |
| `git diff --check` | 通过；另检查新增代码和文档的空白差异 |
| 文案清单结构 | 六份 CSV 列数、逐项记录结构通过检查；入口清单 3,202 项，原因匹配 457 项，原提示退出记录 279 项 |
| 真实组件初始渲染与原因检查 | 通过；包括本地必填校验经统一入口仍显示实际原因，已接收消息等待状态时即使嵌套连接错误也只允许核对 |
| 历史会话 SQL | 无效 JSON、原因对象、模型错误码和缺省值读取检查通过 |
| 浏览器与桌面交互 | 未完成，原因及可取得的启动证据见上文 |

实现未改写历史数据，未执行提交、合并、推送或发布。当前交付为工作树代码、全量清单和独立测试包；界面交互与真实服务场景不能标记为全部验收通过。

## 新用户任务流程与精致原生设计（2026-09-07）

### 已确认方案与实施边界

- 当前工作树：`zeus/ZEUS-0457-task-02`，实施前工作区干净。用户已明确授权按本节方案实施。
- 主流程：选择工作文件夹 → 创建任务 → 推送任务 → 按需接入模型 → 返回推送确认 → 执行。启动不自动弹模型配置；登录和配置成功不自动推送。
- 精致原生视觉：系统字体、主题灰阶、约 720px 左对齐引导、26px 标题、14px 正文、12px 辅助文字；统一控件及 160–220ms 轻量动效，支持深浅主题、键盘和减少动态效果。
- 接入复用已有登录与供应商编辑器，携带当前任务上下文；模型只启用到当前项目，保留其他模型和全局默认。返回保留推送配置与附件，取消和迟到回执不得启动任务。
- 检查与继续恢复分开，检查不能建立线程、准备目录或发送内容；归档期间保留会话画面，失败保留位置和具体原因。
- 优点：新用户围绕自己的任务逐步完成接入，不必提前理解设置结构。代价：需要同步维护返回上下文、异步取消边界和各状态视觉。
- 本轮不新增单元测试体系；工程检查及真实界面、模型验证分别记录。原报错电脑版本、模型和日志仍待补齐，不能将本轮交互改造当成原故障已复现修复。

### 实施与验证记录

#### 已实施的视觉与行为

- 首页在没有项目时显示 720px 左对齐引导、三步进度及唯一主操作“选择工作文件夹”。沿用系统字体和主题变量，标题 26px、正文 14px、辅助文字 12px；选择目录后预填项目名称，创建成功直接进入任务创建。启动不弹模型配置。
- 任务创建保留项目、任务类型及对应内容在首屏；优先级、父任务、标签和禅道导入放入原生折叠区。推送确认先显示任务、模型、工作方式、权限和目录，补充内容、附件、推理配置与上下文按需展开；自动化和扩展入口归入侧栏“更多”。
- 复用现有模型接入弹窗、Codex 登录、供应商选择和 API Key 编辑器。配置导入改为可选动作。接入携带项目与任务名称，仅合并当前项目的模型选择；保留已有选择与全局默认。模型目录可读只代表可选择，不代表真实调用成功。
- 接入成功回到原推送确认页，保留分支、权限、补充内容、附件、展开状态和滚动位置；再次点击“推送任务”才执行。删除重复的任务登录／导入弹窗和“登录后自动推送”调用。关闭、取消及切换上下文会使旧回执失效。
- 空默认模型、明确失效的模型、模型目录为空及读取失败分别处理；项目和任务创建不做模型就绪检查，明确指定的失效模型不静默替换。读取失败在原确认页提供原因和重新检查入口。
- 错误在相关操作附近显示摘要，详情按需展开，移除相关流程重复弹窗。按钮、输入框和折叠标题统一规格；沿用 180ms 轻量入场动效及减少动态效果规则，窄窗口改为纵向排列；共享弹窗焦点循环包含原生折叠标题。
- 归档期间保留原会话；服务端确认后，HTTP 回执与实时通知共用列表移除逻辑，归档前发出的旧列表请求失效，最后一条会话移除时同步清除历史选择标记。归档失败保留原位置和“查看并处理”入口；归档成功后的列表读取失败不会被误报成归档失败。运行、待回答问题及未知结果的服务端归档限制保持有效。
- 恢复请求必须显式携带 `check` 或 `continue`，输入校验、摘要和执行身份均区分两者。`check` 只读取已有线程并更新已确认状态，不建立或恢复线程、不准备目录、不订阅执行、不发送消息；未知结果禁止编辑、取消和再次发送。重启后未确认的本地发送记录先检查，不自动重放。

以上为代码实现约定；涉及真实点击、焦点、视觉和 Provider 行为的条目，仍以以下验收证据为准。

#### 工程与服务端验证

| 验证项 | 本轮结果与证据 |
| --- | --- |
| `pnpm lint` | 通过；日志 `/tmp/zeus-0457-lint-final.log` |
| `pnpm typecheck` | 通过；包含架构边界、依赖和循环检查；日志 `/tmp/zeus-0457-typecheck-final.log` |
| `pnpm build` | 通过；独立打包再次构建当前代码；日志 `/tmp/zeus-0457-build.log` 与 `/tmp/zeus-0457-package-final.log` |
| `pnpm package:mac` | 生成 `dist/test/mac-arm64/Zeus Test.app` 与 `dist/test/Zeus-Test-0.3.102-arm64.dmg`，身份 `dev.hypha.zeus.test`；签名及包结构检查通过 |
| 既有恢复命令运行检查 | `pnpm verify:conversation-dispatch-command` 通过；复用已有脚本，未新增测试文件、依赖或体系。真实路由、命令数据库与只读线程应用联合检查：读取 3 次、确认投影 2 次、显式继续 1 次；所有检查请求均没有继续执行；重复检查不重复读取；检查失败后同身份可安全再查。失败回执为写入前失败，Provider 写入标记为空。缺失／非法意图返回 400，同身份篡改意图并重算摘要返回 409。日志 `/tmp/zeus-0457-recovery-probe.log` |
| 无模型的新用户服务端流程 | 使用额外临时数据库、独立服务身份及关闭的 Codex 执行能力，通过真实本地服务接口创建项目和任务，均返回 201。失效的默认模型引用仍保留，模型列表为空、默认模型为空且可用状态为否；未启动任何真实模型调用。临时资料已清理，日志 `/tmp/zeus-0457-new-user-probe.log` |
| 工作区检查 | `git diff --check` 通过；未执行提交、合并、推送或发布 |

#### 独立原生启动与未完成的界面验收

- 使用本任务构建的 `Zeus Test.app`，全新资料目录，不复制正式应用或其他任务的凭据。首轮资料目录为 `/var/folders/2m/dswmfzbd7wx_39zxs4kvd3fc0000gn/T/zeus-0457-native-fso05p4r/user-data`，启动记录为同级 `launch.log`。
- 当时主外接屏为显示器 3，指定的非主外接屏为显示器 5。启动日志确认 `targetDisplayId=5`、`actualDisplayId=5`、`corrected=false`，窗口从首次创建起就在指定外接屏；执行服务报告 `execution_host.ready`。只读检查数据库 `quick_check=ok`，项目、任务、会话和发送记录均为 0。
- 各次重新打包前均只停止本任务已核对的应用和执行服务进程。一次准备验收目录时，手工创建的资料目录权限为 0755，被已有数据根保护拒绝；修正验收启动过程，使用 0700 的全新目录后启动正常，未修改应用权限检查。最终包启动信息见下方；历史启动日志不作为最终画面截图证据。
- 最终代码在 2026-09-07 16:53（北京时间）以全新资料目录 `/var/folders/2m/dswmfzbd7wx_39zxs4kvd3fc0000gn/T/zeus-0457-native-accepted-5b_ois73/user-data` 启动。应用进程 27410，执行服务进程 27466；`launch.log` 再次确认首次窗口位于显示器 5、没有位置修正，执行服务报告就绪。数据库完整性检查通过，项目、任务、会话和发送记录均为 0。实例清单为同级 `acceptance.json`，当前定位文件为 `/tmp/zeus-0457-native-current.json`。该实例保留供启用原生控制后继续验收。
- 本轮 `zeus_computer` 的应用枚举和窗口观察均被环境拒绝，原因为“Computer Use 仅在 Composer 为本轮明确启用后可调用”。Zeus 内置浏览器列出页面和打开本任务本地页面也均超时。已请求用户启用本轮 Computer Use，未收到启用回执；未改用其他浏览器或绕过原生控制限制。
- 因此没有可交付的真实页面截图。深浅主题、中英文、键盘焦点、窄窗口、等待／失败／再次失败、返回保留输入、取消登录、超时、重复点击、切换任务、重启及“接入后不得自动执行”的整条真实交互验收均待完成。工程检查和接口运行检查不能替代这些验收。
- 原报错电脑的应用版本、模型身份、原始错误和运行日志仍未补齐，原发送与归档故障未在该现场复现。本轮不能宣称已经解决原机故障。

#### 取舍与交付边界

复用已有页面、登录、模型编辑、命令与状态存储，删除重复接入流程；没有引入独立引导系统或视觉依赖。优点是同一任务内能连贯完成操作，状态责任更少；代价是逐屏视觉和模型现场验收尚需可用的原生控制环境与登录条件。当前交付是工作树实现、工程结果和隔离接口证据，整体验收尚未完成。

## 配置直接展示与接入前置（2026-09-07）

- 用户已确认调整：撤掉本轮侧栏、任务创建、推送确认中的“更多”折叠。模型、Skill、模型等级、速度、工作模式、权限、工作区、补充内容与上下文依次直接展示，用间距和分栏保持清晰；错误诊断详情仍按需查看。
- 点击任务页推送入口先检查接入状态，原按钮显示等待。没有账号或供应商时先接入，已有可用模型才进入确认；读取失败不当作没有配置，不自动替换失效模型。
- 首次接入取消返回任务详情；从确认页主动接入的取消返回原表单。接入成功均只进入确认页，用户再次推送才执行。输入、附件和返回位置保留，取消或切换任务后的旧结果失效。
- 沿用当前工作树和前阶段改动，不新增服务端接口、引导系统或依赖。优点是配置容易发现、流程顺序清楚；代价是页面变长，需要紧凑分栏与固定底部操作区。
- 当前阶段：以上调整已实现，工程检查、独立打包与启动核验完成；真实交互及截图待验，实际边界见下文。

### 本次实现

- 侧栏自动化、扩展恢复直接入口；任务创建的导入、优先级、父任务和标签保持展开。推送确认中模型独占一行，Skill、模型等级、速度紧随其后，再展示工作模式、权限、目录、补充内容与历史上下文。删除三处折叠容器、展开状态和专用样式，保留滚动内容区与底部操作区。
- 共享推送入口先并行读取账号／项目模型能力与供应商信息，不使用旧能力缓存判定是否接入。需要接入时立即进入对应弹窗，不先等待 Git 目录检查；已有可用模型时读取确认资料后再打开确认页。
- 没有可用项目模型且没有供应商记录时进入接入方式选择；已有供应商记录时直接进入已有供应商页面。未就绪的 Codex 占位账号不作为可用账号，接入弹窗也不宣称已经确认订阅缺失。读取异常走任务操作旁的错误入口，沿用本地读取接口的 8 秒超时，允许按原阶段重查。
- 原推送按钮显示“正在检查模型…”，入口同步登记本次操作，直到取消、失败或进入执行后才释放，防止同一事件周期或弹窗打开期间重复发起。接入检查失败后主操作变为“重新检查”。
- 接入上下文记录首次入口／确认页入口及取消回调。首次取消返回任务详情；确认页取消返回原表单。接入完成只刷新能力并预选明确选择的模型；普通重查不覆盖当前模型选择。阶段模型、速度和权限继续遵守原有锁定。
- 请求代次与项目、导航位置、任务详情身份同时匹配才接收结果。切换任务或页面立即使旧接入上下文失效；关闭清除入口状态。返回确认页复用原输入与附件，恢复滚动位置和接入前焦点；已不存在的焦点控件回到主操作。
- 本次没有新增服务端接口、依赖、测试文件或脚本；前阶段恢复与归档改动保留。

### 验证记录

| 项目 | 本次结果 |
| --- | --- |
| `pnpm lint` | 通过；`/tmp/zeus-0457-direct-lint.log` |
| `pnpm typecheck` | 通过，含架构检查；`/tmp/zeus-0457-direct-typecheck.log` |
| `pnpm build` | 通过；`/tmp/zeus-0457-direct-build.log` |
| 入口判断运行检查 | 7 种模型状态通过；当前工作面可接收结果，5 种工作面变化均拒绝旧结果；未调用 Provider。日志 `/tmp/zeus-0457-direct-entry-probe.log` |
| `git diff --check` | 通过；三处新增折叠的容器与样式均已移除 |
| `pnpm package:mac` | 通过；`/tmp/zeus-0457-direct-package.log`。产物 `dist/test/mac-arm64/Zeus Test.app`、`dist/test/Zeus-Test-0.3.102-arm64.dmg`，身份 `dev.hypha.zeus.test`，签名检查通过 |
| 独立启动 | 通过；2026-09-07 17:44（北京时间）最终包从首次窗口创建起位于非主外接屏 5，无位置修正；执行服务就绪，数据库完整性正常，项目、任务、会话与发送记录均为 0 |
| 真实界面与截图 | 待验。本轮窗口观察仍被“Computer Use 仅在 Composer 为本轮明确启用后可调用”拒绝；Zeus 内置浏览器列出页面超时。没有以其他浏览器或生成图片替代真实验收 |

本次独立资料目录为 `/var/folders/2m/dswmfzbd7wx_39zxs4kvd3fc0000gn/T/zeus-0457-direct-native-vo3yfjhl/user-data`，应用进程 54467，执行服务进程 54474。窗口日志为同级 `launch.log`，实例清单为 `acceptance.json`；`/tmp/zeus-0457-native-current.json` 指向本次实例。日志确认 `targetDisplayId=5`、`actualDisplayId=5`、`corrected=false`；当时主外接屏为 3。上一份已有 1 个项目和 1 个任务的验收资料目录完整保留，没有复制其中的凭据到新目录。

主文档中的复查命令也已实际执行通过，日志为 `/tmp/zeus-0457-direct-doc-probe.log`。本次未执行提交、合并、推送或发布。

以下命令可在当前目录直接复查入口判断；仅在临时进程运行，不建立测试体系或写入业务数据：

```sh
pnpm exec tsx --input-type=module <<'TS'
import assert from 'node:assert/strict';
import { resolveTaskModelPushEntry } from './apps/desktop/src/renderer/task/TaskModelPushModal.tsx';
import { isTaskModelPushOriginCurrent } from './apps/desktop/src/renderer/features/workspace/workspaceSupport.tsx';
// 账号与模型样例只用于当前进程的入口运行检查。
const account = { generationId: 'observed', signedIn: false, requiresOpenaiAuth: true };
const codex = { id: 'codex:example', sourceId: 'codex', agentKind: 'codex', available: true };
const provider = { id: 'provider:example', sourceId: 'provider', agentKind: 'pi', available: true };
const snapshot = { codexAccount: account, models: [] };
assert.equal(resolveTaskModelPushEntry(snapshot, false), 'choose');
assert.equal(resolveTaskModelPushEntry(snapshot, true), 'custom');
assert.equal(resolveTaskModelPushEntry({ ...snapshot, models: [codex] }, false), 'choose');
assert.equal(resolveTaskModelPushEntry({ ...snapshot, models: [codex], codexAccount: { ...account, signedIn: true } }, false), 'confirmation');
assert.equal(resolveTaskModelPushEntry({ ...snapshot, models: [provider] }, true), 'confirmation');
assert.equal(resolveTaskModelPushEntry({ ...snapshot, models: [{ ...provider, available: false }] }, true), 'custom');
assert.equal(resolveTaskModelPushEntry({ ...snapshot, models: [codex], codexAccount: { ...account, generationId: 'codex-unavailable', requiresOpenaiAuth: false } }, false), 'choose');
// 切换项目、页面、任务或关闭详情后，旧工作面的结果不再适用。
const origin = { projectId: 'project-a', activeNavTarget: 'projects', activeProjectSection: 'tasks', taskDetailPaneTaskId: 'task-a' };
assert.equal(isTaskModelPushOriginCurrent(origin, { ...origin }), true);
for (const changed of [{ projectId: 'project-b' }, { activeNavTarget: 'settings' }, { activeProjectSection: 'sessions' }, { taskDetailPaneTaskId: 'task-b' }, { taskDetailPaneTaskId: undefined }]) {
  assert.equal(isTaskModelPushOriginCurrent(origin, { ...origin, ...changed }), false);
}
console.log('入口状态与工作面身份检查通过');
TS
```

以上运行检查验证了入口选择和身份比较，不替代实际点击、登录回调、重复点击、超时、焦点或截图验收。深浅主题、中英文、窄窗口、固定底部操作区和完整接入／取消／返回流程仍需在本任务独立应用中逐屏核验。原报错电脑的发送和归档故障仍待版本、模型与日志补齐。
