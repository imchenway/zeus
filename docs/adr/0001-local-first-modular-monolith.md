---
status: accepted
---

# 采用本地优先模块化单体与独立执行核心

Zeus 保持一个版本、一个安装包和本机低延迟产品形态，但将 Renderer、Electron Main、本机执行核心、Provider Worker 与重型 Worker 分成明确故障域。选择模块化单体而非云端微服务，是因为同一台 Mac 上的服务数量不能消除设备单点，微服务会增加部署、网络、认证和恢复成本；代价是本机执行核心仍是唯一业务写入主体，必须通过持久状态、租约和恢复协议保证可用性。
