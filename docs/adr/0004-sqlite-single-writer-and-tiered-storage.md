---
status: accepted
---

# 保留 SQLite 单写入者并按生命周期分层存储

核心业务事实继续保存在原生 SQLite/WAL 中，并只允许本机执行核心持有写入租约；完整大型内容进入资产存储，可重建代码索引与缓存可以使用独立数据库。相比 PostgreSQL 或按领域拆分多个业务库，这一选择保留本机原子事务、安装简单和低延迟，同时避免大资产与索引拖累核心库；代价是跨层引用需要 staging、清单和恢复协议，索引投影只能提供明确的最终一致性。
