# PLAN 交互尺寸对齐证据

- Codex 参考截图为 Retina `@2x` 局部截图；验收尺寸先除以 2，再与 Zeus CSS 像素比较。
- 当前阶段只校准字体、行高、圆角、组件高度和问题状态行位置，不将整体会话分栏和实际可用宽度计入“已通过”。
- 第一轮 `20px / 16px / 44px / 30px` 在真实 Zeus 窗口中仍显得偏大，不能继续作为通过基准。
- 当前普通问答目标：面板最大宽度 `736px`、圆角 `18px`、标题 `15/20px`、选项行最小高度 `40px`、编号圆 `28px`、选项标题 `14px`
  、说明 `12/18px`、底部按钮高度 `28px`。
- “正在询问”状态行到面板的间距从 `10px` 调整为 `24px`，匹配参考图中的垂直位置关系。
- 活跃普通问答面板继续属于消息流，但在消息不足一屏时使用 `margin-block-start: auto` 推到 transcript 底部，使它稳定出现在
  composer 上方；消息超过一屏时自动回到正常滚动流，不改成 fixed/modal。
- 计划正文目标：内联正文 `15/24px`、一级标题 `24px`；右侧工作区正文 `15/25px`、一级标题 `28/36px`。
- `request-user-input-size-aligned.jpeg` 与 `implementation-size-aligned.jpeg` 是第一轮偏大实现的历史证据，已被用户否决，不再作为通过证据。
- 第二轮紧凑尺寸的源码契约和自动化测试已通过；macOS 仍处于锁屏状态，当前无法补抓真实窗口，因此视觉结论继续为 blocked。
