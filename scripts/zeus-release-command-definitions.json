{
  "purpose": "把 Zeus 升级发布流程收敛为当前项目的一条端到端受控命令",
  "commands": [
    {
      "name": "zeus-release",
      "aliases": [
        "release"
      ],
      "title": "一键升级发布 Zeus",
      "description": "以本地 main 为候选，自动生成并校验发布内容、递增补丁版本、提交并推送 main、等待 CI、创建不可变标签、触发 Release Workflow，并回验 GitHub Release、DMG、manifest 与 Homebrew Tap。失败后用同一命令从已验证阶段幂等继续。",
      "command": "pnpm release",
      "parameters": [],
      "timeoutSeconds": 3600,
      "enabled": true,
      "telegramEnabled": false,
      "riskFlags": {
        "gitWrite": true,
        "outsideProjectWrite": true,
        "externalServiceWrite": true
      }
    }
  ]
}
